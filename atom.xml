<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>Magnus web site</title>
    <link href="http://magnus.therning.org/atom.xml" rel="self" />
    <link href="http://magnus.therning.org" />
    <id>http://magnus.therning.org/atom.xml</id>
    <author>
        <name>Magnus Therning</name>
        <email>magnus@therning.org</email>
    </author>
    <updated>2020-11-30T00:00:00Z</updated>
    <entry>
    <title>Haskell, Nix and using packages from GitHub</title>
    <link href="http://magnus.therning.org/posts/2020-11-30-1734-haskell__nix_and_using_packages_from_github.html" />
    <id>http://magnus.therning.org/posts/2020-11-30-1734-haskell__nix_and_using_packages_from_github.html</id>
    <published>2020-11-30T00:00:00Z</published>
    <updated>2020-11-30T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other day I bumped into what turned out to be a <a href="https://mail.haskell.org/pipermail/haskell-cafe/2020-November/133021.html">bug in Amazonka</a> where sockets weren’t closed in a timely fashion and thus the process ran out of file descriptors. Some more digging and an <a href="https://github.com/brendanhay/amazonka/issues/608">issue</a> later I found that a fix most likely already in place (mine was possibly a duplicate of an older <a href="https://github.com/brendanhay/amazonka/issues/490">issue</a>). Now I only had to verify if that was the case by using the most recent, and unreleased code on the <code>develop</code> branch of Amazonka.</p>
<p>My first thought was to attempt to instruct Cabal to build the bits of Amazonka I need by putting a few <a href="https://cabal.readthedocs.io/en/3.4/cabal-project.html#specifying-packages-from-remote-version-control-locations"><code>source-repository-package</code> stanzas</a> in my config. That quickly started to look like a bit of a rabbit hole, so I decided to use Nix instead. After finding the <a href="https://stackoverflow.com/questions/54810851/how-can-i-build-a-haskell-dependency-from-a-github-source-nix-file-using-nix">perfect SO post</a> and looking up yet again how to do overrides for Haskell I ran <code>cabal2nix</code> for the three packages I need:</p>
<pre><code>cabal2nix --no-haddock --no-check --subpath amazonka \
  git://github.com/brendanhay/amazonka.git &gt; amazonka.nix
cabal2nix --no-haddock --no-check --subpath core \
  git://github.com/brendanhay/amazonka.git &gt; amazonka-core.nix
cabal2nix --no-haddock --no-check --subpath amazonka-sqs \
  git://github.com/brendanhay/amazonka.git &gt; amazonka-sqs.nix
</code></pre>
<p>The relevant part of the old Nix expression looked like this:</p>
<pre><code>thePkg = haskellPackages.developPackage {
  root = lib.cleanSource ./.;
  name = name;

  modifier = (t.flip t.pipe)
    [hl.dontHaddock
     hl.enableStaticLibraries
     hl.justStaticExecutables
     hl.disableLibraryProfiling
     hl.disableExecutableProfiling];
};</code></pre>
<p>After adding the overrides it looked like this</p>
<pre><code>hp = haskellPackages.override {
  overrides = self: super: {
    amazonka-core = self.callPackage ./amazonka-core.nix {};
    amazonka = self.callPackage ./amazonka.nix {};
    amazonka-sqs = self.callPackage ./amazonka-sqs.nix {};
  };
};

thePkg = hp.developPackage {
  root = lib.cleanSource ./.;
  name = name;

  modifier = (t.flip t.pipe)
    [hl.dontHaddock
     hl.enableStaticLibraries
     hl.justStaticExecutables
     hl.disableLibraryProfiling
     hl.disableExecutableProfiling];
};</code></pre>
<p>After a somewhat longer-than-usual build I could verify that I had indeed bumped into the same issue and my issue was a duplicate.</p>]]></summary>
</entry>
<entry>
    <title>Combining Amazonka and Conduit</title>
    <link href="http://magnus.therning.org/posts/2020-11-11-2222-combining_amazonka_and_conduit.html" />
    <id>http://magnus.therning.org/posts/2020-11-11-2222-combining_amazonka_and_conduit.html</id>
    <published>2020-11-11T00:00:00Z</published>
    <updated>2020-11-11T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Combining <a href="https://hackage.haskell.org/package/amazonka">amazonka</a> and <a href="https://hackage.haskell.org/package/conduit">conduit</a> turned out to be easier than I had expected.</p>
<p>Here’s an SNS sink I put together today</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">snsSink ::</span> (<span class="dt">MonadAWS</span> m, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">T.Text</span> <span class="ot">-&gt;</span> <span class="dt">C.ConduitT</span> <span class="dt">Value</span> <span class="dt">C.Void</span> m ()</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>snsSink topic <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  C.await <span class="op">&gt;&gt;=</span> \<span class="kw">case</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>    <span class="dt">Nothing</span> <span class="ot">-&gt;</span> <span class="fu">pure</span> ()</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>    <span class="dt">Just</span> msg <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>      _ <span class="ot">&lt;-</span> C.lift <span class="op">$</span> publishSNS topic (TL.toStrict <span class="op">$</span> TL.decodeUtf8 <span class="op">$</span> encode msg)</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>      snsSink topic</span></code></pre></div>
<p>Putting it to use can be done with something like</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a>foo <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  <span class="op">...</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>  awsEnv <span class="ot">&lt;-</span> newEnv <span class="dt">Discover</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>  runAWSCond awsEnv <span class="op">$</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>    <span class="op">&lt;</span>source producing <span class="dt">Value</span><span class="op">&gt;</span> <span class="op">.|</span> snsSink topicArn</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>    runAWSCond awsEnv <span class="ot">=</span> runResourceT <span class="op">.</span> runAWS awsEnv <span class="op">.</span> within <span class="dt">Frankfurt</span> <span class="op">.</span> C.runConduit</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>Better Nix setup for Spacemacs</title>
    <link href="http://magnus.therning.org/posts/2020-06-22-000-better-nix-setup-for-spacemacs.html" />
    <id>http://magnus.therning.org/posts/2020-06-22-000-better-nix-setup-for-spacemacs.html</id>
    <published>2020-06-22T00:00:00Z</published>
    <updated>2020-06-22T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In an <a href="/posts/2019-12-07-000-nix-setup-for-spacemacs.html">earlier post</a> I documented my setup for getting <a href="https://www.spacemacs.org/">Spacemacs</a>/Emacs to work with <a href="https://nixos.org/nix/">Nix</a>. I’ve since found a much more elegant solution based on</p>
<ul>
<li><a href="https://github.com/direnv/direnv/">direnv</a>, and</li>
<li><a href="https://github.com/wbolster/emacs-direnv">emacs-direnv</a></li>
</ul>
<p>No more Emacs packages for Nix and no need to defining functions that wrap executables in an invocation of <code>nix-shell</code>.</p>
<p>There’s a nice bonus too, with this setup I don’t need to run <code>nix-shell</code>, which always drops me at a bash prompt, instead I get a working setup in my shell of choice.</p>
<h2 id="setting-up-direnv">Setting up <code>direnv</code></h2>
<p>The steps for setting up <code>direnv</code> depends a bit on your setup, but luckily I found the <a href="https://direnv.net/#basic-installation">official instructions for installing <code>direnv</code></a> to be very clear and easy to follow. There’s not much I can add to that.</p>
<h2 id="setting-up-spacemacs">Setting up Spacemacs</h2>
<p>Since <code>emacs-direnv</code> isn’t included by default in Spacemacs I needed to do a bit of setup. I opted to create a layer for it, rather than just drop it in the list <code>dotspacemacs-additional-packages</code>. Yes, a little more complicated, but not difficult and I nurture an intention of submitting the layer for inclusion in Spacemacs itself at some point. I’ll see where that goes.</p>
<p>For now, I put the following in the file <code>~/.emacs.d/private/layers/direnv/packages.el</code>:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a>(defconst direnv-packages</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>      &#39;(direnv))</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> direnv/init-direnv </span>()</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>  (<span class="kw">use-package</span> direnv</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>    :init</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>    (direnv-mode)))</span></code></pre></div>
<h2 id="setting-up-the-project-folders">Setting up the project folders</h2>
<p>In each project folder I then add the file <code>.envrc</code> containing a single line:</p>
<pre class="shell"><code>use_nix</code></pre>
<p>Then I either run <code>direnv allow</code> from the command line, or run the function <code>direnv-allow</code> after opening the folder in Emacs.</p>
<h2 id="using-it">Using it</h2>
<p>It’s as simple as moving into the folder in a shell – all required envvars are set up on entry and unset on exit.</p>
<p>In Emacs it’s just as simple, just open a file in a project and the envvars are set. When switching to a buffer outside the project the envvars are unset.</p>
<p>There is only one little caveat, <code>nix-build</code> doesn’t work inside a Nix shell. I found out that running</p>
<pre class="shell"><code>IN_NIX_SHELL= nix-build</code></pre>
<p>does work though.</p>]]></summary>
</entry>
<entry>
    <title>X-Ray and WAI</title>
    <link href="http://magnus.therning.org/posts/2020-04-08-000-xray-and-way.html" />
    <id>http://magnus.therning.org/posts/2020-04-08-000-xray-and-way.html</id>
    <published>2020-04-08T00:00:00Z</published>
    <updated>2020-04-08T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a while we’ve been planning on introducing <a href="https://aws.amazon.com/xray/">AWS X-Ray</a> into our system at work. There’s official support for a few languages, but not too surprisingly Haskell isn’t on that list. I found <a href="https://github.com/freckle/aws-xray-client">freckle/aws-xray-client</a> on GitHub, which is so unofficial that it isn’t even published on Hackage. While it looks very good, I suspect it does more than I need and since it lacks licensing information I decided to instead implement a version tailored to our needs.</p>
<p>As a first step I implemented a WAI <em>middleware</em> that wraps an HTTP request and reports the time it took to produce a response. Between the <a href="https://docs.aws.amazon.com/xray/latest/devguide/aws-xray.html">X-Ray Developer Guide</a> and the code in Freckle’s git repo it turned out to be fairly simple.</p>
<p>First off, this is the first step towards X-Ray nirvana, so all I’m aiming for is minimal support. That means all I want is to send <a href="https://docs.aws.amazon.com/xray/latest/devguide/xray-api-segmentdocuments.html#api-segmentdocuments-fields">minimal X-Ray segment</a>s, with the small addition that I want to support <code>parent_id</code> from the start.</p>
<p>The first step then is to parse the HTTP header containing the X-Ray information – <a href="https://docs.aws.amazon.com/xray/latest/devguide/xray-concepts.html#xray-concepts-tracingheader"><code>X-Amzn-Trace-Id</code></a>. For now I’m only interested in two parts, <code>Root</code> and <code>Parent</code>, so for simplicity’s sake I use a tuple to keep them in. The idea is to take the header’s value, split on <code>;</code> to get the parts, then split each part in two, a key and a value, and put them into an association list (<code>[(Text, Text)]</code>) for easy lookup using, well <code>lookup</code>.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">parseXRayTraceIdHdr ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> <span class="dt">Maybe</span> (<span class="dt">Text</span>, <span class="dt">Maybe</span> <span class="dt">Text</span>)</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>parseXRayTraceIdHdr hdr <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  bits <span class="ot">&lt;-</span> <span class="fu">traverse</span> parseHeaderComponent <span class="op">$</span> T.split (<span class="op">==</span> <span class="ch">&#39;;&#39;</span>) hdr</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>  traceId <span class="ot">&lt;-</span> <span class="fu">lookup</span> <span class="st">&quot;Root&quot;</span> bits</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>  <span class="kw">let</span> parent <span class="ot">=</span> <span class="fu">lookup</span> <span class="st">&quot;Parent&quot;</span> bits</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>  <span class="fu">pure</span> (traceId, parent)</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a><span class="ot">parseHeaderComponent ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> <span class="dt">Maybe</span> (<span class="dt">Text</span>, <span class="dt">Text</span>)</span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>parseHeaderComponent cmp <span class="ot">=</span> <span class="kw">case</span> T.split (<span class="op">==</span> <span class="ch">&#39;=&#39;</span>) cmp <span class="kw">of</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>                            [name, value] <span class="ot">-&gt;</span> <span class="dt">Just</span> (name, value)</span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>                            _ <span class="ot">-&gt;</span> <span class="dt">Nothing</span></span></code></pre></div>
<p>The start and end times for processing a request are also required. The docs say that using at least millisecond resolution is a good idea, so I decided to do exactly that. <code>NominalDiffTime</code>, which is what <code>getPOSIXTime</code> produces, supports a resolution of picoseconds (though I doubt my system’s clock does) which requires a bit of (type-based) converting.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="ot">mkTimeInMilli ::</span> <span class="dt">IO</span> <span class="dt">Milli</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>mkTimeInMilli <span class="ot">=</span> ndfToMilli <span class="op">&lt;$&gt;</span> getPOSIXTime</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>    ndfToMilli <span class="ot">=</span> <span class="fu">fromRational</span> <span class="op">.</span> <span class="fu">toRational</span></span></code></pre></div>
<p>The last support function needed is one that creates the segment. Just building the JSON object, using <em>aeson</em>’s <code>object</code>, is enough at this point.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">mkSegment ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> <span class="dt">Milli</span> <span class="ot">-&gt;</span> <span class="dt">Milli</span> <span class="ot">-&gt;</span> (<span class="dt">Text</span>, <span class="dt">Maybe</span> <span class="dt">Text</span>) <span class="ot">-&gt;</span> <span class="dt">Value</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>mkSegment name <span class="fu">id</span> startTime endTime (root, parent) <span class="ot">=</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  object <span class="op">$</span> [ <span class="st">&quot;name&quot;</span> <span class="op">.=</span> name</span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a>           , <span class="st">&quot;id&quot;</span> <span class="op">.=</span> <span class="fu">id</span></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>           , <span class="st">&quot;trace_id&quot;</span> <span class="op">.=</span> root</span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a>           , <span class="st">&quot;start_time&quot;</span> <span class="op">.=</span> startTime</span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a>           , <span class="st">&quot;end_time&quot;</span> <span class="op">.=</span> endTime</span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a>           ] <span class="op">&lt;&gt;</span> p</span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb3-10"><a href="#cb3-10" aria-hidden="true"></a>    p <span class="ot">=</span> <span class="fu">maybe</span> [] (\ v <span class="ot">-&gt;</span> [<span class="st">&quot;parent_id&quot;</span> <span class="op">.=</span> v]) parent</span></code></pre></div>
<p>Armed with all this, I can now put together a WAI middleware that</p>
<ol type="1">
<li>records the start time of the call</li>
<li>processes the request</li>
<li>sends off the response and keeps the result of it</li>
<li>records the end time</li>
<li>parses the tracing header</li>
<li>builds the segment prepended with the <a href="https://docs.aws.amazon.com/xray/latest/devguide/xray-api-sendingdata.html#xray-api-daemon">X-Ray daemon header</a></li>
<li>sends the segment to the X-Ray daemon</li>
</ol>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">traceId ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> <span class="dt">Middleware</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>traceId xrayName app req sendResponse <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  startTime <span class="ot">&lt;-</span> mkTimeInMilli</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>  app req <span class="op">$</span> \ res <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>    rr <span class="ot">&lt;-</span> sendResponse res</span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a>    endTime <span class="ot">&lt;-</span> mkTimeInMilli</span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true"></a>    theId <span class="ot">&lt;-</span> T.pack <span class="op">.</span> (\ v <span class="ot">-&gt;</span> showHex v <span class="st">&quot;&quot;</span>) <span class="op">&lt;$&gt;</span> randomIO <span class="op">@</span><span class="dt">Word64</span></span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true"></a>    <span class="kw">let</span> traceParts <span class="ot">=</span> (decodeUtf8 <span class="op">&lt;$&gt;</span> requestHeaderTraceId req) <span class="op">&gt;&gt;=</span> parseXRayTraceIdHdr</span>
<span id="cb4-9"><a href="#cb4-9" aria-hidden="true"></a>        segment <span class="ot">=</span> mkSegment xrayName theId startTime endTime <span class="op">&lt;$&gt;</span> traceParts</span>
<span id="cb4-10"><a href="#cb4-10" aria-hidden="true"></a>    <span class="kw">case</span> segment <span class="kw">of</span></span>
<span id="cb4-11"><a href="#cb4-11" aria-hidden="true"></a>      <span class="dt">Nothing</span> <span class="ot">-&gt;</span> <span class="fu">pure</span> ()</span>
<span id="cb4-12"><a href="#cb4-12" aria-hidden="true"></a>      <span class="dt">Just</span> segment&#39; <span class="ot">-&gt;</span> sendXRayPayload <span class="op">$</span> toStrict <span class="op">$</span> prepareXRayPayload segment&#39;</span>
<span id="cb4-13"><a href="#cb4-13" aria-hidden="true"></a>    <span class="fu">pure</span> rr</span>
<span id="cb4-14"><a href="#cb4-14" aria-hidden="true"></a></span>
<span id="cb4-15"><a href="#cb4-15" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb4-16"><a href="#cb4-16" aria-hidden="true"></a>    prepareXRayPayload segment <span class="ot">=</span></span>
<span id="cb4-17"><a href="#cb4-17" aria-hidden="true"></a>      <span class="kw">let</span> header <span class="ot">=</span> object [<span class="st">&quot;format&quot;</span> <span class="op">.=</span> (<span class="st">&quot;json&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>), <span class="st">&quot;version&quot;</span> <span class="op">.=</span> (<span class="dv">1</span><span class="ot"> ::</span> <span class="dt">Int</span>)]</span>
<span id="cb4-18"><a href="#cb4-18" aria-hidden="true"></a>      <span class="kw">in</span> encode header <span class="op">&lt;&gt;</span> <span class="st">&quot;\n&quot;</span> <span class="op">&lt;&gt;</span> encode segment</span>
<span id="cb4-19"><a href="#cb4-19" aria-hidden="true"></a></span>
<span id="cb4-20"><a href="#cb4-20" aria-hidden="true"></a>    sendXRayPayload payload <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb4-21"><a href="#cb4-21" aria-hidden="true"></a>      addrInfos <span class="ot">&lt;-</span> S.getAddrInfo <span class="dt">Nothing</span> (<span class="dt">Just</span> <span class="st">&quot;127.0.0.1&quot;</span>) (<span class="dt">Just</span> <span class="st">&quot;2000&quot;</span>)</span>
<span id="cb4-22"><a href="#cb4-22" aria-hidden="true"></a>      <span class="kw">case</span> addrInfos <span class="kw">of</span></span>
<span id="cb4-23"><a href="#cb4-23" aria-hidden="true"></a>        [] <span class="ot">-&gt;</span> <span class="fu">pure</span> () <span class="co">-- silently skip</span></span>
<span id="cb4-24"><a href="#cb4-24" aria-hidden="true"></a>        (xrayAddr<span class="op">:</span>_) <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb4-25"><a href="#cb4-25" aria-hidden="true"></a>          sock <span class="ot">&lt;-</span> S.socket (S.addrFamily xrayAddr) <span class="dt">S.Datagram</span> S.defaultProtocol</span>
<span id="cb4-26"><a href="#cb4-26" aria-hidden="true"></a>          S.connect sock (S.addrAddress xrayAddr)</span>
<span id="cb4-27"><a href="#cb4-27" aria-hidden="true"></a>          sendAll sock payload</span>
<span id="cb4-28"><a href="#cb4-28" aria-hidden="true"></a>          S.close sock</span></code></pre></div>
<p>The next step will be to instrument the actual processing. The service I’m instrumenting is asynchronous, so all the work happens <em>after</em> the response has been sent. My plan for this is to use subsegments to record it. That means I’ll have to</p>
<ul>
<li>keep the <code>Root</code> and ID (<code>theId</code> in <code>traceId</code> above) for use in subsegments</li>
<li>keep the original tracing header, for use in outgoing calls</li>
<li>make sure all outgoing HTTP calls include a tracing header with a proper <code>Parent</code></li>
<li>wrap all outgoing HTTP calls with time keeping and sending a subsegment to the X-Ray daemon</li>
</ul>
<p>I’m saving that work for a rainy day though, or rather, for a day when I’m so upset at Clojure that I don’t want to see another parenthesis.</p>
<p><em>Edit (2020-04-10):</em> Corrected the segment field name for the parent ID, it should be <code>parent_id</code>.</p>]]></summary>
</entry>
<entry>
    <title>My ghcide build for Nix</title>
    <link href="http://magnus.therning.org/posts/2020-02-02-000-my-ghcide-build-for-nix.html" />
    <id>http://magnus.therning.org/posts/2020-02-02-000-my-ghcide-build-for-nix.html</id>
    <published>2020-02-02T00:00:00Z</published>
    <updated>2020-02-02T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I was slightly disappointed to find out that not all packages on Hackage that are marked as present in <a href="https://nixos.org/nixpkgs/">Nix(pkgs)</a> actually are available. Quite a few of them are marked broken and hence not installable. One of these packages is <a href="https://hackage.haskell.org/package/ghcide">ghcide</a>.</p>
<p>There are of course expressions available for getting a working <code>ghcide</code> executable installed, like <a href="https://github.com/hercules-ci/ghcide-nix">ghcide-nix</a>. However, since I have rather simple needs for my Haskell projects I thought I’d play with my own approach to it.</p>
<p>What I care about is:</p>
<ol type="1">
<li>availability of the development tools I use, at the moment it’s mainly <code>ghcide</code> but I’m planning on making use of <a href="https://hackage.haskell.org/package/ormolu">ormolu</a> in the near future</li>
<li>pre-built packages</li>
<li>ease of use</li>
</ol>
<p>So, I put together <a href="https://github.com/magthe/ghcide-for-nix">ghcide-for-nix</a>. It’s basically just a constumized Nixpkgs where the packages needed to un-break <code>ghcide</code> are present.</p>
<p>Usage is a simple <code>import</code> away:</p>
<pre><code>import (builtins.fetchGit {
  name = &quot;ghcide-for-nix&quot;;
  url = https://github.com/magthe/ghcide-for-nix;
  rev = &quot;927a8caa62cece60d9d66dbdfc62b7738d61d75f&quot;;
}) </code></pre>
<p>and it’ll give you a superset of Nixpkgs. Pre-built packages are available on <a href="https://magthe.cachix.org/">Cachix</a>.</p>
<p>It’s not sophisticated, but it’s rather easy to use and suffices for my purposes.</p>]]></summary>
</entry>
<entry>
    <title>Nix setup for Spacemacs</title>
    <link href="http://magnus.therning.org/posts/2019-12-07-000-nix-setup-for-spacemacs.html" />
    <id>http://magnus.therning.org/posts/2019-12-07-000-nix-setup-for-spacemacs.html</id>
    <published>2019-12-07T00:00:00Z</published>
    <updated>2019-12-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p><em>Edit 2020-06-22: I’ve since found a <a href="/posts/2020-06-22-000-better-nix-setup-for-spacemacs.html">better setup for this</a>.</em></p>
<p>When using <code>ghcide</code> and LSP, as I wrote about in my post on <a href="./2019-09-19-000-haskell--ghcide--and-spacemacs.html">Haskell, ghcide, and Spacemacs</a>, I found myself ending up recompiling a little too often. This pushed me to finally start looking at <a href="https://nixos.org/nix/">Nix</a>. After a bit of a fight I managed to <a href="https://discourse.nixos.org/t/import-and-use-ghide-nix">get ghcide from Nix</a>, which brought me the issue of setting up Spacemacs. Inspired by <a href="https://gist.github.com/sevanspowell/23b0135dae2834e59904a502b8a0eb5d">a gist from Samuel Evans-Powell</a> and a guide to <a href="https://github.com/thalesmg/reflex-skeleton/blob/master/Readme.org#setting-up-editor-integration-with-emacs">setting up an environment for Reflex by Thales Macedo Garitezi</a> I ended up with the following setup:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> dotspacemacs/layers </span>()</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  (setq-default</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>   ...</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>   dotspacemacs-additional-packages</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>   &#39;(</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>     nix-sandbox</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>     nix-haskell-mode</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a>     ...</span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>     )</span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>   ...</span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>   ))</span></code></pre></div>
<div class="sourceCode" id="cb2"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> dotspacemacs/user-config </span>()</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  ...</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>  (add-hook &#39;haskell-mode-hook #&#39;lsp)</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>  (add-hook &#39;haskell-mode-hook &#39;nix-haskell-mode)</span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>  (add-hook &#39;haskell-mode-hook</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>            (<span class="kw">lambda</span> ()</span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>              (setq-local flycheck-executable-find</span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>                          (<span class="kw">lambda</span> (cmd)</span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true"></a>                            (nix-executable-find (nix-current-sandbox) cmd)))</span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true"></a>              (setq-local flycheck-command-wrapper-function</span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true"></a>                          (<span class="kw">lambda</span> (argv)</span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true"></a>                            (<span class="kw">apply</span> &#39;nix-shell-command (nix-current-sandbox) argv)))</span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true"></a>              (setq-local haskell-process-wrapper-function</span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true"></a>                          (<span class="kw">lambda</span> (argv)</span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true"></a>                            (<span class="kw">apply</span> &#39;nix-shell-command (nix-current-sandbox) argv)))</span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true"></a>              (setq-local lsp-haskell-process-wrapper-function</span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true"></a>                          (<span class="kw">lambda</span> (argv)</span>
<span id="cb2-18"><a href="#cb2-18" aria-hidden="true"></a>                            `(<span class="st">&quot;nix-shell&quot;</span> <span class="st">&quot;-I&quot;</span> <span class="st">&quot;.&quot;</span> <span class="st">&quot;--command&quot;</span> <span class="st">&quot;ghcide --lsp&quot;</span> ,(nix-current-sandbox))))))</span>
<span id="cb2-19"><a href="#cb2-19" aria-hidden="true"></a>  (add-hook &#39;haskell-mode-hook</span>
<span id="cb2-20"><a href="#cb2-20" aria-hidden="true"></a>            (<span class="kw">lambda</span> ()</span>
<span id="cb2-21"><a href="#cb2-21" aria-hidden="true"></a>              (flycheck-add-next-checker &#39;lsp-ui &#39;(<span class="kw">warning</span> . haskell-stack-ghc))))</span>
<span id="cb2-22"><a href="#cb2-22" aria-hidden="true"></a>  ...</span>
<span id="cb2-23"><a href="#cb2-23" aria-hidden="true"></a>  )</span></code></pre></div>
<p>It seems to work, but please let me know if you have suggestions for improvements.</p>]]></summary>
</entry>
<entry>
    <title>Populating Projectile's cache</title>
    <link href="http://magnus.therning.org/posts/2019-11-05-000-populating-projectile-s-cache.html" />
    <id>http://magnus.therning.org/posts/2019-11-05-000-populating-projectile-s-cache.html</id>
    <published>2019-11-05T00:00:00Z</published>
    <updated>2019-11-05T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>As I track the <em>develop</em> branch of <a href="https://github.com/syl20bnr/spacemacs/">Spacemacs</a> I occasionally clean out my cache of projects known to Projectile. Every time it takes a while before I’m back at a stage where I very rarely have to visit something that isn’t already in the cache.</p>
<p>However, today I found the function <code>projectile-add-known-project</code>, which prompted me to write the following function that’ll help me quickly re-building the cache the next time I need to reset Spacemacs to a known state again.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode lisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> projectile-extra-add-projects-in-subfolders </span>(projects-root)</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  (interactive (<span class="kw">list</span> (read-directory-name <span class="st">&quot;Add to known projects: &quot;</span>)))</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  (message <span class="st">&quot;Searching for projects in %s...&quot;</span> projects-root)</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>  (<span class="kw">let</span> ((dirs (seq-map &#39;file-name-directory (directory-files-recursively projects-root <span class="st">&quot;^.git$&quot;</span> <span class="kw">t</span>))))</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>    (seq-do &#39;projectile-add-known-project dirs)</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>    (message <span class="st">&quot;Added %d projects&quot;</span> (<span class="kw">length</span> dirs))))</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>Ditaa in Org mode</title>
    <link href="http://magnus.therning.org/posts/2019-10-20-000-ditaa-in-org-mode.html" />
    <id>http://magnus.therning.org/posts/2019-10-20-000-ditaa-in-org-mode.html</id>
    <published>2019-10-20T00:00:00Z</published>
    <updated>2019-10-20T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Just found out that Emacs ships with Babel support for <a href="https://writequit.org/denver-emacs/presentations/2016-04-19-whats-new-emacs25-ditaa-artist.html#orgheadline15">ditaa</a> (yes, I’m late to the party).</p>
<p>Sweet! That is yet another argument for converting all our <code>README.md</code>s into <code>README.org</code>s at work.</p>
<p><img src="https://media.giphy.com/media/xl5QdxfNonh3q/giphy.gif" title="Dr. Evil laughing" alt="Dr. Evil" /> </p>
<p>The changes I made to my Spacemacs config are</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode lisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> dotspacemacs/user-config </span>()</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  ...</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  (with-eval-after-load &#39;org</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>    ...</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>    (add-to-list &#39;org-babel-load-languages &#39;(ditaa . <span class="kw">t</span>))</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>    (<span class="kw">setq</span> org-ditaa-jar-path <span class="st">&quot;/usr/share/java/ditaa/ditaa-0.11.jar&quot;</span>))</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>  ...)</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>Haskell, ghcide, and Spacemacs</title>
    <link href="http://magnus.therning.org/posts/2019-09-19-000-haskell--ghcide--and-spacemacs.html" />
    <id>http://magnus.therning.org/posts/2019-09-19-000-haskell--ghcide--and-spacemacs.html</id>
    <published>2019-09-19T00:00:00Z</published>
    <updated>2019-09-19T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other day I read Chris Penner’s post on <a href="https://chrispenner.ca/posts/hie-core">Haskell IDE Support</a> and thought I’d make an attempt to use it with Spacemacs.</p>
<p>After running <code>stack build hie-bios ghcide haskell-lsp --copy-compiler-tool</code> I had a look at the <a href="https://github.com/haskell/haskell-ide-engine#using-hie-with-spacemacs">instructions on using <code>haskell-ide-engine</code> with Spacemacs</a>. After a bit of trial and error I came up with these changes to my <code>~/.spacemacs</code>:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> dotspacemacs/layers </span>()</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  (setq-default</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>   dotspacemacs-configuration-layers</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>   &#39;(</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>    ...</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>    lsp</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>    (haskell :variables</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a>             haskell-completion-backend &#39;lsp</span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>             )</span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>    ...)</span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>  )</span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a>)</span></code></pre></div>
<div class="sourceCode" id="cb2"><pre class="sourceCode commonlisp"><code class="sourceCode commonlisp"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a>(<span class="kw">defun</span><span class="fu"> dotspacemacs/user-config </span>()</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  (<span class="kw">setq</span> lsp-haskell-process-args-hie &#39;(<span class="st">&quot;exec&quot;</span> <span class="st">&quot;ghcide&quot;</span> <span class="st">&quot;--&quot;</span> <span class="st">&quot;--lsp&quot;</span>)</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>        lsp-haskell-process-path-hie <span class="st">&quot;stack&quot;</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>        lsp-haskell-process-wrapper-function (<span class="kw">lambda</span> (argv) (<span class="kw">cons</span> (<span class="kw">car</span> argv) (<span class="kw">cddr</span> argv)))</span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>        )</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>  (add-hook &#39;haskell-mode-hook</span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>            #&#39;lsp)</span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>)</span></code></pre></div>
<p>The slightly weird looking <code>lsp-haskell-process-wrapper-function</code> is removing the pesky <code>--lsp</code> inserted by <a href="https://github.com/emacs-lsp/lsp-haskell/blob/64106be79350f9ce6903d22c66b29761dadb5001/lsp-haskell.el#L179">this line</a>.</p>
<p>That seems to work. Though I have to say I’m not ready to switch from <a href="https://hackage.haskell.org/package/intero">intero</a> just yet. Two things in particular didn’t work with <code>ghcide</code>/LSP:</p>
<ol type="1">
<li>Switching from one the <code>Main.hs</code> in one executable to the <code>Main.hs</code> of another executable in the same project didn’t work as expected – I had hints and types in the first, but nothing in the second.</li>
<li>Jump to the definition of a function defined in the package didn’t work – I’m not willing to use <a href="https://www.gnu.org/software/global/">GNU GLOBAL</a> or some other source tagging system.</li>
</ol>]]></summary>
</entry>
<entry>
    <title>Nested tmux</title>
    <link href="http://magnus.therning.org/posts/2019-09-15-000-nested-tmux.html" />
    <id>http://magnus.therning.org/posts/2019-09-15-000-nested-tmux.html</id>
    <published>2019-09-15T00:00:00Z</published>
    <updated>2019-09-15T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I’ve finally gotten around to sorting out running nested tmux instances. I found the base for the configuration in the article <a href="https://is.gd/slxE45">Tmux in practice: local and nested remote tmux sessions</a>, which links a few other related resources.</p>
<p>What I ended up with was this:</p>
<pre><code># Toggle tmux keybindings on/off, for use with inner tmux
# https://is.gd/slxE45
bind -T root F12  \
  set prefix None \;\
  set key-table off \;\
  set status-left &quot;#[fg=black,bg=blue,bold] OFF &quot; \;\
  refresh-client -S

bind -T off F12 \
  set -u prefix \;\
  set -u key-table \;\
  set -u status-left \;\
  refresh-client -S</code></pre>
<p>It’s slightly simpler than what’s in the article above, but it works and it fits rather nicely with the <a href="https://github.com/arcticicestudio/nord-tmux">nord theme</a>.</p>]]></summary>
</entry>
<entry>
    <title>Hedgehog on a REST API, part 3</title>
    <link href="http://magnus.therning.org/posts/2019-08-19-000-hedgehog-on-a-rest-api--part-3.html" />
    <id>http://magnus.therning.org/posts/2019-08-19-000-hedgehog-on-a-rest-api--part-3.html</id>
    <published>2019-08-19T00:00:00Z</published>
    <updated>2019-08-19T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In my previous post on using Hedgehog on a REST API, <a href="/posts/2019-06-23-000-hedgehog-on-a-rest-api--part-2.html">Hedgehog on a REST API, part 2</a> I ran the test a few times and adjusted the model to deal with the incorrect assumptions I had initially made. In particular, I had to adjust how I modelled the <em>User ID</em>. Because of the simplicity of the API that wasn’t too difficult. However, that kind of completely predictable ID isn’t found in all APIs. In fact, it’s not uncommon to have completely random IDs in API (often they are <a href="https://en.wikipedia.org/wiki/Universally_unique_identifier">UUID</a>s).</p>
<p>So, I set out to try to deal with that. I’m still using the simple API from the previous posts, but this time I’m pretending that I can’t build the ID into the model myself, or, put another way, I’m capturing the ID from the responses.</p>
<h2 id="the-model-state">The model state</h2>
<p>When capturing the ID it’s no longer possible to use a simple <code>Map Int Text</code> for the state, because I don’t actually have the ID until I have an HTTP response. However, the ID is playing an important role in the constructing of a sequence of actions. The trick is to use <code>Var Int v</code> instead of an ordinary <code>Int</code>. As I understand it, and I believe that’s a good enough understanding to make use of Hedgehog possible, is that this way the ID is an opaque blob in the construction phase, and it’s turned into a concrete value during execution. When in the opaque state it implements enough type classes to be useful for my purposes.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">State</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>)<span class="ot">=</span> <span class="dt">State</span> (<span class="dt">M.Map</span> (<span class="dt">Var</span> <span class="dt">Int</span> v) <span class="dt">Text</span>)</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span></code></pre></div>
<h2 id="the-api-calls-add-user">The API calls: add user</h2>
<p>When taking a closer look at the <a href="https://hackage.haskell.org/package/hedgehog-0.5.3/docs/Hedgehog.html#t:Callback"><code>Callback</code></a> type not all the callbacks will get the state in the same form, opaque or concrete, and one of them, <code>Update</code> actually receives the state in both states depending on the phase of execution. This has the most impact on the add user action. To deal with it there’s a need to rearrange the code a bit, to be specific, <code>commandExecute</code> can no longer return a tuple of both the ID and the status of the HTTP response because the update function can’t reach into the tuple, which it needs to update the state.</p>
<p>That means the <code>commandExecute</code> function will have to do tests too. It <em>is</em> nice to keep all tests in the callbacks, but by sticking a <code>MonadTest m</code> constraint on the <code>commandExecute</code> it turns into a nice solution anyway.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="ot">addUser ::</span> (<span class="dt">MonadGen</span> n, <span class="dt">MonadIO</span> m, <span class="dt">MonadTest</span> m) <span class="ot">=&gt;</span> <span class="dt">Command</span> n m <span class="dt">State</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>addUser <span class="ot">=</span> <span class="dt">Command</span> gen exec [ <span class="dt">Update</span> u</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>                           ]</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>    gen _ <span class="ot">=</span> <span class="dt">Just</span> <span class="op">$</span> <span class="dt">AddUser</span> <span class="op">&lt;$&gt;</span> Gen.text (Range.linear <span class="dv">0</span> <span class="dv">42</span>) Gen.alpha</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>    exec (<span class="dt">AddUser</span> n) <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>      (s, ui) <span class="ot">&lt;-</span> liftIO <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true"></a>        mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true"></a>        addReq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/users&quot;</span></span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true"></a>        <span class="kw">let</span> addReq&#39; <span class="ot">=</span> addReq { requestBody <span class="ot">=</span> <span class="dt">RequestBodyLBS</span> (encode <span class="op">$</span> <span class="dt">User</span> <span class="dv">0</span> n)}</span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true"></a>        addResp <span class="ot">&lt;-</span> httpLbs addReq&#39; mgr</span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true"></a>        <span class="kw">let</span> user <span class="ot">=</span> decode (responseBody addResp)<span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">User</span></span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true"></a>        <span class="fu">return</span> (responseStatus addResp, user)</span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true"></a>      status201 <span class="op">===</span> s</span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true"></a>      assert <span class="op">$</span> isJust ui</span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true"></a>      (userName <span class="op">&lt;$&gt;</span> ui) <span class="op">===</span> <span class="dt">Just</span> n</span>
<span id="cb2-18"><a href="#cb2-18" aria-hidden="true"></a>      <span class="fu">return</span> <span class="op">$</span> userId <span class="op">$</span> fromJust ui</span>
<span id="cb2-19"><a href="#cb2-19" aria-hidden="true"></a></span>
<span id="cb2-20"><a href="#cb2-20" aria-hidden="true"></a>    u (<span class="dt">State</span> m) (<span class="dt">AddUser</span> n) o <span class="ot">=</span> <span class="dt">State</span> (M.insert o n m)</span></code></pre></div>
<p>I found that once I’d come around to folding the <code>Ensure</code> callback into the <code>commandExecute</code> function the rest fell out from the types.</p>
<h2 id="the-api-calls-delete-user">The API calls: delete user</h2>
<p>The other actions, deleting a user and getting a user, required only minor changes and the changes were rather similar in both cases.</p>
<p>Not the type for the action needs to take a <code>Var Int v</code> instead of just a plain <code>Int</code>.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">DeleteUser</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>) <span class="ot">=</span> <span class="dt">DeleteUser</span> (<span class="dt">Var</span> <span class="dt">Int</span> v)</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span></code></pre></div>
<p>Which in turn affect the implementation of <code>HTraversable</code></p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HTraversable</span> <span class="dt">DeleteUser</span> <span class="kw">where</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>  htraverse f (<span class="dt">DeleteUser</span> vi) <span class="ot">=</span> <span class="dt">DeleteUser</span> <span class="op">&lt;$&gt;</span> htraverse f vi</span></code></pre></div>
<p>Then the changes to the <code>Command</code> mostly comprise use of <a href="https://hackage.haskell.org/package/hedgehog-0.5.3/docs/Hedgehog.html#v:concrete"><code>concrete</code></a> in places where the real ID is needed.</p>
<pre class="haskll"><code>deleteUser :: (MonadGen n, MonadIO m) =&gt; Command n m State
deleteUser = Command gen exec [ Update u
                              , Require r
                              , Ensure e
                              ]
  where
    gen (State m) = case M.keys m of
      [] -&gt; Nothing
      ks -&gt; Just $ DeleteUser &lt;$&gt; Gen.element ks

    exec (DeleteUser vi) = liftIO $ do
      mgr &lt;- newManager defaultManagerSettings
      delReq &lt;- parseRequest $ &quot;DELETE http://localhost:3000/users/&quot; ++ show (concrete vi)
      delResp &lt;- httpNoBody delReq mgr
      return $ responseStatus delResp

    u (State m) (DeleteUser i) _ = State $ M.delete i m

    r (State m) (DeleteUser i) = i `elem` M.keys m

    e _ _ (DeleteUser _) r = r === status200</code></pre>
<h2 id="conclusion">Conclusion</h2>
<p>This post concludes my playing around with state machines in Hedgehog for this time. I certainly hope I find the time to put it to use on some larger API soon. In particular I’d love to put it to use at work; I think it’d be an excellent addition to the integration tests we currently have.</p>]]></summary>
</entry>
<entry>
    <title>Architecture of a service</title>
    <link href="http://magnus.therning.org/posts/2019-08-10-000-architecture-of-a-service.html" />
    <id>http://magnus.therning.org/posts/2019-08-10-000-architecture-of-a-service.html</id>
    <published>2019-08-10T00:00:00Z</published>
    <updated>2019-08-10T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Early this summer it was finally time to put this one service I’ve been working on into our sandbox environment. It’s been running without hickups so last week I turned it on for production as well. In this post I thought I’d document the how and why of the service in the hope that someone will find it useful.</p>
<p>The service functions as an interface to external SMS-sending services, offering a single place to change if we find that we are unhappy with the service we’re using.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> This service replaces an older one, written in Ruby and no one really dares touch it. Hopefully the Haskell version will prove to be a joy to work with over time.</p>
<h2 id="overview-of-the-architecture">Overview of the architecture</h2>
<p>The service is split into two parts, one web server using <a href="https://hackage.haskell.org/package/scotty">scotty</a>, and streaming data processing using <a href="https://hackage.haskell.org/package/conduit">conduit</a>. Persistent storage is provided by a <a href="https://www.postgresql.org/">PostgreSQL</a> database. The general idea is that events are picked up from the database, acted upon, which in turn results in other events which written to the database. Those are then picked up and round and round we go. The web service accepts requests, turns them into events and writes the to the database.</p>
<p>Hopefully this crude diagram clarifies it somewhat.</p>
<center>
<img src="/imgs/2019-08-10-architecture.png" title="fig:" alt="Diagram of the service architecture" />
</center>
<p>There are a few things that might need some explanation</p>
<ul>
<li><p>In the past we’ve wanted to have the option to use multiple external SMS services at the same time. One is randomly chosen as the request comes in. There’s also a possibility to configure the frequency for each external service.</p>
<p><em>Picker</em> implements the random picking and I’ve written about that earlier in <a href="/posts/2019-02-17-000-choosing-a-conduit-randomly.html">Choosing a conduit randomly</a>.</p>
<p><em>Success</em> and <em>fail</em> are dummy senders. They don’t actually send anything, and the former succeeds at it while the latter fails. I found them useful for manual testing.</p></li>
<li><p>Successfully sending off a request to an external SMS service, getting status 200 back, doesn’t actually mean that the SMS has been sent, or even that it ever will be. Due to the nature of SMS messaging there are no guarantees of timeliness at all. Since we are interested in finding out whether an SMS actually is sent a delayed action is scheduled, which will fetch the status of a sent SMS after a certain time (currently 2 minutes). If an SMS hasn’t been sent after that time it might as well never be – it’s too slow for our end-users.</p>
<p>This is what <em>report-fetcher</em> and <em>fetcher-func</em> do.</p></li>
<li><p>The <em>queue sink</em> and <em>queue src</em> are actually <a href="https://hackage.haskell.org/package/stm-conduit-4.0.1/docs/Data-Conduit-TQueue.html#v:sourceTQueue"><code>sourceTQueue</code></a> and <a href="https://hackage.haskell.org/package/stm-conduit-4.0.1/docs/Data-Conduit-TQueue.html#v:sinkTQueue"><code>sinkTQueue</code></a>. Splitting the stream like that makes it trivial to push in events by using <a href="https://hackage.haskell.org/package/stm-2.5.0.0/docs/Control-Concurrent-STM-TQueue.html#v:writeTQueue"><code>writeTQueue</code></a>.</p></li>
<li><p>I use <a href="https://hackage.haskell.org/package/conduit-1.3.1.1/docs/Data-Conduit.html#v:sequenceConduits"><code>sequenceConduits</code></a> in order to send a single event to multiple <code>Conduit</code>s and then combine all their results back into a single stream. The ease with which this can be done in <a href="https://hackage.haskell.org/package/conduit">conduit</a> is one of the main reasons why I choose to use it.<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a></p></li>
</ul>
<h2 id="effects-and-tests">Effects and tests</h2>
<p>I started out writing everything based on a type like <code>ReaderT &lt;my cfg type&gt; IO</code> and using <code>liftIO</code> for effects that needed lifting. This worked nicely while I was setting up the basic structure of the service, but as soon as I hooked in the database I really wanted to do some testing also of the effectful code.</p>
<p>After reading <a href="https://serokell.io/blog/2018/12/07/tagless-final">Introduction to Tagless Final</a> and <a href="https://www.fpcomplete.com/blog/2017/06/readert-design-pattern">The ReaderT Design Patter</a>, playing a bit with both approaches, and writing <a href="/posts/2019-01-20-000-tagless-final-and-scotty.html">Tagless final and Scotty</a> and <a href="/posts/2019-02-02-000-the-readert-design-pattern-or-tagless-final-.html">The ReaderT design pattern or tagless final?</a>, I finally chose to go down the route of <em>tagless final</em>. There’s no strong reason for that decision, maybe it was just because I read about it first and found it very easy to move in that direction in small steps.</p>
<p>There’s a split between property tests and unit tests:</p>
<ul>
<li><p>Data types, their monad instances (like JSON (de-)serialisation), pure functions and a few effects are tested using properties. I’m using <a href="https://hackage.haskell.org/package/QuickCheck">QuickCheck</a> for that. I’ve since looked a little closer at <a href="https://hackage.haskell.org/package/hedgehog">hedgehog</a> and if I were to do a major overhaul of the property tests I might be tempted to rewrite them using that library instead.</p></li>
<li><p>Most of the <code>Conduit</code>s are tested using <a href="https://hackage.haskell.org/package/HUnit">HUnit</a>.</p></li>
</ul>
<h2 id="configuration">Configuration</h2>
<p>The service will be run in a container and we try to follow the <a href="https://12factor.net/">12-factor app rules</a>, where the third one says that configuration should be stored in the environment. All previous Haskell projects I’ve worked on have been command line tools were configuration is done (mostly) using command line argument. For that I usually use <a href="https://hackage.haskell.org/package/optparse-applicative">optparse-applicative</a>, but it’s not applicable in this setting.</p>
<p>After a bit of searching on <a href="https://hackage.haskell.org/">hackage</a> I settled on <a href="https://hackage.haskell.org/package/etc">etc</a>. It turned out to be nice an easy to work with. The configuration is written in JSON and only specifies environment variables. It’s then embedded in the executable using <a href="https://hackage.haskell.org/package/file-embed">file-embed</a>. The only thing I miss is a <a href="https://hackage.haskell.org/package/aeson/docs/Data-Aeson.html#t:ToJSON"><code>ToJSON</code></a> instance for <a href="https://hackage.haskell.org/package/etc-0.4.1.0/docs/System-Etc.html#t:Config"><code>Config</code></a> – we’ve found it quite useful to log the active configuration when starting a service and that log entry would become a bit nicer if the message was JSON rather than the (somewhat difficult to read) string that <code>Config</code>’s <code>Show</code> instance produces.</p>
<h2 id="logging">Logging</h2>
<p>There are two requirements we have when it comes to logging</p>
<ol type="1">
<li>All log entries tied to a request should have a <a href="https://hilton.org.uk/blog/microservices-correlation-id">correlation ID</a>.</li>
<li>Log requests and responses</li>
</ol>
<p>I’ve written about correlation ID before, <a href="http://magnus.therning.org/posts/2018-10-01-000-using-a-configuration-in-scotty.html">Using a configuration in Scotty</a>.</p>
<p>Logging requests and responses is an area where I’m not very happy with <a href="https://hackage.haskell.org/package/scotty">scotty</a>. It feels natural to solve it using middleware (i.e. using <a href="https://hackage.haskell.org/package/scotty/docs/Web-Scotty.html#v:middleware"><code>middleware</code></a>) but the representation, especially of responses, is a bit complicated so for the time being I’ve skipped logging the body of both. I’d be most interested to hear of libraries that could make that easier.</p>
<h2 id="data-storage-and-picking-up-new-events">Data storage and picking up new events</h2>
<p>The data stream processing depends heavily on being able to pick up when new events are written to the database. Especially when there are more than one instance running (we usually have at least two instance running in the production environment). To get that working I’ve used <a href="https://hackage.haskell.org/package/postgresql-simple">postgresql-simple</a>’s support for <a href="https://www.postgresql.org/docs/current/sql-listen.html"><code>LISTEN</code></a> and <a href="https://www.postgresql.org/docs/current/sql-notify.html"><code>NOTIFY</code></a> via the function <a href="https://hackage.haskell.org/package/postgresql-simple-0.6.2/docs/Database-PostgreSQL-Simple-Notification.html#v:getNotification"><code>getNotification</code></a>.</p>
<p>When I wrote about this earlier, <a href="http://magnus.therning.org/posts/2019-03-03-000-conduit-and-postgresql.html">Conduit and PostgreSQL</a> I got some really good feedback that made my solution more robust.</p>
<h2 id="delayed-actions">Delayed actions</h2>
<p>Some things in Haskell feel almost like cheating. The light-weight threading makes me confident that a <a href="https://hackage.haskell.org/package/base/docs/Control-Concurrent.html#v:forkIO"><code>forkIO</code></a> followed by a <a href="https://hackage.haskell.org/package/base/docs/Control-Concurrent.html#v:threadDelay"><code>threadDelay</code></a> (or in my case, the ones from <a href="https://hackage.haskell.org/package/unliftio">unliftio</a>) will suffice.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>It has happened in the past that we’ve changed SMS service after finding that they weren’t living up to our expectations.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>A while ago I was experimenting with other streaming libraries, but I gave up on getting re-combination to work – <a href="/posts/2018-10-16-000-zipping-streams.html">Zipping streams</a><a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Elasticsearch, types and indices</title>
    <link href="http://magnus.therning.org/posts/2019-07-06-000-elasticsearch--types-and-indices.html" />
    <id>http://magnus.therning.org/posts/2019-07-06-000-elasticsearch--types-and-indices.html</id>
    <published>2019-07-06T00:00:00Z</published>
    <updated>2019-07-06T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other day I added some more logging into a service at work, but not all logs appeared in Kibana. Some messages got lost between CloudWatch Logs and Elasticsearch. After turning up the logging in the Lambda shuffling log messages I was in for a bit of learning about Elasticsearch.</p>
<p>Running the following in a Kibana console will show what the issue was</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode json"><code class="sourceCode json"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="er">PUT</span> <span class="er">idx-0/_doc/1</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="fu">{</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  <span class="dt">&quot;name&quot;</span><span class="fu">:</span> <span class="st">&quot;John&quot;</span><span class="fu">,</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>  <span class="dt">&quot;full_name&quot;</span><span class="fu">:</span> <span class="st">&quot;John Doe&quot;</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a><span class="fu">}</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a><span class="er">PUT</span> <span class="er">idx-0/_doc/2</span></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a><span class="fu">{</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>  <span class="dt">&quot;name&quot;</span><span class="fu">:</span> <span class="st">&quot;Jane&quot;</span><span class="fu">,</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>  <span class="dt">&quot;full_name&quot;</span><span class="fu">:</span> <span class="fu">{</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>    <span class="dt">&quot;first&quot;</span><span class="fu">:</span> <span class="st">&quot;Jane&quot;</span><span class="fu">,</span></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a>    <span class="dt">&quot;last&quot;</span><span class="fu">:</span> <span class="st">&quot;Doe&quot;</span></span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true"></a>  <span class="fu">}</span></span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true"></a><span class="fu">}</span></span></code></pre></div>
<p>Executing them in order results in the following error on the second command</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode json"><code class="sourceCode json"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="fu">{</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  <span class="dt">&quot;error&quot;</span><span class="fu">:</span> <span class="fu">{</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>    <span class="dt">&quot;root_cause&quot;</span><span class="fu">:</span> <span class="ot">[</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>      <span class="fu">{</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>        <span class="dt">&quot;type&quot;</span><span class="fu">:</span> <span class="st">&quot;mapper_parsing_exception&quot;</span><span class="fu">,</span></span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>        <span class="dt">&quot;reason&quot;</span><span class="fu">:</span> <span class="st">&quot;failed to parse field [full_name] of type [text]&quot;</span></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>      <span class="fu">}</span></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>    <span class="ot">]</span><span class="fu">,</span></span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true"></a>    <span class="dt">&quot;type&quot;</span><span class="fu">:</span> <span class="st">&quot;mapper_parsing_exception&quot;</span><span class="fu">,</span></span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true"></a>    <span class="dt">&quot;reason&quot;</span><span class="fu">:</span> <span class="st">&quot;failed to parse field [full_name] of type [text]&quot;</span><span class="fu">,</span></span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true"></a>    <span class="dt">&quot;caused_by&quot;</span><span class="fu">:</span> <span class="fu">{</span></span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true"></a>      <span class="dt">&quot;type&quot;</span><span class="fu">:</span> <span class="st">&quot;illegal_state_exception&quot;</span><span class="fu">,</span></span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true"></a>      <span class="dt">&quot;reason&quot;</span><span class="fu">:</span> <span class="st">&quot;Can&#39;t get text on a START_OBJECT at 3:16&quot;</span></span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true"></a>    <span class="fu">}</span></span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true"></a>  <span class="fu">},</span></span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true"></a>  <span class="dt">&quot;status&quot;</span><span class="fu">:</span> <span class="dv">400</span></span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true"></a><span class="fu">}</span></span></code></pre></div>
<p>The reason for this is that a schema for the data is built up dynamically as documents are pushed in.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> It is possible to turn off dynamic schema building for an index using a <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/dynamic-mapping.html">mapping</a>. For the documents above it’d look something lik this</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode json"><code class="sourceCode json"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="er">PUT</span> <span class="er">idx-0</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a><span class="fu">{</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  <span class="dt">&quot;mappings&quot;</span><span class="fu">:</span> <span class="fu">{</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a>    <span class="dt">&quot;_doc&quot;</span><span class="fu">:</span> <span class="fu">{</span></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>      <span class="dt">&quot;dynamic&quot;</span><span class="fu">:</span> <span class="kw">false</span></span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a>    <span class="fu">}</span></span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a>  <span class="fu">}</span></span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a><span class="fu">}</span></span></code></pre></div>
<p>Now it’s possible to push both documents, however searching is not possible, because, as the <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/dynamic.html">documentation for <code>dynamic</code></a> says:</p>
<blockquote>
<p>fields will not be indexed so will not be searchable but will still appear in the _source field of returned hits</p>
</blockquote>
<p>If there’s something that determines the value of logs it’s them being searchable.</p>
<p>As far as I understand one solution to all of this would have been <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/mapping.html#mapping-type">mapping types</a>, but that’s being removed (see <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/removal-of-types.html">removal of mapping types</a>) so isn’t a solution. I’m not sure if Elasticsearch offers any <em>good</em> solution to it nowadays. There’s however a workaround, more indices.</p>
<p>Using two indices instead of one does work. So modifying the first commands to use separate indices works.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode json"><code class="sourceCode json"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="er">PUT</span> <span class="er">idx-0/_doc/1</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a><span class="fu">{</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  <span class="dt">&quot;name&quot;</span><span class="fu">:</span> <span class="st">&quot;John&quot;</span><span class="fu">,</span></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>  <span class="dt">&quot;full_name&quot;</span><span class="fu">:</span> <span class="st">&quot;John Doe&quot;</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a><span class="fu">}</span></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a></span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true"></a><span class="er">PUT</span> <span class="er">idx-1/_doc/1</span></span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true"></a><span class="fu">{</span></span>
<span id="cb4-9"><a href="#cb4-9" aria-hidden="true"></a>  <span class="dt">&quot;name&quot;</span><span class="fu">:</span> <span class="st">&quot;Jane&quot;</span><span class="fu">,</span></span>
<span id="cb4-10"><a href="#cb4-10" aria-hidden="true"></a>  <span class="dt">&quot;full_name&quot;</span><span class="fu">:</span> <span class="fu">{</span></span>
<span id="cb4-11"><a href="#cb4-11" aria-hidden="true"></a>    <span class="dt">&quot;first&quot;</span><span class="fu">:</span> <span class="st">&quot;Jane&quot;</span><span class="fu">,</span></span>
<span id="cb4-12"><a href="#cb4-12" aria-hidden="true"></a>    <span class="dt">&quot;last&quot;</span><span class="fu">:</span> <span class="st">&quot;Doe&quot;</span></span>
<span id="cb4-13"><a href="#cb4-13" aria-hidden="true"></a>  <span class="fu">}</span></span>
<span id="cb4-14"><a href="#cb4-14" aria-hidden="true"></a><span class="fu">}</span></span></code></pre></div>
<p>When creating an index pattern for <code>idx-*</code> there’s a warning about many analysis functions not working due to the type conflict. However, searching <em>does</em> work and that’s all I really care about in this case.</p>
<p>When shuffling the logs from CloudWatch Logs to Elasticsearch we already use multiple indices. They’re constructed based on service name, deploy environment (<em>staging</em>, <em>production</em>) and date (a new index each day). To deal with these type conflicts I added a <em>log type</em> that’s taken out of the log message itself. It’s not an elegant solution – it puts the solution into the services themselves – but it’s acceptable.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Something that makes me wonder what the definition of <em>schema-free</em> is. I sure didn’t expect there to ever be a type constraint preventing pushing a document into something that’s called schema-free (see the <a href="https://en.wikipedia.org/wiki/Elasticsearch">Wikipedia article</a>). (The initiated say it’s Lucene, not Elasticsearch, but to me that doesn’t make any difference at all.)<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Hedgehog on a REST API, part 2</title>
    <link href="http://magnus.therning.org/posts/2019-06-23-000-hedgehog-on-a-rest-api--part-2.html" />
    <id>http://magnus.therning.org/posts/2019-06-23-000-hedgehog-on-a-rest-api--part-2.html</id>
    <published>2019-06-23T00:00:00Z</published>
    <updated>2019-06-23T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>This is a short follow-up to <a href="/posts/2019-05-30-000-hedgehog-on-a-rest-api.html">Hedgehog on a REST API</a> where I actually run the tests in that post.</p>
<h2 id="fixing-an-issue-with-the-model">Fixing an issue with the model</h2>
<p>The first issue I run into is</p>
<pre><code>━━━ Main ━━━
  ✗ sequential failed after 18 tests and 1 shrink.
  
        ┏━━ tst/test-01.hs ━━━
     89 ┃ getUser :: (MonadGen n, MonadIO m) =&gt; Command n m State
     90 ┃ getUser = Command gen exec [ Require r
     91 ┃                            , Ensure e
     92 ┃                            ]
     93 ┃   where
     94 ┃     gen (State m) = case M.keys m of
     95 ┃       [] -&gt; Nothing
     96 ┃       ks -&gt; Just $ GetUser &lt;$&gt; Gen.element ks
     97 ┃ 
     98 ┃     exec (GetUser i) = liftIO $ do
     99 ┃       mgr &lt;- newManager defaultManagerSettings
    100 ┃       getReq &lt;- parseRequest $ &quot;GET http://localhost:3000/users/&quot; ++ show i
    101 ┃       getResp &lt;- httpLbs getReq mgr
    102 ┃       let us = decode $ responseBody getResp :: Maybe [User]
    103 ┃       return (status200 == responseStatus getResp, us)
    104 ┃ 
    105 ┃     r (State m) (GetUser i) = i `elem` M.keys m
    106 ┃ 
    107 ┃     e _ _ (GetUser _) (r, us) = do
    108 ┃       r === True
    109 ┃       assert $ isJust us
    110 ┃       (length &lt;$&gt; us) === Just 1
        ┃       ^^^^^^^^^^^^^^^^^^^^^^^^^^
        ┃       │ Failed (- lhs =/= + rhs)
        ┃       │ - Just 0
        ┃       │ + Just 1
    
        ┏━━ tst/test-01.hs ━━━
    118 ┃ prop_seq :: Property
    119 ┃ prop_seq = property $ do
    120 ┃   actions &lt;- forAll $ Gen.sequential (Range.linear 1 10) initialState [addUser, deleteUser, getUser]
        ┃   │ Var 0 = AddUser &quot;&quot;
        ┃   │ Var 1 = GetUser 1
    121 ┃   resetWS
    122 ┃   executeSequential initialState actions
    
    This failure can be reproduced by running:
    &gt; recheck (Size 17) (Seed 2158538972777046104 (-1442908127347265675)) sequential
  
  ✗ 1 failed.</code></pre>
<p>It’s easy to verify this using <a href="https://httpie.org/">httpie</a>:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a>$ <span class="ex">http</span> -p b POST :3000/users userId:=0 <span class="st">&quot;userName=&quot;</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a><span class="kw">{</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>    <span class="st">&quot;userId&quot;</span>: <span class="ex">0</span>,</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>    <span class="st">&quot;userName&quot;</span>: <span class="st">&quot;&quot;</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a><span class="kw">}</span></span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>$ <span class="ex">http</span> -p b GET :3000/users/1</span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>[]</span></code></pre></div>
<p>It’s clear that my assumption that <em>User ID</em> starts at 1 is wrong. Luckily fixing that isn’t too difficult. Instead of defining the update function for <code>addUser</code> as</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a>u (<span class="dt">State</span> m) (<span class="dt">AddUser</span> n) _o <span class="ot">=</span> <span class="dt">State</span> <span class="op">$</span> M.insert k n m</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>    k <span class="ot">=</span> <span class="fu">succ</span> <span class="op">$</span> <span class="fu">foldl</span> <span class="fu">max</span> <span class="dv">0</span> (M.keys m)</span></code></pre></div>
<p>I define it as</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a>u (<span class="dt">State</span> m) (<span class="dt">AddUser</span> n) _o <span class="ot">=</span> <span class="dt">State</span> <span class="op">$</span> M.insert k n m</span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>    k <span class="ot">=</span> <span class="kw">case</span> M.keys m <span class="kw">of</span></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>      [] <span class="ot">-&gt;</span> <span class="dv">0</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>      ks <span class="ot">-&gt;</span> <span class="fu">succ</span> <span class="op">$</span> <span class="fu">foldl</span> <span class="fu">max</span> <span class="dv">0</span> ks</span></code></pre></div>
<p>The complete code at this point can be found <a href="https://gist.github.com/8cbbfdb130a173bb51c317df0c00aa15">here</a>.</p>
<h2 id="fixing-another-issue-with-the-model">Fixing another issue with the model</h2>
<p>With that fix in place another issue with the model shows up</p>
<pre><code>━━━ Main ━━━
  ✗ sequential failed after 74 tests and 2 shrinks.
  
        ┏━━ tst/test-01.hs ━━━
     91 ┃ getUser :: (MonadGen n, MonadIO m) =&gt; Command n m State
     92 ┃ getUser = Command gen exec [ Require r
     93 ┃                            , Ensure e
     94 ┃                            ]
     95 ┃   where
     96 ┃     gen (State m) = case M.keys m of
     97 ┃       [] -&gt; Nothing
     98 ┃       ks -&gt; Just $ GetUser &lt;$&gt; Gen.element ks
     99 ┃ 
    100 ┃     exec (GetUser i) = liftIO $ do
    101 ┃       mgr &lt;- newManager defaultManagerSettings
    102 ┃       getReq &lt;- parseRequest $ &quot;GET http://localhost:3000/users/&quot; ++ show i
    103 ┃       getResp &lt;- httpLbs getReq mgr
    104 ┃       let us = decode $ responseBody getResp :: Maybe [User]
    105 ┃       return (status200 == responseStatus getResp, us)
    106 ┃ 
    107 ┃     r (State m) (GetUser i) = i `elem` M.keys m
    108 ┃ 
    109 ┃     e _ _ (GetUser _) (r, us) = do
    110 ┃       r === True
    111 ┃       assert $ isJust us
    112 ┃       (length &lt;$&gt; us) === Just 1
        ┃       ^^^^^^^^^^^^^^^^^^^^^^^^^^
        ┃       │ Failed (- lhs =/= + rhs)
        ┃       │ - Just 0
        ┃       │ + Just 1
    
        ┏━━ tst/test-01.hs ━━━
    120 ┃ prop_seq :: Property
    121 ┃ prop_seq = property $ do
    122 ┃   actions &lt;- forAll $ Gen.sequential (Range.linear 1 10) initialState [addUser, deleteUser, getUser]
        ┃   │ Var 0 = AddUser &quot;&quot;
        ┃   │ Var 1 = DeleteUser 0
        ┃   │ Var 2 = AddUser &quot;&quot;
        ┃   │ Var 3 = GetUser 0
    123 ┃   resetWS
    124 ┃   executeSequential initialState actions
    
    This failure can be reproduced by running:
    &gt; recheck (Size 73) (Seed 3813043122711576923 (-444438259649958339)) sequential
  
  ✗ 1 failed.</code></pre>
<p>Again, verifying this using <a href="https://httpie.org/">httpie</a> shows what the issue is</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>$ <span class="ex">http</span> -p b POST :3000/users userId:=0 <span class="st">&quot;userName=&quot;</span></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a><span class="kw">{</span></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>    <span class="st">&quot;userId&quot;</span>: <span class="ex">0</span>,</span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>    <span class="st">&quot;userName&quot;</span>: <span class="st">&quot;&quot;</span></span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a><span class="kw">}</span></span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>$ <span class="ex">http</span> -p b DELETE :3000/users/0                  </span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true"></a>$ <span class="ex">http</span> -p b POST :3000/users userId:=0 <span class="st">&quot;userName=&quot;</span></span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true"></a><span class="kw">{</span></span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true"></a>    <span class="st">&quot;userId&quot;</span>: <span class="ex">1</span>,</span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true"></a>    <span class="st">&quot;userName&quot;</span>: <span class="st">&quot;&quot;</span></span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true"></a><span class="kw">}</span></span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true"></a>$ <span class="ex">http</span> -p b GET :3000/users/0</span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true"></a>[]</span></code></pre></div>
<p>In other words, the model assumes that the 0 <em>User ID</em> get’s re-used.</p>
<p>To fix this I need a bigger change. The central bit is that the state is changed to keep track of the index more explicitly. That is, it changes from</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">State</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>)<span class="ot">=</span> <span class="dt">State</span> (<span class="dt">M.Map</span> <span class="dt">Int</span> <span class="dt">Text</span>)</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span></code></pre></div>
<p>to</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">State</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>)<span class="ot">=</span> <span class="dt">State</span> <span class="dt">Int</span> (<span class="dt">M.Map</span> <span class="dt">Int</span> <span class="dt">Text</span>)</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span></code></pre></div>
<p>That change does, quite obviously, require a bunch of other changes in the other functions dealing with the state. The complete file can be viewed <a href="https://gist.github.com/e3c3f4a3c35ab8a529045f90f9f4ba71">here</a>.</p>
<h2 id="all-is-well-or-is-it">All is well, or is it?</h2>
<p>After this the tests pass, so all is good in the world, right?</p>
<p>In the test I defined the <a href="/posts/2019-05-30-000-hedgehog-on-a-rest-api.html#the-property-and-test">property</a> over rather short sequences of commands. What happens if I increase the (maximum) length of the sequences a bit? Instead using <code>Range.linear 1 10</code> I’ll use <code>Range.linear 1 1000</code>. Well, besides taking slightly longer to run I get another sequence of commands that triggers an issue:</p>
<pre><code>━━━ Main ━━━
  ✗ sequential failed after 13 tests and 29 shrinks.
  
        ┏━━ tst/test-01.hs ━━━
     87 ┃ getUser :: (MonadGen n, MonadIO m) =&gt; Command n m State
     88 ┃ getUser = Command gen exec [ Require r
     89 ┃                            , Ensure e
     90 ┃                            ]
     91 ┃   where
     92 ┃     gen (State _ m) = case M.keys m of
     93 ┃       [] -&gt; Nothing
     94 ┃       ks -&gt; Just $ GetUser &lt;$&gt; Gen.element ks
     95 ┃ 
     96 ┃     exec (GetUser i) = liftIO $ do
     97 ┃       mgr &lt;- newManager defaultManagerSettings
     98 ┃       getReq &lt;- parseRequest $ &quot;GET http://localhost:3000/users/&quot; ++ show i
     99 ┃       getResp &lt;- httpLbs getReq mgr
    100 ┃       let us = decode $ responseBody getResp :: Maybe [User]
    101 ┃       return (status200 == responseStatus getResp, us)
    102 ┃ 
    103 ┃     r (State _ m) (GetUser i) = i `elem` M.keys m
    104 ┃ 
    105 ┃     e _ _ (GetUser _) (r, us) = do
    106 ┃       r === True
    107 ┃       assert $ isJust us
    108 ┃       (length &lt;$&gt; us) === Just 1
        ┃       ^^^^^^^^^^^^^^^^^^^^^^^^^^
        ┃       │ Failed (- lhs =/= + rhs)
        ┃       │ - Just 0
        ┃       │ + Just 1
    
        ┏━━ tst/test-01.hs ━━━
    116 ┃ prop_seq :: Property
    117 ┃ prop_seq = property $ do
    118 ┃   actions &lt;- forAll $ Gen.sequential (Range.linear 1 1000) initialState [addUser, deleteUser, getUser]
        ┃   │ Var 0 = AddUser &quot;&quot;
        ┃   │ Var 2 = AddUser &quot;&quot;
        ┃   │ Var 5 = AddUser &quot;&quot;
        ┃   │ Var 7 = AddUser &quot;&quot;
        ┃   │ Var 9 = AddUser &quot;&quot;
        ┃   │ Var 11 = AddUser &quot;&quot;
        ┃   │ Var 20 = AddUser &quot;&quot;
        ┃   │ Var 28 = AddUser &quot;&quot;
        ┃   │ Var 30 = AddUser &quot;&quot;
        ┃   │ Var 32 = AddUser &quot;&quot;
        ┃   │ Var 33 = AddUser &quot;&quot;
        ┃   │ Var 34 = AddUser &quot;&quot;
        ┃   │ Var 37 = AddUser &quot;&quot;
        ┃   │ Var 38 = AddUser &quot;&quot;
        ┃   │ Var 41 = AddUser &quot;&quot;
        ┃   │ Var 45 = AddUser &quot;&quot;
        ┃   │ Var 47 = GetUser 15
    119 ┃   resetWS
    120 ┃   executeSequential initialState actions
    
    This failure can be reproduced by running:
    &gt; recheck (Size 12) (Seed 2976784816810995551 (-47094630645854485)) sequential
  
  ✗ 1 failed.</code></pre>
<p>That is, after inserting 16 users, we don’t see any user when trying to get that 16th user (<em>User ID</em> 15). That’s a proper bug in the server.</p>
<p>As a matter of fact, this is the bug I put into the server and was hoping to find. In particular, I wanted <em>hedgehog</em> to find the minimal sequence leading to this bug.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> Which it clearly has!</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>If you recall from the <a href="/posts/2019-05-30-000-hedgehog-on-a-rest-api.html">previous post</a>, I was interested in the integrated shrinking offered by <em>hedgehog</em>.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Hedgehog on a REST API</title>
    <link href="http://magnus.therning.org/posts/2019-05-30-000-hedgehog-on-a-rest-api.html" />
    <id>http://magnus.therning.org/posts/2019-05-30-000-hedgehog-on-a-rest-api.html</id>
    <published>2019-05-30T00:00:00Z</published>
    <updated>2019-05-30T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Last year I wrote a little bit about my attempt to use <a href="http://magnus.therning.org/posts/2018-07-14-000-quickcheck-on-a-rest-api.html"><em>QuickCheck</em> to test a REST API</a>. Back then I got as far as generating test programs, running them, and validating an in-test model against the observed behaviour of the web service under test. One thing that I didn’t implement was shrinking. I had some ideas, and got some better ideas in a comment on that post, but I’ve not taken the time to actually sit down and work it out. Then, during this spring, a couple of blog posts from Oskar Wickström (<a href="https://wickstrom.tech/programming/2019/03/02/property-based-testing-in-a-screencast-editor-introduction.html">intro</a>, <a href="https://wickstrom.tech/programming/2019/03/24/property-based-testing-in-a-screencast-editor-case-study-1.html">part 1</a>, <a href="https://wickstrom.tech/programming/2019/04/17/property-based-testing-in-a-screencast-editor-case-study-2.html">part 2</a>) made me aware of another library for doing property-based testing, <a href="https://hackage.haskell.org/package/hedgehog"><em>hedgehog</em></a>. It differs quite a bit from <a href="https://hackage.haskell.org/package/QuickCheck"><em>QuickCheck</em></a>, most notably the way it uses to generate random data, and, this is the bit that made me sit up and pay attention, it has integrated shrinking.</p>
<p>My first plan was to use the same approach as I used with <em>QuickCheck</em>, but after finding out that there’s explicit support for <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#g:5">state machine tests</a> everything turned out to be a bit easier than I had expected.</p>
<p>Well, it still wasn’t exactly easy to work out the details, but the <a href="https://github.com/hedgehogqa/haskell-hedgehog/blob/master/hedgehog-example/src/Test/Example/Registry.hs">registry example</a> in the <em>hedgehog</em> source repo together with a (slightly dated) <a href="https://teh.id.au/posts/2017/07/15/state-machine-testing/index.html">example</a> I managed to work it out (I think).</p>
<h2 id="the-rest-api">The REST API</h2>
<p>The API is the same as in the <a href="http://magnus.therning.org/posts/2018-07-14-000-quickcheck-on-a-rest-api.html">post on using <em>QuickCheck</em></a>, with one little difference, I’ve been lazy when implementing <code>GET /users/:id</code> and return a list of users (that makes it easy to represent a missing <code>:id</code>).</p>
<table>
<colgroup>
<col style="width: 7%" />
<col style="width: 13%" />
<col style="width: 38%" />
<col style="width: 41%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">Method</th>
<th style="text-align: left;">Route</th>
<th style="text-align: left;">Example in</th>
<th style="text-align: left;">Example out</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>POST</code></td>
<td style="text-align: left;"><code>/users</code></td>
<td style="text-align: left;"><code>{"userId": 0, "userName": "Yogi Berra"}</code></td>
<td style="text-align: left;"><code>{"userId": 42, "userName": "Yogi Berra"}</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>DELETE</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>[0,3,7]</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>[{"userId": 42, "userName": "Yogi Berra"}]</code></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>[]</code> (when there’s no user with <code>:id</code>)</td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>POST</code></td>
<td style="text-align: left;"><code>/reset</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h2 id="the-model-state">The model state</h2>
<p>Just like last time I’m using as simple a model as I think I can get away with, based on the API above:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">State</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>)<span class="ot">=</span> <span class="dt">State</span> (<span class="dt">M.Map</span> <span class="dt">Int</span> <span class="dt">Text</span>)</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a><span class="ot">initialState ::</span> <span class="dt">State</span> v</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>initialState <span class="ot">=</span> <span class="dt">State</span> M.empty</span></code></pre></div>
<p>That extra <code>v</code> is something that <em>hedgehog</em> requires. Why? I don’t really know, and luckily I don’t have to care to make it all work. One thing though, the language pragma <code>KindSignatures</code> is necessary to use that kind of syntax.</p>
<h2 id="representing-api-calls">Representing API calls</h2>
<p>Representing an API call requires three things</p>
<ol type="1">
<li>a type</li>
<li>an implementation of <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#t:HTraversable"><code>HTraversable</code></a> for the type</li>
<li>a function producing a <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#t:Command"><code>Command</code></a> for the type</li>
</ol>
<p>I represent the three API calls with these three types</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">AddUser</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>) <span class="ot">=</span> <span class="dt">AddUser</span> <span class="dt">Text</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">DeleteUser</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>) <span class="ot">=</span> <span class="dt">DeleteUser</span> <span class="dt">Int</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">GetUser</span> (<span class="ot">v ::</span> <span class="op">*</span> <span class="ot">-&gt;</span> <span class="op">*</span>) <span class="ot">=</span> <span class="dt">GetUser</span> <span class="dt">Int</span></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span></code></pre></div>
<p>Again that <code>v</code> pops up, but as with the model state, there’s no need to pay any attention to it.</p>
<p>For the implementation of <code>HTraversable</code> I was greatly helped by the <a href="https://github.com/hedgehogqa/haskell-hedgehog/blob/master/hedgehog-example/src/Test/Example/Registry.hs">registry example</a>. Their implementations are fairly straight forward, which is a good thing since the need for them is internal to <em>hedgehog</em>.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HTraversable</span> <span class="dt">AddUser</span> <span class="kw">where</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>  htraverse _ (<span class="dt">AddUser</span> n) <span class="ot">=</span> <span class="dt">AddUser</span> <span class="op">&lt;$&gt;</span> <span class="fu">pure</span> n</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HTraversable</span> <span class="dt">DeleteUser</span> <span class="kw">where</span></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>  htraverse _ (<span class="dt">DeleteUser</span> i) <span class="ot">=</span> <span class="dt">DeleteUser</span> <span class="op">&lt;$&gt;</span> <span class="fu">pure</span> i</span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a></span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HTraversable</span> <span class="dt">GetUser</span> <span class="kw">where</span></span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a>  htraverse _ (<span class="dt">GetUser</span> i) <span class="ot">=</span> <span class="dt">GetUser</span> <span class="op">&lt;$&gt;</span> <span class="fu">pure</span> i</span></code></pre></div>
<p>Once these two things are out of the way we get to the meat of the implementation of the API calls, a function creating a <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#t:Command"><code>Command</code></a> instance for each type of API call. The exact type for all three functions will be</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a>(<span class="dt">MonadGen</span> n, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">Command</span> n m <span class="dt">State</span></span></code></pre></div>
<p>which doesn’t say a whole lot, I think. After reading the documentation I found it a little clearer, but the two examples, <a href="https://teh.id.au/posts/2017/07/15/state-machine-testing/index.html">state machine testing</a> and <a href="https://github.com/hedgehogqa/haskell-hedgehog/blob/master/hedgehog-example/src/Test/Example/Registry.hs">registry</a>, was what cleared things up for me.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> In an attempt at being overly explicit I wrote these functions in the same style. This is what it ended up looking like for the <code>AddUser</code> type:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">addUser ::</span> (<span class="dt">MonadGen</span> n, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">Command</span> n m <span class="dt">State</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>addUser <span class="ot">=</span> <span class="dt">Command</span> gen exec [ <span class="dt">Update</span> u</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>                           , <span class="dt">Ensure</span> e</span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>                           ]</span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true"></a>    gen _ <span class="ot">=</span> <span class="dt">Just</span> <span class="op">$</span> <span class="dt">AddUser</span> <span class="op">&lt;$&gt;</span> Gen.text (Range.linear <span class="dv">0</span> <span class="dv">42</span>) Gen.alpha</span>
<span id="cb5-7"><a href="#cb5-7" aria-hidden="true"></a></span>
<span id="cb5-8"><a href="#cb5-8" aria-hidden="true"></a>    exec (<span class="dt">AddUser</span> n) <span class="ot">=</span> liftIO <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb5-9"><a href="#cb5-9" aria-hidden="true"></a>      mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</span>
<span id="cb5-10"><a href="#cb5-10" aria-hidden="true"></a>      addReq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/users&quot;</span></span>
<span id="cb5-11"><a href="#cb5-11" aria-hidden="true"></a>      <span class="kw">let</span> addReq&#39; <span class="ot">=</span> addReq { requestBody <span class="ot">=</span> <span class="dt">RequestBodyLBS</span> (encode <span class="op">$</span> <span class="dt">User</span> <span class="dv">0</span> n)}</span>
<span id="cb5-12"><a href="#cb5-12" aria-hidden="true"></a>      addResp <span class="ot">&lt;-</span> httpLbs addReq&#39; mgr</span>
<span id="cb5-13"><a href="#cb5-13" aria-hidden="true"></a>      <span class="kw">let</span> user <span class="ot">=</span> decode (responseBody addResp)<span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">User</span></span>
<span id="cb5-14"><a href="#cb5-14" aria-hidden="true"></a>      <span class="fu">return</span> (responseStatus addResp, user)</span>
<span id="cb5-15"><a href="#cb5-15" aria-hidden="true"></a></span>
<span id="cb5-16"><a href="#cb5-16" aria-hidden="true"></a>    u (<span class="dt">State</span> m) (<span class="dt">AddUser</span> n) _o <span class="ot">=</span> <span class="dt">State</span> <span class="op">$</span> M.insert k n m</span>
<span id="cb5-17"><a href="#cb5-17" aria-hidden="true"></a>      <span class="kw">where</span></span>
<span id="cb5-18"><a href="#cb5-18" aria-hidden="true"></a>        k <span class="ot">=</span> <span class="fu">succ</span> <span class="op">$</span> <span class="fu">foldl</span> <span class="fu">max</span> <span class="dv">0</span> (M.keys m)</span>
<span id="cb5-19"><a href="#cb5-19" aria-hidden="true"></a></span>
<span id="cb5-20"><a href="#cb5-20" aria-hidden="true"></a>    e _ _ (<span class="dt">AddUser</span> n) (r, ui) <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb5-21"><a href="#cb5-21" aria-hidden="true"></a>      r <span class="op">===</span> status201</span>
<span id="cb5-22"><a href="#cb5-22" aria-hidden="true"></a>      assert <span class="op">$</span> isJust ui</span>
<span id="cb5-23"><a href="#cb5-23" aria-hidden="true"></a>      (userName <span class="op">&lt;$&gt;</span> ui) <span class="op">===</span> <span class="dt">Just</span> n</span></code></pre></div>
<p>Piece by piece:</p>
<ol type="1">
<li><code>gen</code> is the generator of data. It takes one argument, the current state, but for <code>AddUser</code> I have no use for it. The user name is generated using a generator for <code>Text</code>, and rather arbitrarily I limit the names to 42 characters.</li>
<li><code>exec</code> is the action that calls the web service. Here I’m using <a href="https://hackage.haskell.org/package/http-client"><code>http-client</code></a> to make the call and <a href="https://hackage.haskell.org/package/aeson"><code>aeson</code></a> to parse the response into a <code>User</code>. It produces <em>output</em>.</li>
<li><code>u</code> is a function for updating the model state. It’s given the current state, the command and the <em>output</em>. All I need to to do for <code>AddUser</code> is to pick a <code>userId</code> and associate it with the generated name.</li>
<li><code>e</code> is a function for checking post-conditions, in other words checking properties that must hold after <code>exec</code> has run and the state has been updated. It’s given four arguments, the previous state, the updated state, the command and the <em>output</em>. The tests here are on the HTTP response code and the returned user name. I think that will do for the time being.</li>
</ol>
<p>The function for <code>DeleteUser</code> follows the same pattern</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a><span class="ot">deleteUser ::</span> (<span class="dt">MonadGen</span> n, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">Command</span> n m <span class="dt">State</span></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a>deleteUser <span class="ot">=</span> <span class="dt">Command</span> gen exec [ <span class="dt">Update</span> u</span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>                              , <span class="dt">Require</span> r</span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>                              , <span class="dt">Ensure</span> e</span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a>                              ]</span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true"></a>    gen (<span class="dt">State</span> m) <span class="ot">=</span> <span class="kw">case</span> M.keys m <span class="kw">of</span></span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true"></a>      [] <span class="ot">-&gt;</span> <span class="dt">Nothing</span></span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true"></a>      ks <span class="ot">-&gt;</span> <span class="dt">Just</span> <span class="op">$</span> <span class="dt">DeleteUser</span> <span class="op">&lt;$&gt;</span> Gen.element ks</span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true"></a></span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true"></a>    exec (<span class="dt">DeleteUser</span> i) <span class="ot">=</span> liftIO <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true"></a>      mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true"></a>      delReq <span class="ot">&lt;-</span> parseRequest <span class="op">$</span> <span class="st">&quot;DELETE http://localhost:3000/users/&quot;</span> <span class="op">++</span> <span class="fu">show</span> i</span>
<span id="cb6-14"><a href="#cb6-14" aria-hidden="true"></a>      delResp <span class="ot">&lt;-</span> httpNoBody delReq mgr</span>
<span id="cb6-15"><a href="#cb6-15" aria-hidden="true"></a>      <span class="fu">return</span> <span class="op">$</span> responseStatus delResp</span>
<span id="cb6-16"><a href="#cb6-16" aria-hidden="true"></a></span>
<span id="cb6-17"><a href="#cb6-17" aria-hidden="true"></a>    u (<span class="dt">State</span> m) (<span class="dt">DeleteUser</span> i) _ <span class="ot">=</span> <span class="dt">State</span> <span class="op">$</span> M.delete i m</span>
<span id="cb6-18"><a href="#cb6-18" aria-hidden="true"></a></span>
<span id="cb6-19"><a href="#cb6-19" aria-hidden="true"></a>    r (<span class="dt">State</span> m) (<span class="dt">DeleteUser</span> i) <span class="ot">=</span> i <span class="ot">`elem`</span> M.keys m</span>
<span id="cb6-20"><a href="#cb6-20" aria-hidden="true"></a></span>
<span id="cb6-21"><a href="#cb6-21" aria-hidden="true"></a>    e _ _ (<span class="dt">DeleteUser</span> _) r <span class="ot">=</span> r <span class="op">===</span> status200</span></code></pre></div>
<p>I think only two pieces need further explanation:</p>
<ol type="1">
<li><code>gen</code> only returns a <code>DeleteUser</code> with an index actually present in the model state. If there are no users in the model then <code>Nothing</code> is returned. As far as I understand that means that generated programs will only make calls to delete existing users.<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a></li>
<li><code>r</code> is a pre-condition that programs only delete users that exist. At first I had skipped this pre-condition, thinking that it’d be enough to have <code>gen</code> only create delete calls for existing users. However, after reading the documentation of <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#t:Command"><code>Command</code></a> and <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#t:Callback"><code>Callback</code></a> a bit more closely I realised that I might need a pre-condition to make sure that this holds true also while shrinking.</li>
</ol>
<p>The final function, for <code>GetUser</code> requires no further explanation so I only present it here</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a><span class="ot">getUser ::</span> (<span class="dt">MonadGen</span> n, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">Command</span> n m <span class="dt">State</span></span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>getUser <span class="ot">=</span> <span class="dt">Command</span> gen exec [ <span class="dt">Require</span> r</span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true"></a>                           , <span class="dt">Ensure</span> e</span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true"></a>                           ]</span>
<span id="cb7-5"><a href="#cb7-5" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb7-6"><a href="#cb7-6" aria-hidden="true"></a>    gen (<span class="dt">State</span> m) <span class="ot">=</span> <span class="kw">case</span> M.keys m <span class="kw">of</span></span>
<span id="cb7-7"><a href="#cb7-7" aria-hidden="true"></a>      [] <span class="ot">-&gt;</span> <span class="dt">Nothing</span></span>
<span id="cb7-8"><a href="#cb7-8" aria-hidden="true"></a>      ks <span class="ot">-&gt;</span> <span class="dt">Just</span> <span class="op">$</span> <span class="dt">GetUser</span> <span class="op">&lt;$&gt;</span> Gen.element ks</span>
<span id="cb7-9"><a href="#cb7-9" aria-hidden="true"></a></span>
<span id="cb7-10"><a href="#cb7-10" aria-hidden="true"></a>    exec (<span class="dt">GetUser</span> i) <span class="ot">=</span> liftIO <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb7-11"><a href="#cb7-11" aria-hidden="true"></a>      mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</span>
<span id="cb7-12"><a href="#cb7-12" aria-hidden="true"></a>      getReq <span class="ot">&lt;-</span> parseRequest <span class="op">$</span> <span class="st">&quot;GET http://localhost:3000/users/&quot;</span> <span class="op">++</span> <span class="fu">show</span> i</span>
<span id="cb7-13"><a href="#cb7-13" aria-hidden="true"></a>      getResp <span class="ot">&lt;-</span> httpLbs getReq mgr</span>
<span id="cb7-14"><a href="#cb7-14" aria-hidden="true"></a>      <span class="kw">let</span> us <span class="ot">=</span> decode <span class="op">$</span> responseBody<span class="ot"> getResp ::</span> <span class="dt">Maybe</span> [<span class="dt">User</span>]</span>
<span id="cb7-15"><a href="#cb7-15" aria-hidden="true"></a>      <span class="fu">return</span> (status200 <span class="op">==</span> responseStatus getResp, us)</span>
<span id="cb7-16"><a href="#cb7-16" aria-hidden="true"></a></span>
<span id="cb7-17"><a href="#cb7-17" aria-hidden="true"></a>    r (<span class="dt">State</span> m) (<span class="dt">GetUser</span> i) <span class="ot">=</span> i <span class="ot">`elem`</span> M.keys m</span>
<span id="cb7-18"><a href="#cb7-18" aria-hidden="true"></a></span>
<span id="cb7-19"><a href="#cb7-19" aria-hidden="true"></a>    e _ _ (<span class="dt">GetUser</span> _) (r, us) <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb7-20"><a href="#cb7-20" aria-hidden="true"></a>      r <span class="op">===</span> <span class="dt">True</span></span>
<span id="cb7-21"><a href="#cb7-21" aria-hidden="true"></a>      assert <span class="op">$</span> isJust us</span>
<span id="cb7-22"><a href="#cb7-22" aria-hidden="true"></a>      (<span class="fu">length</span> <span class="op">&lt;$&gt;</span> us) <span class="op">===</span> <span class="dt">Just</span> <span class="dv">1</span></span></code></pre></div>
<h2 id="the-property-and-test">The property and test</h2>
<p>It looks like there are two obvious top-level properties</p>
<ol type="1">
<li>the web service works as expected when all calls are made one at a time (sequential), and</li>
<li>the web service works as expected when all calls are made in parallel.</li>
</ol>
<p><em>Hedgehog</em> provides two pairs of functions for this</p>
<ol type="1">
<li>a <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog-Gen.html#v:sequential"><code>sequential</code> generator</a> with <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#v:executeSequential"><code>executeSequential</code></a>, and</li>
<li>a <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog-Gen.html#v:parallel"><code>parallel</code> generator</a> with <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#v:executeParallel"><code>executeParallel</code></a>.</li>
</ol>
<p>I started with the former only</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="ot">prop_seq ::</span> <span class="dt">Property</span></span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>prop_seq <span class="ot">=</span> property <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a>  actions <span class="ot">&lt;-</span> forAll <span class="op">$</span> Gen.sequential (Range.linear <span class="dv">1</span> <span class="dv">10</span>) initialState [addUser, deleteUser, getUser]</span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true"></a>  resetWS</span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true"></a>  executeSequential initialState actions</span></code></pre></div>
<p>This first creates a generator of programs of at most length 10<a href="#fn3" class="footnote-ref" id="fnref3" role="doc-noteref"><sup>3</sup></a>, then turning that into a <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#t:Sequential"><code>Sequential</code></a> which can be passed to <a href="https://hackage.haskell.org/package/hedgehog-1.0/docs/Hedgehog.html#v:executeSequential"><code>executeSequential</code></a> to turn into a <a href="https://hackage.haskell.org/package/hedgehog-0.5.3/docs/Hedgehog.html#t:Property"><code>Property</code></a>.</p>
<p>The function <code>resetWS</code> clears out the web service to make sure that the tests start with a clean slate each time. Its definition is</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="ot">resetWS ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> m ()</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a>resetWS <span class="ot">=</span> liftIO <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true"></a>  mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true"></a>  resetReq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/reset&quot;</span></span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true"></a>  void <span class="op">$</span> httpNoBody resetReq mgr</span></code></pre></div>
<p>The final bit is the <code>main</code> function, which I wrote like this</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true"></a>  res <span class="ot">&lt;-</span> checkSequential <span class="op">$</span> <span class="dt">Group</span> <span class="st">&quot;Main&quot;</span> [(<span class="st">&quot;sequential&quot;</span>, prop_seq)]</span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true"></a>  unless res exitFailure</span></code></pre></div>
<p>That is, first run the property sequentially (<a href="https://hackage.haskell.org/package/hedgehog-0.5.3/docs/Hedgehog.html#v:checkSequential"><code>checkSequential</code></a>) and if that fails exit with failure.</p>
<h2 id="running-the-test">Running the test</h2>
<p>When running the test fails and gives me a program that breaks the property, and exactly what fails:</p>
<pre><code>━━━ Main ━━━
  ✗ sequential failed after 13 tests and 1 shrink.
  
        ┏━━ tst/test-01.hs ━━━
     89 ┃ getUser :: (MonadGen n, MonadIO m) =&gt; Command n m State
     90 ┃ getUser = Command gen exec [ Require r
     91 ┃                            , Ensure e
     92 ┃                            ]
     93 ┃   where
     94 ┃     gen (State m) = case M.keys m of
     95 ┃       [] -&gt; Nothing
     96 ┃       ks -&gt; Just $ GetUser &lt;$&gt; Gen.element ks
     97 ┃ 
     98 ┃     exec (GetUser i) = liftIO $ do
     99 ┃       mgr &lt;- newManager defaultManagerSettings
    100 ┃       getReq &lt;- parseRequest $ &quot;GET http://localhost:3000/users/&quot; ++ show i
    101 ┃       getResp &lt;- httpLbs getReq mgr
    102 ┃       let us = decode $ responseBody getResp :: Maybe [User]
    103 ┃       return (status200 == responseStatus getResp, us)
    104 ┃ 
    105 ┃     r (State m) (GetUser i) = i `elem` M.keys m
    106 ┃ 
    107 ┃     e _ _ (GetUser _) (r, us) = do
    108 ┃       r === True
    109 ┃       assert $ isJust us
    110 ┃       (length &lt;$&gt; us) === Just 1
        ┃       ^^^^^^^^^^^^^^^^^^^^^^^^^^
        ┃       │ Failed (- lhs =/= + rhs)
        ┃       │ - Just 0
        ┃       │ + Just 1
    
        ┏━━ tst/test-01.hs ━━━
    118 ┃ prop_seq :: Property
    119 ┃ prop_seq = property $ do
    120 ┃   actions &lt;- forAll $ Gen.sequential (Range.linear 1 10) initialState [addUser, deleteUser, getUser]
        ┃   │ Var 0 = AddUser &quot;&quot;
        ┃   │ Var 1 = GetUser 1
    121 ┃   resetWS
    122 ┃   executeSequential initialState actions
    
    This failure can be reproduced by running:
    &gt; recheck (Size 12) (Seed 6041776208714975061 (-2279196309322888437)) sequential
  
  ✗ 1 failed.</code></pre>
<p>My goodness, that is pretty output!</p>
<p>Anyway, I’d say that the failing program has been shrunk to be minimal so I’d say that all in all this is a big step up from what I had earlier. Sure, using the <em>hedgehog</em> state machine API is slightly involved, but once worked out I find it fairly straight-forward and it most likely is written by people much more knowledgable than me and better than anything I could produce. Having to use generators explicitly (the <em>hedgehog</em> way) is neither easier nor more complicated than defining a few type class instances (the <em>QuickCheck</em> way). Finally, the integrated shrinking is rather brilliant and <em>not</em> having to implement that myself is definitely a big benefit.</p>
<p>Now I only have to fix the errors in the web service that the test reveal. This post is already rather long, so I’ll keep that for a future post.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>There is still one thing that’s unclear to me though, and that’s how to get to the output in an update function.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>Put another way, programs will never test how the web service behaves when asking for non-existing users. I think that, if I want to test that, I’ll opt for using a separate API call type for it.<a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn3" role="doc-endnote"><p>At least that’s my understanding of the impact of <code>Range.linear 1 10</code>.<a href="#fnref3" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Some OrgMode stuff</title>
    <link href="http://magnus.therning.org/posts/2019-05-07-000-some-orgmode-stuff.html" />
    <id>http://magnus.therning.org/posts/2019-05-07-000-some-orgmode-stuff.html</id>
    <published>2019-05-07T00:00:00Z</published>
    <updated>2019-05-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The last few days I’ve watched <a href="https://www.youtube.com/user/koenighaunstetten">Rainer König’s OrgMode videos</a>. It’s resulted in a few new settings that makes <a href="https://orgmode.org/">Org</a> a little more useful.</p>
<table>
<colgroup>
<col style="width: 35%" />
<col style="width: 11%" />
<col style="width: 52%" />
</colgroup>
<thead>
<tr class="header">
<th>Variable</th>
<th>Value</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>calendar-week-start-day</code></td>
<td><code>1</code></td>
<td>Weeks start of Monday!</td>
</tr>
<tr class="even">
<td><code>org-modules</code> (list)</td>
<td><code>org-habit</code></td>
<td>Support for tracking habits</td>
</tr>
<tr class="odd">
<td><code>org-modules</code> (list)</td>
<td><code>org-id</code></td>
<td>Improved support for <code>ID</code> property</td>
</tr>
<tr class="even">
<td><code>org-agenda-start-on-weekday</code></td>
<td><code>1</code></td>
<td>Weeks start on Monday, again!</td>
</tr>
<tr class="odd">
<td><code>org-log-into-drawer</code></td>
<td><code>t</code></td>
<td>Put notes (logs) into a drawer</td>
</tr>
<tr class="even">
<td><code>org-enforce-todo-checkbox-dependencies</code></td>
<td><code>t</code></td>
<td>Checkboxes must be checked before a <em>TODO</em> can become <em>DONE</em></td>
</tr>
<tr class="odd">
<td><code>org-id-link-to-org-use-id</code></td>
<td><code>t</code></td>
<td>Prefer use of <code>ID</code> property for links</td>
</tr>
</tbody>
</table>]]></summary>
</entry>
<entry>
    <title>A simple browser chooser</title>
    <link href="http://magnus.therning.org/posts/2019-04-28-000-a-simple-browser-chooser.html" />
    <id>http://magnus.therning.org/posts/2019-04-28-000-a-simple-browser-chooser.html</id>
    <published>2019-04-28T00:00:00Z</published>
    <updated>2019-04-28T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Up until a few days ago I sort of mixed my private and work web browsing, e.g. I connected my private <a href="https://www.lastpass.com/">LastPass</a> account to my work account. Then I heard about <a href="https://bitwarden.com/">BitWarden</a> and wanted to try it out. A quick export from LastPass and import to BitWarden later I realised that I really ought to try to split my private browsing from my work browsing too – <a href="https://www.mozilla.org/firefox/">Firefox</a> for the former and <a href="https://www.chromium.org/">Chromium</a> for the latter.</p>
<p>After about a day of this I found that having a single default browser was a bit of a limitation. Inspired by behaviour on my Android phone I started looking for a <a href="https://mstdn.io/@magnus/101985705763568037">browser choosing</a>. Unfortunately I didn’t find anything for Linux/Gnome. With the help from <a href="https://is.gd/9ajN0y">a Reddit post</a> I ended putting together a package for <a href="https://www.archlinux.org/">Arch Linux</a>.</p>
<p>I gave it the name <a href="https://gitlab.com/magus/browser-chooser">browser-chooser</a>.</p>]]></summary>
</entry>
<entry>
    <title>Comonadic builders, minor addition</title>
    <link href="http://magnus.therning.org/posts/2019-04-22-000-comonadic-builders--minor-addition.html" />
    <id>http://magnus.therning.org/posts/2019-04-22-000-comonadic-builders--minor-addition.html</id>
    <published>2019-04-22T00:00:00Z</published>
    <updated>2019-04-22T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When reading about <a href="https://chshersh.github.io/posts/2019-03-25-comonadic-builders">Comonadic builders</a> the other day I reacted to this comment:</p>
<blockquote>
<p>The <code>comonad</code> package has the <a href="https://hackage.haskell.org/package/comonad-5.0.4/docs/Control-Comonad-Trans-Traced.html#t:TracedT">Traced</a> <code>newtype</code> wrapper around the function <code>(-&gt;)</code>. The <code>Comonad</code> instance for this <code>newtype</code> gives us the desired behaviour. However, dealing with the <code>newtype</code> wrapping and unwrapping makes our code noisy and truly harder to understand, so let’s use the <code>Comonad</code> instance for the arrow <code>(-&gt;)</code> itself</p>
</blockquote>
<p>So, just for fun I thought I work out the “noisy and truly harder” bits.</p>
<p>To begin with I needed two language extensions and two imports</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">{-# LANGUAGE OverloadedStrings#-}</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="ot">{-# LANGUAGE RecordWildCards #-}</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Control.Comonad.Traced</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Data.Text</span></span></code></pre></div>
<p>After that I could copy quite a bit of stuff directly from the other post</p>
<ul>
<li><code>Settings</code> definition</li>
<li>The <code>Semigroup</code> instance for <code>Settings</code></li>
<li>The <code>Monoid</code> instance for <code>Settings</code></li>
<li><code>Project</code> definition</li>
</ul>
<p>After this everything had only minor changes. First off the <code>ProjectBuilder</code> type had to be changed to</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">ProjectBuilder</span> <span class="ot">=</span> <span class="dt">Traced</span> <span class="dt">Settings</span> <span class="dt">Project</span></span></code></pre></div>
<p>With that done the types of all the functions can actually be left as they are, but of course the definitions have to modified. However, it turned out that the necessary modifications were rather smaller than I had expected. First out <code>buildProject</code> which I decided to call <code>buildProjectW</code> to make it possible to keep the original code and the new code in the same file without causing name clashes:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">buildProjectW ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> <span class="dt">ProjectBuilder</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>buildProjectW <span class="ot">=</span> traced <span class="op">.</span> buildProject</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a>    buildProject projectName <span class="dt">Settings</span>{<span class="op">..</span>} <span class="ot">=</span> <span class="dt">Project</span></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>      { projectHasLibrary <span class="ot">=</span> getAny settingsHasLibrary</span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a>      , projectGitHub     <span class="ot">=</span> getAny settingsGitHub</span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a>      , projectTravis     <span class="ot">=</span> getAny settingsTravis</span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a>      , <span class="op">..</span></span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true"></a>      }</span></code></pre></div>
<p>The only difference is the addition of <code>traced .</code> to wrap it up in the <code>newtype</code>, the rest is copied straight from the original article.</p>
<p>The two simple project combinator functions, which I call <code>hasLibraryBW</code> and <code>gitHubBW</code>, needed a bit of tweaking. In the original version combinators take a <code>builder</code> which is an ordinary function, so it can just be called. Now however, the function is wrapped in a <code>newtype</code> so a bit of unwrapping is necessary:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">hasLibraryBW ::</span> <span class="dt">ProjectBuilder</span> <span class="ot">-&gt;</span> <span class="dt">Project</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>hasLibraryBW builder <span class="ot">=</span> runTraced builder <span class="op">$</span> <span class="fu">mempty</span> { settingsHasLibrary <span class="ot">=</span> <span class="dt">Any</span> <span class="dt">True</span> }</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a><span class="ot">gitHubBW ::</span> <span class="dt">ProjectBuilder</span> <span class="ot">-&gt;</span> <span class="dt">Project</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>gitHubBW builder <span class="ot">=</span> runTraced builder <span class="op">$</span> <span class="fu">mempty</span> { settingsGitHub <span class="ot">=</span> <span class="dt">Any</span> <span class="dt">True</span> }</span></code></pre></div>
<p>Once again it’s rather small differences from the code in the article.</p>
<p>As for the final combinator, which I call <code>travisBW</code>, actually needed no changes at all. I only rewrote it using a <code>when</code> clause, because I prefer that style over <code>let</code>:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">travisBW ::</span> <span class="dt">ProjectBuilder</span> <span class="ot">-&gt;</span> <span class="dt">Project</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>travisBW builder <span class="ot">=</span> project { projectTravis <span class="ot">=</span> projectGitHub project }</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>    project <span class="ot">=</span> extract builder</span></code></pre></div>
<p>Finally, to show that this implementation hasn’t really changed the behaviour</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>λ extract <span class="op">$</span> buildProjectW <span class="st">&quot;travis&quot;</span> <span class="op">=&gt;&gt;</span> travisBW</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a><span class="dt">Project</span> { projectName <span class="ot">=</span> <span class="st">&quot;travis&quot;</span></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>        , projectHasLibrary <span class="ot">=</span> <span class="dt">False</span></span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>        , projectGitHub <span class="ot">=</span> <span class="dt">False</span> </span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a>        , projectTravis <span class="ot">=</span> <span class="dt">False</span></span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>        }</span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true"></a></span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true"></a>λ extract <span class="op">$</span> buildProjectW <span class="st">&quot;github-travis&quot;</span> <span class="op">=&gt;&gt;</span> gitHubBW <span class="op">=&gt;&gt;</span> travisBW</span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true"></a><span class="dt">Project</span> { projectName <span class="ot">=</span> <span class="st">&quot;github-travis&quot;</span></span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true"></a>        , projectHasLibrary <span class="ot">=</span> <span class="dt">False</span></span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true"></a>        , projectGitHub <span class="ot">=</span> <span class="dt">True</span></span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true"></a>        , projectTravis <span class="ot">=</span> <span class="dt">True</span></span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true"></a>        }</span>
<span id="cb6-14"><a href="#cb6-14" aria-hidden="true"></a></span>
<span id="cb6-15"><a href="#cb6-15" aria-hidden="true"></a>λ extract <span class="op">$</span> buildProjectW <span class="st">&quot;travis-github&quot;</span> <span class="op">=&gt;&gt;</span> travisBW <span class="op">=&gt;&gt;</span> gitHubBW</span>
<span id="cb6-16"><a href="#cb6-16" aria-hidden="true"></a><span class="dt">Project</span> { projectName <span class="ot">=</span> <span class="st">&quot;travis-github&quot;</span></span>
<span id="cb6-17"><a href="#cb6-17" aria-hidden="true"></a>        , projectHasLibrary <span class="ot">=</span> <span class="dt">False</span> </span>
<span id="cb6-18"><a href="#cb6-18" aria-hidden="true"></a>        , projectGitHub <span class="ot">=</span> <span class="dt">True</span></span>
<span id="cb6-19"><a href="#cb6-19" aria-hidden="true"></a>        , projectTravis <span class="ot">=</span> <span class="dt">True</span></span>
<span id="cb6-20"><a href="#cb6-20" aria-hidden="true"></a>        }</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>TIL: prompt matters to org-mode</title>
    <link href="http://magnus.therning.org/posts/2019-03-16-000-til--prompt-matters-to-org-mode.html" />
    <id>http://magnus.therning.org/posts/2019-03-16-000-til--prompt-matters-to-org-mode.html</id>
    <published>2019-03-16T00:00:00Z</published>
    <updated>2019-03-16T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>A workmate just embellished some shell code blocks I’d put in a shared <a href="https://orgmode.org/">org-mode</a> file with <code>:session s</code>. When I tried to run the blocks with sessions my emacs just froze up though. I found a <a href="https://emacs.stackexchange.com/questions/19735/emacs-freezes-with-any-org-babel-snippet-using-session">post on the emacs StackExchange</a> that offered a possible cause for it: the prompt.</p>
<p>I’m using <a href="https://github.com/Bash-it/bash-it">bash-it</a> so my prompt is rather far from the default.</p>
<p>After inspecting the session buffer simply added the following to my <code>~/.bashrc</code></p>
<pre class="shell"><code>if [[ ${TERM} == &quot;dumb&quot; ]]; then
    export BASH_IT_THEME=&#39;standard&#39;
else
    export BASH_IT_THEME=&#39;simple&#39;
fi</code></pre>
<p>and now I can finally run shell code blocks in sessions.</p>]]></summary>
</entry>
<entry>
    <title>Conduit and PostgreSQL</title>
    <link href="http://magnus.therning.org/posts/2019-03-03-000-conduit-and-postgresql.html" />
    <id>http://magnus.therning.org/posts/2019-03-03-000-conduit-and-postgresql.html</id>
    <published>2019-03-03T00:00:00Z</published>
    <updated>2019-03-03T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a while now I’ve been playing around with an event-drive software design (<a href="https://en.wikipedia.org/wiki/Event-driven_architecture">EDA</a>) using <a href="https://hackage.haskell.org/package/conduit"><code>conduit</code></a> for processing of events. For this post the processing can basically be viewed as the following diagram</p>
<pre><code>+-----------+   +------------+   +---------+
|           |   |            |   |         |
| PG source |--&gt;| Processing |--&gt;| PG sink |
|           |   |            |   |         |
+-----------+   +------------+   +---------+
     ^                                |
     |            +------+            |
     |            |      |            |
     |            |  PG  |            |
     +------------|  DB  |&lt;-----------+
                  |      |
                  +------+</code></pre>
<p>I started out looking for Conduit components for PostgreSQL on <a href="https://hackage.haskell.org/">Hackage</a> but failed to find something fitting so I started looking into writing them myself using <a href="https://hackage.haskell.org/package/postgresql-simple"><code>postgresql-simple</code></a>.</p>
<p>The sink wasn’t much of a problem, use <code>await</code> to get an event (a tuple) and write it to the database. My almost complete ignorance of using databases resulted in a first version of the source was rather naive and used busy-waiting. Then I stumbled on PostgreSQL’s support for notifications through the <code>LISTEN</code> and <code>NOTIFY</code> commands. I rather like the result and it seems to work well.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a>[]</p>
<p>It looks like this</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Control.Monad.IO.Class</span> (<span class="dt">MonadIO</span>, liftIO)</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Data.Aeson</span> (<span class="dt">Value</span>)</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Data.Conduit</span> <span class="kw">as</span> <span class="dt">C</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Data.Conduit.Combinators</span> <span class="kw">as</span> <span class="dt">CC</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Data.Text</span> (<span class="dt">Text</span>)</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Data.Time.Clock</span> (<span class="dt">UTCTime</span>)</span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Data.UUID</span> (<span class="dt">UUID</span>)</span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Database.PostgreSQL.Simple</span> (<span class="dt">Connection</span>, <span class="dt">Only</span>(..), execute, execute_, query)</span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Database.PostgreSQL.Simple.Notification</span> (getNotification)</span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true"></a></span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true"></a><span class="ot">fst8 ::</span> (a, b, c, d, e, f, g, h) <span class="ot">-&gt;</span> a</span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true"></a>fst8 (a, _, _, _, _, _, _, _) <span class="ot">=</span> a</span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true"></a></span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true"></a><span class="ot">dbSource ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Connection</span> <span class="ot">-&gt;</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">C.ConduitT</span> () (<span class="dt">Int</span>, <span class="dt">UTCTime</span>, <span class="dt">Int</span>, <span class="dt">Int</span>, <span class="dt">Bool</span>, <span class="dt">UUID</span>, <span class="dt">Text</span>, <span class="dt">Value</span>) m ()</span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true"></a>dbSource conn ver <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true"></a>  res <span class="ot">&lt;-</span> liftIO <span class="op">$</span> query conn <span class="st">&quot;SELECT * from events where id &gt; (?) ORDER BY id&quot;</span> (<span class="dt">Only</span> ver)</span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true"></a>  <span class="kw">case</span> res <span class="kw">of</span></span>
<span id="cb2-18"><a href="#cb2-18" aria-hidden="true"></a>    [] <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb2-19"><a href="#cb2-19" aria-hidden="true"></a>      liftIO <span class="op">$</span> execute_ conn <span class="st">&quot;LISTEN MyEvent&quot;</span></span>
<span id="cb2-20"><a href="#cb2-20" aria-hidden="true"></a>      liftIO <span class="op">$</span> getNotification conn</span>
<span id="cb2-21"><a href="#cb2-21" aria-hidden="true"></a>      dbSource conn ver</span>
<span id="cb2-22"><a href="#cb2-22" aria-hidden="true"></a>    _ <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb2-23"><a href="#cb2-23" aria-hidden="true"></a>      <span class="kw">let</span> ver&#39; <span class="ot">=</span> <span class="fu">maximum</span> <span class="op">$</span> <span class="fu">map</span> fst8 res</span>
<span id="cb2-24"><a href="#cb2-24" aria-hidden="true"></a>      CC.yieldMany res</span>
<span id="cb2-25"><a href="#cb2-25" aria-hidden="true"></a>      dbSource conn ver&#39;</span>
<span id="cb2-26"><a href="#cb2-26" aria-hidden="true"></a></span>
<span id="cb2-27"><a href="#cb2-27" aria-hidden="true"></a><span class="ot">dbSink ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Connection</span> <span class="ot">-&gt;</span> <span class="dt">C.ConduitT</span> (<span class="dt">Int</span>, <span class="dt">Int</span>, <span class="dt">Bool</span>, <span class="dt">UUID</span>, <span class="dt">Text</span>, <span class="dt">Value</span>) <span class="dt">C.Void</span> m ()</span>
<span id="cb2-28"><a href="#cb2-28" aria-hidden="true"></a>dbSink conn <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb2-29"><a href="#cb2-29" aria-hidden="true"></a>  evt <span class="ot">&lt;-</span> C.await</span>
<span id="cb2-30"><a href="#cb2-30" aria-hidden="true"></a>  <span class="kw">case</span> evt <span class="kw">of</span></span>
<span id="cb2-31"><a href="#cb2-31" aria-hidden="true"></a>    <span class="dt">Nothing</span> <span class="ot">-&gt;</span> <span class="fu">return</span> ()</span>
<span id="cb2-32"><a href="#cb2-32" aria-hidden="true"></a>    <span class="dt">Just</span> event <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb2-33"><a href="#cb2-33" aria-hidden="true"></a>      liftIO <span class="op">$</span> execute conn <span class="st">&quot;INSERT INTO events \</span></span>
<span id="cb2-34"><a href="#cb2-34" aria-hidden="true"></a><span class="st">                            \(srv_id, stream_id, cmd, cmd_id, correlation_id, event_data) \</span></span>
<span id="cb2-35"><a href="#cb2-35" aria-hidden="true"></a><span class="st">                            \VALUES (?, ?, ?, ?, ?, ?)&quot;</span> event</span>
<span id="cb2-36"><a href="#cb2-36" aria-hidden="true"></a>      liftIO <span class="op">$</span> execute_ conn <span class="st">&quot;NOTIFY MyEvent&quot;</span></span>
<span id="cb2-37"><a href="#cb2-37" aria-hidden="true"></a>      dbSink conn</span></code></pre></div>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>If I’ve missed something crucial I would of course love to hear about it.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Choosing a conduit randomly</title>
    <link href="http://magnus.therning.org/posts/2019-02-17-000-choosing-a-conduit-randomly.html" />
    <id>http://magnus.therning.org/posts/2019-02-17-000-choosing-a-conduit-randomly.html</id>
    <published>2019-02-17T00:00:00Z</published>
    <updated>2019-02-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Lately I’ve been playing around <a href="https://hackage.haskell.org/package/conduit">conduit</a>. One thing I wanted to try out was to set up processing where one processing step was chosen on random from a number of components, based on weights. In short I guess I wanted a function with a type something like this</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">foo ::</span> [(<span class="dt">Int</span>, <span class="dt">ConduitT</span> i o m r)] <span class="ot">-&gt;</span> <span class="dt">ConduitT</span> i o m r</span></code></pre></div>
<p>I have to admit I don’t even know where to start writing such a function<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> but after a little bit of thinking I realised I could get the same effect by controlling how chunks of data is routed. That is, instead of choosing a component randomly, I can choose a route randomly. It would look something like when choosing from three components</p>
<pre><code>                        +---------+   +----------+   +-------------+
                        | Filter  |   | Drop tag |   | Component A |
                    +--&gt;| Value-0 |--&gt;|          |--&gt;|             |--+
                    |   +---------+   +----------+   +-------------+  |
+----------------+  |   +---------+   +----------+   +-------------+  |
| Choose random  |  |   | Filter  |   | Drop tag |   | Component B |  |
| value based on +-----&gt;| Value-1 |--&gt;|          |--&gt;|             |-----&gt;
| weights        |  |   +---------+   +----------+   +-------------+  |
+----------------+  |   +---------+   +----------+   +-------------+  |
                    |   | Filter  |   | Drop tag |   | Component C |  |
                    +--&gt;| Value-2 |--&gt;|          |--&gt;|             |--+
                        +---------+   +----------+   +-------------+


</code></pre>
<p>That is</p>
<ol type="1">
<li>For each chunk that comes in, choose a value randomly based on weights and tag the chunk with the choosen value, then</li>
<li>split the processing into one route for each component,</li>
<li>in each route filter out chunks tagged with a single value, and</li>
<li>remove the tag, then</li>
<li>pass the chunk to the component, and finally</li>
<li>bring the routes back together again.</li>
</ol>
<p>Out of these steps all but the very first one are already available in <a href="https://hackage.haskell.org/package/conduit">conduit</a>:</p>
<ul>
<li>for splitting routes combining them again, use <a href="https://hackage.haskell.org/package/conduit-1.3.1/docs/Data-Conduit.html#v:sequenceConduits"><code>sequenceConduits</code></a></li>
<li>for filtering, use <a href="https://hackage.haskell.org/package/conduit-1.3.1/docs/Data-Conduit-Combinators.html#v:filter"><code>filter</code></a></li>
<li>for dropping the tag, use <a href="https://hackage.haskell.org/package/conduit-1.3.1/docs/Data-Conduit-Combinators.html#v:map"><code>map</code></a></li>
</ul>
<p>What’s left is the beginning. I started with a function to pick a value on random based on weights<a href="#fn2" class="footnote-ref" id="fnref2" role="doc-noteref"><sup>2</sup></a></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">pickByWeight ::</span> [(<span class="dt">Int</span>, b)] <span class="ot">-&gt;</span> <span class="dt">IO</span> b</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>pickByWeight xs <span class="ot">=</span> randomRIO (<span class="dv">1</span>, tot) <span class="op">&gt;&gt;=</span> \ n <span class="ot">-&gt;</span> <span class="fu">return</span> (pick n xs)</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a>    tot <span class="ot">=</span> <span class="fu">sum</span> <span class="op">$</span> <span class="fu">map</span> <span class="fu">fst</span> xs</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a></span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a>    pick n ((k, x)<span class="op">:</span>xs)</span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a>      <span class="op">|</span> n <span class="op">&lt;=</span> k <span class="ot">=</span> x</span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a>      <span class="op">|</span> <span class="fu">otherwise</span> <span class="ot">=</span> pick (n <span class="op">-</span> k) xs</span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true"></a>    pick _ _ <span class="ot">=</span> <span class="fu">error</span> <span class="st">&quot;pick error&quot;</span></span></code></pre></div>
<p>Using that I then made a component that tags chunks</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a>picker ws <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>  evt <span class="ot">&lt;-</span> await</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  <span class="kw">case</span> evt <span class="kw">of</span></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>    <span class="dt">Nothing</span> <span class="ot">-&gt;</span> <span class="fu">return</span> ()</span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>    <span class="dt">Just</span> e <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a>      p <span class="ot">&lt;-</span> liftIO <span class="op">$</span> pickByWeight ws</span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true"></a>      yield (p, e)</span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true"></a>      picker ws</span></code></pre></div>
<p>I was rather happy with this…</p>
<center>
<blockquote class="twitter-tweet" data-lang="en">
<p lang="en" dir="ltr">
<a href="https://twitter.com/snoyberg?ref_src=twsrc%5Etfw"><span class="citation" data-cites="snoyberg">@snoyberg</span></a> just have to let you know, conduit is a joy to use. Thanks for sharing it.
</p>
— Magnus Therning (<span class="citation" data-cites="magthe">@magthe</span>) <a href="https://twitter.com/magthe/status/1093175648993140736?ref_src=twsrc%5Etfw">February 6, 2019</a>
</blockquote>
<script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
</center>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Except maybe by using Template Haskell to generate the code I did come up with.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
<li id="fn2" role="doc-endnote"><p>I used <code>Quickcheck</code>’s <a href="https://hackage.haskell.org/package/QuickCheck-2.12.6.1/docs/Test-QuickCheck-Gen.html#v:frequency"><code>frequency</code></a> as inspiration for writing it.<a href="#fnref2" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Using stack to get around upstream bugs</title>
    <link href="http://magnus.therning.org/posts/2019-02-10-000-using-stack-to-get-around-upstream-bugs.html" />
    <id>http://magnus.therning.org/posts/2019-02-10-000-using-stack-to-get-around-upstream-bugs.html</id>
    <published>2019-02-10T00:00:00Z</published>
    <updated>2019-02-10T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Recently I bumped into <a href="https://github.com/brendanhay/amazonka/issues/517">a bug in <em>amazonka</em></a>.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> I can’t really sit around waiting for Amazon to fix it, and then for <em>amazonka</em> to use the fixed documentation to generate the code and make another release.</p>
<p>Luckily <a href="https://docs.haskellstack.org/en/stable/README/"><code>stack</code></a> contains features that make it fairly simple to work around this bug until it’s properly fixed. Here’s how.</p>
<ol type="1">
<li><p>Put the upstream code in a git repository of your own. In my case I simply forked the <a href="https://github.com/brendanhay/amazonka"><em>amazonka</em> repository on github</a> (my fork is <a href="https://github.com/magthe/amazonka">here</a>).</p></li>
<li><p>Fix the bug and commit the change. <a href="https://github.com/magthe/amazonka/commit/1543b65e3a8b692aa9038ada68aaed9967752983">My change to <em>amazonka-codepipeline</em></a> was simply to remove the missing fields – it was easier than trying to make them optional (i.e. wrapping them in <code>Maybe</code>s).</p></li>
<li><p>Tell <code>slack</code> to use the code from your modified git repository. In my case I added the following to my <code>slack.yaml</code>:</p>
<pre><code> extra-deps:
   - github: magthe/amazonka
     commit: 1543b65e3a8b692aa9038ada68aaed9967752983
     subdirs:
       - amazonka-codepipeline</code></pre></li>
</ol>
<p>That’s it!</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>The guilty party is Amazon, not <em>amazonka</em>, though I was a little surprised that there doesn’t seem to be any established way to modify the Amazon API documentation before it’s used to autogenerate the Haskell code.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>The ReaderT design pattern or tagless final?</title>
    <link href="http://magnus.therning.org/posts/2019-02-02-000-the-readert-design-pattern-or-tagless-final-.html" />
    <id>http://magnus.therning.org/posts/2019-02-02-000-the-readert-design-pattern-or-tagless-final-.html</id>
    <published>2019-02-02T00:00:00Z</published>
    <updated>2019-02-02T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other week I read V. Kevroletin’s <a href="https://serokell.io/blog/2018/12/07/tagless-final">Introduction to Tagless Final</a> and realised that a couple of my projects, both at work and at home, would benefit from a refactoring to that approach. All in all I was happy with the changes I made, even though I haven’t made use of all the way. In particular there I could further improve the tests in a few places by adding more typeclasses. For now it’s good enough and I’ve clearly gotten some value out of it.</p>
<p>I found mr. Kevroletin’s article to be a good introduction so I’ve been passing it on when people on the <a href="https://functionalprogramming.slack.com/">Functional programming slack</a> bring up questions about how to organize their code as applications grow. In particular if they mention that they’re using monad transformers. I did exactly that just the other day _<span class="citation" data-cites="solomon_">@solomon_</span> wrote</p>
<blockquote>
<p>so i’ve created a rats nest of IO where almost all the functions in my program are in <code>ReaderT Env IO ()</code> and I’m not sure how to purify everything and move the IO to the edge of the program</p>
</blockquote>
<p>I proposed tagless final and passed the URL on, and then I got a pointer to the article <a href="https://www.fpcomplete.com/blog/2017/06/readert-design-pattern">The ReaderT Design Patter</a> which I hadn’t seen before.</p>
<p>The two approches are similar, at least to me, and I can’t really judge if one’s better than the other. Just to get a feel for it I thought I’d try to rewrite the example in the <code>ReaderT</code> article in a tagless final style.</p>
<h2 id="a-slightly-changed-example-of-readert-design-pattern">A slightly changed example of <code>ReaderT</code> design pattern</h2>
<p>I decided to make a few changes to the example in the article:</p>
<ul>
<li>I removed the <code>modify</code> function, instead the code uses the typeclass function <code>modifyBalance</code> directly.</li>
<li>I separated the instances needed for the tests spatially in the code just to make it easier to see what’s “production” code and what’s test code.</li>
<li>I combined the <code>main</code> functions from the various examples to that both an example (<code>main0</code>) and the test (<code>main1</code>) are run.</li>
<li>I switched from <code>Control.Concurrent.Async.Lifted.Safe</code> (from <code>monad-control</code>) to <code>UnliftIO.Async</code> (from <code>unliftio</code>)</li>
</ul>
<p>After that the code looks like this</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">{-# LANGUAGE FlexibleContexts #-}</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="ot">{-# LANGUAGE FlexibleInstances #-}</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Control.Concurrent.STM</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Control.Monad.Reader</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Control.Monad.State.Strict</span> <span class="kw">as</span> <span class="dt">State</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Say</span></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Test.Hspec</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">UnliftIO.Async</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">Env</span> <span class="ot">=</span> <span class="dt">Env</span></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a>  {<span class="ot"> envLog ::</span> <span class="op">!</span>(<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ())</span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true"></a>  ,<span class="ot"> envBalance ::</span> <span class="op">!</span>(<span class="dt">TVar</span> <span class="dt">Int</span>)</span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true"></a>  }</span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true"></a></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">HasLog</span> a <span class="kw">where</span></span>
<span id="cb1-17"><a href="#cb1-17" aria-hidden="true"></a><span class="ot">  getLog ::</span> a <span class="ot">-&gt;</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ())</span>
<span id="cb1-18"><a href="#cb1-18" aria-hidden="true"></a></span>
<span id="cb1-19"><a href="#cb1-19" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HasLog</span> <span class="dt">Env</span> <span class="kw">where</span></span>
<span id="cb1-20"><a href="#cb1-20" aria-hidden="true"></a>  getLog <span class="ot">=</span> envLog</span>
<span id="cb1-21"><a href="#cb1-21" aria-hidden="true"></a></span>
<span id="cb1-22"><a href="#cb1-22" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">HasBalance</span> a <span class="kw">where</span></span>
<span id="cb1-23"><a href="#cb1-23" aria-hidden="true"></a><span class="ot">  getBalance ::</span> a <span class="ot">-&gt;</span> <span class="dt">TVar</span> <span class="dt">Int</span></span>
<span id="cb1-24"><a href="#cb1-24" aria-hidden="true"></a></span>
<span id="cb1-25"><a href="#cb1-25" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HasBalance</span> <span class="dt">Env</span> <span class="kw">where</span></span>
<span id="cb1-26"><a href="#cb1-26" aria-hidden="true"></a>  getBalance <span class="ot">=</span> envBalance</span>
<span id="cb1-27"><a href="#cb1-27" aria-hidden="true"></a></span>
<span id="cb1-28"><a href="#cb1-28" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadBalance</span> m <span class="kw">where</span></span>
<span id="cb1-29"><a href="#cb1-29" aria-hidden="true"></a><span class="ot">  modifyBalance ::</span> (<span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Int</span>) <span class="ot">-&gt;</span> m ()</span>
<span id="cb1-30"><a href="#cb1-30" aria-hidden="true"></a></span>
<span id="cb1-31"><a href="#cb1-31" aria-hidden="true"></a><span class="kw">instance</span> (<span class="dt">HasBalance</span> env, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">MonadBalance</span> (<span class="dt">ReaderT</span> env m) <span class="kw">where</span></span>
<span id="cb1-32"><a href="#cb1-32" aria-hidden="true"></a>  modifyBalance f <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-33"><a href="#cb1-33" aria-hidden="true"></a>    env <span class="ot">&lt;-</span> ask</span>
<span id="cb1-34"><a href="#cb1-34" aria-hidden="true"></a>    liftIO <span class="op">$</span> atomically <span class="op">$</span> modifyTVar&#39; (getBalance env) f</span>
<span id="cb1-35"><a href="#cb1-35" aria-hidden="true"></a></span>
<span id="cb1-36"><a href="#cb1-36" aria-hidden="true"></a><span class="ot">logSomething ::</span> (<span class="dt">MonadReader</span> env m, <span class="dt">HasLog</span> env, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> m ()</span>
<span id="cb1-37"><a href="#cb1-37" aria-hidden="true"></a>logSomething msg <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-38"><a href="#cb1-38" aria-hidden="true"></a>  env <span class="ot">&lt;-</span> ask</span>
<span id="cb1-39"><a href="#cb1-39" aria-hidden="true"></a>  liftIO <span class="op">$</span> getLog env msg</span>
<span id="cb1-40"><a href="#cb1-40" aria-hidden="true"></a></span>
<span id="cb1-41"><a href="#cb1-41" aria-hidden="true"></a><span class="ot">main0 ::</span> <span class="dt">IO</span> ()</span>
<span id="cb1-42"><a href="#cb1-42" aria-hidden="true"></a>main0 <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-43"><a href="#cb1-43" aria-hidden="true"></a>  ref <span class="ot">&lt;-</span> newTVarIO <span class="dv">4</span></span>
<span id="cb1-44"><a href="#cb1-44" aria-hidden="true"></a>  <span class="kw">let</span> env <span class="ot">=</span> <span class="dt">Env</span> { envLog <span class="ot">=</span> sayString , envBalance <span class="ot">=</span> ref }</span>
<span id="cb1-45"><a href="#cb1-45" aria-hidden="true"></a>  runReaderT</span>
<span id="cb1-46"><a href="#cb1-46" aria-hidden="true"></a>    (concurrently_</span>
<span id="cb1-47"><a href="#cb1-47" aria-hidden="true"></a>      (modifyBalance (<span class="op">+</span> <span class="dv">1</span>))</span>
<span id="cb1-48"><a href="#cb1-48" aria-hidden="true"></a>      (logSomething <span class="st">&quot;Increasing account balance&quot;</span>))</span>
<span id="cb1-49"><a href="#cb1-49" aria-hidden="true"></a>    env</span>
<span id="cb1-50"><a href="#cb1-50" aria-hidden="true"></a>  balance <span class="ot">&lt;-</span> readTVarIO ref</span>
<span id="cb1-51"><a href="#cb1-51" aria-hidden="true"></a>  sayString <span class="op">$</span> <span class="st">&quot;Final balance: &quot;</span> <span class="op">++</span> <span class="fu">show</span> balance</span>
<span id="cb1-52"><a href="#cb1-52" aria-hidden="true"></a></span>
<span id="cb1-53"><a href="#cb1-53" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HasLog</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()) <span class="kw">where</span></span>
<span id="cb1-54"><a href="#cb1-54" aria-hidden="true"></a>  getLog <span class="ot">=</span> <span class="fu">id</span></span>
<span id="cb1-55"><a href="#cb1-55" aria-hidden="true"></a></span>
<span id="cb1-56"><a href="#cb1-56" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">HasBalance</span> (<span class="dt">TVar</span> <span class="dt">Int</span>) <span class="kw">where</span></span>
<span id="cb1-57"><a href="#cb1-57" aria-hidden="true"></a>  getBalance <span class="ot">=</span> <span class="fu">id</span></span>
<span id="cb1-58"><a href="#cb1-58" aria-hidden="true"></a></span>
<span id="cb1-59"><a href="#cb1-59" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadBalance</span> (<span class="dt">State.StateT</span> <span class="dt">Int</span> m) <span class="kw">where</span></span>
<span id="cb1-60"><a href="#cb1-60" aria-hidden="true"></a>  modifyBalance <span class="ot">=</span> State.modify</span>
<span id="cb1-61"><a href="#cb1-61" aria-hidden="true"></a></span>
<span id="cb1-62"><a href="#cb1-62" aria-hidden="true"></a><span class="ot">main1 ::</span> <span class="dt">IO</span> ()</span>
<span id="cb1-63"><a href="#cb1-63" aria-hidden="true"></a>main1 <span class="ot">=</span> hspec <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-64"><a href="#cb1-64" aria-hidden="true"></a>  describe <span class="st">&quot;modify&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-65"><a href="#cb1-65" aria-hidden="true"></a>    it <span class="st">&quot;works, IO&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-66"><a href="#cb1-66" aria-hidden="true"></a>      var <span class="ot">&lt;-</span> newTVarIO (<span class="dv">1</span><span class="ot"> ::</span> <span class="dt">Int</span>)</span>
<span id="cb1-67"><a href="#cb1-67" aria-hidden="true"></a>      runReaderT (modifyBalance (<span class="op">+</span> <span class="dv">2</span>)) var</span>
<span id="cb1-68"><a href="#cb1-68" aria-hidden="true"></a>      res <span class="ot">&lt;-</span> readTVarIO var</span>
<span id="cb1-69"><a href="#cb1-69" aria-hidden="true"></a>      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></span>
<span id="cb1-70"><a href="#cb1-70" aria-hidden="true"></a>    it <span class="st">&quot;works, pure&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-71"><a href="#cb1-71" aria-hidden="true"></a>      <span class="kw">let</span> res <span class="ot">=</span> State.execState (modifyBalance (<span class="op">+</span> <span class="dv">2</span>)) (<span class="dv">1</span><span class="ot"> ::</span> <span class="dt">Int</span>)</span>
<span id="cb1-72"><a href="#cb1-72" aria-hidden="true"></a>      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></span>
<span id="cb1-73"><a href="#cb1-73" aria-hidden="true"></a>  describe <span class="st">&quot;logSomething&quot;</span> <span class="op">$</span></span>
<span id="cb1-74"><a href="#cb1-74" aria-hidden="true"></a>    it <span class="st">&quot;works&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-75"><a href="#cb1-75" aria-hidden="true"></a>      var <span class="ot">&lt;-</span> newTVarIO <span class="st">&quot;&quot;</span></span>
<span id="cb1-76"><a href="#cb1-76" aria-hidden="true"></a>      <span class="kw">let</span> logFunc msg <span class="ot">=</span> atomically <span class="op">$</span> modifyTVar var (<span class="op">++</span> msg)</span>
<span id="cb1-77"><a href="#cb1-77" aria-hidden="true"></a>          msg1 <span class="ot">=</span> <span class="st">&quot;Hello &quot;</span></span>
<span id="cb1-78"><a href="#cb1-78" aria-hidden="true"></a>          msg2 <span class="ot">=</span> <span class="st">&quot;World\n&quot;</span></span>
<span id="cb1-79"><a href="#cb1-79" aria-hidden="true"></a>      runReaderT (logSomething msg1 <span class="op">&gt;&gt;</span> logSomething msg2) logFunc</span>
<span id="cb1-80"><a href="#cb1-80" aria-hidden="true"></a>      res <span class="ot">&lt;-</span> readTVarIO var</span>
<span id="cb1-81"><a href="#cb1-81" aria-hidden="true"></a>      res <span class="ot">`shouldBe`</span> (msg1 <span class="op">++</span> msg2)</span>
<span id="cb1-82"><a href="#cb1-82" aria-hidden="true"></a></span>
<span id="cb1-83"><a href="#cb1-83" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb1-84"><a href="#cb1-84" aria-hidden="true"></a>main <span class="ot">=</span> main0 <span class="op">&gt;&gt;</span> main1</span></code></pre></div>
<p>I think the distinguising features are</p>
<ul>
<li>The application environmant, <code>Env</code> will contain configuraiton values (not in this example), state, <code>envBalance</code>, and functions we might want to vary, <code>envLog</code></li>
<li>There is no explicit type representing the execution context</li>
<li>Typeclasses are used to abstract over application environment, <code>HasLog</code> and <code>HasBalance</code></li>
<li>Typeclasses are used to abstract over operations, <code>MonadBalance</code></li>
<li>Typeclasses are implemented for both the application environment, <code>HasLog</code> and <code>HasBalance</code>, and the execution context, <code>MonadBalance</code></li>
</ul>
<p>In the end this makes for code with very loose couplings; there’s not really any single concrete type that implements all the constraints to work in the “real” main function (<code>main0</code>). I could of course introduce a type synonym for it</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">App</span> <span class="ot">=</span> <span class="dt">ReaderT</span> <span class="dt">Env</span> <span class="dt">IO</span></span></code></pre></div>
<p>but it brings no value – it wouldn’t be used explicitly anywhere.</p>
<h2 id="a-tagless-final-version">A tagless final version</h2>
<p>In order to compare the <code>ReaderT</code> design pattern to tagless final (as I understand it) I made an attempt to translate the code above. The code below is the result.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">{-# LANGUAGE FlexibleContexts #-}</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a><span class="ot">{-# LANGUAGE FlexibleInstances #-}</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a><span class="ot">{-# LANGUAGE GeneralizedNewtypeDeriving #-}</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a><span class="ot">{-# LANGUAGE MultiParamTypeClasses #-}</span></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a><span class="ot">{-# LANGUAGE TypeFamilies #-}</span></span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a></span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Control.Concurrent.STM</span></span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Control.Monad.Identity</span> <span class="kw">as</span> <span class="dt">Id</span></span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Control.Monad.Reader</span></span>
<span id="cb3-10"><a href="#cb3-10" aria-hidden="true"></a><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Control.Monad.State.Strict</span> <span class="kw">as</span> <span class="dt">State</span></span>
<span id="cb3-11"><a href="#cb3-11" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Say</span></span>
<span id="cb3-12"><a href="#cb3-12" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Test.Hspec</span></span>
<span id="cb3-13"><a href="#cb3-13" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">UnliftIO</span> (<span class="dt">MonadUnliftIO</span>)</span>
<span id="cb3-14"><a href="#cb3-14" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">UnliftIO.Async</span></span>
<span id="cb3-15"><a href="#cb3-15" aria-hidden="true"></a></span>
<span id="cb3-16"><a href="#cb3-16" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">Env</span> <span class="ot">=</span> <span class="dt">Env</span> {<span class="ot">envBalance ::</span> <span class="dt">TVar</span> <span class="dt">Int</span>}</span>
<span id="cb3-17"><a href="#cb3-17" aria-hidden="true"></a></span>
<span id="cb3-18"><a href="#cb3-18" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="ot">=</span> <span class="dt">AppM</span> {<span class="ot">unAppM ::</span> <span class="dt">ReaderT</span> <span class="dt">Env</span> <span class="dt">IO</span> a}</span>
<span id="cb3-19"><a href="#cb3-19" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> <span class="dt">Env</span>, <span class="dt">MonadUnliftIO</span>)</span>
<span id="cb3-20"><a href="#cb3-20" aria-hidden="true"></a></span>
<span id="cb3-21"><a href="#cb3-21" aria-hidden="true"></a><span class="ot">runAppM ::</span> <span class="dt">Env</span> <span class="ot">-&gt;</span> <span class="dt">AppM</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb3-22"><a href="#cb3-22" aria-hidden="true"></a>runAppM env app <span class="ot">=</span> runReaderT (unAppM app) env</span>
<span id="cb3-23"><a href="#cb3-23" aria-hidden="true"></a></span>
<span id="cb3-24"><a href="#cb3-24" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">ModifyM</span> m <span class="kw">where</span></span>
<span id="cb3-25"><a href="#cb3-25" aria-hidden="true"></a><span class="ot">  mModify ::</span> (<span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Int</span>) <span class="ot">-&gt;</span> m ()</span>
<span id="cb3-26"><a href="#cb3-26" aria-hidden="true"></a></span>
<span id="cb3-27"><a href="#cb3-27" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">LogSomethingM</span> m <span class="kw">where</span></span>
<span id="cb3-28"><a href="#cb3-28" aria-hidden="true"></a><span class="ot">  mLogSomething ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> m()</span>
<span id="cb3-29"><a href="#cb3-29" aria-hidden="true"></a></span>
<span id="cb3-30"><a href="#cb3-30" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">ModifyM</span> <span class="dt">AppM</span> <span class="kw">where</span></span>
<span id="cb3-31"><a href="#cb3-31" aria-hidden="true"></a>  mModify f <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb3-32"><a href="#cb3-32" aria-hidden="true"></a>    ref <span class="ot">&lt;-</span> asks envBalance</span>
<span id="cb3-33"><a href="#cb3-33" aria-hidden="true"></a>    liftIO <span class="op">$</span> atomically <span class="op">$</span> modifyTVar&#39; ref f</span>
<span id="cb3-34"><a href="#cb3-34" aria-hidden="true"></a></span>
<span id="cb3-35"><a href="#cb3-35" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">LogSomethingM</span> <span class="dt">AppM</span> <span class="kw">where</span></span>
<span id="cb3-36"><a href="#cb3-36" aria-hidden="true"></a>  mLogSomething <span class="ot">=</span> liftIO <span class="op">.</span> sayString</span>
<span id="cb3-37"><a href="#cb3-37" aria-hidden="true"></a></span>
<span id="cb3-38"><a href="#cb3-38" aria-hidden="true"></a><span class="ot">main0 ::</span> <span class="dt">IO</span> ()</span>
<span id="cb3-39"><a href="#cb3-39" aria-hidden="true"></a>main0 <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb3-40"><a href="#cb3-40" aria-hidden="true"></a>  ref <span class="ot">&lt;-</span> newTVarIO <span class="dv">4</span></span>
<span id="cb3-41"><a href="#cb3-41" aria-hidden="true"></a>  <span class="kw">let</span> env <span class="ot">=</span> <span class="dt">Env</span> ref</span>
<span id="cb3-42"><a href="#cb3-42" aria-hidden="true"></a>  runAppM env</span>
<span id="cb3-43"><a href="#cb3-43" aria-hidden="true"></a>    (concurrently_</span>
<span id="cb3-44"><a href="#cb3-44" aria-hidden="true"></a>      (mModify (<span class="op">+</span> <span class="dv">1</span>))</span>
<span id="cb3-45"><a href="#cb3-45" aria-hidden="true"></a>      (mLogSomething <span class="st">&quot;Increasing account balance&quot;</span>))</span>
<span id="cb3-46"><a href="#cb3-46" aria-hidden="true"></a>  balance <span class="ot">&lt;-</span> readTVarIO ref</span>
<span id="cb3-47"><a href="#cb3-47" aria-hidden="true"></a>  sayString <span class="op">$</span> <span class="st">&quot;Final balance: &quot;</span> <span class="op">++</span> <span class="fu">show</span> balance</span>
<span id="cb3-48"><a href="#cb3-48" aria-hidden="true"></a></span>
<span id="cb3-49"><a href="#cb3-49" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">ModifyAppM</span> a <span class="ot">=</span> <span class="dt">ModifyAppM</span> {<span class="ot">unModifyAppM ::</span> <span class="dt">State.StateT</span> <span class="dt">Int</span> <span class="dt">Id.Identity</span> a}</span>
<span id="cb3-50"><a href="#cb3-50" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">State.MonadState</span> <span class="dt">Int</span>)</span>
<span id="cb3-51"><a href="#cb3-51" aria-hidden="true"></a></span>
<span id="cb3-52"><a href="#cb3-52" aria-hidden="true"></a><span class="ot">runModifyAppM ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">ModifyAppM</span> a <span class="ot">-&gt;</span> (a, <span class="dt">Int</span>)</span>
<span id="cb3-53"><a href="#cb3-53" aria-hidden="true"></a>runModifyAppM s app <span class="ot">=</span> Id.runIdentity <span class="op">$</span> State.runStateT (unModifyAppM app) s</span>
<span id="cb3-54"><a href="#cb3-54" aria-hidden="true"></a></span>
<span id="cb3-55"><a href="#cb3-55" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">ModifyM</span> <span class="dt">ModifyAppM</span> <span class="kw">where</span></span>
<span id="cb3-56"><a href="#cb3-56" aria-hidden="true"></a>  mModify <span class="ot">=</span> State.modify&#39;</span>
<span id="cb3-57"><a href="#cb3-57" aria-hidden="true"></a></span>
<span id="cb3-58"><a href="#cb3-58" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">LogAppM</span> a <span class="ot">=</span> <span class="dt">LogAppM</span> {<span class="ot">unLogAppM ::</span> <span class="dt">ReaderT</span> (<span class="dt">TVar</span> <span class="dt">String</span>) <span class="dt">IO</span> a}</span>
<span id="cb3-59"><a href="#cb3-59" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> (<span class="dt">TVar</span> <span class="dt">String</span>))</span>
<span id="cb3-60"><a href="#cb3-60" aria-hidden="true"></a></span>
<span id="cb3-61"><a href="#cb3-61" aria-hidden="true"></a><span class="ot">runLogAppM ::</span> <span class="dt">TVar</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">LogAppM</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb3-62"><a href="#cb3-62" aria-hidden="true"></a>runLogAppM env app <span class="ot">=</span> runReaderT (unLogAppM app) env</span>
<span id="cb3-63"><a href="#cb3-63" aria-hidden="true"></a></span>
<span id="cb3-64"><a href="#cb3-64" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">LogSomethingM</span> <span class="dt">LogAppM</span> <span class="kw">where</span></span>
<span id="cb3-65"><a href="#cb3-65" aria-hidden="true"></a>  mLogSomething msg <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb3-66"><a href="#cb3-66" aria-hidden="true"></a>    var <span class="ot">&lt;-</span> ask</span>
<span id="cb3-67"><a href="#cb3-67" aria-hidden="true"></a>    liftIO <span class="op">$</span> atomically <span class="op">$</span> modifyTVar var (<span class="op">++</span> msg)</span>
<span id="cb3-68"><a href="#cb3-68" aria-hidden="true"></a></span>
<span id="cb3-69"><a href="#cb3-69" aria-hidden="true"></a><span class="ot">main1 ::</span> <span class="dt">IO</span> ()</span>
<span id="cb3-70"><a href="#cb3-70" aria-hidden="true"></a>main1 <span class="ot">=</span> hspec <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb3-71"><a href="#cb3-71" aria-hidden="true"></a>  describe <span class="st">&quot;mModify&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb3-72"><a href="#cb3-72" aria-hidden="true"></a>    it <span class="st">&quot;works, IO&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb3-73"><a href="#cb3-73" aria-hidden="true"></a>      var <span class="ot">&lt;-</span> newTVarIO <span class="dv">1</span></span>
<span id="cb3-74"><a href="#cb3-74" aria-hidden="true"></a>      runAppM (<span class="dt">Env</span> var) (mModify (<span class="op">+</span> <span class="dv">2</span>))</span>
<span id="cb3-75"><a href="#cb3-75" aria-hidden="true"></a>      res <span class="ot">&lt;-</span> readTVarIO var</span>
<span id="cb3-76"><a href="#cb3-76" aria-hidden="true"></a>      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></span>
<span id="cb3-77"><a href="#cb3-77" aria-hidden="true"></a>    it <span class="st">&quot;works, pure&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb3-78"><a href="#cb3-78" aria-hidden="true"></a>      <span class="kw">let</span> (_, res) <span class="ot">=</span> runModifyAppM <span class="dv">1</span> (mModify (<span class="op">+</span> <span class="dv">2</span>))</span>
<span id="cb3-79"><a href="#cb3-79" aria-hidden="true"></a>      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></span>
<span id="cb3-80"><a href="#cb3-80" aria-hidden="true"></a>  describe <span class="st">&quot;mLogSomething&quot;</span> <span class="op">$</span></span>
<span id="cb3-81"><a href="#cb3-81" aria-hidden="true"></a>    it <span class="st">&quot;works&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb3-82"><a href="#cb3-82" aria-hidden="true"></a>      var <span class="ot">&lt;-</span> newTVarIO <span class="st">&quot;&quot;</span></span>
<span id="cb3-83"><a href="#cb3-83" aria-hidden="true"></a>      runLogAppM var (mLogSomething <span class="st">&quot;Hello&quot;</span> <span class="op">&gt;&gt;</span> mLogSomething <span class="st">&quot;World!&quot;</span>)</span>
<span id="cb3-84"><a href="#cb3-84" aria-hidden="true"></a>      res <span class="ot">&lt;-</span> readTVarIO var</span>
<span id="cb3-85"><a href="#cb3-85" aria-hidden="true"></a>      res <span class="ot">`shouldBe`</span> <span class="st">&quot;HelloWorld!&quot;</span></span>
<span id="cb3-86"><a href="#cb3-86" aria-hidden="true"></a></span>
<span id="cb3-87"><a href="#cb3-87" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb3-88"><a href="#cb3-88" aria-hidden="true"></a>main <span class="ot">=</span> main0 <span class="op">&gt;&gt;</span> main1</span></code></pre></div>
<p>The steps for the “real” part of the program were</p>
<ol type="1">
<li>Introduce an execution type, <code>AppM</code>, with a convenience function for running it, <code>runAppM</code></li>
<li>Remove the log function from the environment type, <code>envLog</code> in <code>Env</code></li>
<li>Remove all the <code>HasX</code> classes</li>
<li>Create a new operations typeclass for logging, <code>LogSomethingM</code></li>
<li>Rename the operations typeclass for modifying the balance to match the naming found in the <a href="https://serokell.io/blog/2018/12/07/tagless-final">tagless article</a> a bit better, <code>ModifyM</code></li>
<li>Implement instances of both operations typeclasses for <code>AppM</code></li>
</ol>
<p>For testing the steps were</p>
<ol type="1">
<li>Define an execution type for each test, <code>ModifyAppM</code> and <code>LogAppM</code>, with some convenience functions for running them, <code>runModifyAppM</code> and <code>runLogAppM</code></li>
<li>Write instances for the operations typeclasses, one for each</li>
</ol>
<p>So I think the distinguising features are</p>
<ul>
<li>There’s both an environment type, <code>Env</code>, and an execution type <code>AppM</code> that wraps it</li>
<li>The environment holds only configuration values (none in this example), and state (<code>envBalance</code>)</li>
<li>Typeclasses are used to abstract over operations, <code>LogSomethingM</code> and <code>ModifyM</code></li>
<li>Typeclasses are only implemented for the execution type</li>
</ul>
<p>This version has slightly more coupling, the execution type specifies the environment to use, and the operations are tied directly to the execution type. However, this coupling doesn’t really make a big difference – looking at the pure modify test the amount of code don’t differ by much.</p>
<h3 id="a-short-note-mostly-to-myself">A short note (mostly to myself)</h3>
<p>I did write it using <code>monad-control</code> first, and then I needed an instance for <code>MonadBaseControl IO</code>. Deriving it automatically requires <code>UndecidableInstances</code> and I didn’t really dare turn that on, so I ended up writing the instance. After some help on <a href="https://mail.haskell.org/pipermail/haskell-cafe/2019-February/130667.html">haskell-cafe</a> it ended up looking like this</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">MonadBaseControl</span> <span class="dt">IO</span> <span class="dt">AppM</span> <span class="kw">where</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>  <span class="kw">type</span> <span class="dt">StM</span> <span class="dt">AppM</span> a <span class="ot">=</span> a</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  liftBaseWith f <span class="ot">=</span> <span class="dt">AppM</span> (liftBaseWith <span class="op">$</span> \ run <span class="ot">-&gt;</span> f (run <span class="op">.</span> unAppM))</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>  restoreM <span class="ot">=</span> <span class="fu">return</span></span></code></pre></div>
<h2 id="conclusion">Conclusion</h2>
<p>My theoretical knowledge isn’t anywhere near good enough to say anything objectively about the difference in expressiveness of the two design patterns. That means that my conclusion comes down to taste, do you like the <code>readerT</code> patter or tagless final better?</p>
<p>I like the slightly looser coupling I get with the <code>ReaderT</code> pattern. Loose coupling is (almost) always a desirable goal. However, I can see that tying the typeclass instances directly to a concrete execution type results in the intent being communicated a little more clearly. Clearly communicating intent in code is also a desirable goal. In particular I suspect it’ll result in more actionable error messages when making changes to the code – the error will tell me that my execution type lacks an instance of a specific typeclass, instead of it telling me that a particular transformer stack does. On the other hand, in the <code>ReaderT</code> pattern that stack is very shallow.</p>
<p>One possibility would be that one pattern is better suited for libraries and the other for applications. I don’t think that’s the case though as in both cases the library code would be written in a style that results in typeclass constraints on the caller and providing instances for those typeclasses is roughly an equal amount of work for both styles.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Please do point out any mistakes I’ve made in this, in particular if they stem from me misunderstanding tagless final completely.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>A missing piece in my Emacs/Spacemacs setup for Haskell development</title>
    <link href="http://magnus.therning.org/posts/2019-01-28-000-a-missing-piece-in-my-emacs-spacemacs-setup-for-haskell-development.html" />
    <id>http://magnus.therning.org/posts/2019-01-28-000-a-missing-piece-in-my-emacs-spacemacs-setup-for-haskell-development.html</id>
    <published>2019-01-28T00:00:00Z</published>
    <updated>2019-01-28T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>With the help of a work mate I’ve finally found this gem that’s been missing from my Spacemacs setup</p>
<pre class="elisp"><code>(with-eval-after-load &#39;intero
  (flycheck-add-next-checker &#39;intero &#39;(warning . haskell-hlint))
  (flycheck-add-next-checker &#39;intero &#39;(warning . haskell-stack-ghc)))</code></pre>]]></summary>
</entry>
<entry>
    <title>Tagless final and Scotty</title>
    <link href="http://magnus.therning.org/posts/2019-01-20-000-tagless-final-and-scotty.html" />
    <id>http://magnus.therning.org/posts/2019-01-20-000-tagless-final-and-scotty.html</id>
    <published>2019-01-20T00:00:00Z</published>
    <updated>2019-01-20T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a little while I’ve been playing around with event sourcing in Haskell using <a href="https://hackage.haskell.org/package/conduit"><code>Conduit</code></a> and <a href="https://hackage.haskell.org/package/scotty"><code>Scotty</code></a>. I’ve come far enough that the basic functionality I’m after is there together with all those little bits that make it a piece of software that’s fit for deployment in production (configuration, logging, etc.). There’s just one thing that’s been nagging me, testability.</p>
<p>The app is built of two main parts, a web server (<code>Scotty</code>) and a pipeline of stream processing components (<code>Conduit</code>). The part using <code>Scotty</code> is utilising a simple monad stack, <code>ReaderT Config IO</code>, and the <code>Conduit</code> part is using <code>Conduit In Out IO</code>. This means that in both parts the outer edge, the part dealing with the outside world, is running in <code>IO</code> directly. Something that isn’t really aiding in testing.</p>
<p>I started out thinking that I’d rewrite what I have using a free monad with a bunch of interpreters. Then I remembered that I have “check out tagless final”. This post is a record of the small experiments I did to see how to use it with <code>Scotty</code> to achieve (and actually improve) on the code I have in my production-ready code.</p>
<h2 id="use-tagless-final-with-scotty">1 - Use tagless final with <code>Scotty</code></h2>
<p>As a first simple little experiment I wrote a tiny little web server that would print a string to <code>stdout</code> when receiving the request to <code>GET /route0</code>.</p>
<p>The printing to <code>stdout</code> is the operation I want to make abstract.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadPrinter</span> m <span class="kw">where</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="ot">  mPutStrLn ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> m ()</span></code></pre></div>
<p>I then created an application type that is an instance of that class.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="ot">=</span> <span class="dt">AppM</span> {<span class="ot"> unAppM ::</span> <span class="dt">IO</span> a }</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>)</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">MonadPrinter</span> <span class="dt">AppM</span> <span class="kw">where</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>  mPutStrLn t <span class="ot">=</span> liftIO <span class="op">$</span> <span class="fu">putStrLn</span> (unpack t)</span></code></pre></div>
<p>Then I added a bit of <code>Scotty</code> boilerplate. It’s not strictly necessary, but does make the code a bit nicer to read.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">FooM</span> <span class="ot">=</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> <span class="dt">AppM</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">FooActionM</span> <span class="ot">=</span> <span class="dt">ActionT</span> <span class="dt">Text</span> <span class="dt">AppM</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a><span class="ot">foo ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Port</span> <span class="ot">-&gt;</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> <span class="dt">AppM</span> () <span class="ot">-&gt;</span> m ()</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>foo port <span class="ot">=</span> scottyT port unAppM</span></code></pre></div>
<p>With that in place the web server itself is just a matter of tying it all together.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  foo <span class="dv">3000</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>    get <span class="st">&quot;/route0&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>      lift <span class="op">$</span> mPutStrLn <span class="st">&quot;getting /route0&quot;</span></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a>      json <span class="op">$</span> object [<span class="st">&quot;route0&quot;</span> <span class="op">.=</span> (<span class="st">&quot;ok&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true"></a>    notFound <span class="op">$</span> json <span class="op">$</span> object [<span class="st">&quot;error&quot;</span> <span class="op">.=</span> (<span class="st">&quot;not found&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span></code></pre></div>
<p>That was simple enough.</p>
<h2 id="add-configuration">2 - Add configuration</h2>
<p>In order to try out how to deal with configuration I added a class for doing some simple logging</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadLogger</span> m <span class="kw">where</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a><span class="ot">  mLog ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> m ()</span></code></pre></div>
<p>The straight forward way to deal with configuration is to create a monad stack with <code>ReaderT</code> and since it’s logging I want to do the configuration consists of a single <code>LoggerSet</code> (from <a href="https://hackage.haskell.org/package/fast-logger"><code>fast-logger</code></a>).</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="ot">=</span> <span class="dt">AppM</span> {<span class="ot"> unAppM ::</span> <span class="dt">ReaderT</span> <span class="dt">LoggerSet</span> <span class="dt">IO</span> a }</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> <span class="dt">LoggerSet</span>)</span></code></pre></div>
<p>That means the class instance can be implemented like this</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">MonadLogger</span> <span class="dt">AppM</span> <span class="kw">where</span></span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>  mLog msg <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true"></a>    ls <span class="ot">&lt;-</span> ask</span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true"></a>    liftIO <span class="op">$</span> pushLogStrLn ls <span class="op">$</span> toLogStr msg</span></code></pre></div>
<p>Of course <code>foo</code> has to be changed too, and it becomes a little easier with a wrapper for <code>runReaderT</code> and <code>unAppM</code>.</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="ot">foo ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">LoggerSet</span> <span class="ot">-&gt;</span> <span class="dt">Port</span> <span class="ot">-&gt;</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> <span class="dt">AppM</span> () <span class="ot">-&gt;</span> m ()</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>foo ls port <span class="ot">=</span> scottyT port (<span class="ot">`runAppM`</span> ls)</span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a></span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true"></a><span class="ot">runAppM ::</span> <span class="dt">AppM</span> a <span class="ot">-&gt;</span> <span class="dt">LoggerSet</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true"></a>runAppM app ls <span class="ot">=</span> runReaderT (unAppM app) ls</span></code></pre></div>
<p>With that in place the printing to <code>stdout</code> can be replaced by a writing to the log.</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true"></a>  ls <span class="ot">&lt;-</span> newStdoutLoggerSet defaultBufSize</span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true"></a>  foo ls <span class="dv">3000</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true"></a>    get <span class="st">&quot;/route0&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true"></a>      lift <span class="op">$</span> mLog <span class="st">&quot;log: getting /route0&quot;</span></span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true"></a>      json <span class="op">$</span> object [<span class="st">&quot;route0&quot;</span> <span class="op">.=</span> (<span class="st">&quot;ok&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span>
<span id="cb9-8"><a href="#cb9-8" aria-hidden="true"></a>    notFound <span class="op">$</span> json <span class="op">$</span> object [<span class="st">&quot;error&quot;</span> <span class="op">.=</span> (<span class="st">&quot;not found&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span></code></pre></div>
<p>Not really a big change, I’d say. Extending the configuration is clearly straight forward too.</p>
<h2 id="per-request-configuration">3 - Per-request configuration</h2>
<p>At work we use correlation IDs<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> and I think that the most convenient way to deal with it is to put the correlation ID into the configuration after extracting it. That is, I want to modify the configuration on each request. Luckily it turns out to be possible to do that, despite using <code>ReaderT</code> for holding the configuration.</p>
<p>I can’t be bothered with a full implementation of correlation ID for this little experiment, but as long as I can get a new <code>AppM</code> by running a function on the configuration it’s just a matter of extracting the correct header from the request. For this experiment it’ll do to just modify an integer in the configuration.</p>
<p>I start with defining a type for the configuration and changing <code>AppM</code>.</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">Config</span> <span class="ot">=</span> (<span class="dt">LoggerSet</span>, <span class="dt">Int</span>)</span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true"></a></span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="ot">=</span> <span class="dt">AppM</span> {<span class="ot"> unAppM ::</span> <span class="dt">ReaderT</span> <span class="dt">Config</span> <span class="dt">IO</span> a }</span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> <span class="dt">Config</span>)</span></code></pre></div>
<p>The logger instance has to be changed accordingly of course.</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">MonadLogger</span> <span class="dt">AppM</span> <span class="kw">where</span></span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true"></a>  mLog msg <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true"></a>    (ls, i) <span class="ot">&lt;-</span> ask</span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true"></a>    liftIO <span class="op">$</span> pushLogStrLn ls <span class="op">$</span> toLogStr msg <span class="op">&lt;&gt;</span> toLogStr (<span class="st">&quot;:&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>) <span class="op">&lt;&gt;</span> toLogStr (<span class="fu">show</span> i)</span></code></pre></div>
<p>The <code>get</code> function that comes with <code>scotty</code> isn’t going to cut it, since it has no way of modifying the configuration, so I’ll need a new one.</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true"></a><span class="ot">mGet ::</span> <span class="dt">ScottyError</span> e <span class="ot">=&gt;</span> <span class="dt">RoutePattern</span> <span class="ot">-&gt;</span> <span class="dt">ActionT</span> e <span class="dt">AppM</span> () <span class="ot">-&gt;</span> <span class="dt">ScottyT</span> e <span class="dt">AppM</span> ()</span>
<span id="cb12-2"><a href="#cb12-2" aria-hidden="true"></a>mGet p a <span class="ot">=</span> get p <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb12-3"><a href="#cb12-3" aria-hidden="true"></a>  withCfg (\ (ls, i) <span class="ot">-&gt;</span> (ls, <span class="fu">succ</span> i)) a</span></code></pre></div>
<p>The tricky bit is in the <code>withCfg</code> function. It’s indeed not very easy to read, I think</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb13-1"><a href="#cb13-1" aria-hidden="true"></a>withCfg <span class="ot">=</span> mapActionT <span class="op">.</span> withAppM</span>
<span id="cb13-2"><a href="#cb13-2" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb13-3"><a href="#cb13-3" aria-hidden="true"></a>    mapActionT f (<span class="dt">ActionT</span> a) <span class="ot">=</span> <span class="dt">ActionT</span> <span class="op">$</span> (mapExceptT <span class="op">.</span> mapReaderT <span class="op">.</span> mapStateT) f a</span>
<span id="cb13-4"><a href="#cb13-4" aria-hidden="true"></a>    withAppM f a <span class="ot">=</span> <span class="dt">AppM</span> <span class="op">$</span> withReaderT f (unAppM a)</span></code></pre></div>
<p>Basically it reaches into the guts of <code>scotty</code>’s <code>ActionT</code> type (the details are exposed in <code>Web.Scotty.Internal.Types</code>, thanks for not hiding it completely), and modifies the <code>ReaderT Config</code> I’ve supplied.</p>
<p>The new server has two routes, the original one and a new one at <code>GET /route1</code>.</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb14-1"><a href="#cb14-1" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb14-2"><a href="#cb14-2" aria-hidden="true"></a>main <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb14-3"><a href="#cb14-3" aria-hidden="true"></a>  <span class="fu">putStrLn</span> <span class="st">&quot;Starting&quot;</span></span>
<span id="cb14-4"><a href="#cb14-4" aria-hidden="true"></a>  ls <span class="ot">&lt;-</span> newStdoutLoggerSet defaultBufSize</span>
<span id="cb14-5"><a href="#cb14-5" aria-hidden="true"></a>  foo (ls, <span class="dv">0</span>) <span class="dv">3000</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb14-6"><a href="#cb14-6" aria-hidden="true"></a>    get <span class="st">&quot;/route0&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb14-7"><a href="#cb14-7" aria-hidden="true"></a>      lift <span class="op">$</span> mLog <span class="st">&quot;log: getting /route0&quot;</span></span>
<span id="cb14-8"><a href="#cb14-8" aria-hidden="true"></a>      json <span class="op">$</span> object [<span class="st">&quot;route0&quot;</span> <span class="op">.=</span> (<span class="st">&quot;ok&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span>
<span id="cb14-9"><a href="#cb14-9" aria-hidden="true"></a>    mGet <span class="st">&quot;/route1&quot;</span> <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb14-10"><a href="#cb14-10" aria-hidden="true"></a>      lift <span class="op">$</span> mLog <span class="st">&quot;log: getting /route1&quot;</span></span>
<span id="cb14-11"><a href="#cb14-11" aria-hidden="true"></a>      json <span class="op">$</span> object [<span class="st">&quot;route1&quot;</span> <span class="op">.=</span> (<span class="st">&quot;bar&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span>
<span id="cb14-12"><a href="#cb14-12" aria-hidden="true"></a>    notFound <span class="op">$</span> json <span class="op">$</span> object [<span class="st">&quot;error&quot;</span> <span class="op">.=</span> (<span class="st">&quot;not found&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</span></code></pre></div>
<p>It’s now easy to verify that the original route, <code>GET /route0</code>, logs a string containing the integer ‘0’, while the new route, <code>GET /route1</code>, logs a string containing the integer ‘1’.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>If you don’t know what it is you’ll find multiple sources by searching for “http correlation-id”. <a href="https://theburningmonk.com/2015/05/a-consistent-approach-to-track-correlation-ids-through-microservices/">A consistent approach to track correlation IDs through microservices</a> is as good a place to start as any.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Systemd for auto deploy from AWS</title>
    <link href="http://magnus.therning.org/posts/2019-01-13-000-systemd-for-auto-deploy-from-aws.html" />
    <id>http://magnus.therning.org/posts/2019-01-13-000-systemd-for-auto-deploy-from-aws.html</id>
    <published>2019-01-13T00:00:00Z</published>
    <updated>2019-01-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Over the last week I’ve completely rebuilt the the only on-premise system we have at work. One of the bits I’m rather happy with is the replacement of <a href="https://mmonit.com/">M/Monit</a> with just plain <a href="https://www.freedesktop.org/wiki/Software/systemd/">systemd</a>. We didn’t use any of the advanced features of M/Monit, we only wanted ordinary process monitoring and restart on a single system. It really was a bit of overkill.</p>
<p>So, instead of</p>
<ul>
<li>using M/Monit to monitor processes</li>
<li>a monit configuration for the app (both start and stop instructions)</li>
<li>a script to start the app and capture its PID in a file</li>
<li>a script to resync the app against the S3 bucket where Travis puts the build artifacts, and if a new version has arrived, remove the PID file thereby triggering M/Monit to restart the app</li>
<li>a crontab entry to run the sync/restart script every few minutes</li>
</ul>
<p>we now have</p>
<ul>
<li>a (simplified) script to start the app<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></li>
<li>a service unit (<code>app.service</code>) for the app</li>
<li>a timer unit (<code>app-sync.timer</code>) to trigger the resync of the app against the S3 bucket</li>
<li>a oneshot service unit (<code>app-sync.service</code>), triggered by the timer, to perform the sync of the app with the latest build, i.e. call <code>aws s3 sync</code></li>
<li>a path unit (<code>app-restart.path</code>) to monitor one of the build artifacts, i.e. to pick up that a new version has arrived</li>
<li>a oneshot service unit (<code>app-restart.service</code>), triggered by the path unit, calling <code>systemctl restart app.service</code></li>
</ul>
<p>There’s one more piece to the setup, but</p>
<ul>
<li>the start script is simplified since it no longer needs to push things to the background and capture the PID</li>
<li>the sync/restart script is completely gone (arguably the more complicated of the two scripts in the M/Monit setup)</li>
<li>responsibility is cleanly separated leading to a solution that’s easier to understand (as long as you know a bit about systemd of course)</li>
</ul>
<p>so I think it’s a net improvement.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>All it does is set a bunch of environment variables and then start the app, so I’m planning on moving the environment variables into a separate file and put the start command the service unit file instead.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Components feel so not FP</title>
    <link href="http://magnus.therning.org/posts/2019-01-05-000-components-feel-so-not-fp.html" />
    <id>http://magnus.therning.org/posts/2019-01-05-000-components-feel-so-not-fp.html</id>
    <published>2019-01-05T00:00:00Z</published>
    <updated>2019-01-05T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>At work we’re writing most of the new stuff using Clojure. That’s been going on since before I started working there, and from the beginning there’s been exploration of style and libraries, yes, even of frameworks (ugh!). Now there’s discussion of standardising. The discussion is still in its infancy, but it prompted me to start thinking about what I’m come across in the code base we have now, what I like and what I dislike. The first thing that came to mind was how our different services are set up. I mean set up internally. Like, what’s actually in the <code>main</code> function (or in <code>-main</code>, since we’re talking Clojure). In many services we use <a href="https://clojars.org/com.stuartsierra/component">Stuart Sierra’s <code>component</code></a>, in a growing number of services we use <a href="https://clojars.org/integrant"><code>integrant</code></a>, and in one we use <a href="https://clojars.org/mount"><code>mount</code></a>.</p>
<p>The current discussion is going in the direction of <code>integrant</code>, and I don’t like it!</p>
<p>AFAICS <code>integrant</code> suffers from all the same things as <code>component</code> (the author of <code>mount</code> has put it into words better than I ever could in his text on <code>mount</code>‘s <a href="https://github.com/tolitius/mount/blob/master/doc/differences-from-component.md#differences-from-component">differences from component</a>’) plus one more thing to boot: systems are “configured into being”. It’s touted as “data-driven architecture”, I tend to see it as <em>architecture defined in a language separate from my functions</em>. The <code>integrant</code> README says that one of its strengths over <code>component</code> is that</p>
<blockquote>
<p>In Integrant, systems are created from a configuration data structure, typically loaded from an edn resource. The architecture of the application is defined through data, rather than code.</p>
</blockquote>
<p>Somehow that statement makes me think of OO and DI frameworks. I suspect the above paragraph is just as true with the two replacements <code>s/Integrant/Spring/</code> and <code>s/edn/XML/</code>. I’m not convinced this is a strength at all! My experience with DI frameworks in OO is limited (I’ve never used external configuration), but the enduring impression is that it’s unwieldy. In particular it was very far between cause and effect of errors. So far this is true for <code>integrant</code> as well.</p>
<p>Also, it makes me think of “functional in the small, OO in the large”<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a>, which is a comment coming out of the f-sharp world. Maybe there’s a connection here. Maybe “OO in the large” is something that resonates with OO-turned-FP developers. Maybe that means it’s only a quesiton of time (and exposure to FP) before they embrace “functional all the way”? Or, maybe I’m simply missing something crucial.</p>
<p>In any case I’m going to have to take a closer look at <code>mount</code> in the near future. I’ll also have to take a look at its brother <a href="https://github.com/tolitius/yurt"><code>yurt</code></a>, and at its distant cousin <a href="https://github.com/aroemers/mount-lite"><code>mount-lite</code></a>.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p><a href="https://www.johndcook.com/blog/2009/03/23/functional-in-the-small-oo-in-the-large/" class="uri">https://www.johndcook.com/blog/2009/03/23/functional-in-the-small-oo-in-the-large/</a><a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Is this a good way to do JSON validation?</title>
    <link href="http://magnus.therning.org/posts/2018-12-09-000-is-this-a-good-way-to-do-json-validation-.html" />
    <id>http://magnus.therning.org/posts/2018-12-09-000-is-this-a-good-way-to-do-json-validation-.html</id>
    <published>2018-12-09T00:00:00Z</published>
    <updated>2018-12-09T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>At work, where we use Clojure, we’ve been improving our error messages in the public API to</p>
<ol type="1">
<li>return as many errors as possible in a response, and</li>
<li>be in humanly readable English.</li>
</ol>
<p>If one adopts <a href="https://clojuredocs.org/clojure.spec.alpha">spec</a> as we have one gets the former for free, but the output of <code>spec</code> can hardly be called humanly readable. For the latter part we chose to use <a href="https://clojars.org/phrase">phrase</a>.</p>
<p>Given that I’d like to see Haskell used more at work (currently there are 2 minor services written in Haskell and around a score in Clojure) I thought I’d take a look at JSON validation in Haskell. I ended up beig less than impressed. We have at least one great library for parsing JSON, <a href="https://hackage.haskell.org/package/aeson">aeson</a>, but there are probably a few more that I haven’t noticed. It’s of course possible to mix in validation with the parsing, but since parsers, and this is true for <code>aeson</code>’s parser too, tend to be monads and that means that item 1 above, finding as many errors as possible, isn’t on the table.</p>
<p>A quick look at <a href="https://hackage.haskell.org/">Hackage</a> gave that</p>
<ul>
<li>there is a package called <a href="https://hackage.haskell.org/package/aeson-better-errors">aeson-better-errors</a> that looked promising but didn’t fi my needs (I explain at the end why it isn’t passing muster)</li>
<li>the support for <a href="https://json-schema.org/specification.html">JSON Schema</a> is very lacking in Haskell, <a href="https://hackage.haskell.org/package/hjsonschema">hjsonschema</a> is deprecated and <a href="https://hackage.haskell.org/package/aeson-schema">aeson-schema</a> only supports version 3 of the draft (the current version is 7) and the authors claim that that <code>hjsonschema</code> is more moderna and more actively maintained</li>
</ul>
<p>So, a bit disappointed I started playing with the problem myself and found that, just as is stated in the description of the <a href="https://hackage.haskell.org/package/validation">validation</a> library, I want something that’s isomorphic to <code>Either</code> but accumulates on the error side. That is, something like</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">JSONValidationResult</span> <span class="ot">=</span> <span class="dt">JVRInvalid</span> [<span class="dt">JSONValidationFailure</span>]</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>                          <span class="op">|</span> <span class="dt">JVRValid</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>                          <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">Semigroup</span> <span class="dt">JSONValidationResult</span> <span class="kw">where</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>  (<span class="dt">JVRInvalid</span> es0) <span class="op">&lt;&gt;</span> (<span class="dt">JVRInvalid</span> es1) <span class="ot">=</span> <span class="dt">JVRInvalid</span> <span class="op">$</span> es0 <span class="op">&lt;&gt;</span> es1</span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>  <span class="dt">JVRValid</span> <span class="op">&lt;&gt;</span> r <span class="ot">=</span> r</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a>  r <span class="op">&lt;&gt;</span> <span class="dt">JVRValid</span> <span class="ot">=</span> r</span></code></pre></div>
<p>I decided it was all right to limit validation to proper JSON expressions, i.e. a validator could have the type <code>Value -&gt; JSONValidationResult</code>. I want to combine validators so I decided to wrap it in a <code>newtype</code> and write a <code>SemiGroup</code> instance for it as well:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">JSONValidator</span> <span class="ot">=</span> <span class="dt">JV</span> (<span class="dt">A.Value</span> <span class="ot">-&gt;</span> <span class="dt">JSONValidationResult</span>)</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">Semigroup</span> <span class="dt">JSONValidator</span> <span class="kw">where</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>  (<span class="dt">JV</span> v0) <span class="op">&lt;&gt;</span> (<span class="dt">JV</span> v1) <span class="ot">=</span> <span class="dt">JV</span> <span class="op">$</span> \ val <span class="ot">-&gt;</span> v0 val <span class="op">&lt;&gt;</span> v1 val</span></code></pre></div>
<p>The function to actually run the validation is rather straight forward</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a>runJSONValidator (<span class="dt">JV</span> validator) val <span class="ot">=</span> validator val</span></code></pre></div>
<p>After writing a few validators I realised a few patterns emerged and the following functions simplified things a bit:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a>mapInvalid _ <span class="dt">JVRValid</span> <span class="ot">=</span> <span class="dt">JVRValid</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>mapInvalid f (<span class="dt">JVRInvalid</span> es) <span class="ot">=</span> <span class="dt">JVRInvalid</span> <span class="op">$</span> <span class="fu">map</span> f es</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>valid <span class="ot">=</span> <span class="dt">JVRValid</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>invalid s <span class="ot">=</span> <span class="dt">JVRInvalid</span> [<span class="dt">JVFDesc</span> s]</span></code></pre></div>
<p>With this in place I started writing validators for the basic JSON types:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a>isNumber <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>    go (<span class="dt">A.Number</span> _) <span class="ot">=</span> valid</span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not a number&quot;</span></span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true"></a></span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true"></a>isString <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb5-7"><a href="#cb5-7" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-8"><a href="#cb5-8" aria-hidden="true"></a>    go (<span class="dt">A.String</span> _) <span class="ot">=</span> valid</span>
<span id="cb5-9"><a href="#cb5-9" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not a string&quot;</span></span>
<span id="cb5-10"><a href="#cb5-10" aria-hidden="true"></a></span>
<span id="cb5-11"><a href="#cb5-11" aria-hidden="true"></a>isBool <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb5-12"><a href="#cb5-12" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-13"><a href="#cb5-13" aria-hidden="true"></a>    go (<span class="dt">A.Bool</span> _) <span class="ot">=</span> valid</span>
<span id="cb5-14"><a href="#cb5-14" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not a bool&quot;</span></span>
<span id="cb5-15"><a href="#cb5-15" aria-hidden="true"></a></span>
<span id="cb5-16"><a href="#cb5-16" aria-hidden="true"></a>isNull <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb5-17"><a href="#cb5-17" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-18"><a href="#cb5-18" aria-hidden="true"></a>    go <span class="dt">A.Null</span> <span class="ot">=</span> valid</span>
<span id="cb5-19"><a href="#cb5-19" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not &#39;null&#39;&quot;</span></span></code></pre></div>
<p>The number type in JSON is a float (well, in <code>aeson</code> it’s a <a href="https://hackage.haskell.org/package/scientific-0.3.6.2/docs/Data-Scientific.html#t:Scientific"><code>Scientific</code></a>), so to check for an integer a bit more than the above is needed</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>isInt <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>    go (<span class="dt">A.Number</span> i) <span class="ot">=</span> <span class="kw">if</span> i <span class="op">==</span> <span class="fu">fromInteger</span> (<span class="fu">round</span> i)</span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>                      <span class="kw">then</span> valid</span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a>                      <span class="kw">else</span> invalid <span class="st">&quot;not an integer&quot;</span></span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not an integer&quot;</span></span></code></pre></div>
<p>as well as functions that check for the presence of a specific key</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a>reqKey n v <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true"></a>    go (<span class="dt">A.Object</span> obj) <span class="ot">=</span> <span class="kw">case</span> HM.lookup n obj <span class="kw">of</span></span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true"></a>                          <span class="dt">Nothing</span> <span class="ot">-&gt;</span> invalid <span class="op">$</span> <span class="st">&quot;required key &#39;&quot;</span> <span class="op">&lt;&gt;</span> n <span class="op">&lt;&gt;</span> <span class="st">&quot;&#39; is missing&quot;</span></span>
<span id="cb7-5"><a href="#cb7-5" aria-hidden="true"></a>                          <span class="dt">Just</span> val <span class="ot">-&gt;</span> mapInvalid (<span class="dt">JVFPath</span> n) <span class="op">$</span> runJSONValidator v val</span>
<span id="cb7-6"><a href="#cb7-6" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not an object&quot;</span></span>
<span id="cb7-7"><a href="#cb7-7" aria-hidden="true"></a>    </span>
<span id="cb7-8"><a href="#cb7-8" aria-hidden="true"></a>optKey n v <span class="ot">=</span> <span class="dt">JV</span> go</span>
<span id="cb7-9"><a href="#cb7-9" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb7-10"><a href="#cb7-10" aria-hidden="true"></a>    go (<span class="dt">A.Object</span> obj) <span class="ot">=</span> <span class="kw">case</span> HM.lookup n obj <span class="kw">of</span></span>
<span id="cb7-11"><a href="#cb7-11" aria-hidden="true"></a>                          <span class="dt">Nothing</span> <span class="ot">-&gt;</span> valid</span>
<span id="cb7-12"><a href="#cb7-12" aria-hidden="true"></a>                          <span class="dt">Just</span> val <span class="ot">-&gt;</span> mapInvalid (<span class="dt">JVFPath</span> n) <span class="op">$</span> runJSONValidator v val</span>
<span id="cb7-13"><a href="#cb7-13" aria-hidden="true"></a>    go _ <span class="ot">=</span> invalid <span class="st">&quot;not an object&quot;</span></span></code></pre></div>
<p>With this in place I can now create a validator for a person with a name and an age:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a>vPerson <span class="ot">=</span> reqKey <span class="st">&quot;name&quot;</span> isString <span class="op">&lt;&gt;</span></span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>          reqKey <span class="st">&quot;age&quot;</span> isInt</span></code></pre></div>
<p>and run it on a <code>Value</code>:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="op">*&gt;</span> runJSONValidator vPerson <span class="op">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;name\&quot;: \&quot;Alice\&quot;, \&quot;age\&quot;: 32}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a><span class="dt">Just</span> <span class="dt">JVRValid</span></span></code></pre></div>
<p>and all failures are picked up</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a><span class="op">*&gt;</span> runJSONValidator vPerson <span class="op">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;name\&quot;: \&quot;Alice\&quot;, \&quot;age\&quot;: \&quot;foo\&quot;}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true"></a><span class="dt">Just</span> (<span class="dt">JVRInvalid</span> [<span class="dt">JVFPath</span> <span class="st">&quot;age&quot;</span> (<span class="dt">JVFDesc</span> <span class="st">&quot;not an integer&quot;</span>)])</span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true"></a></span>
<span id="cb10-4"><a href="#cb10-4" aria-hidden="true"></a><span class="op">*&gt;</span>runJSONValidator vPerson <span class="op">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;name\&quot;: \&quot;Alice\&quot;}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</span>
<span id="cb10-5"><a href="#cb10-5" aria-hidden="true"></a><span class="dt">Just</span> (<span class="dt">JVRInvalid</span> [<span class="dt">JVFDesc</span> <span class="st">&quot;required key &#39;age&#39; is missing&quot;</span>])</span>
<span id="cb10-6"><a href="#cb10-6" aria-hidden="true"></a></span>
<span id="cb10-7"><a href="#cb10-7" aria-hidden="true"></a>runJSONValidator vPerson <span class="op">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;nam\&quot;: \&quot;Alice\&quot;}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</span>
<span id="cb10-8"><a href="#cb10-8" aria-hidden="true"></a><span class="dt">Just</span> (<span class="dt">JVRInvalid</span> [<span class="dt">JVFDesc</span> <span class="st">&quot;required key &#39;name&#39; is missing&quot;</span>,<span class="dt">JVFDesc</span> <span class="st">&quot;required key &#39;age&#39; is missing&quot;</span>])</span></code></pre></div>
<h2 id="reflections">Reflections</h2>
<ol type="1">
<li>I quickly realised I wanted slightly more complex validation of course, so all the validators for basic JSON types above have a version taking a custom validator of type <code>a -&gt; JSONValidationResult</code> (where <code>a</code> is the Haskell type contained in the particulare <code>Value</code>).</li>
<li>I started out thinking that I want an <code>Applicative</code> for my validations, but slowly I relaxed that to <code>SemiGroup</code>. I’m still not sure about this decision, because I can see a real use of <em>or</em> which I don’t really have now. Maybe that means I should switch back towards <code>Applicative</code>, just so I can implement an <code>Alternative</code> instance for validators.</li>
<li>Well, I simply don’t know if this is even a good way to implement validators. I’d love to hear suggestions both for improvements and for completely different ways of tackling the problems.</li>
<li>I would love to find out that there already is a library that does all this in a much better way. Please point me in its direction!</li>
</ol>
<h2 id="appendix-a-look-at-aeson-better-errors">Appendix: A look at <code>aeson-better-errors</code></h2>
<p>The issue with <code>aeson-better-errors</code> is easiest to illustrate using the same example as in <a href="https://harry.garrood.me/blog/aeson-better-errors/">its announcement</a>:</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true"></a><span class="ot">{-# LANGUAGE OverloadedStrings #-}</span></span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true"></a><span class="kw">module</span> <span class="dt">Play</span> <span class="kw">where</span></span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true"></a></span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Data.Aeson</span></span>
<span id="cb11-5"><a href="#cb11-5" aria-hidden="true"></a><span class="kw">import</span>           <span class="dt">Data.Aeson.BetterErrors</span></span>
<span id="cb11-6"><a href="#cb11-6" aria-hidden="true"></a></span>
<span id="cb11-7"><a href="#cb11-7" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">Person</span> <span class="ot">=</span> <span class="dt">Person</span> <span class="dt">String</span> <span class="dt">Int</span></span>
<span id="cb11-8"><a href="#cb11-8" aria-hidden="true"></a>  <span class="kw">deriving</span> (<span class="dt">Show</span>)</span>
<span id="cb11-9"><a href="#cb11-9" aria-hidden="true"></a></span>
<span id="cb11-10"><a href="#cb11-10" aria-hidden="true"></a><span class="ot">asPerson ::</span> <span class="dt">Parse</span> e <span class="dt">Person</span></span>
<span id="cb11-11"><a href="#cb11-11" aria-hidden="true"></a>asPerson <span class="ot">=</span> <span class="dt">Person</span> <span class="op">&lt;$&gt;</span> key <span class="st">&quot;name&quot;</span> asString <span class="op">&lt;*&gt;</span> key <span class="st">&quot;age&quot;</span> asIntegral</span></code></pre></div>
<p>and with this loaded in GHCi (and make sure to either pass <code>-XOverloadedStrings</code> on the command line, or <code>:set -XOverloadedStrings</code> in GHCi itself)</p>
<pre><code>*&gt; parse asPerson &quot;{\&quot;name\&quot;: \&quot;Alice\&quot;, \&quot;age\&quot;: 32}&quot;
Right (Person &quot;Alice&quot; 32)
*&gt; parse asPerson &quot;{\&quot;name\&quot;: \&quot;Alice\&quot;}&quot;
Left (BadSchema [] (KeyMissing &quot;age&quot;))
*&gt; parse asPerson &quot;{\&quot;nam\&quot;: \&quot;Alice\&quot;}&quot;
Left (BadSchema [] (KeyMissing &quot;name&quot;))</code></pre>
<p>Clearly <code>aeson-better-errors</code> isn’t fulfilling the bit about reporting as many errors as possible. Something that I would have realised right away if I had bothered reading its API reference on Hackage a bit more carefully, the parser type <code>ParseT</code> is an instance of <code>Monad</code>!</p>]]></summary>
</entry>
<entry>
    <title>Zipping streams</title>
    <link href="http://magnus.therning.org/posts/2018-10-16-000-zipping-streams.html" />
    <id>http://magnus.therning.org/posts/2018-10-16-000-zipping-streams.html</id>
    <published>2018-10-16T00:00:00Z</published>
    <updated>2018-10-16T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Writing the following is easy after glancing through the documentation for <a href="https://hackage.haskell.org/package/conduit">conduit</a>:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a>foo <span class="ot">=</span> <span class="kw">let</span> src <span class="ot">=</span> <span class="fu">mapM_</span> C.yield [<span class="dv">0</span><span class="op">..</span><span class="dv">9</span><span class="ot"> ::</span> <span class="dt">Int</span>]</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>          p0 <span class="ot">=</span> CC.map (\ i <span class="ot">-&gt;</span> (<span class="st">&quot;p0&quot;</span>, <span class="fu">succ</span> i))</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>          p1 <span class="ot">=</span> CC.filter <span class="fu">odd</span> <span class="op">.|</span> CC.map (\ i <span class="ot">-&gt;</span> (<span class="st">&quot;p1&quot;</span>, i))</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>          p <span class="ot">=</span> C.getZipConduit <span class="op">$</span> <span class="dt">C.ZipConduit</span> p0 <span class="op">&lt;*</span> <span class="dt">C.ZipConduit</span> p1</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>          sink <span class="ot">=</span> CC.mapM_ <span class="fu">print</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>      <span class="kw">in</span> C.runConduit <span class="op">$</span> src <span class="op">.|</span> p <span class="op">.|</span> sink</span></code></pre></div>
<p>Neither <a href="https://hackage.haskell.org/package/pipes">pipes</a> nor <a href="https://hackage.haskell.org/package/streaming">streaming</a> make it as easy to figure out. I must be missing something! What functions should I be looking at?</p>]]></summary>
</entry>
<entry>
    <title>Picking up new rows in a DB</title>
    <link href="http://magnus.therning.org/posts/2018-10-14-000-picking-up-new-rows-in-a-db.html" />
    <id>http://magnus.therning.org/posts/2018-10-14-000-picking-up-new-rows-in-a-db.html</id>
    <published>2018-10-14T00:00:00Z</published>
    <updated>2018-10-14T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I’ve been playing around a little with <a href="https://hackage.haskell.org/package/postgresql-simple"><code>postgresql-simple</code></a> in combination with <a href="https://hackage.haskell.org/package/streaming"><code>streaming</code></a> for picking up inserted rows in a DB table. What I came up with was</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">eventStreamer ::</span> <span class="dt">Connection</span> <span class="ot">-&gt;</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Stream</span> (<span class="dt">Of</span> (<span class="dt">Int</span>, <span class="dt">Int</span>)) <span class="dt">IO</span> ()</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>eventStreamer conn ver <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  res <span class="ot">&lt;-</span> liftIO <span class="op">$</span> query conn  <span class="st">&quot;SELECT * FROM events WHERE id &gt; (?) ORDER BY id&quot;</span> (<span class="dt">Only</span> ver)</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>  <span class="kw">case</span> res <span class="kw">of</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>    [] <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>      liftIO <span class="op">$</span> threadDelay <span class="dv">1000000</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>      eventStreamer conn ver</span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a>    _ <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>      <span class="kw">let</span> ver&#39; <span class="ot">=</span> <span class="fu">maximum</span> <span class="op">$</span> <span class="fu">map</span> <span class="fu">fst</span> res</span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>      each res</span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>      eventStreamer conn ver&#39;</span></code></pre></div>
<p>It seems almost too simple so I’m suspecting I missed something.</p>
<ul>
<li>Am I missing something?</li>
<li>Should I be using <code>streaming</code>, or is <code>pipes</code> or <code>conduit</code> better (in case they are, in what way)?</li>
</ul>]]></summary>
</entry>
<entry>
    <title>Using a configuration in Scotty</title>
    <link href="http://magnus.therning.org/posts/2018-10-01-000-using-a-configuration-in-scotty.html" />
    <id>http://magnus.therning.org/posts/2018-10-01-000-using-a-configuration-in-scotty.html</id>
    <published>2018-10-01T00:00:00Z</published>
    <updated>2018-10-01T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>At work we’re only now getting around to put <a href="https://hilton.org.uk/blog/microservices-correlation-id">correlation IDs</a> into use. We write most our code in Clojure but since I’d really like to use more Haskell at work I thought I’d dive into <a href="https://hackage.haskell.org/package/scotty">Scotty</a> and see how to deal with logging and then especially how to get correlation IDs into the logs.</p>
<h2 id="the-types">The types</h2>
<p>For configuration it decided to use the reader monad inside <code>ActionT</code> from Scotty. Enter Chell:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">ChellM</span> c <span class="ot">=</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> (<span class="dt">ReaderT</span> c <span class="dt">IO</span>)</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">ChellActionM</span> c <span class="ot">=</span> <span class="dt">ActionT</span> <span class="dt">Text</span> (<span class="dt">ReaderT</span> c <span class="dt">IO</span>)</span></code></pre></div>
<p>In order to run it I wrote a function corresponding to <code>scotty</code>:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="ot">chell ::</span> c <span class="ot">-&gt;</span> <span class="dt">Port</span> <span class="ot">-&gt;</span> <span class="dt">ChellM</span> () <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>chell cfg port a <span class="ot">=</span> scottyOptsT opts (<span class="fu">flip</span> runReaderT cfg) a</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>    opts <span class="ot">=</span> def { verbose <span class="ot">=</span> <span class="dv">0</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>               , settings <span class="ot">=</span> (settings def) { settingsPort <span class="ot">=</span> port }</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>               }</span></code></pre></div>
<h2 id="correlation-id">Correlation ID</h2>
<p>To deal with the correlation ID each incoming request should be checked for the HTTP header <code>X-Correlation-Id</code> and if present it should be used during logging. If no such header is present then a new correlation ID should be created. Since it’s per request it feels natural to create a WAI middleware for this.</p>
<p>The easiest way I could come up with was to push the correlation ID into the request’s headers before it’s passed on:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">requestHeaderCorrelationId ::</span> <span class="dt">Request</span> <span class="ot">-&gt;</span> <span class="dt">Maybe</span> <span class="dt">ByteString</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>requestHeaderCorrelationId <span class="ot">=</span> <span class="fu">lookup</span> <span class="st">&quot;X-Correlation-Id&quot;</span> <span class="op">.</span> requestHeaders</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a><span class="ot">correlationId ::</span>  <span class="dt">Middleware</span></span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>correlationId app req sendResponse <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a>  u <span class="ot">&lt;-</span> (<span class="ot">randomIO ::</span> <span class="dt">IO</span> <span class="dt">UUID</span>)</span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a>  <span class="kw">let</span> corrId <span class="ot">=</span> <span class="fu">maybe</span> (toASCIIBytes u) <span class="fu">id</span> (requestHeaderCorrelationId req)</span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a>      newHeaders <span class="ot">=</span> (<span class="st">&quot;X-Correlation-Id&quot;</span>, corrId) <span class="op">:</span> (requestHeaders req)</span>
<span id="cb3-9"><a href="#cb3-9" aria-hidden="true"></a>  app (req { requestHeaders <span class="ot">=</span> newHeaders }) <span class="op">$</span> \ res <span class="ot">-&gt;</span> sendResponse res</span></code></pre></div>
<p>It also turns out to be useful to have both a default correlation ID and a function for pulling it out of the headers:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">defaultCorrelationString ::</span> <span class="dt">ByteString</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>defaultCorrelationString <span class="ot">=</span> <span class="st">&quot;no-correlation-id&quot;</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a><span class="ot">getCorrelationId ::</span> <span class="dt">Request</span> <span class="ot">-&gt;</span> <span class="dt">ByteString</span></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>getCorrelationId r <span class="ot">=</span> <span class="fu">maybe</span> defaultCorrelationString <span class="fu">id</span> (requestHeaderCorrelationId r)</span></code></pre></div>
<h2 id="getting-the-correlation-id-into-the-configuration">Getting the correlation ID into the configuration</h2>
<p>Since the correlation ID should be picked out of the request on handling of every request it’s useful to have it the configuration when running the <code>ChellActionM</code> actions. However, since the correlation ID isn’t available when running the reader (the call to <code>runReaderT</code> in <code>chell</code>) something else is called for. When looking around I found <code>local</code> (and later I was pointed to the more general <code>withReaderT</code>) but it doesn’t have a suitable type. After some <a href="https://twitter.com/EyalL/status/1046696148218580993">help on Twitter</a> I arrived at <code>withConfig</code> which allows me to run an action in a modified configuration:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">withConfig ::</span> (c <span class="ot">-&gt;</span> c&#39;) <span class="ot">-&gt;</span> <span class="dt">ChellActionM</span> c&#39; () <span class="ot">-&gt;</span> <span class="dt">ChellActionM</span> c ()</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>withConfig <span class="ot">=</span> mapActionT <span class="op">.</span> withReaderT</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>    mapActionT f (<span class="dt">ActionT</span> a) <span class="ot">=</span> <span class="dt">ActionT</span> <span class="op">$</span> (mapExceptT <span class="op">.</span> mapReaderT <span class="op">.</span> mapStateT) f a</span></code></pre></div>
<h2 id="making-it-handy-to-use">Making it handy to use</h2>
<p>Armed with this I can put together some functions to replace Scotty’s <code>get</code>, <code>post</code>, etc. With a configuration type like this:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">Config</span> <span class="ot">=</span> <span class="dt">Cfg</span> <span class="dt">LoggerSet</span> <span class="dt">ByteString</span></span></code></pre></div>
<p>The modified <code>get</code> looks like this (Scotty’s original is <code>S.get</code>)</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a><span class="ot">get ::</span> <span class="dt">RoutePattern</span> <span class="ot">-&gt;</span> <span class="dt">ChellActionM</span> <span class="dt">Config</span> () <span class="ot">-&gt;</span> <span class="dt">ChellM</span> <span class="dt">Config</span> ()</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>get p a <span class="ot">=</span> S.get p <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true"></a>  r <span class="ot">&lt;-</span> request</span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true"></a>  <span class="kw">let</span> corrId <span class="ot">=</span> getCorrelationId r</span>
<span id="cb7-5"><a href="#cb7-5" aria-hidden="true"></a>  withConfig (\ (<span class="dt">Cfg</span> l _) <span class="ot">-&gt;</span> <span class="dt">Cfg</span> l corrId) a</span></code></pre></div>
<p>With this in place I can use the simpler <code>ReaderT Config IO</code> for inner functions that need to log.</p>]]></summary>
</entry>
<entry>
    <title>QuickCheck on a REST API</title>
    <link href="http://magnus.therning.org/posts/2018-07-14-000-quickcheck-on-a-rest-api.html" />
    <id>http://magnus.therning.org/posts/2018-07-14-000-quickcheck-on-a-rest-api.html</id>
    <published>2018-07-14T00:00:00Z</published>
    <updated>2018-07-14T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Since I’m working with web stuff nowadays I thought I’d play a little with translating my old post on <a href="/posts/2015-06-15-000-using-quickcheck-to-test-c-apis.html">using QuickCheck to test C APIs</a> to the web.</p>
<h2 id="the-goal-and-how-to-reach-it">The goal and how to reach it</h2>
<p>I want to use <a href="https://hackage.haskell.org/package/QuickCheck">QuickCheck</a> to test a REST API, just like in the case of the C API the idea is to</p>
<ol type="1">
<li>generate a sequence of API calls (a <em>program</em>), then</li>
<li>run the sequence against a model, as well as</li>
<li>run the sequence against the web service, and finally</li>
<li>compare the resulting model against reality.</li>
</ol>
<h2 id="the-rest-api">The REST API</h2>
<p>I’ll use a small web service I’m working on, and then concentrate on only a small part of the API to begin with.</p>
<p>The parts of the API I’ll use for the programs at this stage are</p>
<table>
<colgroup>
<col style="width: 7%" />
<col style="width: 13%" />
<col style="width: 39%" />
<col style="width: 40%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">Method</th>
<th style="text-align: left;">Route</th>
<th style="text-align: left;">Example in</th>
<th style="text-align: left;">Example out</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>POST</code></td>
<td style="text-align: left;"><code>/users</code></td>
<td style="text-align: left;"><code>{"userId": 0, "userName": "Yogi Berra"}</code></td>
<td style="text-align: left;"><code>{"userId": 42, "userName": "Yogi Berra"}</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>DELETE</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<p>The following API calls will also be used, but not in the programs</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">Method</th>
<th style="text-align: left;">Route</th>
<th style="text-align: left;">Example in</th>
<th style="text-align: left;">Example out</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>[0,3,7]</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>{"userId": 42, "userName": "Yogi Berra"}</code></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>POST</code></td>
<td style="text-align: left;"><code>/reset</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h2 id="representing-api-calls">Representing API calls</h2>
<p>Given the information about the API above it seems the following is enough to represent the two calls of interest together with a constructor representing the end of a program</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">ApiCall</span> <span class="ot">=</span> <span class="dt">AddUser</span> <span class="dt">Text</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>             <span class="op">|</span> <span class="dt">DeleteUser</span> <span class="dt">Int</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>             <span class="op">|</span> <span class="dt">EndProgram</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>             <span class="kw">deriving</span> (<span class="dt">Show</span>)</span></code></pre></div>
<p>and a program is just a sequence of calls, so list of <code>ApiCall</code> will do. However, since I want to generate sequences of calls, i.e. implement <code>Arbitrary</code>, I’ll wrap it in a <code>newtype</code></p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">newtype</span> <span class="dt">Program</span> <span class="ot">=</span> <span class="dt">Prog</span> [<span class="dt">ApiCall</span>]</span></code></pre></div>
<h2 id="running-against-a-model-simulation">Running against a model (simulation)</h2>
<p>First of all I need to decide what model to use. Based on the part of the API I’m using I’ll use an ordinary dictionary of <code>Int</code> and <code>Text</code></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">Model</span> <span class="ot">=</span> <span class="dt">M.Map</span> <span class="dt">Int</span> <span class="dt">Text</span></span></code></pre></div>
<p>Simulating execution of a program is simulating each call against a model that’s updated with each step. I expect the final model to correspond to the state of the real service after the program is run for real. The simulation begins with an empty dictionary.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">simulateProgram ::</span> <span class="dt">Program</span> <span class="ot">-&gt;</span> <span class="dt">Model</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>simulateProgram (<span class="dt">Prog</span> cs) <span class="ot">=</span> <span class="fu">foldl</span> simulateCall M.empty cs</span></code></pre></div>
<p>The simulation of the API calls must then be a function taking a model and a call, returning an updated model</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">simulateCall ::</span> <span class="dt">Model</span> <span class="ot">-&gt;</span> <span class="dt">ApiCall</span> <span class="ot">-&gt;</span> <span class="dt">Model</span></span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>simulateCall m (<span class="dt">AddUser</span> t) <span class="ot">=</span> M.insert k t m</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>    k <span class="ot">=</span> <span class="fu">succ</span> <span class="op">$</span> <span class="fu">foldl</span> <span class="fu">max</span> <span class="dv">0</span> (M.keys m)</span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true"></a>simulateCall m (<span class="dt">DeleteUser</span> k) <span class="ot">=</span> M.delete k m</span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true"></a>simulateCall m <span class="dt">EndProgram</span> <span class="ot">=</span> m</span></code></pre></div>
<p>Here I have to make a few assumptions. First, I assume the indeces for the users start on <code>1</code>. Second, that the next index used always is the successor of highest currently used index. We’ll see how well this holds up to reality later on.</p>
<h2 id="running-against-the-web-service">Running against the web service</h2>
<p>Running the program against the actual web service follows the same pattern, but here I’m dealing with the real world, so it’s a little more messy, i.e. <code>IO</code> is involved. First the running of a single call</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a><span class="ot">runCall ::</span> <span class="dt">Manager</span> <span class="ot">-&gt;</span> <span class="dt">ApiCall</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a>runCall mgr (<span class="dt">AddUser</span> t) <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>  ireq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/users&quot;</span></span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>  <span class="kw">let</span> req <span class="ot">=</span> ireq { requestBody <span class="ot">=</span> <span class="dt">RequestBodyLBS</span> (encode <span class="op">$</span> <span class="dt">User</span> <span class="dv">0</span> t)}</span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a>  resp <span class="ot">&lt;-</span> httpLbs req mgr</span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>  guard (status201 <span class="op">==</span> responseStatus resp)</span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true"></a></span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true"></a>runCall mgr (<span class="dt">DeleteUser</span> k) <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true"></a>  req <span class="ot">&lt;-</span> parseRequest <span class="op">$</span> <span class="st">&quot;DELETE http://localhost:3000/users/&quot;</span> <span class="op">++</span> <span class="fu">show</span> k</span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true"></a>  resp <span class="ot">&lt;-</span> httpNoBody req mgr</span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true"></a>  guard (status200 <span class="op">==</span> responseStatus resp)</span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true"></a></span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true"></a>runCall _ <span class="dt">EndProgram</span> <span class="ot">=</span> <span class="fu">return</span> ()</span></code></pre></div>
<p>The running of a program is slightly more involved. Of course I have to set up the <code>Manager</code> needed for the HTTP calls, but I also need to</p>
<ol type="1">
<li>ensure that the web service is in a well-known state before starting, and</li>
<li>extract the state of the web service after running the program, so I can compare it to the model</li>
</ol>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a><span class="ot">runProgram ::</span> <span class="dt">Program</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">Model</span></span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>runProgram (<span class="dt">Prog</span> cs) <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true"></a>  mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true"></a>  resetReq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/reset&quot;</span></span>
<span id="cb7-5"><a href="#cb7-5" aria-hidden="true"></a>  httpNoBody resetReq mgr</span>
<span id="cb7-6"><a href="#cb7-6" aria-hidden="true"></a>  <span class="fu">mapM_</span> (runCall mgr) cs</span>
<span id="cb7-7"><a href="#cb7-7" aria-hidden="true"></a>  model <span class="ot">&lt;-</span> extractModel mgr</span>
<span id="cb7-8"><a href="#cb7-8" aria-hidden="true"></a>  <span class="fu">return</span> model</span></code></pre></div>
<p>The call to <code>POST /reset</code> resets the web service. I would have liked to simply restart the service completely, but I failed in automating it. I think I’ll have to take a closer look at the implementation of <a href="http://hackage.haskell.org/package/scotty">scotty</a> to find a way.</p>
<p>Extracting the web service state and packaging it in a <code>Model</code> is a matter of calling <code>GET /users</code> and then repeatedly calling <code>GET /users/:id</code> with each <code>id</code> gotten from the first call</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="ot">extractModel ::</span> <span class="dt">Manager</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">Model</span></span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>extractModel mgr <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a>  req <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;http://localhost:3000/users&quot;</span></span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true"></a>  resp <span class="ot">&lt;-</span> httpLbs req mgr</span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true"></a>  <span class="kw">let</span> (<span class="dt">Just</span> ids) <span class="ot">=</span> decode (responseBody resp)<span class="ot"> ::</span> <span class="dt">Maybe</span> [<span class="dt">Int</span>]</span>
<span id="cb8-6"><a href="#cb8-6" aria-hidden="true"></a>  users <span class="ot">&lt;-</span> forM ids <span class="op">$</span> \ <span class="fu">id</span> <span class="ot">-&gt;</span> <span class="kw">do</span></span>
<span id="cb8-7"><a href="#cb8-7" aria-hidden="true"></a>    req <span class="ot">&lt;-</span> parseRequest <span class="op">$</span> <span class="st">&quot;http://localhost:3000/users/&quot;</span> <span class="op">++</span> <span class="fu">show</span> <span class="fu">id</span></span>
<span id="cb8-8"><a href="#cb8-8" aria-hidden="true"></a>    resp <span class="ot">&lt;-</span> httpLbs req mgr</span>
<span id="cb8-9"><a href="#cb8-9" aria-hidden="true"></a>    <span class="kw">let</span> (<span class="dt">Just</span> (user<span class="op">:</span>_)) <span class="ot">=</span> decode (responseBody resp)<span class="ot"> ::</span> <span class="dt">Maybe</span> [<span class="dt">User</span>]</span>
<span id="cb8-10"><a href="#cb8-10" aria-hidden="true"></a>    <span class="fu">return</span> user</span>
<span id="cb8-11"><a href="#cb8-11" aria-hidden="true"></a>  <span class="fu">return</span> <span class="op">$</span> <span class="fu">foldl</span> (\ <span class="fu">map</span> (<span class="dt">User</span> <span class="fu">id</span> name) <span class="ot">-&gt;</span> M.insert <span class="fu">id</span> name <span class="fu">map</span>) M.empty users</span></code></pre></div>
<h2 id="generating-programs">Generating programs</h2>
<p>My approach to generating a program is based on the idea that given a certain state there is only a limited number of possible calls that make sense. Given a model <code>m</code> it makes sense to make one of the following calls:</p>
<ul>
<li>add a new user</li>
<li>delete an existing user</li>
<li>end the program</li>
</ul>
<p>Based on this writing <code>genProgram</code> is rather straight forward</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="ot">genProgram ::</span> <span class="dt">Gen</span> <span class="dt">Program</span></span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a>genProgram <span class="ot">=</span> <span class="dt">Prog</span> <span class="op">&lt;$&gt;</span> go M.empty</span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true"></a>    possibleAddUser _ <span class="ot">=</span> [<span class="dt">AddUser</span> <span class="op">&lt;$&gt;</span> arbitrary]</span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true"></a>    possibleDeleteUser m <span class="ot">=</span> <span class="fu">map</span> (<span class="fu">return</span> <span class="op">.</span> <span class="dt">DeleteUser</span>) (M.keys m)</span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true"></a>    possibleEndProgram _ <span class="ot">=</span> [<span class="fu">return</span> <span class="dt">EndProgram</span>]</span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true"></a></span>
<span id="cb9-8"><a href="#cb9-8" aria-hidden="true"></a>    go m <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb9-9"><a href="#cb9-9" aria-hidden="true"></a>      <span class="kw">let</span> possibles <span class="ot">=</span> possibleDeleteUser m <span class="op">++</span> possibleAddUser m <span class="op">++</span> possibleEndProgram m</span>
<span id="cb9-10"><a href="#cb9-10" aria-hidden="true"></a>      s <span class="ot">&lt;-</span> oneof possibles</span>
<span id="cb9-11"><a href="#cb9-11" aria-hidden="true"></a>      <span class="kw">let</span> m&#39; <span class="ot">=</span> simulateCall m s</span>
<span id="cb9-12"><a href="#cb9-12" aria-hidden="true"></a>      <span class="kw">case</span> s <span class="kw">of</span></span>
<span id="cb9-13"><a href="#cb9-13" aria-hidden="true"></a>        <span class="dt">EndProgram</span> <span class="ot">-&gt;</span> <span class="fu">return</span> []</span>
<span id="cb9-14"><a href="#cb9-14" aria-hidden="true"></a>        _ <span class="ot">-&gt;</span> (s<span class="op">:</span>) <span class="op">&lt;$&gt;</span> go m&#39;</span></code></pre></div>
<p>Armed with that the <code>Arbitrary</code> instance for <code>Program</code> can be implemented as<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a></p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">Arbitrary</span> <span class="dt">Program</span> <span class="kw">where</span></span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true"></a>  arbitrary <span class="ot">=</span> genProgram</span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true"></a>  shrink p <span class="ot">=</span> []</span></code></pre></div>
<h2 id="the-property-of-an-api">The property of an API</h2>
<p>The steps in the <a href="#the-goal-and-how-to-reach-it">first section</a> can be used as a recipe for writing the property</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true"></a><span class="ot">prop_progCorrectness ::</span> <span class="dt">Program</span> <span class="ot">-&gt;</span> <span class="dt">Property</span></span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true"></a>prop_progCorrectness program <span class="ot">=</span> monadicIO <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true"></a>  <span class="kw">let</span> simulatedModel <span class="ot">=</span> simulateProgram program</span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true"></a>  runModel <span class="ot">&lt;-</span> run <span class="op">$</span> runProgram program</span>
<span id="cb11-5"><a href="#cb11-5" aria-hidden="true"></a>  assert <span class="op">$</span> simulatedModel <span class="op">==</span> runModel</span></code></pre></div>
<h2 id="what-next">What next?</h2>
<p>There are some improvements that I’d like to make:</p>
<ul>
<li>Make the generation of <code>Program</code> better in the sense that the programs become longer. I think this is important as I start tackling larger APIs.</li>
<li>Write an implementation of <code>shrink</code> for <code>Program</code>. With longer programs it’s of course more important to actually implement <code>shrink</code>.</li>
</ul>
<p>I’d love to hear if others are using <a href="https://hackage.haskell.org/package/QuickCheck">QuickCheck</a> to test REST APIs in some way, if anyone has suggestions for improvements, and of course ideas for how to implement <code>shrink</code> in a nice way.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Yes, I completely skip the issue of shrinking programs at this point. This is OK at this point though, because the generated <code>Programs</code>s do end up to be very short indeed.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>A simple zaw widget for jumping to git projects</title>
    <link href="http://magnus.therning.org/posts/2018-06-19-000-a-simple-zaw-widget.html" />
    <id>http://magnus.therning.org/posts/2018-06-19-000-a-simple-zaw-widget.html</id>
    <published>2018-06-19T00:00:00Z</published>
    <updated>2018-06-19T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>A colleague at work showed me a script he put together to quickly jump to the numerous git projects we work with. It’s based on <code>dmenu</code> and looks rather nice. However, I’d rather have something based on <code>zsh</code>, but when looking around I didn’t find anything that really fit. So, instead I ended up writing a simple widget for <a href="https://github.com/zsh-users/zaw">zaw</a>.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode zsh"><code class="sourceCode zsh"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">function</span><span class="fu"> magus-git-projs()</span> <span class="kw">{</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>    <span class="ot">projs=</span><span class="st">&quot;</span><span class="ot">$(</span><span class="kw">find</span> ~/projs -maxdepth 2 -name .git -exec <span class="kw">dirname</span> <span class="dt">{}</span> <span class="dt">\;</span><span class="ot">)</span><span class="st">&quot;</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>    <span class="ot">candidates=(</span>$<span class="dt">{(f)projs}</span><span class="ot">)</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>    <span class="ot">actions=(</span>magus-git-projs-cd</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>             magus-git-projs-edit-first)</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a><span class="kw">}</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a><span class="kw">function</span><span class="fu"> magus-git-projs-cd()</span> <span class="kw">{</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>    zaw-callback-execute <span class="st">&quot;cd </span><span class="ot">${1}</span><span class="st">&quot;</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a><span class="kw">}</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a><span class="kw">function</span><span class="fu"> magus-git-projs-edit-first()</span> <span class="kw">{</span></span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true"></a>    zaw-callback-replace-buffer <span class="st">&quot;cd </span><span class="ot">${1}</span><span class="st">&quot;</span></span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true"></a><span class="kw">}</span></span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true"></a></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true"></a>zaw-register-src -n git-projs magus-git-projs</span></code></pre></div>
<p>I then attached bound it like this</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode zsh"><code class="sourceCode zsh"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">bindkey</span> -M vicmd <span class="st">&#39;zgp&#39;</span> zaw-git-projs</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>On mocks and stubs in python (free monad or interpreter pattern)</title>
    <link href="http://magnus.therning.org/posts/2017-01-31-000-on-mocks-and-stubs-in-python--free-monad-or-interpreter-pattern-.html" />
    <id>http://magnus.therning.org/posts/2017-01-31-000-on-mocks-and-stubs-in-python--free-monad-or-interpreter-pattern-.html</id>
    <published>2017-01-31T00:00:00Z</published>
    <updated>2017-01-31T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>A few weeks ago I watched a video where <a href="https://youtu.be/EaxDl5NPuCA">Ken Scambler talks about mocks and stubs</a>. In particular he talks about how to get rid of them.</p>
<p>One part is about coding IO operatioins as data and using the GoF interpreter pattern to</p>
<p>What he’s talking about is of course free monads, but I feel he’s glossing over a lot of details. Based on some of the questions asked during the talk I think I share that feeling with some people in the audience. Specifically I feel he skipped over the following:</p>
<ul>
<li>How does one <em>actually</em> write such code in a mainstream OO/imperative language?</li>
<li>What’s required of the language in order to allow using the techniques he’s talking about?</li>
<li>Errors tend to break abstractions, so how does one deal with error (i.e. exceptions)?</li>
</ul>
<p>Every time I’ve used mocks and stubs for unit testing I’ve had a feeling that “this can’t be how it’s supposed to be done!” So to me, Ken’s talk offered some hope, and I <em>really</em> want to know how applicable the ideas are in mainstream OO/imperative languages.</p>
<h3 id="the-example">The example</h3>
<p>To play around with this I picked the following function (in Python):</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">def</span> count_chars_of_file(fn):</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>    fd <span class="op">=</span> os.<span class="bu">open</span>(fn, os.O_RDONLY)</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>    text <span class="op">=</span> os.read(fd, <span class="dv">10000</span>)</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>    n <span class="op">=</span> <span class="bu">len</span>(text)</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>    os.close(fd)</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>    <span class="cf">return</span> n</span></code></pre></div>
<p>It’s small and simple, but I think it suffices to highlight a few important points. So the goal is to rewrite this function such that calls to IO operations (actions) (e.g. <code>os.read</code>) are replaced by data (an instance of some data type) conveying the intent of the operation. This data can later be passed to an <em>interpreter of actions</em>.</p>
<h3 id="thoughts-on-the-execution-of-actions-and-the-interpreter-pattern">Thoughts on the execution of actions and the interpreter pattern</h3>
<p>When reading the examples in the <a href="http://www.oodesign.com/interpreter-pattern.html">description of the interpreter pattern</a> what stands out to me is that they are either</p>
<ol type="1">
<li>a list of expressions, or</li>
<li>a tree of expressions</li>
</ol>
<p>that is passed to an interpreter. Will this do for us when trying to rewrite <code>count_chars_of_file</code>?</p>
<p>No, it won’t! Here’s why:</p>
<ul>
<li>A tree of actions doesn’t really make sense. Our actions are small and simple, they encode the intent of a single IO operation.</li>
<li>A list of actions can’t deal with interspersed non-actions, in this case it’s the line <code>n = len(text)</code> that causes a problem.</li>
</ul>
<p>The interpreter pattern misses something that is crucial in this case: the running of the interpreter must be intermingled with running non-interpreted code. The way I think of it is that not only the action needs to be present and dealt with, but also <em>the rest of the program</em>, that latter thing is commonly called a <em>continuation</em>.</p>
<p>So, can we introduce actions and rewrite <code>count_chars_of_file</code> such that we pause the program when interpretation of an action is required, interpret it, and then resume where we left off?</p>
<p>Sure, but it’s not really idiomatic Python code!</p>
<h3 id="actions-and-continuations">Actions and continuations</h3>
<p>The IO operations (actions) are represented as a named tuple:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a>Op <span class="op">=</span> collections.namedtuple(<span class="st">&#39;Op&#39;</span>, [<span class="st">&#39;op&#39;</span>, <span class="st">&#39;args&#39;</span>, <span class="st">&#39;k&#39;</span>])</span></code></pre></div>
<p>and the functions returning actions can then be written as</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">def</span> cps_open(fn, k):</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>    <span class="cf">return</span> Op(<span class="st">&#39;open&#39;</span>, [fn], k)</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a><span class="kw">def</span> cps_read(fd, k):</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>    <span class="cf">return</span> Op(<span class="st">&#39;read&#39;</span>, [fd], k)</span>
<span id="cb3-6"><a href="#cb3-6" aria-hidden="true"></a></span>
<span id="cb3-7"><a href="#cb3-7" aria-hidden="true"></a><span class="kw">def</span> cps_close(fd, k):</span>
<span id="cb3-8"><a href="#cb3-8" aria-hidden="true"></a>    <span class="cf">return</span> Op(<span class="st">&#39;close&#39;</span>, [fd], k)</span></code></pre></div>
<p>The interpreter is then an <em>if</em> statement checking the value of <code>op.op</code> with each branch executing the IO operation and passing the result to the rest of the program. I decided to wrap it directly in the program runner:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="kw">def</span> runProgram(prog):</span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>    <span class="kw">def</span> runOp(op):</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>        <span class="cf">if</span> op.op <span class="op">==</span> <span class="st">&#39;open&#39;</span>:</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>            fd <span class="op">=</span> os.<span class="bu">open</span>(<span class="op">*</span>op.args, os.O_RDONLY)</span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>            <span class="cf">return</span> op.k(fd)</span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a>        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;read&#39;</span>:</span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true"></a>            text <span class="op">=</span> os.read(<span class="op">*</span>op.args, <span class="dv">10000</span>)</span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true"></a>            <span class="cf">return</span> op.k(text)</span>
<span id="cb4-9"><a href="#cb4-9" aria-hidden="true"></a>        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;close&#39;</span>:</span>
<span id="cb4-10"><a href="#cb4-10" aria-hidden="true"></a>            os.close(<span class="op">*</span>op.args)</span>
<span id="cb4-11"><a href="#cb4-11" aria-hidden="true"></a>            <span class="cf">return</span> op.k()</span>
<span id="cb4-12"><a href="#cb4-12" aria-hidden="true"></a></span>
<span id="cb4-13"><a href="#cb4-13" aria-hidden="true"></a>    <span class="cf">while</span> <span class="bu">isinstance</span>(prog, Op):</span>
<span id="cb4-14"><a href="#cb4-14" aria-hidden="true"></a>        prog <span class="op">=</span> runOp(prog)</span>
<span id="cb4-15"><a href="#cb4-15" aria-hidden="true"></a></span>
<span id="cb4-16"><a href="#cb4-16" aria-hidden="true"></a>    <span class="cf">return</span> prog</span></code></pre></div>
<p>So far so good, but what will <code>count_char_of_file</code> all of this do to <code>count_chars_of_file</code>?</p>
<p>Well, it’s not quite as easy to read any more (basically it’s rewritten in <a href="https://en.wikipedia.org/wiki/Continuation-passing_style">CPS</a>):</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="kw">def</span> count_chars_of_file(fn):</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>    <span class="kw">def</span> cont_1(text, fd):</span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>        n <span class="op">=</span> <span class="bu">len</span>(text)</span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true"></a>        <span class="cf">return</span> cps_close(fd, <span class="kw">lambda</span> n<span class="op">=</span>n: n)</span>
<span id="cb5-6"><a href="#cb5-6" aria-hidden="true"></a></span>
<span id="cb5-7"><a href="#cb5-7" aria-hidden="true"></a>    <span class="kw">def</span> cont_0(fd):</span>
<span id="cb5-8"><a href="#cb5-8" aria-hidden="true"></a>        <span class="cf">return</span> cps_read(fd, <span class="kw">lambda</span> text, fd<span class="op">=</span>fd: cont_1(text, fd))</span>
<span id="cb5-9"><a href="#cb5-9" aria-hidden="true"></a></span>
<span id="cb5-10"><a href="#cb5-10" aria-hidden="true"></a>    <span class="cf">return</span> cps_open(fn, cont_0)</span></code></pre></div>
<h3 id="generators-to-the-rescue">Generators to the rescue</h3>
<p>Python does have a notion of continuations in the form of generators.<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> By making <code>count_char_of_file</code> into a generator it’s possible to remove the explicit continuations and the program actually resembles the original one again.</p>
<p>The type for the actions loses one member, and the functions creating them lose an argument:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>Op <span class="op">=</span> collections.namedtuple(<span class="st">&#39;Op&#39;</span>, [<span class="st">&#39;op&#39;</span>, <span class="st">&#39;args&#39;</span>])</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a></span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a><span class="kw">def</span> gen_open(fn):</span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>    <span class="cf">return</span> Op(<span class="st">&#39;open&#39;</span>, [fn])</span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a></span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a><span class="kw">def</span> gen_read(fd):</span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true"></a>    <span class="cf">return</span> Op(<span class="st">&#39;read&#39;</span>, [fd])</span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true"></a></span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true"></a><span class="kw">def</span> gen_close(fd):</span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true"></a>    <span class="cf">return</span> Op(<span class="st">&#39;close&#39;</span>, [fd])</span></code></pre></div>
<p>The interpreter and program runner must be modified to step the generator until its end:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a><span class="kw">def</span> runProgram(prog):</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>    <span class="kw">def</span> runOp(op):</span>
<span id="cb7-3"><a href="#cb7-3" aria-hidden="true"></a>        <span class="cf">if</span> op.op <span class="op">==</span> <span class="st">&#39;open&#39;</span>:</span>
<span id="cb7-4"><a href="#cb7-4" aria-hidden="true"></a>            fd <span class="op">=</span> os.<span class="bu">open</span>(op.args[<span class="dv">0</span>], os.O_RDONLY)</span>
<span id="cb7-5"><a href="#cb7-5" aria-hidden="true"></a>            <span class="cf">return</span> fd</span>
<span id="cb7-6"><a href="#cb7-6" aria-hidden="true"></a>        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;read&#39;</span>:</span>
<span id="cb7-7"><a href="#cb7-7" aria-hidden="true"></a>            text <span class="op">=</span> os.read(op.args[<span class="dv">0</span>], <span class="dv">10000</span>)</span>
<span id="cb7-8"><a href="#cb7-8" aria-hidden="true"></a>            <span class="cf">return</span> text</span>
<span id="cb7-9"><a href="#cb7-9" aria-hidden="true"></a>        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;close&#39;</span>:</span>
<span id="cb7-10"><a href="#cb7-10" aria-hidden="true"></a>            os.close(op.args[<span class="dv">0</span>])</span>
<span id="cb7-11"><a href="#cb7-11" aria-hidden="true"></a>            <span class="cf">return</span> <span class="va">None</span></span>
<span id="cb7-12"><a href="#cb7-12" aria-hidden="true"></a></span>
<span id="cb7-13"><a href="#cb7-13" aria-hidden="true"></a>    <span class="cf">try</span>:</span>
<span id="cb7-14"><a href="#cb7-14" aria-hidden="true"></a>        op <span class="op">=</span> prog.send(<span class="va">None</span>)</span>
<span id="cb7-15"><a href="#cb7-15" aria-hidden="true"></a>        <span class="cf">while</span> <span class="va">True</span>:</span>
<span id="cb7-16"><a href="#cb7-16" aria-hidden="true"></a>            r <span class="op">=</span> runOp(op)</span>
<span id="cb7-17"><a href="#cb7-17" aria-hidden="true"></a>            op <span class="op">=</span> prog.send(r)</span>
<span id="cb7-18"><a href="#cb7-18" aria-hidden="true"></a>    <span class="cf">except</span> <span class="pp">StopIteration</span> <span class="im">as</span> e:</span>
<span id="cb7-19"><a href="#cb7-19" aria-hidden="true"></a>        <span class="cf">return</span> e.value</span></code></pre></div>
<p>Finally, the generator-version of <code>count_chars_of_file</code> goes back to being a bit more readable:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="kw">def</span> count_chars_of_file(fn):</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>    fd <span class="op">=</span> <span class="cf">yield</span> gen_open(fn)</span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a>    text <span class="op">=</span> <span class="cf">yield</span> gen_read(fd)</span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true"></a>    n <span class="op">=</span> <span class="bu">len</span>(text)</span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true"></a>    <span class="cf">yield</span> gen_close(fd)</span>
<span id="cb8-6"><a href="#cb8-6" aria-hidden="true"></a>    <span class="cf">return</span> n</span></code></pre></div>
<h3 id="generators-all-the-way">Generators all the way</h3>
<p>Limitations of Python generators mean that we have either have to push the interpreter (<code>runProgram</code>) down to where <code>count_char_of_file</code> is used, or make all intermediate layers into generators and rewrite the interpreter to deal with this. It could look something like this then:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="kw">def</span> runProgram(prog):</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a>    <span class="kw">def</span> runOp(op):</span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true"></a>        <span class="cf">if</span> op.op <span class="op">==</span> <span class="st">&#39;open&#39;</span>:</span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true"></a>            fd <span class="op">=</span> os.<span class="bu">open</span>(op.args[<span class="dv">0</span>], os.O_RDONLY)</span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true"></a>            <span class="cf">return</span> fd</span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true"></a>        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;read&#39;</span>:</span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true"></a>            text <span class="op">=</span> os.read(op.args[<span class="dv">0</span>], <span class="dv">10000</span>)</span>
<span id="cb9-8"><a href="#cb9-8" aria-hidden="true"></a>            <span class="cf">return</span> text</span>
<span id="cb9-9"><a href="#cb9-9" aria-hidden="true"></a>        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;close&#39;</span>:</span>
<span id="cb9-10"><a href="#cb9-10" aria-hidden="true"></a>            os.close(op.args[<span class="dv">0</span>])</span>
<span id="cb9-11"><a href="#cb9-11" aria-hidden="true"></a>            <span class="cf">return</span> <span class="va">None</span></span>
<span id="cb9-12"><a href="#cb9-12" aria-hidden="true"></a></span>
<span id="cb9-13"><a href="#cb9-13" aria-hidden="true"></a>    <span class="cf">try</span>:</span>
<span id="cb9-14"><a href="#cb9-14" aria-hidden="true"></a>        op <span class="op">=</span> prog.send(<span class="va">None</span>)</span>
<span id="cb9-15"><a href="#cb9-15" aria-hidden="true"></a>        <span class="cf">while</span> <span class="va">True</span>:</span>
<span id="cb9-16"><a href="#cb9-16" aria-hidden="true"></a>            <span class="cf">if</span> <span class="bu">isinstance</span>(op, Op):</span>
<span id="cb9-17"><a href="#cb9-17" aria-hidden="true"></a>                r <span class="op">=</span> runOp(op)</span>
<span id="cb9-18"><a href="#cb9-18" aria-hidden="true"></a>                op <span class="op">=</span> prog.send(r)</span>
<span id="cb9-19"><a href="#cb9-19" aria-hidden="true"></a>            <span class="cf">elif</span> <span class="bu">isinstance</span>(op, types.GeneratorType):</span>
<span id="cb9-20"><a href="#cb9-20" aria-hidden="true"></a>                r <span class="op">=</span> runProgram(op)</span>
<span id="cb9-21"><a href="#cb9-21" aria-hidden="true"></a>                op <span class="op">=</span> prog.send(r)</span>
<span id="cb9-22"><a href="#cb9-22" aria-hidden="true"></a></span>
<span id="cb9-23"><a href="#cb9-23" aria-hidden="true"></a>    <span class="cf">except</span> <span class="pp">StopIteration</span> <span class="im">as</span> e:</span>
<span id="cb9-24"><a href="#cb9-24" aria-hidden="true"></a>        <span class="cf">return</span> e.value</span></code></pre></div>
<h3 id="final-thoughts">Final thoughts</h3>
<p>I think I’ve shown one way to achieve, at least parts of, what Ken talks about. The resulting code looks almost like “normal Python”. There are some things to note:</p>
<ol type="1">
<li>Exception handling is missing. I know of no way to inject an exception into a generator in Python so I’m guessing that exceptions from running the IO operations would have to be passed in via <code>generator.send</code> as a normal value, which means that exception handling code would have to look decidedly non-Pythonic.</li>
<li>Using this approach means the language must have support for generators (or some other way to represent <em>the rest of the program</em>). I think this rules out Java, but probably it can be done in C#.</li>
<li>I’ve only used a single interpreter here, but I see no issues with combining interpreters (to combine domains of operations like <em>file operations</em> and <em>network operations</em>). I also think it’d be possible to use it to realize what De Goes calls <a href="http://degoes.net/articles/modern-fp-part-2#onion-architecture">Onion Architecture</a>.</li>
</ol>
<p>Would I ever advocate this approach for a larger Python project, or even any project in an OO/imperative language?</p>
<p>I’m not sure! I think that testing using mocks and stubs has lead to a smelly code base each an every time I’ve used it, but this approach feels a little too far from how OO/imperative code usually is written. I would love to try it out and see what the implications really are.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>I know, I know, <em>coroutines</em>! I’m simplifying and brushing over details here, but I don’t think I’m brushing over any details that are <em>important</em> for this example.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>That's a large project</title>
    <link href="http://magnus.therning.org/posts/2016-10-21-000-large-project.html" />
    <id>http://magnus.therning.org/posts/2016-10-21-000-large-project.html</id>
    <published>2016-10-21T00:00:00Z</published>
    <updated>2016-10-21T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>From <a href="http://lwn.net/Articles/702177/">LWN October 6, 2016</a>:</p>
<blockquote>
<p>Over the course of the last year, the project accepted about eight changes per hour — every hour — from over 4,000 developers sponsored by over 400 companies.</p>
</blockquote>]]></summary>
</entry>
<entry>
    <title>Free, take 2</title>
    <link href="http://magnus.therning.org/posts/2016-06-18-free--take-2.html" />
    <id>http://magnus.therning.org/posts/2016-06-18-free--take-2.html</id>
    <published>2016-06-18T00:00:00Z</published>
    <updated>2016-06-18T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other day I read <a href="https://ocharles.org.uk/blog/posts/2016-01-26-transformers-free-monads-mtl-laws.html">a blog post on monads and stuff</a> after which I felt rather silly about my <a href="/posts/2016-01-13-000-free-play--part-one.html">earlier</a> <a href="/posts/2016-01-15-000-free-play--part-two.html">posts</a> on <a href="/posts/2016-01-17-000-free-play--part-three.html">Free</a>.</p>
<p>I think this is probably the post I should have written instead :)</p>
<p>I’ll use three pieces of code, each one builds on the one before:</p>
<ul>
<li><code>Free1.hs</code> - Uses a free monad for a single algebra/API (full code <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free1-hs">here</a>).</li>
<li><code>Free2.hs</code> - Uses a free monad for a two algebras/APIs, where one decorates the other (full code <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free2-hs">here</a>).</li>
<li><code>Free3.hs</code> - Uses a free monad for a three algebras/APIs, where two are used in the program and the remaing one decorates the other two (full code <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free3-hs">here</a>).</li>
</ul>
<h3 id="the-first---one-algebra">The first - one algebra</h3>
<p>I’m re-using the algebras from my previous posts, but I believe it makes it easier to follow along if the amount of jumping between posts is minimized so here is the first one once again:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">SimpleFileF</span> a</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  <span class="ot">=</span> <span class="dt">LoadFile</span> <span class="dt">FilePath</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> a)</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  <span class="op">|</span> <span class="dt">SaveFile</span> <span class="dt">FilePath</span> <span class="dt">String</span> a</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>  <span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">SimpleFileAPI</span> <span class="ot">=</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a><span class="ot">loadFile ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> <span class="dt">String</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>loadFile fp <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">LoadFile</span> fp <span class="fu">id</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a><span class="ot">saveFile ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> ()</span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a>saveFile fp d <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">SaveFile</span> fp d ()</span></code></pre></div>
<p>It’s a ridiculously small one, but I believe it’s good enough to work with. In the previous posts I implemented the <code>Functor</code> instances manually. I couldn’t be bothered this time around; I think I pretty much know how to do that for this kind of types now.</p>
<p>Having a type for the algebra, <code>SimpleFileAPI</code>, is convenient already now, even more so in the other examples.</p>
<p>The two convenience functions on the end makes it straight forward to write functions using the algebra:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="ot">withSimpleFile ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> ()</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>withSimpleFile f fp <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>  d <span class="ot">&lt;-</span> loadFile fp</span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>  <span class="kw">let</span> result <span class="ot">=</span> f d</span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>  saveFile (fp <span class="op">++</span> <span class="st">&quot;_new&quot;</span>) result</span></code></pre></div>
<p>This is simple, straight forward monadic code. Easy to read and work with. Of course it doesn’t actually do anything at all yet. For that I need need an interpreter, something that translates (reduces) the algebra, the API, the commands, call them what you will, into the (side-)effects I want. For <a href="http://hackage.haskell.org/package/free-4.12.4/docs/Control-Monad-Free.html#t:Free"><code>Free</code></a> that is <a href="http://hackage.haskell.org/package/free-4.12.4/docs/Control-Monad-Free.html#v:foldFree"><code>foldFree</code></a> together with a suitable function <code class="sourceCode haskell"><span class="ot">f ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</code>.</p>
<p>I want <code>LoadFile</code> to translate to a file being read and <code>SaveFile</code> to some data being saved to a file. That makes it pretty obvious how that <code>f</code> needs to be written:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">runSimpleFile ::</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>runSimpleFile <span class="ot">=</span> foldFree f</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a>    f (<span class="dt">LoadFile</span> fp f&#39;) <span class="ot">=</span> f&#39; <span class="op">&lt;$&gt;</span> <span class="fu">readFile</span> fp</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>    f (<span class="dt">SaveFile</span> fp d r) <span class="ot">=</span> <span class="fu">writeFile</span> fp d <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span></code></pre></div>
<p>At this point it might be good to explain the constructors of <code>SimpleFileF</code> a bit more. At first I thought they looked a bit funny. I mean, why does <code>SaveFile</code> have an <code>a</code> at all since it obviously always should result in <code>()</code>? And what’s up with that function in <code>Loadfile</code>?</p>
<p>It did become a little clearer to me after some thought and having a look at <code>Free</code>:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">Free</span> f a <span class="ot">=</span> <span class="dt">Pure</span> a <span class="op">|</span> <span class="dt">Free</span> (f (<span class="dt">Free</span> f a))</span></code></pre></div>
<p>I personally find the latter constructor a bit mind-bending. I can handle recursive functions fairly well, but recursive types have a tendency to confuse me. From what I understand one can think of <code>Free</code> as similar to a list. <code>Pure</code> ends the list (<code>Nil</code>) and <code>Free</code> one instance of <code>f</code> to the rest of the list (<code>Cons</code>). Since <code>Free f a</code> is a monad one can think of <code>a</code> as the result of the command.</p>
<p>If I were to write <code>saveFile</code> explicitly it’d look like this</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a>saveFile fp d <span class="ot">=</span> <span class="dt">Free</span> (<span class="dt">SaveFile</span> fp d (<span class="dt">Pure</span> ()))</span></code></pre></div>
<p>and for <code>loadFile</code>:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>loadFile fp <span class="ot">=</span> <span class="dt">Free</span> (<span class="dt">LoadFile</span> fp (\ s <span class="ot">-&gt;</span> <span class="dt">Pure</span> s))</span></code></pre></div>
<p>But let’s get back to the type and why ‘a’ occurs like it does in the two constructors. As Gabriel G shows in his post <a href="http://www.haskellforall.com/2012/06/you-could-have-invented-free-monads.html">Why free monads matter</a> a constructor without <code>a</code> would result in termination. In other words, if <code>SaveFile</code> didn’t hold an <code>a</code> I’d not be able to write, in a natural way, a function that saves two files.</p>
<p>Another limiting factor is that <code>foldFree</code> of the <code>Free</code> implementation I’m using has the type <code class="sourceCode haskell"><span class="dt">Monad</span> m <span class="ot">=&gt;</span> (<span class="kw">forall</span> x<span class="op">.</span> f x <span class="ot">-&gt;</span> m x) <span class="ot">-&gt;</span> <span class="dt">Free</span> f a <span class="ot">-&gt;</span> m a</code>. This sets a requirement on what the function translating from my API into effects may look like, i.e. what <code>f</code> in <code>runSimpleFile</code> may look like. If <code>SaveFile</code> had no <code>a</code> to return what would <code>f (SaveFile {})</code> return, how could it ever satisfy the required type?</p>
<p>The reason for <code>LoadFile</code> having a function <code>String -&gt; a</code> is simply that there is no data yet, but I still want to be able to manipulate it. Using a function and function composition is the ticket then.</p>
<p>I think that’s all there is to say about to the first piece of code. To run it take a look at the comment at the end of <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free1-hs">the file</a> and then play with it. If you want to turn all characters of a file <code>foo</code> into upper case you can use</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a>runSimpleFile <span class="op">$</span> withSimpleFile (<span class="fu">map</span> <span class="fu">toUpper</span>) <span class="st">&quot;foo&quot;</span></span></code></pre></div>
<h3 id="the-second---two-algebras-one-decorating-the-other">The second - two algebras, one decorating the other</h3>
<p>The second piece of code almost only adds to the first one. There is one exception though, the function <code>runSimpleFile</code> is removed. Instead I’ve taken the transformation function, which used to be called <code>f</code> and was internal to <code>runSimpleFile</code> and moved it out. It’s called <code>stepSimpleFile</code>:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="ot">stepSimpleFile ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>stepSimpleFile (<span class="dt">LoadFile</span> fp f&#39;) <span class="ot">=</span> f&#39; <span class="op">&lt;$&gt;</span> <span class="fu">readFile</span> fp</span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a>stepSimpleFile (<span class="dt">SaveFile</span> fp d r) <span class="ot">=</span> <span class="fu">writeFile</span> fp d <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span></code></pre></div>
<p>The logging API, <code>LogAPI</code>, follows the same pattern as <code>SimpleFileAPI</code> and I’m counting on the description above being clear enough to not have to repeat myself. For completeness I include the code:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">LogF</span> a <span class="ot">=</span> <span class="dt">Log</span> <span class="dt">String</span> a</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a>          <span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true"></a></span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">LogAPI</span> <span class="ot">=</span> <span class="dt">Free</span> <span class="dt">LogF</span></span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true"></a></span>
<span id="cb9-6"><a href="#cb9-6" aria-hidden="true"></a><span class="ot">stepLog ::</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb9-7"><a href="#cb9-7" aria-hidden="true"></a>stepLog (<span class="dt">Log</span> s r) <span class="ot">=</span> <span class="fu">putStrLn</span> s <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span></code></pre></div>
<p>I intend the <code>LogAPI</code> to be used as embellishments on the <code>SimpleFileAPI</code>, in other words I somehow have to turn an operation of <code>SimpleFileAPI</code> into an operation of <code>LogAPI</code>, i.e. I need a transformation. I called it <code>logSimpleFileT</code> and let it turn operations in <code>SimpleFileF</code> (i.e. not exactly <code>SimpleFileAPI</code>) into <code>LogAPI</code> (if you are wondering about my choice of type I hope it’ll become clear below, just trust me for now that this is a good choice):</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a><span class="ot">logSimpleFileT ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">LogAPI</span> ()</span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true"></a>logSimpleFileT (<span class="dt">LoadFile</span> fp _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** load file &quot;</span> <span class="op">++</span> fp) ()</span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true"></a>logSimpleFileT (<span class="dt">SaveFile</span> fp _ _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** save file &quot;</span> <span class="op">++</span> fp) ()</span></code></pre></div>
<p>So far everything is hopefully very straight forward and unsurprising. Now I need to combine the two APIs, I need to add them, in other words, I need a sum type:</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb11-1"><a href="#cb11-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">S</span> a1 a2 t <span class="ot">=</span> <span class="dt">A1</span> (a1 t) <span class="op">|</span> <span class="dt">A2</span> (a2 t)</span>
<span id="cb11-2"><a href="#cb11-2" aria-hidden="true"></a>              <span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb11-3"><a href="#cb11-3" aria-hidden="true"></a></span>
<span id="cb11-4"><a href="#cb11-4" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">SumAPI</span> <span class="ot">=</span> <span class="dt">Free</span> (<span class="dt">S</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>)</span></code></pre></div>
<p>The next question is how to turn my two original APIs, <code>SimpleFileAPI</code> and <code>LogAPI</code>, into <code>SumAPI</code>. Luckily that’s already solved by the function <a href="http://hackage.haskell.org/package/free-4.12.4/docs/Control-Monad-Free.html#v:hoistFree"><code>hoistFree</code></a>:</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true"></a><span class="ot">hoistFree ::</span> <span class="dt">Functor</span> g <span class="ot">=&gt;</span> (<span class="kw">forall</span> a<span class="op">.</span> f a <span class="ot">-&gt;</span> g a) <span class="ot">-&gt;</span> <span class="dt">Free</span> f b <span class="ot">-&gt;</span> <span class="dt">Free</span> g b </span></code></pre></div>
<p>With this and <code>logSimpleFileT</code> from above I can use <code>foldFree</code> to decorate each operation with a logging operation like this:</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb13-1"><a href="#cb13-1" aria-hidden="true"></a><span class="ot">logSimpleFile ::</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> a</span>
<span id="cb13-2"><a href="#cb13-2" aria-hidden="true"></a>logSimpleFile <span class="ot">=</span> foldFree f</span>
<span id="cb13-3"><a href="#cb13-3" aria-hidden="true"></a><span class="kw">where</span></span>
<span id="cb13-4"><a href="#cb13-4" aria-hidden="true"></a>  f op <span class="ot">=</span> hoistFree <span class="dt">A1</span> (logSimpleFileT op) <span class="op">*&gt;</span> hoistFree <span class="dt">A2</span> (liftF op)</span></code></pre></div>
<p>This is where the type of <code>logSimpleFileT</code> hopefully makes sense!</p>
<p>Just as in the first section of this post, I need an interpreter for my API (<code>SumAPI</code> this time). Once again it’s written using <code>foldFree</code>, but this time I provide the interpreters for the sub-algegras (what I’ve chosen to call step functions):</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb14-1"><a href="#cb14-1" aria-hidden="true"></a><span class="ot">runSum ::</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> (<span class="kw">forall</span> a<span class="op">.</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> m a) <span class="ot">-&gt;</span> (<span class="kw">forall</span> a<span class="op">.</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> m a) <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> b <span class="ot">-&gt;</span> m b</span>
<span id="cb14-2"><a href="#cb14-2" aria-hidden="true"></a>runSum f1 f2 <span class="ot">=</span> foldFree f</span>
<span id="cb14-3"><a href="#cb14-3" aria-hidden="true"></a><span class="kw">where</span></span>
<span id="cb14-4"><a href="#cb14-4" aria-hidden="true"></a>  f (<span class="dt">A1</span> op) <span class="ot">=</span> f1 op</span>
<span id="cb14-5"><a href="#cb14-5" aria-hidden="true"></a>  f (<span class="dt">A2</span> op) <span class="ot">=</span> f2 op</span></code></pre></div>
<p><a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free2-hs">The file</a> has a comment at the end for how to run it. The same example as in the previous section, but now with logging, looks like this</p>
<div class="sourceCode" id="cb15"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb15-1"><a href="#cb15-1" aria-hidden="true"></a>runSum stepLog stepSimpleFile <span class="op">$</span> logSimpleFile <span class="op">$</span> withSimpleFile (<span class="fu">map</span> <span class="fu">toUpper</span>) <span class="st">&quot;foo&quot;</span></span></code></pre></div>
<h3 id="the-third---three-algebras-one-decorating-the-other-two">The third - three algebras, one decorating the other two</h3>
<p>To combine three algebras I simply take what’s in the previous section and extend it, i.e. a sum type with three constructors:</p>
<div class="sourceCode" id="cb16"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb16-1"><a href="#cb16-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">S</span> a1 a2 a3 t <span class="ot">=</span> <span class="dt">A1</span> (a1 t) <span class="op">|</span> <span class="dt">A2</span> (a2 t) <span class="op">|</span> <span class="dt">A3</span> (a3 t)</span>
<span id="cb16-2"><a href="#cb16-2" aria-hidden="true"></a>                <span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb16-3"><a href="#cb16-3" aria-hidden="true"></a></span>
<span id="cb16-4"><a href="#cb16-4" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">SumAPI</span> <span class="ot">=</span> <span class="dt">Free</span> (<span class="dt">S</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span> <span class="dt">StdIoF</span>)</span>
<span id="cb16-5"><a href="#cb16-5" aria-hidden="true"></a></span>
<span id="cb16-6"><a href="#cb16-6" aria-hidden="true"></a><span class="ot">runSum ::</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> (<span class="kw">forall</span> a<span class="op">.</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> m a)</span>
<span id="cb16-7"><a href="#cb16-7" aria-hidden="true"></a>      <span class="ot">-&gt;</span> (<span class="kw">forall</span> a<span class="op">.</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> m a)</span>
<span id="cb16-8"><a href="#cb16-8" aria-hidden="true"></a>      <span class="ot">-&gt;</span> (<span class="kw">forall</span> a<span class="op">.</span> <span class="dt">StdIoF</span> a <span class="ot">-&gt;</span> m a)</span>
<span id="cb16-9"><a href="#cb16-9" aria-hidden="true"></a>      <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> b <span class="ot">-&gt;</span> m b</span>
<span id="cb16-10"><a href="#cb16-10" aria-hidden="true"></a>runSum f1 f2 f3 <span class="ot">=</span> foldFree f</span>
<span id="cb16-11"><a href="#cb16-11" aria-hidden="true"></a><span class="kw">where</span></span>
<span id="cb16-12"><a href="#cb16-12" aria-hidden="true"></a>  f (<span class="dt">A1</span> op) <span class="ot">=</span> f1 op</span>
<span id="cb16-13"><a href="#cb16-13" aria-hidden="true"></a>  f (<span class="dt">A2</span> op) <span class="ot">=</span> f2 op</span>
<span id="cb16-14"><a href="#cb16-14" aria-hidden="true"></a>  f (<span class="dt">A3</span> op) <span class="ot">=</span> f3 op</span></code></pre></div>
<p>With this I’ve already revealed that my three APIs are the two from previous sections, <code>LogAPI</code> (for decorating the other APIs), <code>SimpleFileAPI</code> and a new one, <code>StdIoAPI</code>.</p>
<p>I want to combine them in such a wat that I can write functions using both APIs at the same time. Then I modify <code>withSimpleFile</code> into</p>
<div class="sourceCode" id="cb17"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb17-1"><a href="#cb17-1" aria-hidden="true"></a><span class="ot">withSimpleFile ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span>  <span class="dt">SumAPI</span> ()</span>
<span id="cb17-2"><a href="#cb17-2" aria-hidden="true"></a>withSimpleFile f fp <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb17-3"><a href="#cb17-3" aria-hidden="true"></a>  d <span class="ot">&lt;-</span> loadFile fp</span>
<span id="cb17-4"><a href="#cb17-4" aria-hidden="true"></a>  <span class="kw">let</span> result <span class="ot">=</span> f d</span>
<span id="cb17-5"><a href="#cb17-5" aria-hidden="true"></a>  saveFile (fp <span class="op">++</span> <span class="st">&quot;_new&quot;</span>) result</span></code></pre></div>
<p>and I can add another function that uses it with <code>StdIoAPI</code>:</p>
<div class="sourceCode" id="cb18"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb18-1"><a href="#cb18-1" aria-hidden="true"></a><span class="ot">prog ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> ()</span>
<span id="cb18-2"><a href="#cb18-2" aria-hidden="true"></a>prog fn <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb18-3"><a href="#cb18-3" aria-hidden="true"></a>  stdioPut <span class="st">&quot;About to start&quot;</span></span>
<span id="cb18-4"><a href="#cb18-4" aria-hidden="true"></a>  withSimpleFile (<span class="fu">map</span> <span class="fu">toUpper</span>) fn</span>
<span id="cb18-5"><a href="#cb18-5" aria-hidden="true"></a>  stdioPut <span class="st">&quot;Done!&quot;</span></span></code></pre></div>
<p>The way to allow the APIs to be combined this way is to bake in <code>S</code> already in the convenience functions. This means the code for <code>SimpleFileAPI</code> has to change slightly (note the use of <code>A2</code> in <code>loadFile</code> and <code>saveFile</code>):</p>
<div class="sourceCode" id="cb19"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb19-1"><a href="#cb19-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">SimpleFileF</span> a</span>
<span id="cb19-2"><a href="#cb19-2" aria-hidden="true"></a><span class="ot">=</span> <span class="dt">LoadFile</span> <span class="dt">FilePath</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> a)</span>
<span id="cb19-3"><a href="#cb19-3" aria-hidden="true"></a><span class="op">|</span> <span class="dt">SaveFile</span> <span class="dt">FilePath</span> <span class="dt">String</span> a</span>
<span id="cb19-4"><a href="#cb19-4" aria-hidden="true"></a><span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb19-5"><a href="#cb19-5" aria-hidden="true"></a></span>
<span id="cb19-6"><a href="#cb19-6" aria-hidden="true"></a><span class="ot">loadFile ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> <span class="dt">String</span></span>
<span id="cb19-7"><a href="#cb19-7" aria-hidden="true"></a>loadFile fp <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">A2</span> <span class="op">$</span> <span class="dt">LoadFile</span> fp <span class="fu">id</span></span>
<span id="cb19-8"><a href="#cb19-8" aria-hidden="true"></a></span>
<span id="cb19-9"><a href="#cb19-9" aria-hidden="true"></a><span class="ot">saveFile ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> ()</span>
<span id="cb19-10"><a href="#cb19-10" aria-hidden="true"></a>saveFile fp d <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">A2</span> <span class="op">$</span> <span class="dt">SaveFile</span> fp d ()</span>
<span id="cb19-11"><a href="#cb19-11" aria-hidden="true"></a></span>
<span id="cb19-12"><a href="#cb19-12" aria-hidden="true"></a><span class="ot">stepSimpleFile ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb19-13"><a href="#cb19-13" aria-hidden="true"></a>stepSimpleFile (<span class="dt">LoadFile</span> fp f&#39;) <span class="ot">=</span> f&#39; <span class="op">&lt;$&gt;</span> <span class="fu">readFile</span> fp</span>
<span id="cb19-14"><a href="#cb19-14" aria-hidden="true"></a>stepSimpleFile (<span class="dt">SaveFile</span> fp d r) <span class="ot">=</span> <span class="fu">writeFile</span> fp d <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span></code></pre></div>
<p>The new API, <code>StdIoAPI</code>, has only one operation:</p>
<div class="sourceCode" id="cb20"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb20-1"><a href="#cb20-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">StdIoF</span> a <span class="ot">=</span> <span class="dt">PutStrLn</span> <span class="dt">String</span> a</span>
<span id="cb20-2"><a href="#cb20-2" aria-hidden="true"></a>              <span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb20-3"><a href="#cb20-3" aria-hidden="true"></a></span>
<span id="cb20-4"><a href="#cb20-4" aria-hidden="true"></a><span class="ot">stdioPut ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> ()</span>
<span id="cb20-5"><a href="#cb20-5" aria-hidden="true"></a>stdioPut s <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">A3</span> <span class="op">$</span> <span class="dt">PutStrLn</span> s ()</span>
<span id="cb20-6"><a href="#cb20-6" aria-hidden="true"></a></span>
<span id="cb20-7"><a href="#cb20-7" aria-hidden="true"></a><span class="ot">stepStdIo ::</span> <span class="dt">StdIoF</span> b <span class="ot">-&gt;</span> <span class="dt">IO</span> b</span>
<span id="cb20-8"><a href="#cb20-8" aria-hidden="true"></a>stepStdIo (<span class="dt">PutStrLn</span> s a) <span class="ot">=</span> <span class="fu">putStrLn</span> s <span class="op">&gt;&gt;</span> <span class="fu">return</span> a</span></code></pre></div>
<p>The logging API, <code>LogAPI</code>, looks exactly the same but I now need two transformation functions, one for <code>SimpleFileAPI</code> and one for <code>StdIoAPI</code>.</p>
<div class="sourceCode" id="cb21"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb21-1"><a href="#cb21-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">LogF</span> a <span class="ot">=</span> <span class="dt">Log</span> <span class="dt">String</span> a</span>
<span id="cb21-2"><a href="#cb21-2" aria-hidden="true"></a>            <span class="kw">deriving</span>(<span class="dt">Functor</span>)</span>
<span id="cb21-3"><a href="#cb21-3" aria-hidden="true"></a></span>
<span id="cb21-4"><a href="#cb21-4" aria-hidden="true"></a><span class="kw">type</span> <span class="dt">LogAPI</span> <span class="ot">=</span> <span class="dt">Free</span> <span class="dt">LogF</span></span>
<span id="cb21-5"><a href="#cb21-5" aria-hidden="true"></a></span>
<span id="cb21-6"><a href="#cb21-6" aria-hidden="true"></a><span class="ot">stepLog ::</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb21-7"><a href="#cb21-7" aria-hidden="true"></a>stepLog (<span class="dt">Log</span> s r) <span class="ot">=</span> <span class="fu">putStrLn</span> s <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span>
<span id="cb21-8"><a href="#cb21-8" aria-hidden="true"></a></span>
<span id="cb21-9"><a href="#cb21-9" aria-hidden="true"></a><span class="ot">logSimpleFileT ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">LogAPI</span> ()</span>
<span id="cb21-10"><a href="#cb21-10" aria-hidden="true"></a>logSimpleFileT (<span class="dt">LoadFile</span> fp _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** load file &quot;</span> <span class="op">++</span> fp) ()</span>
<span id="cb21-11"><a href="#cb21-11" aria-hidden="true"></a>logSimpleFileT (<span class="dt">SaveFile</span> fp _ _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** save file &quot;</span> <span class="op">++</span> fp) ()</span>
<span id="cb21-12"><a href="#cb21-12" aria-hidden="true"></a></span>
<span id="cb21-13"><a href="#cb21-13" aria-hidden="true"></a><span class="ot">logStdIoT ::</span> <span class="dt">StdIoF</span> a <span class="ot">-&gt;</span> <span class="dt">LogAPI</span> ()</span>
<span id="cb21-14"><a href="#cb21-14" aria-hidden="true"></a>logStdIoT (<span class="dt">PutStrLn</span> s _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** on stdio &quot;</span> <span class="op">++</span> s) ()</span></code></pre></div>
<p>The new version of <code>logT</code> needs to operate on <code>S</code> in order to decorate both APIs.</p>
<div class="sourceCode" id="cb22"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb22-1"><a href="#cb22-1" aria-hidden="true"></a><span class="ot">logT ::</span> <span class="dt">SumAPI</span> a <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> a</span>
<span id="cb22-2"><a href="#cb22-2" aria-hidden="true"></a>logT <span class="ot">=</span> foldFree f</span>
<span id="cb22-3"><a href="#cb22-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb22-4"><a href="#cb22-4" aria-hidden="true"></a>    f (<span class="dt">A2</span> op) <span class="ot">=</span> hoistFree <span class="dt">A1</span> (logSimpleFileT op) <span class="op">*&gt;</span> hoistFree <span class="dt">A2</span> (liftF op)</span>
<span id="cb22-5"><a href="#cb22-5" aria-hidden="true"></a>    f (<span class="dt">A3</span> op) <span class="ot">=</span> hoistFree <span class="dt">A1</span> (logStdIoT op) <span class="op">*&gt;</span> hoistFree <span class="dt">A3</span> (liftF op)</span>
<span id="cb22-6"><a href="#cb22-6" aria-hidden="true"></a>    f a<span class="op">@</span>(<span class="dt">A1</span> _) <span class="ot">=</span> liftF a</span></code></pre></div>
<p><a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free3-hs">This file</a> also has comments on how to run it at the end. This time there are two examples, one on how to run it without logging</p>
<div class="sourceCode" id="cb23"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb23-1"><a href="#cb23-1" aria-hidden="true"></a>runSum <span class="fu">undefined</span> stepSimpleFile stepStdIo <span class="op">$</span> prog <span class="st">&quot;foo&quot;</span></span></code></pre></div>
<p>and one with logging</p>
<div class="sourceCode" id="cb24"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb24-1"><a href="#cb24-1" aria-hidden="true"></a>runSum stepLog stepSimpleFile stepStdIo (logT <span class="op">$</span> prog <span class="st">&quot;foo&quot;</span>)</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>CMake, ExternalData, and custom fetch script</title>
    <link href="http://magnus.therning.org/posts/2016-05-17-000-cmake--externaldata--and-custom-fetch-script.html" />
    <id>http://magnus.therning.org/posts/2016-05-17-000-cmake--externaldata--and-custom-fetch-script.html</id>
    <published>2016-05-17T00:00:00Z</published>
    <updated>2016-05-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I failed to find a concrete example on how to use the <a href="https://cmake.org/documentation/">CMake</a> module <a href="https://cmake.org/cmake/help/v3.5/module/ExternalData.html">ExternalData</a> with a custom fetch script. Since I finally manage to work out how to use it I thought I’d try to help out the next person who needs to go down this route.</p>
<h3 id="why-externaldata">Why ExternalData?</h3>
<p>I thought I’d start with a short justification of why I was looking at the module at all.</p>
<p>At work I work with a product that processes images and video. When writing tests we often need some rather large files (from MiB to GiB) as input. The two obvious options are:</p>
<ol type="1">
<li>Check the files into our Git repo, or</li>
<li>Put them on shared storage</li>
</ol>
<p>Neither of these are very appealing. The former just doesn’t feel quite right, these are large binary files that rarely, if ever, change, why place them under version control at all? And if they do change the Git repo is likely to balloon in size and impact cloning times negatively. The latter makes it difficult to run our tests on a machine that isn’t on the office network and any changes to the files will break older tests, unless we always only add files, never modify any in place. On the other hand, the former guarantees that the files needed for testing are always available and it is possible to modify the files without breaking older tests. The pro of the latter is that we only download the files needed for the current tests.</p>
<p>ExternalData is <em>one</em> option to address this. On some level it feels like it offers a combination of both options above:</p>
<ul>
<li>It’s possible to use the shared storage</li>
<li>When the shared storage isn’t available it’s possible to fall back on downloading the files via other means</li>
<li>The layout of the storage is such that modifying in place is much less likely</li>
<li>Only the files needed for the currest tests will be downloaded when building off-site</li>
</ul>
<h3 id="the-object-store">The object store</h3>
<p>We do our building in <a href="https://www.docker.com/">docker</a> images that do have our shared storage mapped in, so I’d like them to take advantage of that. At the same time I want the builds performed off-site to download the files. To get this behaviour I defined two object stores:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode cmake"><code class="sourceCode cmake"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">set</span>(<span class="dv">ExternalData_OBJECT_STORES</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  <span class="dv">${CMAKE_BINARY_DIR}</span>/ExternalData/Objects</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  /mnt/shared/over/nfs/Objects</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>  )</span></code></pre></div>
<p>The module will search each of these for the required files and download only if they aren’t found. Downloaded files will be put into the first of the stores. Oh, and it’s <em>very</em> important that the first store is given with an absolute path!</p>
<p>The store on the shared storage looks something like this:</p>
<pre><code>/mnt/share/over/nfs/Objects
└── MD5
    ├── 94ed17f9b6c74a732fba7b243ab945ff
    └── a2036177b190fbee6e9e038b718f1c20</code></pre>
<p>I can then drop a file <code>MyInput.avi.md5</code> in my source tree with the md5 of the real file (e.g. <code>a2036177b190fbee6e9e038b718f1c20</code>) as the content. Once that is done I can follow the example found in the <a href="https://cmake.org/cmake/help/v3.5/module/ExternalData.html#introduction">introduction</a> of the reference documentation.</p>
<h3 id="curl-vs-sftp">curl vs sftp</h3>
<p>So far so good. This now works on-site, but for off-site use I need to fetch the needed files. The last section of the reference documentation is called <a href="https://cmake.org/cmake/help/v3.5/module/ExternalData.html#custom-fetch-scripts">Custom Fetch Scripts</a>. It mentions that files are normally downloaded using <code>file(DOWNLOAD)</code>. Neither there, nor in the <a href="https://cmake.org/cmake/help/v3.5/command/file.html#command:file">documentation for file</a> is there a mention of what is used under the hood to fetch the files. After asking on in <code>#cmake</code> I found out that it’s <a href="https://curl.haxx.se/">curl</a>. While <code>curl</code> does handle SFTP I didn’t get it to work with my <code>known_hosts</code> file, nor with my SSH agent (both from <a href="http://www.openssh.com/">OpenSSH</a>). On the other hand it was rather easy to configure <code>sftp</code> to fetch a file from the internet-facing SSH server we have. Now I just had to hook it into CMake somehow.</p>
<h3 id="custom-fetch-script">Custom fetch script</h3>
<p>As the section on “Custom Fetch Scripts” mention three things are needed:</p>
<ol type="1">
<li>Specify the script via the <code>ExternalDataCustomScript://</code> protocol.</li>
<li>Tell CMake where it can find the fetch script.</li>
<li>The fetch script itself.</li>
</ol>
<p>The first two steps are done by providing a URL template and pointing to the script via a special variable:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode cmake"><code class="sourceCode cmake"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">set</span>(<span class="dv">ExternalData_URL_TEMPLATES</span> <span class="st">&quot;ExternalDataCustomScript://sftp/mnt/shared/over/nfs/Objects/%(algo)/%(hash)&quot;</span>)</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a><span class="kw">set</span>(<span class="dv">ExternalData_CUSTOM_SCRIPT_sftp</span> <span class="dv">${CMAKE_SOURCE_DIR}</span>/cmake/FetchFromSftp.cmake)</span></code></pre></div>
<p>It took me a ridiculous amount of time to work out how to write a script that turns out to be rather short. This is an experience that seems to repeat itself when using CMake; it could say something about me, or something about CMake.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode cmake"><code class="sourceCode cmake"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="kw">get_filename_component</span>(FFS_ObjStoreDir <span class="dv">${ExternalData_CUSTOM_FILE}</span> <span class="ot">DIRECTORY</span>)</span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a><span class="kw">get_filename_component</span>(FFS_InputFilename <span class="dv">${ExternalData_CUSTOM_LOCATION}</span> <span class="ot">NAME</span>)</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a><span class="kw">get_filename_component</span>(FFS_OutputFilename <span class="dv">${ExternalData_CUSTOM_FILE}</span> <span class="ot">NAME</span>)</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a><span class="kw">execute_process</span>(<span class="ot">COMMAND</span> sftp sftp.company.com:/<span class="dv">${ExternalData_CUSTOM_LOCATION}</span></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a>  <span class="ot">RESULT_VARIABLE</span> FFS_SftpResult</span>
<span id="cb4-7"><a href="#cb4-7" aria-hidden="true"></a>  <span class="ot">OUTPUT_QUIET</span></span>
<span id="cb4-8"><a href="#cb4-8" aria-hidden="true"></a>  <span class="ot">ERROR_VARIABLE</span> FFS_SftpErr</span>
<span id="cb4-9"><a href="#cb4-9" aria-hidden="true"></a>  )</span>
<span id="cb4-10"><a href="#cb4-10" aria-hidden="true"></a></span>
<span id="cb4-11"><a href="#cb4-11" aria-hidden="true"></a><span class="kw">if</span>(FFS_SftpResult)</span>
<span id="cb4-12"><a href="#cb4-12" aria-hidden="true"></a>  <span class="kw">set</span>(<span class="dv">ExternalData_CUSTOM_ERROR</span> <span class="st">&quot;Failed to fetch from SFTP - </span><span class="dv">${</span>FFS_SftpErr<span class="dv">}</span><span class="st">&quot;</span>)</span>
<span id="cb4-13"><a href="#cb4-13" aria-hidden="true"></a><span class="kw">else</span>(FFS_SftpResult)</span>
<span id="cb4-14"><a href="#cb4-14" aria-hidden="true"></a>  <span class="kw">file</span>(<span class="ot">MAKE_DIRECTORY</span> <span class="dv">${</span>FFS_ObjStoreDir<span class="dv">}</span>)</span>
<span id="cb4-15"><a href="#cb4-15" aria-hidden="true"></a>  <span class="kw">file</span>(<span class="ot">RENAME</span> <span class="dv">${</span>FFS_InputFilename<span class="dv">}</span> <span class="dv">${</span>FFS_ObjStoreDir<span class="dv">}</span>/<span class="dv">${</span>FFS_OutputFilename<span class="dv">}</span>)</span>
<span id="cb4-16"><a href="#cb4-16" aria-hidden="true"></a><span class="kw">endif</span>(FFS_SftpResult)</span></code></pre></div>
<p>This script is run with <code>cmake -P</code> in the binary dir of the <code>CMakeLists.txt</code> where the test is defined, which means it’s oblivious about the project it’s part of. <code>PROJECT_BINARY_DIR</code> is empty and <code>CMAKE_BINARY_DIR</code> is the same as <code>CMAKE_CURRENT_BINARY_DIR</code>. This is the reason why the first store in <code>ExternalData_OBJECT_STORES</code> has to be an absolute path – it’s very difficult, if not impossible, to find the correct placement of the object store otherwise.</p>]]></summary>
</entry>
<entry>
    <title>Qt5+D-Bus+CMake, a complete example</title>
    <link href="http://magnus.therning.org/posts/2016-04-07-000-qt5%2Bdbus%2Bcmake--a-complete-example.html" />
    <id>http://magnus.therning.org/posts/2016-04-07-000-qt5%2Bdbus%2Bcmake--a-complete-example.html</id>
    <published>2016-04-07T00:00:00Z</published>
    <updated>2016-04-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Yesterday I started digging into Qt5 and D-Bus. I never found a complete example, so I put one together myself: <a href="https://gist.github.com/magthe/2cf7220655bd8bf431259cc7dee99f64" class="uri">https://gist.github.com/magthe/2cf7220655bd8bf431259cc7dee99f64</a>.</p>]]></summary>
</entry>
<entry>
    <title>Final version of JSON to sum type</title>
    <link href="http://magnus.therning.org/posts/2016-03-08-000-final-version-of-json-to-sum-type.html" />
    <id>http://magnus.therning.org/posts/2016-03-08-000-final-version-of-json-to-sum-type.html</id>
    <published>2016-03-08T00:00:00Z</published>
    <updated>2016-03-08T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>After some feedback on my <a href="/posts/2016-03-06-000-from-json-to-sum-type.html">previous post</a> I ended up with the following implementation.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">FromJSON</span> <span class="dt">V.VersionRange</span> <span class="kw">where</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  parseJSON <span class="ot">=</span> withObject <span class="st">&quot;VersionRange&quot;</span> go</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>    <span class="kw">where</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>      go o <span class="ot">=</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>        V.thisVersion <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;ThisVersion&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>        V.laterVersion <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;LaterVersion&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>        V.earlierVersion <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;EarlierVersion&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a>        <span class="dt">V.WildcardVersion</span> <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;WildcardVersion&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>        nullaryOp V.anyVersion <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;AnyVersion&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>        binaryOp V.unionVersionRanges <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;UnionVersionRanges&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>        binaryOp V.intersectVersionRanges <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;IntersectVersionRanges&quot;</span> <span class="op">&lt;|&gt;</span></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a>        <span class="dt">V.VersionRangeParens</span> <span class="op">&lt;$&gt;</span> o <span class="op">.:</span> <span class="st">&quot;VersionRangeParens&quot;</span></span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true"></a></span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true"></a><span class="ot">      nullaryOp ::</span> a <span class="ot">-&gt;</span> <span class="dt">Value</span> <span class="ot">-&gt;</span> a</span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true"></a>      nullaryOp <span class="ot">=</span> <span class="fu">const</span></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true"></a></span>
<span id="cb1-17"><a href="#cb1-17" aria-hidden="true"></a>      binaryOp f [a, b] <span class="ot">=</span> f a b</span></code></pre></div>
<p>Thanks David for your suggestions.</p>]]></summary>
</entry>
<entry>
    <title>From JSON to sum type</title>
    <link href="http://magnus.therning.org/posts/2016-03-06-000-from-json-to-sum-type.html" />
    <id>http://magnus.therning.org/posts/2016-03-06-000-from-json-to-sum-type.html</id>
    <published>2016-03-06T00:00:00Z</published>
    <updated>2016-03-06T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a while I’ve been planning to take full ownership of the JSON serialisation and parsing in <a href="https://github.com/magthe/cblrepo/">cblrepo</a>. The recent inclusion of instances of <code>ToJSON</code> and <code>FromJSON</code> for <a href="http://hackage.haskell.org/package/base-4.8.2.0/docs/Data-Version.html#t:Version"><code>Version</code></a> pushed me to take the first step by writing my own instances for all external types.</p>
<p>When doing this I noticed that all examples in the <a href="http://hackage.haskell.org/package/aeson">aeson</a> docs use a product</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">Person</span> <span class="ot">=</span> <span class="dt">Person</span> {</span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="ot">      name ::</span> <span class="dt">Text</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>    ,<span class="ot"> age  ::</span> <span class="dt">Int</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>    }</span></code></pre></div>
<p>whereas I had to deal with quite a few sums, e.g. <a href="http://hackage.haskell.org/package/Cabal-1.22.8.0/docs/Distribution-Version.html#t:VersionRange"><code>VersionRange</code></a>. At first I struggled a little with how to write an instance of <code>FromJSON</code>. After quite a bit of thinking I came up with the following, which I think is fairly nice, but I’d really like to hear what others think about it. Maybe I’ve just missed a much simpler way of implementing <code>parseJSON</code>:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">FromJSON</span> <span class="dt">V.VersionRange</span> <span class="kw">where</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  parseJSON <span class="ot">=</span> withObject <span class="st">&quot;VersionRange&quot;</span> go</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>    <span class="kw">where</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>      go o <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>        lv <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;LaterVersion&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> V.laterVersion</span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>        tv <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;ThisVersion&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> V.thisVersion</span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>        ev <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;EarlierVersion&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> V.earlierVersion</span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>        av <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;AnyVersion&quot;</span>) <span class="op">&gt;&gt;=</span> \ (<span class="ot">_::</span><span class="dt">Maybe</span> [(<span class="dt">Int</span>,<span class="dt">Int</span>)]) <span class="ot">-&gt;</span> <span class="fu">return</span> <span class="op">$</span> <span class="dt">Just</span> V.anyVersion</span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true"></a>        wv <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;WildcardVersion&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> <span class="dt">V.WildcardVersion</span></span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true"></a>        uvr <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;UnionVersionRanges&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> toUvr</span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true"></a>        ivr <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;IntersectVersionRanges&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> toIvr</span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true"></a>        vrp <span class="ot">&lt;-</span> (o <span class="op">.:?</span> <span class="st">&quot;VersionRangeParens&quot;</span>) <span class="op">&gt;&gt;=</span> <span class="fu">return</span> <span class="op">.</span> <span class="fu">fmap</span> <span class="dt">V.VersionRangeParens</span></span>
<span id="cb2-13"><a href="#cb2-13" aria-hidden="true"></a>        <span class="fu">maybe</span> (typeMismatch <span class="st">&quot;VersionRange&quot;</span> <span class="op">$</span> <span class="dt">Object</span> o)</span>
<span id="cb2-14"><a href="#cb2-14" aria-hidden="true"></a>          <span class="fu">return</span></span>
<span id="cb2-15"><a href="#cb2-15" aria-hidden="true"></a>          (lv <span class="op">&lt;|&gt;</span> tv <span class="op">&lt;|&gt;</span> ev <span class="op">&lt;|&gt;</span> uvr <span class="op">&lt;|&gt;</span> ivr <span class="op">&lt;|&gt;</span> wv <span class="op">&lt;|&gt;</span> vrp <span class="op">&lt;|&gt;</span> av)</span>
<span id="cb2-16"><a href="#cb2-16" aria-hidden="true"></a></span>
<span id="cb2-17"><a href="#cb2-17" aria-hidden="true"></a>      toUvr [v0, v1] <span class="ot">=</span> V.unionVersionRanges v0 v1</span>
<span id="cb2-18"><a href="#cb2-18" aria-hidden="true"></a>      toIvr [v0, v1] <span class="ot">=</span> V.intersectVersionRanges v0 v1</span></code></pre></div>
<p>Any and all comments and suggestions are more than welcome!</p>]]></summary>
</entry>
<entry>
    <title>Freer play with effects</title>
    <link href="http://magnus.therning.org/posts/2016-01-24-000-freer-play-with-effects.html" />
    <id>http://magnus.therning.org/posts/2016-01-24-000-freer-play-with-effects.html</id>
    <published>2016-01-24T00:00:00Z</published>
    <updated>2016-01-24T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In the previous posts on my <a href="/posts/2016-01-13-000-free-play--part-one.html">playing</a> <a href="/posts/2016-01-15-000-free-play--part-two.html">with</a> <a href="/posts/2016-01-17-000-free-play--part-three.html">free</a> I got stuck at combining APIs. I recalled reading a <a href="http://okmij.org/ftp/Haskell/extensible/exteff.pdf">paper on extensible effects</a> as an alternatve to monad transformers. I have to admit to not having finished the paper, and not quite understanding the part I did read. When looking it up again I found that the work had continued and that there is a <a href="http://okmij.org/ftp/Haskell/extensible/more.pdf">paper on more extensible effects</a>. (I got to it via <a href="http://okmij.org/ftp/Haskell/extensible/" class="uri">http://okmij.org/ftp/Haskell/extensible/</a>.)</p>
<p>A quick search of <a href="http://hackage.haskell.org/">Hackage</a> revealed the package <a href="http://hackage.haskell.org/package/extensible-effects">extensible-effects</a> with an implementation of the ideas, including the stuff in the latter paper. So, what would the examples from my previous posts look like using extensible effects?</p>
<h3 id="opening">Opening</h3>
<p>The examples require a few extensions and modules:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="ot">{-# LANGUAGE FlexibleContexts #-}</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="ot">{-# LANGUAGE GADTs #-}</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a><span class="ot">{-# LANGUAGE TypeOperators #-}</span></span></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Control.Eff</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Control.Eff.Lift</span></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Control.Eff.Operational</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Data.Typeable</span></span></code></pre></div>
<h3 id="just-an-api">Just an API</h3>
<p>This part was greatly helped by the fact that there is a <a href="http://hackage.haskell.org/package/extensible-effects-1.11.0.1/docs/Control-Eff-Operational-Example.html">example in extensible-effects</a>.</p>
<p>I start with defining the <code>SimpleFile</code> API using GADTs</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">SimpleFileAPI</span> a <span class="kw">where</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>  <span class="dt">LoadFile</span><span class="ot"> ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> <span class="dt">String</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  <span class="dt">SaveFile</span><span class="ot"> ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> ()</span></code></pre></div>
<p>The usage of the constructors need to be wrapped up in <code>singleton</code>. To remember that I create two convenience functions</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">loadFile ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r <span class="ot">=&gt;</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r <span class="dt">String</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>loadFile <span class="ot">=</span> singleton <span class="op">.</span> <span class="dt">LoadFile</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a></span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a><span class="ot">saveFile ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r <span class="ot">=&gt;</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>saveFile fp <span class="ot">=</span> singleton <span class="op">.</span> <span class="dt">SaveFile</span> fp</span></code></pre></div>
<p>For <code>withSimpleFile</code> I only have to modify the type</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">withSimpleFile ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r <span class="ot">=&gt;</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>withSimpleFile f fp <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>  d <span class="ot">&lt;-</span> loadFile fp</span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>  <span class="kw">let</span> result <span class="ot">=</span> f d</span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true"></a>  saveFile (fp <span class="op">++</span> <span class="st">&quot;_new&quot;</span>) result</span></code></pre></div>
<p>Now for the gut of it, the interpreter.</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a><span class="ot">runSimpleFile ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">Eff</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span> <span class="op">:&gt;</span> r) a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a>runSimpleFile <span class="ot">=</span> runProgram f</span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a><span class="ot">    f ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a>    f (<span class="dt">LoadFile</span> fp) <span class="ot">=</span> lift <span class="op">$</span> <span class="fu">readFile</span> fp</span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>    f (<span class="dt">SaveFile</span> fp s) <span class="ot">=</span> lift <span class="op">$</span> <span class="fu">writeFile</span> fp s</span></code></pre></div>
<p>Runnnig it is fairly simple after this</p>
<pre><code>&gt; :! cat test.txt 
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runLift $ runSimpleFile $ withSimpleFile (map toUpper) &quot;test.txt&quot;
&gt; :! cat test.txt_new 
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.</code></pre>
<p>Now, that was pretty easy. It looks almost exactly like when using <code>Free</code>, only without the <code>Functor</code> instance and rather more complicated types.</p>
<h3 id="combining-two-apis">Combining two APIs</h3>
<p>Now I get to the stuff that I didn’t manage to do using <code>Free</code>; combining two different APIs.</p>
<p>I start with defining another API. This one is truly a play example, sorry for that, but it doesn’t really matter. The type with convenience function looks like this</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">StdIoAPI</span> a <span class="kw">where</span></span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>  <span class="dt">WriteStrLn</span><span class="ot"> ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">StdIoAPI</span> ()</span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a></span>
<span id="cb8-4"><a href="#cb8-4" aria-hidden="true"></a><span class="ot">writeStrLn ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">StdIoAPI</span>) r <span class="ot">=&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</span>
<span id="cb8-5"><a href="#cb8-5" aria-hidden="true"></a>writeStrLn <span class="ot">=</span> singleton <span class="op">.</span> <span class="dt">WriteStrLn</span></span></code></pre></div>
<p>The interpreter then is straight forward</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a><span class="ot">runStdIo ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">Eff</span> (<span class="dt">Program</span> <span class="dt">StdIoAPI</span> <span class="op">:&gt;</span> r) a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb9-2"><a href="#cb9-2" aria-hidden="true"></a>runStdIo <span class="ot">=</span> runProgram f</span>
<span id="cb9-3"><a href="#cb9-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb9-4"><a href="#cb9-4" aria-hidden="true"></a><span class="ot">    f ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">StdIoAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb9-5"><a href="#cb9-5" aria-hidden="true"></a>    f (<span class="dt">WriteStrLn</span> s) <span class="ot">=</span> lift <span class="op">$</span> <span class="fu">putStrLn</span> s</span></code></pre></div>
<p>Now I just need a program that combines the two APIs</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a><span class="ot">verboseWithSimpleFile ::</span> (<span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">StdIoAPI</span>) r, <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r) <span class="ot">=&gt;</span></span>
<span id="cb10-2"><a href="#cb10-2" aria-hidden="true"></a>                         (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</span>
<span id="cb10-3"><a href="#cb10-3" aria-hidden="true"></a>verboseWithSimpleFile f fp <span class="ot">=</span> writeStrLn (<span class="st">&quot;verboseWithSimpleFile on &quot;</span> <span class="op">++</span> fp) <span class="op">&gt;&gt;</span> withSimpleFile f fp</span></code></pre></div>
<p>That type is surprisingly clear I find, albeit a bit on the long side. Running it is just a matter of combining <code>runStdIo</code> and <code>runSimpleFile</code>.</p>
<pre><code>&gt; :! cat test.txt 
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runLift $ runSimpleFile $ runStdIo $ verboseWithSimpleFile (map toUpper) &quot;test.txt&quot;
verboseWithSimpleFile on test.txt
&gt; :! cat test.txt_new 
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.</code></pre>
<p>Oh, and it doesn’t matter in what order the interpreters are run!</p>
<p>At this point I got really excited about <code>Eff</code> because now it’s obvious that I’ll be able to write the logging “decorator”, in fact it’s clear that it’ll be rather simple too.</p>
<h3 id="the-logging">The logging</h3>
<p>As before I start with a data type and a convenience function</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb12-1"><a href="#cb12-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">LoggerAPI</span> a <span class="kw">where</span></span>
<span id="cb12-2"><a href="#cb12-2" aria-hidden="true"></a>  <span class="dt">Log</span><span class="ot"> ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">LoggerAPI</span> ()</span>
<span id="cb12-3"><a href="#cb12-3" aria-hidden="true"></a></span>
<span id="cb12-4"><a href="#cb12-4" aria-hidden="true"></a><span class="ot">logStr ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">LoggerAPI</span>) r <span class="ot">=&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</span>
<span id="cb12-5"><a href="#cb12-5" aria-hidden="true"></a>logStr <span class="ot">=</span> singleton <span class="op">.</span> <span class="dt">Log</span></span></code></pre></div>
<p>For the decorating I can make use of the fact that APIs can be combined like I did above. That is, I don’t need to bother with any <em>coproduct</em> (<code>Sum</code>) or anything like that, I can simply just push in a call to <code>logStr</code> before each use of <code>SimpleFileAPI</code></p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb13-1"><a href="#cb13-1" aria-hidden="true"></a><span class="ot">logSimpleFileOp ::</span> (<span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r, <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">LoggerAPI</span>) r) <span class="ot">=&gt;</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb13-2"><a href="#cb13-2" aria-hidden="true"></a>logSimpleFileOp op<span class="op">@</span>(<span class="dt">LoadFile</span> fp) <span class="ot">=</span> logStr (<span class="st">&quot;LoadFile &quot;</span> <span class="op">++</span> fp) <span class="op">*&gt;</span> singleton op</span>
<span id="cb13-3"><a href="#cb13-3" aria-hidden="true"></a>logSimpleFileOp op<span class="op">@</span>(<span class="dt">SaveFile</span> fp _) <span class="ot">=</span> logStr (<span class="st">&quot;SaveFile &quot;</span> <span class="op">++</span> fp) <span class="op">*&gt;</span> singleton op</span></code></pre></div>
<p>Of course an interpreter is needed as well</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb14-1"><a href="#cb14-1" aria-hidden="true"></a><span class="ot">runLogger ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">Eff</span> (<span class="dt">Program</span> <span class="dt">LoggerAPI</span> <span class="op">:&gt;</span> r) a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb14-2"><a href="#cb14-2" aria-hidden="true"></a>runLogger <span class="ot">=</span> runProgram f</span>
<span id="cb14-3"><a href="#cb14-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb14-4"><a href="#cb14-4" aria-hidden="true"></a><span class="ot">    f ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">LoggerAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</span>
<span id="cb14-5"><a href="#cb14-5" aria-hidden="true"></a>    f (<span class="dt">Log</span> s) <span class="ot">=</span> lift <span class="op">$</span> <span class="fu">putStrLn</span> s</span></code></pre></div>
<p>Running is, once again, a matter of stacking interpreters</p>
<pre><code>&gt; :! cat test.txt
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runLift $ runLogger $ runSimpleFile $ runProgram logSimpleFileOp $ withSimpleFile (map toUpper) &quot;test.txt&quot;
LoadFile test.txt
SaveFile test.txt_new
&gt; :! cat test.txt_new 
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.</code></pre>
<h3 id="closing-thoughts">Closing thoughts</h3>
<p>With <code>Eff</code> I’ve pretty much arrived where I wanted, I can</p>
<ul>
<li>define APIs of operations in a simple way (simpler than when using <code>Free</code> even).</li>
<li>write a definitional interpreter for the operations.</li>
<li>combine two different APIs in the same function.</li>
<li>translate from one API to another (or even to a set of other APIs).</li>
</ul>
<p>On top, I can do this without having to write a ridiculous amount of code.</p>
<p>I’m sure there are drawbacks as well. There’s a mention of some of them in the paper. However, for my typical uses of Haskell I haven’t read anything that would be a deal breaker.</p>]]></summary>
</entry>
<entry>
    <title>Free play, part three</title>
    <link href="http://magnus.therning.org/posts/2016-01-17-000-free-play--part-three.html" />
    <id>http://magnus.therning.org/posts/2016-01-17-000-free-play--part-three.html</id>
    <published>2016-01-17T00:00:00Z</published>
    <updated>2016-01-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The code in the <a href="/posts/2016-01-15-000-free-play--part-two.html">previous post</a> can do with a bit of cleaning up. I start with introducing a type class for an API that can be run</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">class</span> <span class="dt">RunnableF</span> f <span class="kw">where</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a><span class="ot">  runOp ::</span> f a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span></code></pre></div>
<p>and a function that actually runs iteFile</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="ot">runF ::</span> (<span class="dt">RunnableF</span> o) <span class="ot">=&gt;</span> <span class="dt">Free</span> o a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>runF <span class="ot">=</span> foldFree runOp</span></code></pre></div>
<p>A coproduct (<code>Sum</code>) of two runnable APIs can itself be runnable</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="kw">instance</span> (<span class="dt">RunnableF</span> f, <span class="dt">RunnableF</span> g) <span class="ot">=&gt;</span> <span class="dt">RunnableF</span> (<span class="dt">Sum</span> f g) <span class="kw">where</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>  runOp (<span class="dt">InL</span> op) <span class="ot">=</span> runOp op</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>  runOp (<span class="dt">InR</span> op) <span class="ot">=</span> runOp op</span></code></pre></div>
<p>After this all I have to do is to move the guts of <code>runSimpleF</code> and <code>runLogging</code> into implementations for <code>SimpleFil</code> and <code>LogF</code> respectively</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">RunnableF</span> <span class="dt">SimpleFileF</span> <span class="kw">where</span></span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>  runOp (<span class="dt">LoadFile</span> fp f&#39;) <span class="ot">=</span> liftM f&#39; <span class="op">$</span> <span class="fu">readFile</span> fp</span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  runOp (<span class="dt">SaveFile</span> fp d r) <span class="ot">=</span> <span class="fu">writeFile</span> fp d <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a></span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">RunnableF</span> <span class="dt">LogF</span> <span class="kw">where</span></span>
<span id="cb4-6"><a href="#cb4-6" aria-hidden="true"></a>  runOp (<span class="dt">Log</span> s a)<span class="ot">=</span> <span class="fu">putStrLn</span> s <span class="op">&gt;&gt;</span> <span class="fu">return</span> a</span></code></pre></div>
<p>The rest is left as is. Running now looks like this</p>
<pre><code>&gt; :! cat test.txt
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runF $ withSimpleFile (map toUpper) &quot;test.txt&quot;
&gt; :! cat test.txt_new
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.
&gt; runF $ foldFree loggingSimpleFileI $ withSimpleFile (map toLower) &quot;test.txt&quot;
** load file test.txt
** save file test.txt_new
&gt; :! cat test.txt_new
lorem ipsum dolor sit amet, consectetur adipiscing elit. donec a diam lectus.
sed sit amet ipsum mauris. maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.</code></pre>
<h3 id="what-else">What else?</h3>
<p>With these changes it becomes slightly esier to create new basic API types. but there are still a few things I think could be useful:</p>
<ul>
<li>More generic decorators. <code>logSimpleFileI</code> is tied to decorating only <code>SimpleFileF</code>. I’m fairly sure this could be dealt with by a type class <code>LoggableF</code> and have APIs implement it. I also think there’s a rather natural implementation of <code>LoggableF</code> for <code>Sum f g</code>.</li>
<li>Combining decorators (and other interpreters). I’m guessing this is what John refers to as “generalizing interpreters.”</li>
<li>Combining APIs. I’d prefer making small APIs of related operations, but then that also means I need a nice way of combining APIs. I had a go at using <a href="http://hackage.haskell.org/package/free-4.12.1/docs/Control-Monad-Trans-Free.html"><code>FreeT</code></a> but simply gave up on bending the types to my will. In any case I’m not completely sure that a stack of various <code>FreeT</code> is a good direction to go.</li>
</ul>]]></summary>
</entry>
<entry>
    <title>Free play, part two</title>
    <link href="http://magnus.therning.org/posts/2016-01-15-000-free-play--part-two.html" />
    <id>http://magnus.therning.org/posts/2016-01-15-000-free-play--part-two.html</id>
    <published>2016-01-15T00:00:00Z</published>
    <updated>2016-01-15T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>This post on <code>Free</code> play builds on my <a href="/posts/2016-01-13-000-free-play--part-one.html">previous one</a>. It was <a href="http://degoes.net/articles/modern-fp">John A De Goes’ post</a> that finally got me started on playing with <code>Free</code> and his post contian the following:</p>
<div class="line-block">Moreover, not only do algebras compose (that is, if you have algebras f and g, you can compose them into a composite algebra Coproduct f g for some suitable definition of Coproduct), but interpreters also compose — both horizontally and vertically.</div>
<p>And a little bit later he offers a few types and some details, but not <em>all</em> details. How could something like that look in Haskell?</p>
<p>Starting from the code in the previous post I first created a new type for a logging action</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">LogF</span> a <span class="ot">=</span> <span class="dt">Log</span> <span class="dt">String</span> a</span></code></pre></div>
<p>This type has to be a <code>Functor</code></p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">Functor</span> <span class="dt">LogF</span> <span class="kw">where</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  <span class="fu">fmap</span> f (<span class="dt">Log</span> s a) <span class="ot">=</span> <span class="dt">Log</span> s (f a)</span></code></pre></div>
<p>The logging should basically decorate a <code>SimpleFileF a</code> action, so I need a function to map one into a <code>Free LogF a</code></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">logSimpleFileI ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">LogF</span> ()</span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>logSimpleFileI (<span class="dt">LoadFile</span> fp _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** load file &quot;</span> <span class="op">++</span> fp) ()</span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a>logSimpleFileI (<span class="dt">SaveFile</span> fp _ _) <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">Log</span> (<span class="st">&quot;** save file &quot;</span> <span class="op">++</span> fp) ()</span></code></pre></div>
<p>Now I needed a <code>Coproduct</code> for <code>Functor</code>. Searching <a href="http://hackage.haskell.org/">hackage</a> only offered up one for <code>Monoid</code> (in <a href="http://hackage.haskell.org/package/monoid-extras">monoid-extras</a>) so I first translated one from PureScript, but later I got some help via Twitter and was pointed to two in Haskell, <a href="http://hackage.haskell.org/package/comonad-4.2.7.2/docs/Data-Functor-Coproduct.html"><code>Data.Functor.Coproduct</code></a> from comonad and <a href="http://hackage.haskell.org/package/transformers-0.5.0.0/docs/Data-Functor-Sum.html"><code>Data.Functor.Sum</code></a> from transformers, I decided on the one from transformers because of its shorter name and the fact that it was very different from my translated-from-PureScript version.</p>
<p>Following John’s example I use <code>Applicative</code> to combine the logging with the file action</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">loggingSimpleFileI ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">Free</span> (<span class="dt">Sum</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>) a</span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>loggingSimpleFileI op <span class="ot">=</span> toLeft (logSimpleFileI op) <span class="op">*&gt;</span> toRight (liftF op)</span></code></pre></div>
<p>with <code>toLeft</code> and <code>toRight</code> defined like this</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">toLeft ::</span> (<span class="dt">Functor</span> f, <span class="dt">Functor</span> g) <span class="ot">=&gt;</span> <span class="dt">Free</span> f a <span class="ot">-&gt;</span> <span class="dt">Free</span> (<span class="dt">Sum</span> f g) a</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>toLeft <span class="ot">=</span> hoistFree <span class="dt">InL</span></span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a><span class="ot">toRight ::</span> (<span class="dt">Functor</span> f, <span class="dt">Functor</span> g) <span class="ot">=&gt;</span> <span class="dt">Free</span> g a <span class="ot">-&gt;</span> <span class="dt">Free</span> (<span class="dt">Sum</span> f g) a</span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>toRight <span class="ot">=</span> hoistFree <span class="dt">InR</span></span></code></pre></div>
<p>With all of this in place I can decorate the program from the last post like this <code>foldFree loggingSimpleFileI (withSimpleF toUpper "FreePlay.hs")</code>. What’s left is a way to run it. The function for that is a natural extension of <code>runsimpleFile</code></p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a></span>
<span id="cb6-2"><a href="#cb6-2" aria-hidden="true"></a><span class="ot">runLogging ::</span> <span class="dt">Free</span> (<span class="dt">Sum</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>) a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb6-3"><a href="#cb6-3" aria-hidden="true"></a>runLogging <span class="ot">=</span> foldFree f</span>
<span id="cb6-4"><a href="#cb6-4" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb6-5"><a href="#cb6-5" aria-hidden="true"></a><span class="ot">    f ::</span> (<span class="dt">Sum</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>) a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb6-6"><a href="#cb6-6" aria-hidden="true"></a>    f (<span class="dt">InL</span> op) <span class="ot">=</span> g op</span>
<span id="cb6-7"><a href="#cb6-7" aria-hidden="true"></a>    f (<span class="dt">InR</span> op) <span class="ot">=</span> h op</span>
<span id="cb6-8"><a href="#cb6-8" aria-hidden="true"></a></span>
<span id="cb6-9"><a href="#cb6-9" aria-hidden="true"></a><span class="ot">    g ::</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb6-10"><a href="#cb6-10" aria-hidden="true"></a>    g (<span class="dt">Log</span> s a)<span class="ot">=</span> <span class="fu">putStrLn</span> s <span class="op">&gt;&gt;</span> <span class="fu">return</span> a</span>
<span id="cb6-11"><a href="#cb6-11" aria-hidden="true"></a></span>
<span id="cb6-12"><a href="#cb6-12" aria-hidden="true"></a><span class="ot">    h ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb6-13"><a href="#cb6-13" aria-hidden="true"></a>    h (<span class="dt">LoadFile</span> fp f&#39;) <span class="ot">=</span> liftM f&#39; <span class="op">$</span> <span class="fu">readFile</span> fp</span>
<span id="cb6-14"><a href="#cb6-14" aria-hidden="true"></a>    h (<span class="dt">SaveFile</span> fp d r) <span class="ot">=</span> <span class="fu">writeFile</span> fp d <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span></code></pre></div>
<p>Running the decorated program</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a>runLogging <span class="op">$</span> foldFree loggingSimpleFileI (withSimpleF <span class="fu">toUpper</span> <span class="st">&quot;FreePlay.hs&quot;</span>)</span></code></pre></div>
<p>does indeed result in the expected output</p>
<pre><code>** load file FreePlay.hs
** save file FreePlay.hs_new</code></pre>
<p>and the file <code>FreePlay.hs_new</code> contains only uppercase letters.</p>
<h3 id="my-thoughts">My thoughts</h3>
<p>This ability to decorate actions (or compose algebras) is very nice. There’s probably value in the “multiple interpreters for a program” in some domains, but I have a feeling that it could be a hard sell. However, combining it with this kind of composability adds quite a bit of value in my opinion. I must say I don’t think <em>my</em> code scales very well for adding more decorators (composing more algebras), but hopefully some type wizard can show me a way to improve on that.</p>
<p>The code above is rather crude though, and I have another version that cleans it up quite a bit. That’ll be in the next post.</p>]]></summary>
</entry>
<entry>
    <title>Free play, part one</title>
    <link href="http://magnus.therning.org/posts/2016-01-13-000-free-play--part-one.html" />
    <id>http://magnus.therning.org/posts/2016-01-13-000-free-play--part-one.html</id>
    <published>2016-01-13T00:00:00Z</published>
    <updated>2016-01-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When I read John A De Goes post <a href="http://degoes.net/articles/modern-fp">A Modern Architecture for FP</a> I found it to be a bit too vague for me, but the topic was just interesting enough to finally push me to play a little with <em>free monads</em>. It’s not the first post I’ve read on the topic, there have <a href="http://www.haskellforall.com/2012/06/you-could-have-invented-free-monads.html">been</a> <a href="https://www.schoolofhaskell.com/user/dolio/many-roads-to-free-monads">many</a> <a href="http://okmij.org/ftp/Computation/free-monad.html">before</a>. None have quite managed to push me into actually doing something though!</p>
<h3 id="a-file-api">A file API</h3>
<p>To make it concrete but still short enough to not bore readers I came up with a small API for working with files:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">data</span> <span class="dt">SimpleFileF</span> a <span class="ot">=</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>  <span class="dt">LoadFile</span> <span class="dt">FilePath</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> a)</span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a>  <span class="op">|</span> <span class="dt">SaveFile</span> <span class="dt">FilePath</span> <span class="dt">String</span> a</span></code></pre></div>
<p>The free monad wraps a <em>functor</em>, so here’s an implementation of that</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="kw">instance</span> <span class="dt">Functor</span> <span class="dt">SimpleFileF</span> <span class="kw">where</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a>  <span class="fu">fmap</span> f (<span class="dt">LoadFile</span> fp f&#39;)<span class="ot">=</span> <span class="dt">LoadFile</span> fp (f <span class="op">.</span> f&#39;)</span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a>  <span class="fu">fmap</span> f (<span class="dt">SaveFile</span> fp d a) <span class="ot">=</span> <span class="dt">SaveFile</span> fp d (f a)</span></code></pre></div>
<p>Now for some convenient functions to work with the API type</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a><span class="ot">loadFile ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> <span class="dt">String</span></span>
<span id="cb3-2"><a href="#cb3-2" aria-hidden="true"></a>loadFile fp <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">LoadFile</span> fp <span class="fu">id</span></span>
<span id="cb3-3"><a href="#cb3-3" aria-hidden="true"></a></span>
<span id="cb3-4"><a href="#cb3-4" aria-hidden="true"></a><span class="ot">saveFile ::</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> ()</span>
<span id="cb3-5"><a href="#cb3-5" aria-hidden="true"></a>saveFile fp d <span class="ot">=</span> liftF <span class="op">$</span> <span class="dt">SaveFile</span> fp d ()</span></code></pre></div>
<p>With this in place I can write a somewhat more complex one</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a><span class="ot">withSimpleFile ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">FilePath</span> <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> ()</span>
<span id="cb4-2"><a href="#cb4-2" aria-hidden="true"></a>withSimpleFile f fp <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb4-3"><a href="#cb4-3" aria-hidden="true"></a>  d <span class="ot">&lt;-</span> loadFile fp</span>
<span id="cb4-4"><a href="#cb4-4" aria-hidden="true"></a>  <span class="kw">let</span> result <span class="ot">=</span> f d</span>
<span id="cb4-5"><a href="#cb4-5" aria-hidden="true"></a>  saveFile (fp <span class="op">++</span> <span class="st">&quot;_new&quot;</span>) result</span></code></pre></div>
<p>Now I need a way to run programs using the API</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a><span class="ot">runSimpleFile ::</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</span>
<span id="cb5-2"><a href="#cb5-2" aria-hidden="true"></a>runSimpleFile <span class="ot">=</span> foldFree f</span>
<span id="cb5-3"><a href="#cb5-3" aria-hidden="true"></a>  <span class="kw">where</span></span>
<span id="cb5-4"><a href="#cb5-4" aria-hidden="true"></a>    f (<span class="dt">LoadFile</span> fp f&#39;) <span class="ot">=</span> liftM f&#39; <span class="op">$</span> <span class="fu">readFile</span> fp</span>
<span id="cb5-5"><a href="#cb5-5" aria-hidden="true"></a>    f (<span class="dt">SaveFile</span> fp d r) <span class="ot">=</span> <span class="fu">writeFile</span> fp d <span class="op">&gt;&gt;</span> <span class="fu">return</span> r</span></code></pre></div>
<p>If this code was save in the file <code>FreePlay.hs</code> I can now convert it all to upper case by using</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>runSimpleFile <span class="op">$</span> withSimpleFile <span class="fu">toUpper</span> <span class="st">&quot;FreePlay.hs&quot;</span></span></code></pre></div>
<p>which of course will create the file <code>FreePlay.hs_new</code>.</p>
<h3 id="what-have-i-bought-so-far">What have I bought so far?</h3>
<p>Well, not much really.</p>
<p>So far it’s not much more than a rather roundabout way to limit what IO actions are available. In other words it’s not much more than what can be done by creating a limited IO, as in my earlier posts <a href="/posts/2009-09-07-738-wrapping-io-part-1.html">here</a> and <a href="/posts/2009-09-07-742-wrapping-io-part-2.html">here</a>.</p>
<p>Of course it would be possible to write another <em>run</em> function, e.g. one that doesn’t actually perform the file actions but just says what it <em>would</em> do. The nice thing though is, to use that same metaphor as John does, that I can add a layer to the onion. In other words, I can decorate each use of my <code>SimpleFileF</code> API with some other API. I think it was John’s description of this layering that pushed me to play with <code>Free</code>.</p>]]></summary>
</entry>
<entry>
    <title>Trick for pre-processing source in CMake</title>
    <link href="http://magnus.therning.org/posts/2015-11-26-001-trick-for-pre-processing-source-in-cmake.html" />
    <id>http://magnus.therning.org/posts/2015-11-26-001-trick-for-pre-processing-source-in-cmake.html</id>
    <published>2015-11-26T00:00:00Z</published>
    <updated>2015-11-26T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When using <a href="https://cmake.org/cmake/help/v3.4/prop_tgt/LANG_COMPILER_LAUNCHER.html"><code>&lt;LANG&gt;_COMPILER_LAUCHER</code></a> or <a href="https://cmake.org/cmake/help/v3.3/prop_gbl/RULE_LAUNCH_COMPILE.html"><code>RULE_LAUNCH_COMPILER</code></a> the following is a nice little pattern to deal with the arguments.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="co">#! /usr/bin/python3</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a><span class="im">import</span> argparse</span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a><span class="im">import</span> subprocess</span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a><span class="im">import</span> sys</span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a><span class="co"># create an argumentparser for the stuff the launcher script needs, some</span></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a><span class="co"># specific to it and some of the compiler arguments</span></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a>parser <span class="op">=</span> argparse.ArgumentParser(description<span class="op">=</span><span class="st">&#39;Compiler launcher for C.&#39;</span>)</span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>parser.add_argument(<span class="st">&#39;--flag-for-wrapper&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>parser.add_argument(<span class="st">&#39;-I&#39;</span>, dest<span class="op">=</span><span class="st">&#39;includes&#39;</span>, action<span class="op">=</span><span class="st">&#39;append&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>, default<span class="op">=</span>[])</span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a>parser.add_argument(<span class="st">&#39;-D&#39;</span>, dest<span class="op">=</span><span class="st">&#39;defines&#39;</span>, action<span class="op">=</span><span class="st">&#39;append&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>, default<span class="op">=</span>[])</span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true"></a>parser.add_argument(<span class="st">&#39;-o&#39;</span>, dest<span class="op">=</span><span class="st">&#39;output&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true"></a>parser.add_argument(<span class="st">&#39;-c&#39;</span>, dest<span class="op">=</span><span class="st">&#39;input&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true"></a></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true"></a>args, rest <span class="op">=</span> parser.parse_known_args(sys.argv)</span>
<span id="cb1-17"><a href="#cb1-17" aria-hidden="true"></a></span>
<span id="cb1-18"><a href="#cb1-18" aria-hidden="true"></a><span class="co"># do stuff with the source, e.g. pass it through the C pre-processor (using the</span></span>
<span id="cb1-19"><a href="#cb1-19" aria-hidden="true"></a><span class="co"># arguments picked out above) and then process the output</span></span>
<span id="cb1-20"><a href="#cb1-20" aria-hidden="true"></a></span>
<span id="cb1-21"><a href="#cb1-21" aria-hidden="true"></a><span class="co"># build another argument parser to prepare the call to the compiler</span></span>
<span id="cb1-22"><a href="#cb1-22" aria-hidden="true"></a>cc_parser <span class="op">=</span> argparse.ArgumentParser(description<span class="op">=</span><span class="st">&#39;Parser for removing launcher-specific arguments.&#39;</span>)</span>
<span id="cb1-23"><a href="#cb1-23" aria-hidden="true"></a>cc_parser.add_argument(<span class="st">&#39;--flag-for-wrapper&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</span>
<span id="cb1-24"><a href="#cb1-24" aria-hidden="true"></a>cc_cmd <span class="op">=</span> cc_parser.parse_known_args(sys.argv)[<span class="dv">1</span>][<span class="dv">1</span>:]</span>
<span id="cb1-25"><a href="#cb1-25" aria-hidden="true"></a></span>
<span id="cb1-26"><a href="#cb1-26" aria-hidden="true"></a>subprocess.call(cc_cmd)</span></code></pre></div>
<p>That is, first create a parser for launcher-specific arguments and the compiler arguments<a href="#fn1" class="footnote-ref" id="fnref1" role="doc-noteref"><sup>1</sup></a> that are of interest to the launcher. Then perform the magic of the launcher. Finally, create another parser and use it to remove the launcher-specific arguments and use the remainder to perform the compilation.</p>
<section class="footnotes" role="doc-endnotes">
<hr />
<ol>
<li id="fn1" role="doc-endnote"><p>Here I’m relying on CMake always putting <em>-c</em> just before the source file, which seems to hold most of the time.<a href="#fnref1" class="footnote-back" role="doc-backlink">↩︎</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Elm 0.16 with stack</title>
    <link href="http://magnus.therning.org/posts/2015-11-23-000-elm-0-16-with-stack.html" />
    <id>http://magnus.therning.org/posts/2015-11-23-000-elm-0-16-with-stack.html</id>
    <published>2015-11-23T00:00:00Z</published>
    <updated>2015-11-23T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I just noticed that there’s been a new release of the <a href="http://elm-lang.org">elm tools</a>. The previous release was fairly easy to <a href="/posts/2015-10-13-000-building-elm-with-stack.html">build using stack</a>, but this release made it a little bit easier still.</p>
<p>The script for cloning should be modified to look like this:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="co">#! /usr/bin/zsh</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a><span class="va">repos=(</span><span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-compiler.git&quot;</span></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a>       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-package.git&quot;</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a>       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-make.git&quot;</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a>       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-reactor.git&quot;</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a>       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-repl.git&quot;</span></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a>      )</span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a><span class="kw">for</span> <span class="ex">r</span> in <span class="va">${repos[@]}</span><span class="kw">;</span> <span class="kw">do</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a>    <span class="bu">eval</span> <span class="va">${r}</span></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a><span class="kw">done</span></span></code></pre></div>
<p>When creating the inital <code>stack.yaml</code> tell <em>stack</em> to use release 7.10.2 of <em>ghc</em>:</p>
<pre><code>% stack init --resolver ghc-7.10.2</code></pre>
<p>That makes it possible to use the installed version of <em>ghc</em>. It’s also unecessary to provide any flag for <em>aeson</em>, which means there’s no need for any manual changed to <code>stack.yaml</code> prior to running (add <code>--install-ghc</code> if you don’t have it on your system already)</p>
<pre><code>% stack solver --modify-stack-yaml</code></pre>
<p>Now just build and install as before:</p>
<pre><code>% stack install elm-{compiler,make,package,repl}
% stack install elm-reactor</code></pre>
<p>That’s it!</p>]]></summary>
</entry>
<entry>
    <title>How can I unit test failure cases?</title>
    <link href="http://magnus.therning.org/posts/2015-11-13-000-unit-testing-failure-cases.html" />
    <id>http://magnus.therning.org/posts/2015-11-13-000-unit-testing-failure-cases.html</id>
    <published>2015-11-13T00:00:00Z</published>
    <updated>2015-11-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When writing unit tests, especially when using mock objects, there’s always a risk of falling into the trap of writing tests for the implementation rather than for the API. In my experience the most obvious indication of that is that any refactoring of the code requires major rework of the tests.</p>
<p>Right now I’m sitting with the task of writing tests for a function that promises to return <code>NULL</code> on failure. The function in question allocates memory up to three times, opens two message queues, sends a message on one and expects a reponse on the other. If all goes well it returns the pointer to one of the memory areas allocated. And if it fails, for whatever reason, it returns <code>NULL</code>.</p>
<p><em>Is it even possible to write a set of tests for it without testing the specific implementation?</em></p>
<p>I know some of the calls can be re-arranged, e.g. moving all memory allocation to the start of the function, without affecting the caller of the function. However, if I use mock objects to simulate failure in the external libraries and system calls I’d be forced to modify the tests on each such re-arranging of calls. In other words I’m testing the implementation!</p>
<p>Should I think differently about testing failure cases?</p>
<p>Should the API be changed in some way to make unit tests a better fit for testing failures?</p>]]></summary>
</entry>
<entry>
    <title>Docker container with ip address on local network, p2</title>
    <link href="http://magnus.therning.org/posts/2015-11-01-000-docker-container-with-ip-address-on-local-network--p2.html" />
    <id>http://magnus.therning.org/posts/2015-11-01-000-docker-container-with-ip-address-on-local-network--p2.html</id>
    <published>2015-11-01T00:00:00Z</published>
    <updated>2015-11-01T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In my <a href="/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html">latest post</a> I described described a way to give a docker container an address on the local network. That’s a general way of achieving it, but it’s also rather complicated. If you know exactly what ports need to be exposed there is a simpler way.</p>
<h2 id="add-a-second-ip-to-the-system-interface">Add a second IP to the system interface</h2>
<p>There’s a good description on how to give an interface multiple in Debian at the <a href="https://wiki.debian.org/NetworkConfiguration#Multiple_IP_addresses_on_one_Interface">Debian Wiki</a>. The completely manual way is</p>
<pre><code>ip addr add 192.168.1.199/24 dev eth0 label eth0:0</code></pre>
<p>The configuration in Debian is</p>
<pre><code>auto eth0:0
iface eth0:0 inet static
  address 192.168.1.199
  netmask 255.255.255.0</code></pre>
<p>If you’re using Systemd it seems to be enough to just add extra <code>Address=...</code> statements to the <code>.network</code> file:</p>
<pre><code>[Match]
Name=eth0

[Network]
Address=192.168.1.2/24
Address=192.168.1.199/24
Gateway=192.168.1.1</code></pre>
<h2 id="bind-ports-of-the-container-to-a-specific-ip">Bind ports of the container to a specific IP</h2>
<p>Using the image from my <a href="/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html">latest post</a> it’s just to run it using the <code>--publish</code> option:</p>
<pre><code>docker --rm --interactive --tty --name=echo --publish=192.168.1.199:2048:2048 nw /home/myusre/echosrv</code></pre>
<p>Just like the last time it’s easy to verify everything’s working using <code>netcat</code>.</p>]]></summary>
</entry>
<entry>
    <title>Docker container with ip address on local network</title>
    <link href="http://magnus.therning.org/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html" />
    <id>http://magnus.therning.org/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html</id>
    <published>2015-10-22T00:00:00Z</published>
    <updated>2015-10-22T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Shortly after I started my new job in August I put an instance of gitlab on a server and started promoting using it over the git-repos-on-an-NFS-share that had been used thus far. Thanks to the <a href="https://hub.docker.com/r/gitlab/gitlab-ce/">Docker image of Gitlab CE</a> it only took about 5 minutes to have a running instance – setting up authentication via LDAP only took slightly longer. So far I’ve been running it on non-standard ports, just like the <a href="http://doc.gitlab.com/omnibus/docker/">guide</a> suggests, but I’ve gotten to the point where I’d like to make it a bit more official. That means I’d like to get it off non-standard ports, but I do really like running it in Docker. In short, I need to give the Docker image an ip address on the local network.</p>
<p>Unfortunately Docker doesn’t make it easy to do that, but I found an article on <a href="http://blog.oddbit.com/2014/08/11/four-ways-to-connect-a-docker/">four ways to connect a docker container to a local network</a>. What follows is my take on the instruction for how to use a <code>macvlan</code> device to achieve it.</p>
<h2 id="an-image-for-testing">An image for testing</h2>
<p>First I looked around for a simple echo server to run in my testing image:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><span id="cb1-1"><a href="#cb1-1" aria-hidden="true"></a><span class="kw">module</span> <span class="dt">Main</span></span>
<span id="cb1-2"><a href="#cb1-2" aria-hidden="true"></a>       <span class="kw">where</span></span>
<span id="cb1-3"><a href="#cb1-3" aria-hidden="true"></a></span>
<span id="cb1-4"><a href="#cb1-4" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Control.Concurrent</span></span>
<span id="cb1-5"><a href="#cb1-5" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Control.Monad</span></span>
<span id="cb1-6"><a href="#cb1-6" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">Network.Socket</span></span>
<span id="cb1-7"><a href="#cb1-7" aria-hidden="true"></a><span class="kw">import</span> <span class="dt">System.IO</span></span>
<span id="cb1-8"><a href="#cb1-8" aria-hidden="true"></a></span>
<span id="cb1-9"><a href="#cb1-9" aria-hidden="true"></a><span class="ot">srvPort ::</span> <span class="dt">PortNumber</span></span>
<span id="cb1-10"><a href="#cb1-10" aria-hidden="true"></a>srvPort <span class="ot">=</span> <span class="dv">2048</span></span>
<span id="cb1-11"><a href="#cb1-11" aria-hidden="true"></a></span>
<span id="cb1-12"><a href="#cb1-12" aria-hidden="true"></a><span class="ot">main ::</span> <span class="dt">IO</span> ()</span>
<span id="cb1-13"><a href="#cb1-13" aria-hidden="true"></a>main <span class="ot">=</span> withSocketsDo <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-14"><a href="#cb1-14" aria-hidden="true"></a>  newSocket <span class="ot">&lt;-</span> socket <span class="dt">AF_INET</span> <span class="dt">Stream</span> defaultProtocol</span>
<span id="cb1-15"><a href="#cb1-15" aria-hidden="true"></a>  setSocketOption newSocket <span class="dt">ReuseAddr</span> <span class="dv">1</span></span>
<span id="cb1-16"><a href="#cb1-16" aria-hidden="true"></a>  bindSocket newSocket <span class="op">$</span> <span class="dt">SockAddrInet</span> srvPort iNADDR_ANY</span>
<span id="cb1-17"><a href="#cb1-17" aria-hidden="true"></a>  listen newSocket <span class="dv">2</span></span>
<span id="cb1-18"><a href="#cb1-18" aria-hidden="true"></a>  runServer echo newSocket</span>
<span id="cb1-19"><a href="#cb1-19" aria-hidden="true"></a></span>
<span id="cb1-20"><a href="#cb1-20" aria-hidden="true"></a><span class="ot">runServer ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">Socket</span> <span class="ot">-&gt;</span> <span class="dt">IO</span>()</span>
<span id="cb1-21"><a href="#cb1-21" aria-hidden="true"></a>runServer f s <span class="ot">=</span> forever <span class="op">$</span> <span class="kw">do</span></span>
<span id="cb1-22"><a href="#cb1-22" aria-hidden="true"></a>  (usableSocket,_) <span class="ot">&lt;-</span> accept s</span>
<span id="cb1-23"><a href="#cb1-23" aria-hidden="true"></a>  forkIO <span class="op">$</span> interactWithSocket f usableSocket</span>
<span id="cb1-24"><a href="#cb1-24" aria-hidden="true"></a></span>
<span id="cb1-25"><a href="#cb1-25" aria-hidden="true"></a><span class="ot">interactWithSocket ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">Socket</span> <span class="ot">-&gt;</span> <span class="dt">IO</span>()</span>
<span id="cb1-26"><a href="#cb1-26" aria-hidden="true"></a>interactWithSocket f s <span class="ot">=</span> <span class="kw">do</span></span>
<span id="cb1-27"><a href="#cb1-27" aria-hidden="true"></a>  handle <span class="ot">&lt;-</span> socketToHandle s <span class="dt">ReadWriteMode</span></span>
<span id="cb1-28"><a href="#cb1-28" aria-hidden="true"></a>  forever <span class="op">$</span> f <span class="op">&lt;$&gt;</span> hGetLine handle <span class="op">&gt;&gt;=</span> hPutStrLn handle</span>
<span id="cb1-29"><a href="#cb1-29" aria-hidden="true"></a></span>
<span id="cb1-30"><a href="#cb1-30" aria-hidden="true"></a><span class="ot">echo ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span></span>
<span id="cb1-31"><a href="#cb1-31" aria-hidden="true"></a>echo <span class="ot">=</span> (<span class="st">&quot;R: &quot;</span> <span class="op">++</span>)</span></code></pre></div>
<p>Run it and test it by pointing <em>netcat</em> to port 2048.</p>
<p>Once that was built I put together a <em>Dockerfile</em> that uses Debian Jessie as a base and copies in the echo server (I based it on another image):</p>
<pre><code>FROM debian:8.2
MAINTAINER Magnus Therning &lt;magnus@therning.org&gt;

RUN TERM=vt220 apt-get update &amp;&amp; \
    TERM=vt220 DEBIAN_FRONTEND=noninteractive apt-get -y install \
        apt-utils \
        dialog \
        libgmp10 \
    &amp;&amp; true

RUN useradd -G users -m -s /bin/bash myuser &amp;&amp; \
    echo &quot;root:root&quot; | chpasswd

USER myuser
COPY echosrv /home/myuser/
CMD bash --login</code></pre>
<p>I popped the <em>Dockerfile</em> and the binary into a folder <code>srv/</code> so I can build the image using</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb3-1"><a href="#cb3-1" aria-hidden="true"></a>$ <span class="ex">docker</span> build --rm --tag=nw srv</span></code></pre></div>
<p>and then started using</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb4-1"><a href="#cb4-1" aria-hidden="true"></a>$ <span class="ex">docker</span> run --rm --interactive --tty --name=echo nw /home/myuser/echosrv</span></code></pre></div>
<h2 id="a-handy-alias">A handy alias</h2>
<p>The following alias will turn out to be very handy indeed</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb5-1"><a href="#cb5-1" aria-hidden="true"></a>$ <span class="bu">alias</span> docker-pid=<span class="st">&quot;docker inspect --format &#39;{{ .State.Pid }}&#39;&quot;</span></span></code></pre></div>
<h2 id="the-network-setup">The network setup</h2>
<p>The local network is <code>192.168.1.0/24</code> and I found an unused address, <code>192.168.1.199</code> that I decided to use for my experiment. The gateway is on <code>192.168.1.1</code>.</p>
<p>First create the <code>macvlan</code> device:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb6-1"><a href="#cb6-1" aria-hidden="true"></a>$ <span class="fu">sudo</span> ip link add mybridge link enp0s25 type macvlan mode bridge</span></code></pre></div>
<p>Then put it into the network namespace of the running container, and bring it up:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb7-1"><a href="#cb7-1" aria-hidden="true"></a>$ <span class="fu">sudo</span> ip link set netns <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> mybridge</span>
<span id="cb7-2"><a href="#cb7-2" aria-hidden="true"></a>$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip link set mybridge up</span></code></pre></div>
<p>Now the device need its address and the routing has to be set up:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb8-1"><a href="#cb8-1" aria-hidden="true"></a>$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip route del default</span>
<span id="cb8-2"><a href="#cb8-2" aria-hidden="true"></a>$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip addr add 192.168.1.199/24 dev mybridge</span>
<span id="cb8-3"><a href="#cb8-3" aria-hidden="true"></a>$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip route add default via 192.168.1.1 dev mybridge</span></code></pre></div>
<p>That’s it, now the docker container is reachable on <code>192.168.1.199</code>. Well, it’s reachable on that ip from any computer on the network except for the host. In order for the host to be able to reach it add another route:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb9-1"><a href="#cb9-1" aria-hidden="true"></a>$ <span class="fu">sudo</span> ip route add 192.168.1.199 dev docker0</span></code></pre></div>
<h2 id="dhcp">DHCP</h2>
<p>If you’d rather have the container on an ip handed out by a DHCP server, then you replace the three commands for setting ip and route with:</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb10-1"><a href="#cb10-1" aria-hidden="true"></a>$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n -- dhclient -d maclan</span></code></pre></div>]]></summary>
</entry>
<entry>
    <title>Building elm with stack</title>
    <link href="http://magnus.therning.org/posts/2015-10-13-000-building-elm-with-stack.html" />
    <id>http://magnus.therning.org/posts/2015-10-13-000-building-elm-with-stack.html</id>
    <published>2015-10-13T00:00:00Z</published>
    <updated>2015-10-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Building <a href="http://elm-lang.org/">elm</a> is a slightly invasive procedure if your distro doesn’t have any pre-built packages for it and has a non-ancient version of <em>ghc</em>. Removing all haskell packages and installing <a href="https://www.haskell.org/platform/">HaskellPlatform</a> didn’t really appeal to me. Then it occured to me that <a href="http://www.stackage.org/">stack</a> ought to be perfectly suited for this task. It took me a while to sort it out, but here’s how I got a working build of the elm tools installed on Archlinux.</p>
<h2 id="installing-stack-and-cabal">Installing <em>stack</em> and <em>cabal</em></h2>
<p>Add the <em>ArchHaskell</em> repo by following the instructions <a href="https://wiki.archlinux.org/index.php/ArchHaskell">on the wiki</a>. Then install the <code>haskell-stack-bin</code> package. For <em>stack</em> to work fully one also needs the <a href="https://aur.archlinux.org/packages/libtinfo/">libtinfo package from AUR</a>.</p>
<p>If you aren’t using Archlinux there are instructions on the <em>stack</em> page.</p>
<p>Now build <em>cabal-install</em> using stack:</p>
<pre><code>% stack --install-ghc install cabal-install</code></pre>
<p>After this make sure <code>~/.local/bin</code> is in your <code>$PATH</code>.</p>
<h2 id="cloning-the-elm-tool-repos">Cloning the elm tool repos</h2>
<p>The elm developers have decided to not use Hackage so to get the tools one has to clone the repos from GitHub. I used the following shell script to get them all cloned in one go:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode bash"><code class="sourceCode bash"><span id="cb2-1"><a href="#cb2-1" aria-hidden="true"></a><span class="co">#! /usr/bin/zsh</span></span>
<span id="cb2-2"><a href="#cb2-2" aria-hidden="true"></a></span>
<span id="cb2-3"><a href="#cb2-3" aria-hidden="true"></a><span class="va">repos=(</span><span class="st">&quot;git clone https://github.com/elm-lang/elm-compiler.git -b 0.15.1&quot;</span></span>
<span id="cb2-4"><a href="#cb2-4" aria-hidden="true"></a>       <span class="st">&quot;git clone https://github.com/elm-lang/elm-package.git -b 0.5.1&quot;</span></span>
<span id="cb2-5"><a href="#cb2-5" aria-hidden="true"></a>       <span class="st">&quot;git clone https://github.com/elm-lang/elm-make.git -b 0.2&quot;</span></span>
<span id="cb2-6"><a href="#cb2-6" aria-hidden="true"></a>       <span class="st">&quot;git clone https://github.com/elm-lang/elm-reactor.git -b 0.3.2&quot;</span></span>
<span id="cb2-7"><a href="#cb2-7" aria-hidden="true"></a>       <span class="st">&quot;git clone https://github.com/elm-lang/elm-repl.git -b 0.4.2&quot;</span></span>
<span id="cb2-8"><a href="#cb2-8" aria-hidden="true"></a>      )</span>
<span id="cb2-9"><a href="#cb2-9" aria-hidden="true"></a></span>
<span id="cb2-10"><a href="#cb2-10" aria-hidden="true"></a><span class="kw">for</span> <span class="ex">r</span> in <span class="va">${repos[@]}</span><span class="kw">;</span> <span class="kw">do</span></span>
<span id="cb2-11"><a href="#cb2-11" aria-hidden="true"></a>    <span class="bu">eval</span> <span class="va">${r}</span></span>
<span id="cb2-12"><a href="#cb2-12" aria-hidden="true"></a><span class="kw">done</span></span></code></pre></div>
<h2 id="creating-stack.yaml">Creating <code>stack.yaml</code></h2>
<p>To create the initial <code>stack.yalm</code> run</p>
<pre><code>% stack init --resolver ghc-7.8.4</code></pre>
<p>Now open it and make a couple of modifications. First add the line</p>
<pre><code>system-ghc: false</code></pre>
<p>This is to make sure <em>stack</em> won’t try to use any <em>ghc</em> version already installed on the system. Then add a flag for <code>aeson</code>:</p>
<pre><code>flags:
  aeson:
    old-locale: true</code></pre>
<p>Now use <em>stack</em> to create the full <code>stack.yaml</code>:</p>
<pre><code>% stack --install-ghc solver --modify-stack-yaml</code></pre>
<h2 id="patch-elm-reactor">Patch <em>elm-reactor</em></h2>
<p>In order to build <em>elm-reactor</em> it first needs a tiny patch:</p>
<pre><code>diff --git a/backend/Socket.hs b/backend/Socket.hs
index b80a98f..69fe488 100644
--- a/backend/Socket.hs
+++ b/backend/Socket.hs
@@ -18,7 +18,7 @@ fileChangeApp :: FilePath -&gt; WS.ServerApp
 fileChangeApp watchedFile pendingConnection =
   do  connection &lt;- WS.acceptRequest pendingConnection
       Notify.withManager $ \notifyManager -&gt;
-        do  _ &lt;- NDevel.treeExtExists notifyManager &quot;.&quot; &quot;elm&quot; (sendHotSwap watchedFile connection)
+        do  _ &lt;- NDevel.treeExtExists notifyManager &quot;.&quot; &quot;elm&quot; (sendHotSwap watchedFile connection . FP.decodeString)
             keepAlive connection</code></pre>
<h2 id="building-the-tools">Building the tools</h2>
<p>Now build the elm tools (it’s important that <em>elm-reactor</em> is built after <em>elm-make</em>):</p>
<pre><code>% stack install elm-{compiler,make,package,repl}
% stack install elm-reactor</code></pre>
<p>Now just sit back and wait for the building to finish.</p>
<h2 id="verify-that-it-works">Verify that it works</h2>
<p>Follow the instructions for running <a href="https://github.com/evancz/elm-architecture-tutorial/tree/master/examples/1">example 1</a> of the <a href="https://github.com/evancz/elm-architecture-tutorial/">elm architecture tutorial</a>. I had to use the following two commands to get it running properly:</p>
<pre><code>% elm make
% elm reactor</code></pre>
<p>Then I can point my browser at <a href="http://localhost:8000/Main.elm?debug" class="uri">http://localhost:8000/Main.elm?debug</a> and play with the counter.</p>]]></summary>
</entry>

</feed>
