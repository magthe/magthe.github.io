<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title>Magnus web site</title>
    <link href="http://magnus.therning.org/atom.xml" rel="self" />
    <link href="http://magnus.therning.org" />
    <id>http://magnus.therning.org/atom.xml</id>
    <author>
        <name>Magnus Therning</name>
        <email>magnus@therning.org</email>
    </author>
    <updated>2019-03-16T00:00:00Z</updated>
    <entry>
    <title>TIL: prompt matters to org-mode</title>
    <link href="http://magnus.therning.org/posts/2019-03-16-000-til--prompt-matters-to-org-mode.html" />
    <id>http://magnus.therning.org/posts/2019-03-16-000-til--prompt-matters-to-org-mode.html</id>
    <published>2019-03-16T00:00:00Z</published>
    <updated>2019-03-16T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>A workmate just embellished some shell code blocks I’d put in a shared <a href="https://orgmode.org/">org-mode</a> file with <code>:session s</code>. When I tried to run the blocks with sessions my emacs just froze up though. I found a <a href="https://emacs.stackexchange.com/questions/19735/emacs-freezes-with-any-org-babel-snippet-using-session">post on the emacs StackExchange</a> that offered a possible cause for it: the prompt.</p>
<p>I’m using <a href="https://github.com/Bash-it/bash-it">bash-it</a> so my prompt is rather far from the default.</p>
<p>After inspecting the session buffer simply added the following to the top of my <code>~/.bashrc</code></p>
<pre class="shell"><code># Check for org-mode
if [[ ${TERM} == &quot;dumb&quot; ]]; then
    exit 0
fi</code></pre>
<p>and now I can finally run shell code blocks in sessions.</p>]]></summary>
</entry>
<entry>
    <title>Conduit and PostgreSQL</title>
    <link href="http://magnus.therning.org/posts/2019-03-03-000-conduit-and-postgresql.html" />
    <id>http://magnus.therning.org/posts/2019-03-03-000-conduit-and-postgresql.html</id>
    <published>2019-03-03T00:00:00Z</published>
    <updated>2019-03-03T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a while now I’ve been playing around with an event-drive software design (<a href="https://en.wikipedia.org/wiki/Event-driven_architecture">EDA</a>) using <a href="https://hackage.haskell.org/package/conduit"><code>conduit</code></a> for processing of events. For this post the processing can basically be viewed as the following diagram</p>
<pre><code>+-----------+   +------------+   +---------+
|           |   |            |   |         |
| PG source |--&gt;| Processing |--&gt;| PG sink |
|           |   |            |   |         |
+-----------+   +------------+   +---------+
     ^                                |
     |            +------+            |
     |            |      |            |
     |            |  PG  |            |
     +------------|  DB  |&lt;-----------+
                  |      |
                  +------+</code></pre>
<p>I started out looking for Conduit components for PostgreSQL on <a href="https://hackage.haskell.org/">Hackage</a> but failed to find something fitting so I started looking into writing them myself using <a href="https://hackage.haskell.org/package/postgresql-simple"><code>postgresql-simple</code></a>.</p>
<p>The sink wasn’t much of a problem, use <code>await</code> to get an event (a tuple) and write it to the database. My almost complete ignorance of using databases resulted in a first version of the source was rather naive and used busy-waiting. Then I stumbled on PostgreSQL’s support for notifications through the <code>LISTEN</code> and <code>NOTIFY</code> commands. I rather like the result and it seems to work well.<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a>[]</p>
<p>It looks like this</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">import</span>           <span class="dt">Control.Monad.IO.Class</span> (<span class="dt">MonadIO</span>, liftIO)</a>
<a class="sourceLine" id="cb2-2" data-line-number="2"><span class="kw">import</span>           <span class="dt">Data.Aeson</span> (<span class="dt">Value</span>)</a>
<a class="sourceLine" id="cb2-3" data-line-number="3"><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Data.Conduit</span> <span class="kw">as</span> <span class="dt">C</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4"><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Data.Conduit.Combinators</span> <span class="kw">as</span> <span class="dt">CC</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5"><span class="kw">import</span>           <span class="dt">Data.Text</span> (<span class="dt">Text</span>)</a>
<a class="sourceLine" id="cb2-6" data-line-number="6"><span class="kw">import</span>           <span class="dt">Data.Time.Clock</span> (<span class="dt">UTCTime</span>)</a>
<a class="sourceLine" id="cb2-7" data-line-number="7"><span class="kw">import</span>           <span class="dt">Data.UUID</span> (<span class="dt">UUID</span>)</a>
<a class="sourceLine" id="cb2-8" data-line-number="8"><span class="kw">import</span>           <span class="dt">Database.PostgreSQL.Simple</span> (<span class="dt">Connection</span>, <span class="dt">Only</span>(..), execute, execute_, query)</a>
<a class="sourceLine" id="cb2-9" data-line-number="9"><span class="kw">import</span>           <span class="dt">Database.PostgreSQL.Simple.Notification</span> (getNotification)</a>
<a class="sourceLine" id="cb2-10" data-line-number="10"></a>
<a class="sourceLine" id="cb2-11" data-line-number="11"><span class="ot">fst8 ::</span> (a, b, c, d, e, f, g, h) <span class="ot">-&gt;</span> a</a>
<a class="sourceLine" id="cb2-12" data-line-number="12">fst8 (a, _, _, _, _, _, _, _) <span class="fu">=</span> a</a>
<a class="sourceLine" id="cb2-13" data-line-number="13"></a>
<a class="sourceLine" id="cb2-14" data-line-number="14"><span class="ot">dbSource ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Connection</span> <span class="ot">-&gt;</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">C.ConduitT</span> () (<span class="dt">Int</span>, <span class="dt">UTCTime</span>, <span class="dt">Int</span>, <span class="dt">Int</span>, <span class="dt">Bool</span>, <span class="dt">UUID</span>, <span class="dt">Text</span>, <span class="dt">Value</span>) m ()</a>
<a class="sourceLine" id="cb2-15" data-line-number="15">dbSource conn ver <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-16" data-line-number="16">  res <span class="ot">&lt;-</span> liftIO <span class="fu">$</span> query conn <span class="st">&quot;SELECT * from events where id &gt; (?) ORDER BY id&quot;</span> (<span class="dt">Only</span> ver)</a>
<a class="sourceLine" id="cb2-17" data-line-number="17">  <span class="kw">case</span> res <span class="kw">of</span></a>
<a class="sourceLine" id="cb2-18" data-line-number="18">    [] <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-19" data-line-number="19">      liftIO <span class="fu">$</span> execute_ conn <span class="st">&quot;LISTEN MyEvent&quot;</span></a>
<a class="sourceLine" id="cb2-20" data-line-number="20">      liftIO <span class="fu">$</span> getNotification conn</a>
<a class="sourceLine" id="cb2-21" data-line-number="21">      dbSource conn ver</a>
<a class="sourceLine" id="cb2-22" data-line-number="22">    _ <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-23" data-line-number="23">      <span class="kw">let</span> ver&#39; <span class="fu">=</span> maximum <span class="fu">$</span> map fst8 res</a>
<a class="sourceLine" id="cb2-24" data-line-number="24">      CC.yieldMany res</a>
<a class="sourceLine" id="cb2-25" data-line-number="25">      dbSource conn ver&#39;</a>
<a class="sourceLine" id="cb2-26" data-line-number="26"></a>
<a class="sourceLine" id="cb2-27" data-line-number="27"><span class="ot">dbSink ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Connection</span> <span class="ot">-&gt;</span> <span class="dt">C.ConduitT</span> (<span class="dt">Int</span>, <span class="dt">Int</span>, <span class="dt">Bool</span>, <span class="dt">UUID</span>, <span class="dt">Text</span>, <span class="dt">Value</span>) <span class="dt">C.Void</span> m ()</a>
<a class="sourceLine" id="cb2-28" data-line-number="28">dbSink conn <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-29" data-line-number="29">  evt <span class="ot">&lt;-</span> C.await</a>
<a class="sourceLine" id="cb2-30" data-line-number="30">  <span class="kw">case</span> evt <span class="kw">of</span></a>
<a class="sourceLine" id="cb2-31" data-line-number="31">    <span class="dt">Nothing</span> <span class="ot">-&gt;</span> return ()</a>
<a class="sourceLine" id="cb2-32" data-line-number="32">    <span class="dt">Just</span> event <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-33" data-line-number="33">      liftIO <span class="fu">$</span> execute conn <span class="st">&quot;INSERT INTO events \</span></a>
<a class="sourceLine" id="cb2-34" data-line-number="34"><span class="st">                            \(srv_id, stream_id, cmd, cmd_id, correlation_id, event_data) \</span></a>
<a class="sourceLine" id="cb2-35" data-line-number="35"><span class="st">                            \VALUES (?, ?, ?, ?, ?, ?)&quot;</span> event</a>
<a class="sourceLine" id="cb2-36" data-line-number="36">      liftIO <span class="fu">$</span> execute_ conn <span class="st">&quot;NOTIFY MyEvent&quot;</span></a>
<a class="sourceLine" id="cb2-37" data-line-number="37">      dbSink conn</a></code></pre></div>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>If I’ve missed something crucial I would of course love to hear about it.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Choosing a conduit randomly</title>
    <link href="http://magnus.therning.org/posts/2019-02-17-000-choosing-a-conduit-randomly.html" />
    <id>http://magnus.therning.org/posts/2019-02-17-000-choosing-a-conduit-randomly.html</id>
    <published>2019-02-17T00:00:00Z</published>
    <updated>2019-02-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Lately I’ve been playing around <a href="https://hackage.haskell.org/package/conduit">conduit</a>. One thing I wanted to try out was to set up processing where one processing step was chosen on random from a number of components, based on weights. In short I guess I wanted a function with a type something like this</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">foo ::</span> [(<span class="dt">Int</span>, <span class="dt">ConduitT</span> i o m r)] <span class="ot">-&gt;</span> <span class="dt">ConduitT</span> i o m r</a></code></pre></div>
<p>I have to admit I don’t even know where to start writing such a function<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a> but after a little bit of thinking I realised I could get the same effect by controlling how chunks of data is routed. That is, instead of choosing a component randomly, I can choose a route randomly. It would look something like when choosing from three components</p>
<pre><code>                        +---------+   +----------+   +-------------+
                        | Filter  |   | Drop tag |   | Component A |
                    +--&gt;| Value-0 |--&gt;|          |--&gt;|             |--+
                    |   +---------+   +----------+   +-------------+  |
+----------------+  |   +---------+   +----------+   +-------------+  |
| Choose random  |  |   | Filter  |   | Drop tag |   | Component B |  |
| value based on +-----&gt;| Value-1 |--&gt;|          |--&gt;|             |-----&gt;
| weights        |  |   +---------+   +----------+   +-------------+  |
+----------------+  |   +---------+   +----------+   +-------------+  |
                    |   | Filter  |   | Drop tag |   | Component C |  |
                    +--&gt;| Value-2 |--&gt;|          |--&gt;|             |--+
                        +---------+   +----------+   +-------------+


</code></pre>
<p>That is</p>
<ol type="1">
<li>For each chunk that comes in, choose a value randomly based on weights and tag the chunk with the choosen value, then</li>
<li>split the processing into one route for each component,</li>
<li>in each route filter out chunks tagged with a single value, and</li>
<li>remove the tag, then</li>
<li>pass the chunk to the component, and finally</li>
<li>bring the routes back together again.</li>
</ol>
<p>Out of these steps all but the very first one are already available in <a href="https://hackage.haskell.org/package/conduit">conduit</a>:</p>
<ul>
<li>for splitting routes combining them again, use <a href="https://hackage.haskell.org/package/conduit-1.3.1/docs/Data-Conduit.html#v:sequenceConduits"><code>sequenceConduits</code></a></li>
<li>for filtering, use <a href="https://hackage.haskell.org/package/conduit-1.3.1/docs/Data-Conduit-Combinators.html#v:filter"><code>filter</code></a></li>
<li>for dropping the tag, use <a href="https://hackage.haskell.org/package/conduit-1.3.1/docs/Data-Conduit-Combinators.html#v:map"><code>map</code></a></li>
</ul>
<p>What’s left is the beginning. I started with a function to pick a value on random based on weights<a href="#fn2" class="footnote-ref" id="fnref2"><sup>2</sup></a></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">pickByWeight ::</span> [(<span class="dt">Int</span>, b)] <span class="ot">-&gt;</span> <span class="dt">IO</span> b</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">pickByWeight xs <span class="fu">=</span> randomRIO (<span class="dv">1</span>, tot) <span class="fu">&gt;&gt;=</span> \ n <span class="ot">-&gt;</span> return (pick n xs)</a>
<a class="sourceLine" id="cb3-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-4" data-line-number="4">    tot <span class="fu">=</span> sum <span class="fu">$</span> map fst xs</a>
<a class="sourceLine" id="cb3-5" data-line-number="5"></a>
<a class="sourceLine" id="cb3-6" data-line-number="6">    pick n ((k, x)<span class="fu">:</span>xs)</a>
<a class="sourceLine" id="cb3-7" data-line-number="7">      <span class="fu">|</span> n <span class="fu">&lt;=</span> k <span class="fu">=</span> x</a>
<a class="sourceLine" id="cb3-8" data-line-number="8">      <span class="fu">|</span> otherwise <span class="fu">=</span> pick (n <span class="fu">-</span> k) xs</a>
<a class="sourceLine" id="cb3-9" data-line-number="9">    pick _ _ <span class="fu">=</span> error <span class="st">&quot;pick error&quot;</span></a></code></pre></div>
<p>Using that I then made a component that tags chunks</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1">picker ws <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">  evt <span class="ot">&lt;-</span> await</a>
<a class="sourceLine" id="cb4-3" data-line-number="3">  <span class="kw">case</span> evt <span class="kw">of</span></a>
<a class="sourceLine" id="cb4-4" data-line-number="4">    <span class="dt">Nothing</span> <span class="ot">-&gt;</span> return ()</a>
<a class="sourceLine" id="cb4-5" data-line-number="5">    <span class="dt">Just</span> e <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb4-6" data-line-number="6">      p <span class="ot">&lt;-</span> liftIO <span class="fu">$</span> pickByWeight ws</a>
<a class="sourceLine" id="cb4-7" data-line-number="7">      yield (p, e)</a>
<a class="sourceLine" id="cb4-8" data-line-number="8">      picker ws</a></code></pre></div>
<p>I was rather happy with this…</p>
<center>
<blockquote class="twitter-tweet" data-lang="en">
<p lang="en" dir="ltr">
<a href="https://twitter.com/snoyberg?ref_src=twsrc%5Etfw"><span class="citation" data-cites="snoyberg">@snoyberg</span></a> just have to let you know, conduit is a joy to use. Thanks for sharing it.
</p>
— Magnus Therning (<span class="citation" data-cites="magthe">@magthe</span>) <a href="https://twitter.com/magthe/status/1093175648993140736?ref_src=twsrc%5Etfw">February 6, 2019</a>
</blockquote>
<script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>
</center>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>Except maybe by using Template Haskell to generate the code I did come up with.<a href="#fnref1" class="footnote-back">↩</a></p></li>
<li id="fn2"><p>I used <code>Quickcheck</code>’s <a href="https://hackage.haskell.org/package/QuickCheck-2.12.6.1/docs/Test-QuickCheck-Gen.html#v:frequency"><code>frequency</code></a> as inspiration for writing it.<a href="#fnref2" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Using stack to get around upstream bugs</title>
    <link href="http://magnus.therning.org/posts/2019-02-10-000-using-stack-to-get-around-upstream-bugs.html" />
    <id>http://magnus.therning.org/posts/2019-02-10-000-using-stack-to-get-around-upstream-bugs.html</id>
    <published>2019-02-10T00:00:00Z</published>
    <updated>2019-02-10T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Recently I bumped into <a href="https://github.com/brendanhay/amazonka/issues/517">a bug in <em>amazonka</em></a>.<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a> I can’t really sit around waiting for Amazon to fix it, and then for <em>amazonka</em> to use the fixed documentation to generate the code and make another release.</p>
<p>Luckily <a href="https://docs.haskellstack.org/en/stable/README/"><code>slack</code></a> contains features that make it fairly simple to work around this bug until it’s properly fixed. Here’s how.</p>
<ol type="1">
<li>Put the upstream code in a git repository of your own. In my case I simply forked the <a href="https://github.com/brendanhay/amazonka"><em>amazonka</em> repository on github</a> (my fork is <a href="https://github.com/magthe/amazonka">here</a>).</li>
<li>Fix the bug and commit the change. <a href="https://github.com/magthe/amazonka/commit/1543b65e3a8b692aa9038ada68aaed9967752983">My change to <em>amazonka-codepipeline</em></a> was simply to remove the missing fields – it was easier than trying to make them optional (i.e. wrapping them in <code>Maybe</code>s).</li>
<li><p>Tell <code>slack</code> to use the code from your modified git repository. In my case I added the following to my <code>slack.yaml</code>:</p>
<pre><code> extra-deps:
   - github: magthe/amazonka
     commit: 1543b65e3a8b692aa9038ada68aaed9967752983
     subdirs:
       - amazonka-codepipeline</code></pre></li>
</ol>
<p>That’s it!</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>The guilty party is Amazon, not <em>amazonka</em>, though I was a little surprised that there doesn’t seem to be any established way to modify the Amazon API documentation before it’s used to autogenerate the Haskell code.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>The ReaderT design pattern or tagless final?</title>
    <link href="http://magnus.therning.org/posts/2019-02-02-000-the-readert-design-pattern-or-tagless-final-.html" />
    <id>http://magnus.therning.org/posts/2019-02-02-000-the-readert-design-pattern-or-tagless-final-.html</id>
    <published>2019-02-02T00:00:00Z</published>
    <updated>2019-02-02T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other week I read V. Kevroletin’s <a href="https://serokell.io/blog/2018/12/07/tagless-final">Introduction to Tagless Final</a> and realised that a couple of my projects, both at work and at home, would benefit from a refactoring to that approach. All in all I was happy with the changes I made, even though I haven’t made use of all the way. In particular there I could further improve the tests in a few places by adding more typeclasses. For now it’s good enough and I’ve clearly gotten some value out of it.</p>
<p>I found mr. Kevroletin’s article to be a good introduction so I’ve been passing it on when people on the <a href="https://functionalprogramming.slack.com/">Functional programming slack</a> bring up questions about how to organize their code as applications grow. In particular if they mention that they’re using monad transformers. I did exactly that just the other day _<span class="citation" data-cites="solomon_">@solomon_</span> wrote</p>
<blockquote>
<p>so i’ve created a rats nest of IO where almost all the functions in my program are in <code>ReaderT Env IO ()</code> and I’m not sure how to purify everything and move the IO to the edge of the program</p>
</blockquote>
<p>I proposed tagless final and passed the URL on, and then I got a pointer to the article <a href="https://www.fpcomplete.com/blog/2017/06/readert-design-pattern">The ReaderT Design Patter</a> which I hadn’t seen before.</p>
<p>The two approches are similar, at least to me, and I can’t really judge if one’s better than the other. Just to get a feel for it I thought I’d try to rewrite the example in the <code>ReaderT</code> article in a tagless final style.</p>
<h2 id="a-slightly-changed-example-of-readert-design-pattern">A slightly changed example of <code>ReaderT</code> design pattern</h2>
<p>I decided to make a few changes to the example in the article:</p>
<ul>
<li>I removed the <code>modify</code> function, instead the code uses the typeclass function <code>modifyBalance</code> directly.</li>
<li>I separated the instances needed for the tests spatially in the code just to make it easier to see what’s “production” code and what’s test code.</li>
<li>I combined the <code>main</code> functions from the various examples to that both an example (<code>main0</code>) and the test (<code>main1</code>) are run.</li>
<li>I switched from <code>Control.Concurrent.Async.Lifted.Safe</code> (from <code>monad-control</code>) to <code>UnliftIO.Async</code> (from <code>unliftio</code>)</li>
</ul>
<p>After that the code looks like this</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">{-# LANGUAGE FlexibleContexts #-}</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="ot">{-# LANGUAGE FlexibleInstances #-}</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="kw">import</span>           <span class="dt">Control.Concurrent.STM</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="kw">import</span>           <span class="dt">Control.Monad.Reader</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Control.Monad.State.Strict</span> <span class="kw">as</span> <span class="dt">State</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"><span class="kw">import</span>           <span class="dt">Say</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="kw">import</span>           <span class="dt">Test.Hspec</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9"><span class="kw">import</span>           <span class="dt">UnliftIO.Async</span></a>
<a class="sourceLine" id="cb1-10" data-line-number="10"></a>
<a class="sourceLine" id="cb1-11" data-line-number="11"><span class="kw">data</span> <span class="dt">Env</span> <span class="fu">=</span> <span class="dt">Env</span></a>
<a class="sourceLine" id="cb1-12" data-line-number="12">  {<span class="ot"> envLog ::</span> <span class="fu">!</span>(<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ())</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">  ,<span class="ot"> envBalance ::</span> <span class="fu">!</span>(<span class="dt">TVar</span> <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb1-14" data-line-number="14">  }</a>
<a class="sourceLine" id="cb1-15" data-line-number="15"></a>
<a class="sourceLine" id="cb1-16" data-line-number="16"><span class="kw">class</span> <span class="dt">HasLog</span> a <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-17" data-line-number="17"><span class="ot">  getLog ::</span> a <span class="ot">-&gt;</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ())</a>
<a class="sourceLine" id="cb1-18" data-line-number="18"></a>
<a class="sourceLine" id="cb1-19" data-line-number="19"><span class="kw">instance</span> <span class="dt">HasLog</span> <span class="dt">Env</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-20" data-line-number="20">  getLog <span class="fu">=</span> envLog</a>
<a class="sourceLine" id="cb1-21" data-line-number="21"></a>
<a class="sourceLine" id="cb1-22" data-line-number="22"><span class="kw">class</span> <span class="dt">HasBalance</span> a <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-23" data-line-number="23"><span class="ot">  getBalance ::</span> a <span class="ot">-&gt;</span> <span class="dt">TVar</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb1-24" data-line-number="24"></a>
<a class="sourceLine" id="cb1-25" data-line-number="25"><span class="kw">instance</span> <span class="dt">HasBalance</span> <span class="dt">Env</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-26" data-line-number="26">  getBalance <span class="fu">=</span> envBalance</a>
<a class="sourceLine" id="cb1-27" data-line-number="27"></a>
<a class="sourceLine" id="cb1-28" data-line-number="28"><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadBalance</span> m <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-29" data-line-number="29"><span class="ot">  modifyBalance ::</span> (<span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Int</span>) <span class="ot">-&gt;</span> m ()</a>
<a class="sourceLine" id="cb1-30" data-line-number="30"></a>
<a class="sourceLine" id="cb1-31" data-line-number="31"><span class="kw">instance</span> (<span class="dt">HasBalance</span> env, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">MonadBalance</span> (<span class="dt">ReaderT</span> env m) <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-32" data-line-number="32">  modifyBalance f <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-33" data-line-number="33">    env <span class="ot">&lt;-</span> ask</a>
<a class="sourceLine" id="cb1-34" data-line-number="34">    liftIO <span class="fu">$</span> atomically <span class="fu">$</span> modifyTVar&#39; (getBalance env) f</a>
<a class="sourceLine" id="cb1-35" data-line-number="35"></a>
<a class="sourceLine" id="cb1-36" data-line-number="36"><span class="ot">logSomething ::</span> (<span class="dt">MonadReader</span> env m, <span class="dt">HasLog</span> env, <span class="dt">MonadIO</span> m) <span class="ot">=&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> m ()</a>
<a class="sourceLine" id="cb1-37" data-line-number="37">logSomething msg <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-38" data-line-number="38">  env <span class="ot">&lt;-</span> ask</a>
<a class="sourceLine" id="cb1-39" data-line-number="39">  liftIO <span class="fu">$</span> getLog env msg</a>
<a class="sourceLine" id="cb1-40" data-line-number="40"></a>
<a class="sourceLine" id="cb1-41" data-line-number="41"><span class="ot">main0 ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-42" data-line-number="42">main0 <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-43" data-line-number="43">  ref <span class="ot">&lt;-</span> newTVarIO <span class="dv">4</span></a>
<a class="sourceLine" id="cb1-44" data-line-number="44">  <span class="kw">let</span> env <span class="fu">=</span> <span class="dt">Env</span> { envLog <span class="fu">=</span> sayString , envBalance <span class="fu">=</span> ref }</a>
<a class="sourceLine" id="cb1-45" data-line-number="45">  runReaderT</a>
<a class="sourceLine" id="cb1-46" data-line-number="46">    (concurrently_</a>
<a class="sourceLine" id="cb1-47" data-line-number="47">      (modifyBalance (<span class="fu">+</span> <span class="dv">1</span>))</a>
<a class="sourceLine" id="cb1-48" data-line-number="48">      (logSomething <span class="st">&quot;Increasing account balance&quot;</span>))</a>
<a class="sourceLine" id="cb1-49" data-line-number="49">    env</a>
<a class="sourceLine" id="cb1-50" data-line-number="50">  balance <span class="ot">&lt;-</span> readTVarIO ref</a>
<a class="sourceLine" id="cb1-51" data-line-number="51">  sayString <span class="fu">$</span> <span class="st">&quot;Final balance: &quot;</span> <span class="fu">++</span> show balance</a>
<a class="sourceLine" id="cb1-52" data-line-number="52"></a>
<a class="sourceLine" id="cb1-53" data-line-number="53"><span class="kw">instance</span> <span class="dt">HasLog</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()) <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-54" data-line-number="54">  getLog <span class="fu">=</span> id</a>
<a class="sourceLine" id="cb1-55" data-line-number="55"></a>
<a class="sourceLine" id="cb1-56" data-line-number="56"><span class="kw">instance</span> <span class="dt">HasBalance</span> (<span class="dt">TVar</span> <span class="dt">Int</span>) <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-57" data-line-number="57">  getBalance <span class="fu">=</span> id</a>
<a class="sourceLine" id="cb1-58" data-line-number="58"></a>
<a class="sourceLine" id="cb1-59" data-line-number="59"><span class="kw">instance</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadBalance</span> (<span class="dt">State.StateT</span> <span class="dt">Int</span> m) <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-60" data-line-number="60">  modifyBalance <span class="fu">=</span> State.modify</a>
<a class="sourceLine" id="cb1-61" data-line-number="61"></a>
<a class="sourceLine" id="cb1-62" data-line-number="62"><span class="ot">main1 ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-63" data-line-number="63">main1 <span class="fu">=</span> hspec <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-64" data-line-number="64">  describe <span class="st">&quot;modify&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-65" data-line-number="65">    it <span class="st">&quot;works, IO&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-66" data-line-number="66">      var <span class="ot">&lt;-</span> newTVarIO (<span class="dv">1</span><span class="ot"> ::</span> <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb1-67" data-line-number="67">      runReaderT (modifyBalance (<span class="fu">+</span> <span class="dv">2</span>)) var</a>
<a class="sourceLine" id="cb1-68" data-line-number="68">      res <span class="ot">&lt;-</span> readTVarIO var</a>
<a class="sourceLine" id="cb1-69" data-line-number="69">      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></a>
<a class="sourceLine" id="cb1-70" data-line-number="70">    it <span class="st">&quot;works, pure&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-71" data-line-number="71">      <span class="kw">let</span> res <span class="fu">=</span> State.execState (modifyBalance (<span class="fu">+</span> <span class="dv">2</span>)) (<span class="dv">1</span><span class="ot"> ::</span> <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb1-72" data-line-number="72">      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></a>
<a class="sourceLine" id="cb1-73" data-line-number="73">  describe <span class="st">&quot;logSomething&quot;</span> <span class="fu">$</span></a>
<a class="sourceLine" id="cb1-74" data-line-number="74">    it <span class="st">&quot;works&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-75" data-line-number="75">      var <span class="ot">&lt;-</span> newTVarIO <span class="st">&quot;&quot;</span></a>
<a class="sourceLine" id="cb1-76" data-line-number="76">      <span class="kw">let</span> logFunc msg <span class="fu">=</span> atomically <span class="fu">$</span> modifyTVar var (<span class="fu">++</span> msg)</a>
<a class="sourceLine" id="cb1-77" data-line-number="77">          msg1 <span class="fu">=</span> <span class="st">&quot;Hello &quot;</span></a>
<a class="sourceLine" id="cb1-78" data-line-number="78">          msg2 <span class="fu">=</span> <span class="st">&quot;World\n&quot;</span></a>
<a class="sourceLine" id="cb1-79" data-line-number="79">      runReaderT (logSomething msg1 <span class="fu">&gt;&gt;</span> logSomething msg2) logFunc</a>
<a class="sourceLine" id="cb1-80" data-line-number="80">      res <span class="ot">&lt;-</span> readTVarIO var</a>
<a class="sourceLine" id="cb1-81" data-line-number="81">      res <span class="ot">`shouldBe`</span> (msg1 <span class="fu">++</span> msg2)</a>
<a class="sourceLine" id="cb1-82" data-line-number="82"></a>
<a class="sourceLine" id="cb1-83" data-line-number="83"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-84" data-line-number="84">main <span class="fu">=</span> main0 <span class="fu">&gt;&gt;</span> main1</a></code></pre></div>
<p>I think the distinguising features are</p>
<ul>
<li>The application environmant, <code>Env</code> will contain configuraiton values (not in this example), state, <code>envBalance</code>, and functions we might want to vary, <code>envLog</code></li>
<li>There is no explicit type representing the execution context</li>
<li>Typeclasses are used to abstract over application environment, <code>HasLog</code> and <code>HasBalance</code></li>
<li>Typeclasses are used to abstract over operations, <code>MonadBalance</code></li>
<li>Typeclasses are implemented for both the application environment, <code>HasLog</code> and <code>HasBalance</code>, and the execution context, <code>MonadBalance</code></li>
</ul>
<p>In the end this makes for code with very loose couplings; there’s not really any single concrete type that implements all the constraints to work in the “real” main function (<code>main0</code>). I could of course introduce a type synonym for it</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">type</span> <span class="dt">App</span> <span class="fu">=</span> <span class="dt">ReaderT</span> <span class="dt">Env</span> <span class="dt">IO</span></a></code></pre></div>
<p>but it brings no value – it wouldn’t be used explicitly anywhere.</p>
<h2 id="a-tagless-final-version">A tagless final version</h2>
<p>In order to compare the <code>ReaderT</code> design pattern to tagless final (as I understand it) I made an attempt to translate the code above. The code below is the result.<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">{-# LANGUAGE FlexibleContexts #-}</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2"><span class="ot">{-# LANGUAGE FlexibleInstances #-}</span></a>
<a class="sourceLine" id="cb3-3" data-line-number="3"><span class="ot">{-# LANGUAGE GeneralizedNewtypeDeriving #-}</span></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="ot">{-# LANGUAGE MultiParamTypeClasses #-}</span></a>
<a class="sourceLine" id="cb3-5" data-line-number="5"><span class="ot">{-# LANGUAGE TypeFamilies #-}</span></a>
<a class="sourceLine" id="cb3-6" data-line-number="6"></a>
<a class="sourceLine" id="cb3-7" data-line-number="7"><span class="kw">import</span>           <span class="dt">Control.Concurrent.STM</span></a>
<a class="sourceLine" id="cb3-8" data-line-number="8"><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Control.Monad.Identity</span> <span class="kw">as</span> <span class="dt">Id</span></a>
<a class="sourceLine" id="cb3-9" data-line-number="9"><span class="kw">import</span>           <span class="dt">Control.Monad.Reader</span></a>
<a class="sourceLine" id="cb3-10" data-line-number="10"><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Control.Monad.State.Strict</span> <span class="kw">as</span> <span class="dt">State</span></a>
<a class="sourceLine" id="cb3-11" data-line-number="11"><span class="kw">import</span>           <span class="dt">Say</span></a>
<a class="sourceLine" id="cb3-12" data-line-number="12"><span class="kw">import</span>           <span class="dt">Test.Hspec</span></a>
<a class="sourceLine" id="cb3-13" data-line-number="13"><span class="kw">import</span>           <span class="dt">UnliftIO</span> (<span class="dt">MonadUnliftIO</span>)</a>
<a class="sourceLine" id="cb3-14" data-line-number="14"><span class="kw">import</span>           <span class="dt">UnliftIO.Async</span></a>
<a class="sourceLine" id="cb3-15" data-line-number="15"></a>
<a class="sourceLine" id="cb3-16" data-line-number="16"><span class="kw">newtype</span> <span class="dt">Env</span> <span class="fu">=</span> <span class="dt">Env</span> {<span class="ot">envBalance ::</span> <span class="dt">TVar</span> <span class="dt">Int</span>}</a>
<a class="sourceLine" id="cb3-17" data-line-number="17"></a>
<a class="sourceLine" id="cb3-18" data-line-number="18"><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="fu">=</span> <span class="dt">AppM</span> {<span class="ot">unAppM ::</span> <span class="dt">ReaderT</span> <span class="dt">Env</span> <span class="dt">IO</span> a}</a>
<a class="sourceLine" id="cb3-19" data-line-number="19">  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> <span class="dt">Env</span>, <span class="dt">MonadUnliftIO</span>)</a>
<a class="sourceLine" id="cb3-20" data-line-number="20"></a>
<a class="sourceLine" id="cb3-21" data-line-number="21"><span class="ot">runAppM ::</span> <span class="dt">Env</span> <span class="ot">-&gt;</span> <span class="dt">AppM</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb3-22" data-line-number="22">runAppM env app <span class="fu">=</span> runReaderT (unAppM app) env</a>
<a class="sourceLine" id="cb3-23" data-line-number="23"></a>
<a class="sourceLine" id="cb3-24" data-line-number="24"><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">ModifyM</span> m <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-25" data-line-number="25"><span class="ot">  mModify ::</span> (<span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Int</span>) <span class="ot">-&gt;</span> m ()</a>
<a class="sourceLine" id="cb3-26" data-line-number="26"></a>
<a class="sourceLine" id="cb3-27" data-line-number="27"><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">LogSomethingM</span> m <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-28" data-line-number="28"><span class="ot">  mLogSomething ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> m()</a>
<a class="sourceLine" id="cb3-29" data-line-number="29"></a>
<a class="sourceLine" id="cb3-30" data-line-number="30"><span class="kw">instance</span> <span class="dt">ModifyM</span> <span class="dt">AppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-31" data-line-number="31">  mModify f <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-32" data-line-number="32">    ref <span class="ot">&lt;-</span> asks envBalance</a>
<a class="sourceLine" id="cb3-33" data-line-number="33">    liftIO <span class="fu">$</span> atomically <span class="fu">$</span> modifyTVar&#39; ref f</a>
<a class="sourceLine" id="cb3-34" data-line-number="34"></a>
<a class="sourceLine" id="cb3-35" data-line-number="35"><span class="kw">instance</span> <span class="dt">LogSomethingM</span> <span class="dt">AppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-36" data-line-number="36">  mLogSomething <span class="fu">=</span> liftIO <span class="fu">.</span> sayString</a>
<a class="sourceLine" id="cb3-37" data-line-number="37"></a>
<a class="sourceLine" id="cb3-38" data-line-number="38"><span class="ot">main0 ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb3-39" data-line-number="39">main0 <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-40" data-line-number="40">  ref <span class="ot">&lt;-</span> newTVarIO <span class="dv">4</span></a>
<a class="sourceLine" id="cb3-41" data-line-number="41">  <span class="kw">let</span> env <span class="fu">=</span> <span class="dt">Env</span> ref</a>
<a class="sourceLine" id="cb3-42" data-line-number="42">  runAppM env</a>
<a class="sourceLine" id="cb3-43" data-line-number="43">    (concurrently_</a>
<a class="sourceLine" id="cb3-44" data-line-number="44">      (mModify (<span class="fu">+</span> <span class="dv">1</span>))</a>
<a class="sourceLine" id="cb3-45" data-line-number="45">      (mLogSomething <span class="st">&quot;Increasing account balance&quot;</span>))</a>
<a class="sourceLine" id="cb3-46" data-line-number="46">  balance <span class="ot">&lt;-</span> readTVarIO ref</a>
<a class="sourceLine" id="cb3-47" data-line-number="47">  sayString <span class="fu">$</span> <span class="st">&quot;Final balance: &quot;</span> <span class="fu">++</span> show balance</a>
<a class="sourceLine" id="cb3-48" data-line-number="48"></a>
<a class="sourceLine" id="cb3-49" data-line-number="49"><span class="kw">newtype</span> <span class="dt">ModifyAppM</span> a <span class="fu">=</span> <span class="dt">ModifyAppM</span> {<span class="ot">unModifyAppM ::</span> <span class="dt">State.StateT</span> <span class="dt">Int</span> <span class="dt">Id.Identity</span> a}</a>
<a class="sourceLine" id="cb3-50" data-line-number="50">  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">State.MonadState</span> <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb3-51" data-line-number="51"></a>
<a class="sourceLine" id="cb3-52" data-line-number="52"><span class="ot">runModifyAppM ::</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">ModifyAppM</span> a <span class="ot">-&gt;</span> (a, <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb3-53" data-line-number="53">runModifyAppM s app <span class="fu">=</span> Id.runIdentity <span class="fu">$</span> State.runStateT (unModifyAppM app) s</a>
<a class="sourceLine" id="cb3-54" data-line-number="54"></a>
<a class="sourceLine" id="cb3-55" data-line-number="55"><span class="kw">instance</span> <span class="dt">ModifyM</span> <span class="dt">ModifyAppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-56" data-line-number="56">  mModify <span class="fu">=</span> State.modify&#39;</a>
<a class="sourceLine" id="cb3-57" data-line-number="57"></a>
<a class="sourceLine" id="cb3-58" data-line-number="58"><span class="kw">newtype</span> <span class="dt">LogAppM</span> a <span class="fu">=</span> <span class="dt">LogAppM</span> {<span class="ot">unLogAppM ::</span> <span class="dt">ReaderT</span> (<span class="dt">TVar</span> <span class="dt">String</span>) <span class="dt">IO</span> a}</a>
<a class="sourceLine" id="cb3-59" data-line-number="59">  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> (<span class="dt">TVar</span> <span class="dt">String</span>))</a>
<a class="sourceLine" id="cb3-60" data-line-number="60"></a>
<a class="sourceLine" id="cb3-61" data-line-number="61"><span class="ot">runLogAppM ::</span> <span class="dt">TVar</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">LogAppM</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb3-62" data-line-number="62">runLogAppM env app <span class="fu">=</span> runReaderT (unLogAppM app) env</a>
<a class="sourceLine" id="cb3-63" data-line-number="63"></a>
<a class="sourceLine" id="cb3-64" data-line-number="64"><span class="kw">instance</span> <span class="dt">LogSomethingM</span> <span class="dt">LogAppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-65" data-line-number="65">  mLogSomething msg <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-66" data-line-number="66">    var <span class="ot">&lt;-</span> ask</a>
<a class="sourceLine" id="cb3-67" data-line-number="67">    liftIO <span class="fu">$</span> atomically <span class="fu">$</span> modifyTVar var (<span class="fu">++</span> msg)</a>
<a class="sourceLine" id="cb3-68" data-line-number="68"></a>
<a class="sourceLine" id="cb3-69" data-line-number="69"><span class="ot">main1 ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb3-70" data-line-number="70">main1 <span class="fu">=</span> hspec <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-71" data-line-number="71">  describe <span class="st">&quot;mModify&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-72" data-line-number="72">    it <span class="st">&quot;works, IO&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-73" data-line-number="73">      var <span class="ot">&lt;-</span> newTVarIO <span class="dv">1</span></a>
<a class="sourceLine" id="cb3-74" data-line-number="74">      runAppM (<span class="dt">Env</span> var) (mModify (<span class="fu">+</span> <span class="dv">2</span>))</a>
<a class="sourceLine" id="cb3-75" data-line-number="75">      res <span class="ot">&lt;-</span> readTVarIO var</a>
<a class="sourceLine" id="cb3-76" data-line-number="76">      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></a>
<a class="sourceLine" id="cb3-77" data-line-number="77">    it <span class="st">&quot;works, pure&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-78" data-line-number="78">      <span class="kw">let</span> (_, res) <span class="fu">=</span> runModifyAppM <span class="dv">1</span> (mModify (<span class="fu">+</span> <span class="dv">2</span>))</a>
<a class="sourceLine" id="cb3-79" data-line-number="79">      res <span class="ot">`shouldBe`</span> <span class="dv">3</span></a>
<a class="sourceLine" id="cb3-80" data-line-number="80">  describe <span class="st">&quot;mLogSomething&quot;</span> <span class="fu">$</span></a>
<a class="sourceLine" id="cb3-81" data-line-number="81">    it <span class="st">&quot;works&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-82" data-line-number="82">      var <span class="ot">&lt;-</span> newTVarIO <span class="st">&quot;&quot;</span></a>
<a class="sourceLine" id="cb3-83" data-line-number="83">      runLogAppM var (mLogSomething <span class="st">&quot;Hello&quot;</span> <span class="fu">&gt;&gt;</span> mLogSomething <span class="st">&quot;World!&quot;</span>)</a>
<a class="sourceLine" id="cb3-84" data-line-number="84">      res <span class="ot">&lt;-</span> readTVarIO var</a>
<a class="sourceLine" id="cb3-85" data-line-number="85">      res <span class="ot">`shouldBe`</span> <span class="st">&quot;HelloWorld!&quot;</span></a>
<a class="sourceLine" id="cb3-86" data-line-number="86"></a>
<a class="sourceLine" id="cb3-87" data-line-number="87"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb3-88" data-line-number="88">main <span class="fu">=</span> main0 <span class="fu">&gt;&gt;</span> main1</a></code></pre></div>
<p>The steps for the “real” part of the program were</p>
<ol type="1">
<li>Introduce an execution type, <code>AppM</code>, with a convenience function for running it, <code>runAppM</code></li>
<li>Remove the log function from the environment type, <code>envLog</code> in <code>Env</code></li>
<li>Remove all the <code>HasX</code> classes</li>
<li>Create a new operations typeclass for logging, <code>LogSomethingM</code></li>
<li>Rename the operations typeclass for modifying the balance to match the naming found in the <a href="https://serokell.io/blog/2018/12/07/tagless-final">tagless article</a> a bit better, <code>ModifyM</code></li>
<li>Implement instances of both operations typeclasses for <code>AppM</code></li>
</ol>
<p>For testing the steps were</p>
<ol type="1">
<li>Define an execution type for each test, <code>ModifyAppM</code> and <code>LogAppM</code>, with some convenience functions for running them, <code>runModifyAppM</code> and <code>runLogAppM</code></li>
<li>Write instances for the operations typeclasses, one for each</li>
</ol>
<p>So I think the distinguising features are</p>
<ul>
<li>There’s both an environment type, <code>Env</code>, and an execution type <code>AppM</code> that wraps it</li>
<li>The environment holds only configuration values (none in this example), and state (<code>envBalance</code>)</li>
<li>Typeclasses are used to abstract over operations, <code>LogSomethingM</code> and <code>ModifyM</code></li>
<li>Typeclasses are only implemented for the execution type</li>
</ul>
<p>This version has slightly more coupling, the execution type specifies the environment to use, and the operations are tied directly to the execution type. However, this coupling doesn’t really make a big difference – looking at the pure modify test the amount of code don’t differ by much.</p>
<h3 id="a-short-note-mostly-to-myself">A short note (mostly to myself)</h3>
<p>I did write it using <code>monad-control</code> first, and then I needed an instance for <code>MonadBaseControl IO</code>. Deriving it automatically requires <code>UndecidableInstances</code> and I didn’t really dare turn that on, so I ended up writing the instance. After some help on <a href="https://mail.haskell.org/pipermail/haskell-cafe/2019-February/130667.html">haskell-cafe</a> it ended up looking like this</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">MonadBaseControl</span> <span class="dt">IO</span> <span class="dt">AppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">  <span class="kw">type</span> <span class="dt">StM</span> <span class="dt">AppM</span> a <span class="fu">=</span> a</a>
<a class="sourceLine" id="cb4-3" data-line-number="3">  liftBaseWith f <span class="fu">=</span> <span class="dt">AppM</span> (liftBaseWith <span class="fu">$</span> \ run <span class="ot">-&gt;</span> f (run <span class="fu">.</span> unAppM))</a>
<a class="sourceLine" id="cb4-4" data-line-number="4">  restoreM <span class="fu">=</span> return</a></code></pre></div>
<h2 id="conclusion">Conclusion</h2>
<p>My theoretical knowledge isn’t anywhere near good enough to say anything objectively about the difference in expressiveness of the two design patterns. That means that my conclusion comes down to taste, do you like the <code>readerT</code> patter or tagless final better?</p>
<p>I like the slightly looser coupling I get with the <code>ReaderT</code> pattern. Loose coupling is (almost) always a desirable goal. However, I can see that tying the typeclass instances directly to a concrete execution type results in the intent being communicated a little more clearly. Clearly communicating intent in code is also a desirable goal. In particular I suspect it’ll result in more actionable error messages when making changes to the code – the error will tell me that my execution type lacks an instance of a specific typeclass, instead of it telling me that a particular transformer stack does. On the other hand, in the <code>ReaderT</code> pattern that stack is very shallow.</p>
<p>One possibility would be that one pattern is better suited for libraries and the other for applications. I don’t think that’s the case though as in both cases the library code would be written in a style that results in typeclass constraints on the caller and providing instances for those typeclasses is roughly an equal amount of work for both styles.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>Please do point out any mistakes I’ve made in this, in particular if they stem from me misunderstanding tagless final completely.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>A missing piece in my Emacs/Spacemacs setup for Haskell development</title>
    <link href="http://magnus.therning.org/posts/2019-01-28-000-a-missing-piece-in-my-emacs-spacemacs-setup-for-haskell-development.html" />
    <id>http://magnus.therning.org/posts/2019-01-28-000-a-missing-piece-in-my-emacs-spacemacs-setup-for-haskell-development.html</id>
    <published>2019-01-28T00:00:00Z</published>
    <updated>2019-01-28T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>With the help of a work mate I’ve finally found this gem that’s been missing from my Spacemacs setup</p>
<pre class="elisp"><code>(with-eval-after-load &#39;intero
  (flycheck-add-next-checker &#39;intero &#39;(warning . haskell-hlint))
  (flycheck-add-next-checker &#39;intero &#39;(warning . haskell-stack-ghc)))</code></pre>]]></summary>
</entry>
<entry>
    <title>Tagless final and Scotty</title>
    <link href="http://magnus.therning.org/posts/2019-01-20-000-tagless-final-and-scotty.html" />
    <id>http://magnus.therning.org/posts/2019-01-20-000-tagless-final-and-scotty.html</id>
    <published>2019-01-20T00:00:00Z</published>
    <updated>2019-01-20T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a little while I’ve been playing around with event sourcing in Haskell using <a href="https://hackage.haskell.org/package/conduit"><code>Conduit</code></a> and <a href="https://hackage.haskell.org/package/scotty"><code>Scotty</code></a>. I’ve come far enough that the basic functionality I’m after is there together with all those little bits that make it a piece of software that’s fit for deployment in production (configuration, logging, etc.). There’s just one thing that’s been nagging me, testability.</p>
<p>The app is built of two main parts, a web server (<code>Scotty</code>) and a pipeline of stream processing components (<code>Conduit</code>). The part using <code>Scotty</code> is utilising a simple monad stack, <code>ReaderT Config IO</code>, and the <code>Conduit</code> part is using <code>Conduit In Out IO</code>. This means that in both parts the outer edge, the part dealing with the outside world, is running in <code>IO</code> directly. Something that isn’t really aiding in testing.</p>
<p>I started out thinking that I’d rewrite what I have using a free monad with a bunch of interpreters. Then I remembered that I have “check out tagless final”. This post is a record of the small experiments I did to see how to use it with <code>Scotty</code> to achieve (and actually improve) on the code I have in my production-ready code.</p>
<h2 id="use-tagless-final-with-scotty">1 - Use tagless final with <code>Scotty</code></h2>
<p>As a first simple little experiment I wrote a tiny little web server that would print a string to <code>stdout</code> when receiving the request to <code>GET /route0</code>.</p>
<p>The printing to <code>stdout</code> is the operation I want to make abstract.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadPrinter</span> m <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="ot">  mPutStrLn ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> m ()</a></code></pre></div>
<p>I then created an application type that is an instance of that class.</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="fu">=</span> <span class="dt">AppM</span> {<span class="ot"> unAppM ::</span> <span class="dt">IO</span> a }</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>)</a>
<a class="sourceLine" id="cb2-3" data-line-number="3"></a>
<a class="sourceLine" id="cb2-4" data-line-number="4"><span class="kw">instance</span> <span class="dt">MonadPrinter</span> <span class="dt">AppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">  mPutStrLn t <span class="fu">=</span> liftIO <span class="fu">$</span> putStrLn (unpack t)</a></code></pre></div>
<p>Then I added a bit of <code>Scotty</code> boilerplate. It’s not strictly necessary, but does make the code a bit nicer to read.</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">type</span> <span class="dt">FooM</span> <span class="fu">=</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> <span class="dt">AppM</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2"><span class="kw">type</span> <span class="dt">FooActionM</span> <span class="fu">=</span> <span class="dt">ActionT</span> <span class="dt">Text</span> <span class="dt">AppM</span></a>
<a class="sourceLine" id="cb3-3" data-line-number="3"></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="ot">foo ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Port</span> <span class="ot">-&gt;</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> <span class="dt">AppM</span> () <span class="ot">-&gt;</span> m ()</a>
<a class="sourceLine" id="cb3-5" data-line-number="5">foo port <span class="fu">=</span> scottyT port unAppM</a></code></pre></div>
<p>With that in place the web server itself is just a matter of tying it all together.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb4-2" data-line-number="2">main <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb4-3" data-line-number="3">  foo <span class="dv">3000</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb4-4" data-line-number="4">    get <span class="st">&quot;/route0&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb4-5" data-line-number="5">      lift <span class="fu">$</span> mPutStrLn <span class="st">&quot;getting /route0&quot;</span></a>
<a class="sourceLine" id="cb4-6" data-line-number="6">      json <span class="fu">$</span> object [<span class="st">&quot;route0&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;ok&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a>
<a class="sourceLine" id="cb4-7" data-line-number="7">    notFound <span class="fu">$</span> json <span class="fu">$</span> object [<span class="st">&quot;error&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;not found&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a></code></pre></div>
<p>That was simple enough.</p>
<h2 id="add-configuration">2 - Add configuration</h2>
<p>In order to try out how to deal with configuration I added a class for doing some simple logging</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="kw">class</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> <span class="dt">MonadLogger</span> m <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2"><span class="ot">  mLog ::</span> <span class="dt">Text</span> <span class="ot">-&gt;</span> m ()</a></code></pre></div>
<p>The straight forward way to deal with configuration is to create a monad stack with <code>ReaderT</code> and since it’s logging I want to do the configuration consists of a single <code>LoggerSet</code> (from <a href="https://hackage.haskell.org/package/fast-logger"><code>fast-logger</code></a>).</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="fu">=</span> <span class="dt">AppM</span> {<span class="ot"> unAppM ::</span> <span class="dt">ReaderT</span> <span class="dt">LoggerSet</span> <span class="dt">IO</span> a }</a>
<a class="sourceLine" id="cb6-2" data-line-number="2">  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> <span class="dt">LoggerSet</span>)</a></code></pre></div>
<p>That means the class instance can be implemented like this</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">MonadLogger</span> <span class="dt">AppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb7-2" data-line-number="2">  mLog msg <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb7-3" data-line-number="3">    ls <span class="ot">&lt;-</span> ask</a>
<a class="sourceLine" id="cb7-4" data-line-number="4">    liftIO <span class="fu">$</span> pushLogStrLn ls <span class="fu">$</span> toLogStr msg</a></code></pre></div>
<p>Of course <code>foo</code> has to be changed too, and it becomes a little easier with a wrapper for <code>runReaderT</code> and <code>unAppM</code>.</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="ot">foo ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">LoggerSet</span> <span class="ot">-&gt;</span> <span class="dt">Port</span> <span class="ot">-&gt;</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> <span class="dt">AppM</span> () <span class="ot">-&gt;</span> m ()</a>
<a class="sourceLine" id="cb8-2" data-line-number="2">foo ls port <span class="fu">=</span> scottyT port (<span class="ot">`runAppM`</span> ls)</a>
<a class="sourceLine" id="cb8-3" data-line-number="3"></a>
<a class="sourceLine" id="cb8-4" data-line-number="4"><span class="ot">runAppM ::</span> <span class="dt">AppM</span> a <span class="ot">-&gt;</span> <span class="dt">LoggerSet</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb8-5" data-line-number="5">runAppM app ls <span class="fu">=</span> runReaderT (unAppM app) ls</a></code></pre></div>
<p>With that in place the printing to <code>stdout</code> can be replaced by a writing to the log.</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb9-2" data-line-number="2">main <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-3" data-line-number="3">  ls <span class="ot">&lt;-</span> newStdoutLoggerSet defaultBufSize</a>
<a class="sourceLine" id="cb9-4" data-line-number="4">  foo ls <span class="dv">3000</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-5" data-line-number="5">    get <span class="st">&quot;/route0&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-6" data-line-number="6">      lift <span class="fu">$</span> mLog <span class="st">&quot;log: getting /route0&quot;</span></a>
<a class="sourceLine" id="cb9-7" data-line-number="7">      json <span class="fu">$</span> object [<span class="st">&quot;route0&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;ok&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a>
<a class="sourceLine" id="cb9-8" data-line-number="8">    notFound <span class="fu">$</span> json <span class="fu">$</span> object [<span class="st">&quot;error&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;not found&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a></code></pre></div>
<p>Not really a big change, I’d say. Extending the configuration is clearly straight forward too.</p>
<h2 id="per-request-configuration">3 - Per-request configuration</h2>
<p>At work we use correlation IDs<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a> and I think that the most convenient way to deal with it is to put the correlation ID into the configuration after extracting it. That is, I want to modify the configuration on each request. Luckily it turns out to be possible to do that, despite using <code>ReaderT</code> for holding the configuration.</p>
<p>I can’t be bothered with a full implementation of correlation ID for this little experiment, but as long as I can get a new <code>AppM</code> by running a function on the configuration it’s just a matter of extracting the correct header from the request. For this experiment it’ll do to just modify an integer in the configuration.</p>
<p>I start with defining a type for the configuration and changing <code>AppM</code>.</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb10-1" data-line-number="1"><span class="kw">type</span> <span class="dt">Config</span> <span class="fu">=</span> (<span class="dt">LoggerSet</span>, <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb10-2" data-line-number="2"></a>
<a class="sourceLine" id="cb10-3" data-line-number="3"><span class="kw">newtype</span> <span class="dt">AppM</span> a <span class="fu">=</span> <span class="dt">AppM</span> {<span class="ot"> unAppM ::</span> <span class="dt">ReaderT</span> <span class="dt">Config</span> <span class="dt">IO</span> a }</a>
<a class="sourceLine" id="cb10-4" data-line-number="4">  <span class="kw">deriving</span> (<span class="dt">Functor</span>, <span class="dt">Applicative</span>, <span class="dt">Monad</span>, <span class="dt">MonadIO</span>, <span class="dt">MonadReader</span> <span class="dt">Config</span>)</a></code></pre></div>
<p>The logger instance has to be changed accordingly of course.</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb11-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">MonadLogger</span> <span class="dt">AppM</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb11-2" data-line-number="2">  mLog msg <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb11-3" data-line-number="3">    (ls, i) <span class="ot">&lt;-</span> ask</a>
<a class="sourceLine" id="cb11-4" data-line-number="4">    liftIO <span class="fu">$</span> pushLogStrLn ls <span class="fu">$</span> toLogStr msg <span class="fu">&lt;&gt;</span> toLogStr (<span class="st">&quot;:&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>) <span class="fu">&lt;&gt;</span> toLogStr (show i)</a></code></pre></div>
<p>The <code>get</code> function that comes with <code>scotty</code> isn’t going to cut it, since it has no way of modifying the configuration, so I’ll need a new one.</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb12-1" data-line-number="1"><span class="ot">mGet ::</span> <span class="dt">ScottyError</span> e <span class="ot">=&gt;</span> <span class="dt">RoutePattern</span> <span class="ot">-&gt;</span> <span class="dt">ActionT</span> e <span class="dt">AppM</span> () <span class="ot">-&gt;</span> <span class="dt">ScottyT</span> e <span class="dt">AppM</span> ()</a>
<a class="sourceLine" id="cb12-2" data-line-number="2">mGet p a <span class="fu">=</span> get p <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb12-3" data-line-number="3">  withCfg (\ (ls, i) <span class="ot">-&gt;</span> (ls, succ i)) a</a></code></pre></div>
<p>The tricky bit is in the <code>withCfg</code> function. It’s indeed not very easy to read, I think</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb13-1" data-line-number="1">withCfg <span class="fu">=</span> mapActionT <span class="fu">.</span> withAppM</a>
<a class="sourceLine" id="cb13-2" data-line-number="2">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb13-3" data-line-number="3">    mapActionT f (<span class="dt">ActionT</span> a) <span class="fu">=</span> <span class="dt">ActionT</span> <span class="fu">$</span> (mapExceptT <span class="fu">.</span> mapReaderT <span class="fu">.</span> mapStateT) f a</a>
<a class="sourceLine" id="cb13-4" data-line-number="4">    withAppM f a <span class="fu">=</span> <span class="dt">AppM</span> <span class="fu">$</span> withReaderT f (unAppM a)</a></code></pre></div>
<p>Basically it reaches into the guts of <code>scotty</code>’s <code>ActionT</code> type (the details are exposed in <code>Web.Scotty.Internal.Types</code>, thanks for not hiding it completely), and modifies the <code>ReaderT Config</code> I’ve supplied.</p>
<p>The new server has two routes, the original one and a new one at <code>GET /route1</code>.</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb14-1" data-line-number="1"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb14-2" data-line-number="2">main <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb14-3" data-line-number="3">  putStrLn <span class="st">&quot;Starting&quot;</span></a>
<a class="sourceLine" id="cb14-4" data-line-number="4">  ls <span class="ot">&lt;-</span> newStdoutLoggerSet defaultBufSize</a>
<a class="sourceLine" id="cb14-5" data-line-number="5">  foo (ls, <span class="dv">0</span>) <span class="dv">3000</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb14-6" data-line-number="6">    get <span class="st">&quot;/route0&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb14-7" data-line-number="7">      lift <span class="fu">$</span> mLog <span class="st">&quot;log: getting /route0&quot;</span></a>
<a class="sourceLine" id="cb14-8" data-line-number="8">      json <span class="fu">$</span> object [<span class="st">&quot;route0&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;ok&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a>
<a class="sourceLine" id="cb14-9" data-line-number="9">    mGet <span class="st">&quot;/route1&quot;</span> <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb14-10" data-line-number="10">      lift <span class="fu">$</span> mLog <span class="st">&quot;log: getting /route1&quot;</span></a>
<a class="sourceLine" id="cb14-11" data-line-number="11">      json <span class="fu">$</span> object [<span class="st">&quot;route1&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;bar&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a>
<a class="sourceLine" id="cb14-12" data-line-number="12">    notFound <span class="fu">$</span> json <span class="fu">$</span> object [<span class="st">&quot;error&quot;</span> <span class="fu">.=</span> (<span class="st">&quot;not found&quot;</span><span class="ot"> ::</span> <span class="dt">String</span>)]</a></code></pre></div>
<p>It’s now easy to verify that the original route, <code>GET /route0</code>, logs a string containing the integer ‘0’, while the new route, <code>GET /route1</code>, logs a string containing the integer ‘1’.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>If you don’t know what it is you’ll find multiple sources by searching for “http correlation-id”. <a href="https://theburningmonk.com/2015/05/a-consistent-approach-to-track-correlation-ids-through-microservices/">A consistent approach to track correlation IDs through microservices</a> is as good a place to start as any.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Systemd for auto deploy from AWS</title>
    <link href="http://magnus.therning.org/posts/2019-01-13-000-systemd-for-auto-deploy-from-aws.html" />
    <id>http://magnus.therning.org/posts/2019-01-13-000-systemd-for-auto-deploy-from-aws.html</id>
    <published>2019-01-13T00:00:00Z</published>
    <updated>2019-01-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Over the last week I’ve completely rebuilt the the only on-premise system we have at work. One of the bits I’m rather happy with is the replacement of <a href="https://mmonit.com/">M/Monit</a> with just plain <a href="https://www.freedesktop.org/wiki/Software/systemd/">systemd</a>. We didn’t use any of the advanced features of M/Monit, we only wanted ordinary process monitoring and restart on a single system. It really was a bit of overkill.</p>
<p>So, instead of</p>
<ul>
<li>using M/Monit to monitor processes</li>
<li>a monit configuration for the app (both start and stop instructions)</li>
<li>a script to start the app and capture its PID in a file</li>
<li>a script to resync the app against the S3 bucket where Travis puts the build artifacts, and if a new version has arrived, remove the PID file thereby triggering M/Monit to restart the app</li>
<li>a crontab entry to run the sync/restart script every few minutes</li>
</ul>
<p>we now have</p>
<ul>
<li>a (simplified) script to start the app<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a></li>
<li>a service unit (<code>app.service</code>) for the app</li>
<li>a timer unit (<code>app-sync.timer</code>) to trigger the resync of the app against the S3 bucket</li>
<li>a oneshot service unit (<code>app-sync.service</code>), triggered by the timer, to perform the sync of the app with the latest build, i.e. call <code>aws s3 sync</code></li>
<li>a path unit (<code>app-restart.path</code>) to monitor one of the build artifacts, i.e. to pick up that a new version has arrived</li>
<li>a oneshot service unit (<code>app-restart.service</code>), triggered by the path unit, calling <code>systemctl restart app.service</code></li>
</ul>
<p>There’s one more piece to the setup, but</p>
<ul>
<li>the start script is simplified since it no longer needs to push things to the background and capture the PID</li>
<li>the sync/restart script is completely gone (arguably the more complicated of the two scripts in the M/Monit setup)</li>
<li>responsibility is cleanly separated leading to a solution that’s easier to understand (as long as you know a bit about systemd of course)</li>
</ul>
<p>so I think it’s a net improvement.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>All it does is set a bunch of environment variables and then start the app, so I’m planning on moving the environment variables into a separate file and put the start command the service unit file instead.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Components feel so not FP</title>
    <link href="http://magnus.therning.org/posts/2019-01-05-000-components-feel-so-not-fp.html" />
    <id>http://magnus.therning.org/posts/2019-01-05-000-components-feel-so-not-fp.html</id>
    <published>2019-01-05T00:00:00Z</published>
    <updated>2019-01-05T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>At work we’re writing most of the new stuff using Clojure. That’s been going on since before I started working there, and from the beginning there’s been exploration of style and libraries, yes, even of frameworks (ugh!). Now there’s discussion of standardising. The discussion is still in its infancy, but it prompted me to start thinking about what I’m come across in the code base we have now, what I like and what I dislike. The first thing that came to mind was how our different services are set up. I mean set up internally. Like, what’s actually in the <code>main</code> function (or in <code>-main</code>, since we’re talking Clojure). In many services we use <a href="https://clojars.org/com.stuartsierra/component">Stuart Sierra’s <code>component</code></a>, in a growing number of services we use <a href="https://clojars.org/integrant"><code>integrant</code></a>, and in one we use <a href="https://clojars.org/mount"><code>mount</code></a>.</p>
<p>The current discussion is going in the direction of <code>integrant</code>, and I don’t like it!</p>
<p>AFAICS <code>integrant</code> suffers from all the same things as <code>component</code> (the author of <code>mount</code> has put it into words better than I ever could in his text on <code>mount</code>‘s <a href="https://github.com/tolitius/mount/blob/master/doc/differences-from-component.md#differences-from-component">differences from component</a>’) plus one more thing to boot: systems are “configured into being”. It’s touted as “data-driven architecture”, I tend to see it as <em>architecture defined in a language separate from my functions</em>. The <code>integrant</code> README says that one of its strengths over <code>component</code> is that</p>
<blockquote>
<p>In Integrant, systems are created from a configuration data structure, typically loaded from an edn resource. The architecture of the application is defined through data, rather than code.</p>
</blockquote>
<p>Somehow that statement makes me think of OO and DI frameworks. I suspect the above paragraph is just as true with the two replacements <code>s/Integrant/Spring/</code> and <code>s/edn/XML/</code>. I’m not convinced this is a strength at all! My experience with DI frameworks in OO is limited (I’ve never used external configuration), but the enduring impression is that it’s unwieldy. In particular it was very far between cause and effect of errors. So far this is true for <code>integrant</code> as well.</p>
<p>Also, it makes me think of “functional in the small, OO in the large”<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a>, which is a comment coming out of the f-sharp world. Maybe there’s a connection here. Maybe “OO in the large” is something that resonates with OO-turned-FP developers. Maybe that means it’s only a quesiton of time (and exposure to FP) before they embrace “functional all the way”? Or, maybe I’m simply missing something crucial.</p>
<p>In any case I’m going to have to take a closer look at <code>mount</code> in the near future. I’ll also have to take a look at its brother <a href="https://github.com/tolitius/yurt"><code>yurt</code></a>, and at its distant cousin <a href="https://github.com/aroemers/mount-lite"><code>mount-lite</code></a>.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p><a href="https://www.johndcook.com/blog/2009/03/23/functional-in-the-small-oo-in-the-large/" class="uri">https://www.johndcook.com/blog/2009/03/23/functional-in-the-small-oo-in-the-large/</a><a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Is this a good way to do JSON validation?</title>
    <link href="http://magnus.therning.org/posts/2018-12-09-000-is-this-a-good-way-to-do-json-validation-.html" />
    <id>http://magnus.therning.org/posts/2018-12-09-000-is-this-a-good-way-to-do-json-validation-.html</id>
    <published>2018-12-09T00:00:00Z</published>
    <updated>2018-12-09T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>At work, where we use Clojure, we’ve been improving our error messages in the public API to</p>
<ol type="1">
<li>return as many errors as possible in a response, and</li>
<li>be in humanly readable English.</li>
</ol>
<p>If one adopts <a href="https://clojuredocs.org/clojure.spec.alpha">spec</a> as we have one gets the former for free, but the output of <code>spec</code> can hardly be called humanly readable. For the latter part we chose to use <a href="https://clojars.org/phrase">phrase</a>.</p>
<p>Given that I’d like to see Haskell used more at work (currently there are 2 minor services written in Haskell and around a score in Clojure) I thought I’d take a look at JSON validation in Haskell. I ended up beig less than impressed. We have at least one great library for parsing JSON, <a href="https://hackage.haskell.org/package/aeson">aeson</a>, but there are probably a few more that I haven’t noticed. It’s of course possible to mix in validation with the parsing, but since parsers, and this is true for <code>aeson</code>’s parser too, tend to be monads and that means that item 1 above, finding as many errors as possible, isn’t on the table.</p>
<p>A quick look at <a href="https://hackage.haskell.org/">Hackage</a> gave that</p>
<ul>
<li>there is a package called <a href="https://hackage.haskell.org/package/aeson-better-errors">aeson-better-errors</a> that looked promising but didn’t fi my needs (I explain at the end why it isn’t passing muster)</li>
<li>the support for <a href="https://json-schema.org/specification.html">JSON Schema</a> is very lacking in Haskell, <a href="https://hackage.haskell.org/package/hjsonschema">hjsonschema</a> is deprecated and <a href="https://hackage.haskell.org/package/aeson-schema">aeson-schema</a> only supports version 3 of the draft (the current version is 7) and the authors claim that that <code>hjsonschema</code> is more moderna and more actively maintained</li>
</ul>
<p>So, a bit disappointed I started playing with the problem myself and found that, just as is stated in the description of the <a href="https://hackage.haskell.org/package/validation">validation</a> library, I want something that’s isomorphic to <code>Either</code> but accumulates on the error side. That is, something like</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">JSONValidationResult</span> <span class="fu">=</span> <span class="dt">JVRInvalid</span> [<span class="dt">JSONValidationFailure</span>]</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">                          <span class="fu">|</span> <span class="dt">JVRValid</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">                          <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb1-4" data-line-number="4"></a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="kw">instance</span> <span class="dt">Semigroup</span> <span class="dt">JSONValidationResult</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">  (<span class="dt">JVRInvalid</span> es0) <span class="fu">&lt;&gt;</span> (<span class="dt">JVRInvalid</span> es1) <span class="fu">=</span> <span class="dt">JVRInvalid</span> <span class="fu">$</span> es0 <span class="fu">&lt;&gt;</span> es1</a>
<a class="sourceLine" id="cb1-7" data-line-number="7">  <span class="dt">JVRValid</span> <span class="fu">&lt;&gt;</span> r <span class="fu">=</span> r</a>
<a class="sourceLine" id="cb1-8" data-line-number="8">  r <span class="fu">&lt;&gt;</span> <span class="dt">JVRValid</span> <span class="fu">=</span> r</a></code></pre></div>
<p>I decided it was all right to limit validation to proper JSON expressions, i.e. a validator could have the type <code>Value -&gt; JSONValidationResult</code>. I want to combine validators so I decided to wrap it in a <code>newtype</code> and write a <code>SemiGroup</code> instance for it as well:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">newtype</span> <span class="dt">JSONValidator</span> <span class="fu">=</span> <span class="dt">JV</span> (<span class="dt">A.Value</span> <span class="ot">-&gt;</span> <span class="dt">JSONValidationResult</span>)</a>
<a class="sourceLine" id="cb2-2" data-line-number="2"></a>
<a class="sourceLine" id="cb2-3" data-line-number="3"><span class="kw">instance</span> <span class="dt">Semigroup</span> <span class="dt">JSONValidator</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4">  (<span class="dt">JV</span> v0) <span class="fu">&lt;&gt;</span> (<span class="dt">JV</span> v1) <span class="fu">=</span> <span class="dt">JV</span> <span class="fu">$</span> \ val <span class="ot">-&gt;</span> v0 val <span class="fu">&lt;&gt;</span> v1 val</a></code></pre></div>
<p>The function to actually run the validation is rather straight forward</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1">runJSONValidator (<span class="dt">JV</span> validator) val <span class="fu">=</span> validator val</a></code></pre></div>
<p>After writing a few validators I realised a few patterns emerged and the following functions simplified things a bit:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1">mapInvalid _ <span class="dt">JVRValid</span> <span class="fu">=</span> <span class="dt">JVRValid</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">mapInvalid f (<span class="dt">JVRInvalid</span> es) <span class="fu">=</span> <span class="dt">JVRInvalid</span> <span class="fu">$</span> map f es</a>
<a class="sourceLine" id="cb4-3" data-line-number="3"></a>
<a class="sourceLine" id="cb4-4" data-line-number="4">valid <span class="fu">=</span> <span class="dt">JVRValid</span></a>
<a class="sourceLine" id="cb4-5" data-line-number="5">invalid s <span class="fu">=</span> <span class="dt">JVRInvalid</span> [<span class="dt">JVFDesc</span> s]</a></code></pre></div>
<p>With this in place I started writing validators for the basic JSON types:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1">isNumber <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb5-2" data-line-number="2">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-3" data-line-number="3">    go (<span class="dt">A.Number</span> _) <span class="fu">=</span> valid</a>
<a class="sourceLine" id="cb5-4" data-line-number="4">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not a number&quot;</span></a>
<a class="sourceLine" id="cb5-5" data-line-number="5"></a>
<a class="sourceLine" id="cb5-6" data-line-number="6">isString <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb5-7" data-line-number="7">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-8" data-line-number="8">    go (<span class="dt">A.String</span> _) <span class="fu">=</span> valid</a>
<a class="sourceLine" id="cb5-9" data-line-number="9">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not a string&quot;</span></a>
<a class="sourceLine" id="cb5-10" data-line-number="10"></a>
<a class="sourceLine" id="cb5-11" data-line-number="11">isBool <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb5-12" data-line-number="12">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-13" data-line-number="13">    go (<span class="dt">A.Bool</span> _) <span class="fu">=</span> valid</a>
<a class="sourceLine" id="cb5-14" data-line-number="14">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not a bool&quot;</span></a>
<a class="sourceLine" id="cb5-15" data-line-number="15"></a>
<a class="sourceLine" id="cb5-16" data-line-number="16">isNull <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb5-17" data-line-number="17">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-18" data-line-number="18">    go <span class="dt">A.Null</span> <span class="fu">=</span> valid</a>
<a class="sourceLine" id="cb5-19" data-line-number="19">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not &#39;null&#39;&quot;</span></a></code></pre></div>
<p>The number type in JSON is a float (well, in <code>aeson</code> it’s a <a href="https://hackage.haskell.org/package/scientific-0.3.6.2/docs/Data-Scientific.html#t:Scientific"><code>Scientific</code></a>), so to check for an integer a bit more than the above is needed</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1">isInt <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb6-2" data-line-number="2">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb6-3" data-line-number="3">    go (<span class="dt">A.Number</span> i) <span class="fu">=</span> <span class="kw">if</span> i <span class="fu">==</span> fromInteger (round i)</a>
<a class="sourceLine" id="cb6-4" data-line-number="4">                      <span class="kw">then</span> valid</a>
<a class="sourceLine" id="cb6-5" data-line-number="5">                      <span class="kw">else</span> invalid <span class="st">&quot;not an integer&quot;</span></a>
<a class="sourceLine" id="cb6-6" data-line-number="6">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not an integer&quot;</span></a></code></pre></div>
<p>as well as functions that check for the presence of a specific key</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1">reqKey n v <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb7-2" data-line-number="2">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb7-3" data-line-number="3">    go (<span class="dt">A.Object</span> obj) <span class="fu">=</span> <span class="kw">case</span> HM.lookup n obj <span class="kw">of</span></a>
<a class="sourceLine" id="cb7-4" data-line-number="4">                          <span class="dt">Nothing</span> <span class="ot">-&gt;</span> invalid <span class="fu">$</span> <span class="st">&quot;required key &#39;&quot;</span> <span class="fu">&lt;&gt;</span> n <span class="fu">&lt;&gt;</span> <span class="st">&quot;&#39; is missing&quot;</span></a>
<a class="sourceLine" id="cb7-5" data-line-number="5">                          <span class="dt">Just</span> val <span class="ot">-&gt;</span> mapInvalid (<span class="dt">JVFPath</span> n) <span class="fu">$</span> runJSONValidator v val</a>
<a class="sourceLine" id="cb7-6" data-line-number="6">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not an object&quot;</span></a>
<a class="sourceLine" id="cb7-7" data-line-number="7">    </a>
<a class="sourceLine" id="cb7-8" data-line-number="8">optKey n v <span class="fu">=</span> <span class="dt">JV</span> go</a>
<a class="sourceLine" id="cb7-9" data-line-number="9">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb7-10" data-line-number="10">    go (<span class="dt">A.Object</span> obj) <span class="fu">=</span> <span class="kw">case</span> HM.lookup n obj <span class="kw">of</span></a>
<a class="sourceLine" id="cb7-11" data-line-number="11">                          <span class="dt">Nothing</span> <span class="ot">-&gt;</span> valid</a>
<a class="sourceLine" id="cb7-12" data-line-number="12">                          <span class="dt">Just</span> val <span class="ot">-&gt;</span> mapInvalid (<span class="dt">JVFPath</span> n) <span class="fu">$</span> runJSONValidator v val</a>
<a class="sourceLine" id="cb7-13" data-line-number="13">    go _ <span class="fu">=</span> invalid <span class="st">&quot;not an object&quot;</span></a></code></pre></div>
<p>With this in place I can now create a validator for a person with a name and an age:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1">vPerson <span class="fu">=</span> reqKey <span class="st">&quot;name&quot;</span> isString <span class="fu">&lt;&gt;</span></a>
<a class="sourceLine" id="cb8-2" data-line-number="2">          reqKey <span class="st">&quot;age&quot;</span> isInt</a></code></pre></div>
<p>and run it on a <code>Value</code>:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="fu">*&gt;</span> runJSONValidator vPerson <span class="fu">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;name\&quot;: \&quot;Alice\&quot;, \&quot;age\&quot;: 32}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</a>
<a class="sourceLine" id="cb9-2" data-line-number="2"><span class="dt">Just</span> <span class="dt">JVRValid</span></a></code></pre></div>
<p>and all failures are picked up</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb10-1" data-line-number="1"><span class="fu">*&gt;</span> runJSONValidator vPerson <span class="fu">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;name\&quot;: \&quot;Alice\&quot;, \&quot;age\&quot;: \&quot;foo\&quot;}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</a>
<a class="sourceLine" id="cb10-2" data-line-number="2"><span class="dt">Just</span> (<span class="dt">JVRInvalid</span> [<span class="dt">JVFPath</span> <span class="st">&quot;age&quot;</span> (<span class="dt">JVFDesc</span> <span class="st">&quot;not an integer&quot;</span>)])</a>
<a class="sourceLine" id="cb10-3" data-line-number="3"></a>
<a class="sourceLine" id="cb10-4" data-line-number="4"><span class="fu">*&gt;</span>runJSONValidator vPerson <span class="fu">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;name\&quot;: \&quot;Alice\&quot;}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</a>
<a class="sourceLine" id="cb10-5" data-line-number="5"><span class="dt">Just</span> (<span class="dt">JVRInvalid</span> [<span class="dt">JVFDesc</span> <span class="st">&quot;required key &#39;age&#39; is missing&quot;</span>])</a>
<a class="sourceLine" id="cb10-6" data-line-number="6"></a>
<a class="sourceLine" id="cb10-7" data-line-number="7">runJSONValidator vPerson <span class="fu">&lt;$&gt;</span> (decode <span class="st">&quot;{\&quot;nam\&quot;: \&quot;Alice\&quot;}&quot;</span><span class="ot"> ::</span> <span class="dt">Maybe</span> <span class="dt">Value</span>)</a>
<a class="sourceLine" id="cb10-8" data-line-number="8"><span class="dt">Just</span> (<span class="dt">JVRInvalid</span> [<span class="dt">JVFDesc</span> <span class="st">&quot;required key &#39;name&#39; is missing&quot;</span>,<span class="dt">JVFDesc</span> <span class="st">&quot;required key &#39;age&#39; is missing&quot;</span>])</a></code></pre></div>
<h2 id="reflections">Reflections</h2>
<ol type="1">
<li>I quickly realised I wanted slightly more complex validation of course, so all the validators for basic JSON types above have a version taking a custom validator of type <code>a -&gt; JSONValidationResult</code> (where <code>a</code> is the Haskell type contained in the particulare <code>Value</code>).</li>
<li>I started out thinking that I want an <code>Applicative</code> for my validations, but slowly I relaxed that to <code>SemiGroup</code>. I’m still not sure about this decision, because I can see a real use of <em>or</em> which I don’t really have now. Maybe that means I should switch back towards <code>Applicative</code>, just so I can implement an <code>Alternative</code> instance for validators.</li>
<li>Well, I simply don’t know if this is even a good way to implement validators. I’d love to hear suggestions both for improvements and for completely different ways of tackling the problems.</li>
<li>I would love to find out that there already is a library that does all this in a much better way. Please point me in its direction!</li>
</ol>
<h2 id="appendix-a-look-at-aeson-better-errors">Appendix: A look at <code>aeson-better-errors</code></h2>
<p>The issue with <code>aeson-better-errors</code> is easiest to illustrate using the same example as in <a href="https://harry.garrood.me/blog/aeson-better-errors/">its announcement</a>:</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb11-1" data-line-number="1"><span class="ot">{-# LANGUAGE OverloadedStrings #-}</span></a>
<a class="sourceLine" id="cb11-2" data-line-number="2"><span class="kw">module</span> <span class="dt">Play</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb11-3" data-line-number="3"></a>
<a class="sourceLine" id="cb11-4" data-line-number="4"><span class="kw">import</span>           <span class="dt">Data.Aeson</span></a>
<a class="sourceLine" id="cb11-5" data-line-number="5"><span class="kw">import</span>           <span class="dt">Data.Aeson.BetterErrors</span></a>
<a class="sourceLine" id="cb11-6" data-line-number="6"></a>
<a class="sourceLine" id="cb11-7" data-line-number="7"><span class="kw">data</span> <span class="dt">Person</span> <span class="fu">=</span> <span class="dt">Person</span> <span class="dt">String</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb11-8" data-line-number="8">  <span class="kw">deriving</span> (<span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb11-9" data-line-number="9"></a>
<a class="sourceLine" id="cb11-10" data-line-number="10"><span class="ot">asPerson ::</span> <span class="dt">Parse</span> e <span class="dt">Person</span></a>
<a class="sourceLine" id="cb11-11" data-line-number="11">asPerson <span class="fu">=</span> <span class="dt">Person</span> <span class="fu">&lt;$&gt;</span> key <span class="st">&quot;name&quot;</span> asString <span class="fu">&lt;*&gt;</span> key <span class="st">&quot;age&quot;</span> asIntegral</a></code></pre></div>
<p>and with this loaded in GHCi (and make sure to either pass <code>-XOverloadedStrings</code> on the command line, or <code>:set -XOverloadedStrings</code> in GHCi itself)</p>
<pre><code>*&gt; parse asPerson &quot;{\&quot;name\&quot;: \&quot;Alice\&quot;, \&quot;age\&quot;: 32}&quot;
Right (Person &quot;Alice&quot; 32)
*&gt; parse asPerson &quot;{\&quot;name\&quot;: \&quot;Alice\&quot;}&quot;
Left (BadSchema [] (KeyMissing &quot;age&quot;))
*&gt; parse asPerson &quot;{\&quot;nam\&quot;: \&quot;Alice\&quot;}&quot;
Left (BadSchema [] (KeyMissing &quot;name&quot;))</code></pre>
<p>Clearly <code>aeson-better-errors</code> isn’t fulfilling the bit about reporting as many errors as possible. Something that I would have realised right away if I had bothered reading its API reference on Hackage a bit more carefully, the parser type <code>ParseT</code> is an instance of <code>Monad</code>!</p>]]></summary>
</entry>
<entry>
    <title>Zipping streams</title>
    <link href="http://magnus.therning.org/posts/2018-10-16-000-zipping-streams.html" />
    <id>http://magnus.therning.org/posts/2018-10-16-000-zipping-streams.html</id>
    <published>2018-10-16T00:00:00Z</published>
    <updated>2018-10-16T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Writing the following is easy after glancing through the documentation for <a href="https://hackage.haskell.org/package/conduit">conduit</a>:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1">foo <span class="fu">=</span> <span class="kw">let</span> src <span class="fu">=</span> mapM_ C.yield [<span class="dv">0</span><span class="fu">..</span><span class="dv">9</span><span class="ot"> ::</span> <span class="dt">Int</span>]</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">          p0 <span class="fu">=</span> CC.map (\ i <span class="ot">-&gt;</span> (<span class="st">&quot;p0&quot;</span>, succ i))</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">          p1 <span class="fu">=</span> CC.filter odd <span class="fu">.|</span> CC.map (\ i <span class="ot">-&gt;</span> (<span class="st">&quot;p1&quot;</span>, i))</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">          p <span class="fu">=</span> C.getZipConduit <span class="fu">$</span> <span class="dt">C.ZipConduit</span> p0 <span class="fu">&lt;*</span> <span class="dt">C.ZipConduit</span> p1</a>
<a class="sourceLine" id="cb1-5" data-line-number="5">          sink <span class="fu">=</span> CC.mapM_ print</a>
<a class="sourceLine" id="cb1-6" data-line-number="6">      <span class="kw">in</span> C.runConduit <span class="fu">$</span> src <span class="fu">.|</span> p <span class="fu">.|</span> sink</a></code></pre></div>
<p>Neither <a href="https://hackage.haskell.org/package/pipes">pipes</a> nor <a href="https://hackage.haskell.org/package/streaming">streaming</a> make it as easy to figure out. I must be missing something! What functions should I be looking at?</p>]]></summary>
</entry>
<entry>
    <title>Picking up new rows in a DB</title>
    <link href="http://magnus.therning.org/posts/2018-10-14-000-picking-up-new-rows-in-a-db.html" />
    <id>http://magnus.therning.org/posts/2018-10-14-000-picking-up-new-rows-in-a-db.html</id>
    <published>2018-10-14T00:00:00Z</published>
    <updated>2018-10-14T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I’ve been playing around a little with <a href="https://hackage.haskell.org/package/postgresql-simple"><code>postgresql-simple</code></a> in combination with <a href="https://hackage.haskell.org/package/streaming"><code>streaming</code></a> for picking up inserted rows in a DB table. What I came up with was</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">eventStreamer ::</span> <span class="dt">Connection</span> <span class="ot">-&gt;</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Stream</span> (<span class="dt">Of</span> (<span class="dt">Int</span>, <span class="dt">Int</span>)) <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">eventStreamer conn ver <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">  res <span class="ot">&lt;-</span> liftIO <span class="fu">$</span> query conn  <span class="st">&quot;SELECT * FROM events WHERE id &gt; (?) ORDER BY id&quot;</span> (<span class="dt">Only</span> ver)</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">  <span class="kw">case</span> res <span class="kw">of</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">    [] <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">      liftIO <span class="fu">$</span> threadDelay <span class="dv">1000000</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7">      eventStreamer conn ver</a>
<a class="sourceLine" id="cb1-8" data-line-number="8">    _ <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">      <span class="kw">let</span> ver&#39; <span class="fu">=</span> maximum <span class="fu">$</span> map fst res</a>
<a class="sourceLine" id="cb1-10" data-line-number="10">      each res</a>
<a class="sourceLine" id="cb1-11" data-line-number="11">      eventStreamer conn ver&#39;</a></code></pre></div>
<p>It seems almost too simple so I’m suspecting I missed something.</p>
<ul>
<li>Am I missing something?</li>
<li>Should I be using <code>streaming</code>, or is <code>pipes</code> or <code>conduit</code> better (in case they are, in what way)?</li>
</ul>]]></summary>
</entry>
<entry>
    <title>Using a configuration in Scotty</title>
    <link href="http://magnus.therning.org/posts/2018-10-01-000-using-a-configuration-in-scotty.html" />
    <id>http://magnus.therning.org/posts/2018-10-01-000-using-a-configuration-in-scotty.html</id>
    <published>2018-10-01T00:00:00Z</published>
    <updated>2018-10-01T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>At work we’re only now getting around to put <a href="https://hilton.org.uk/blog/microservices-correlation-id">correlation IDs</a> into use. We write most our code in Clojure but since I’d really like to use more Haskell at work I thought I’d dive into <a href="https://hackage.haskell.org/package/scotty">Scotty</a> and see how to deal with logging and then especially how to get correlation IDs into the logs.</p>
<h2 id="the-types">The types</h2>
<p>For configuration it decided to use the reader monad inside <code>ActionT</code> from Scotty. Enter Chell:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">type</span> <span class="dt">ChellM</span> c <span class="fu">=</span> <span class="dt">ScottyT</span> <span class="dt">Text</span> (<span class="dt">ReaderT</span> c <span class="dt">IO</span>)</a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="kw">type</span> <span class="dt">ChellActionM</span> c <span class="fu">=</span> <span class="dt">ActionT</span> <span class="dt">Text</span> (<span class="dt">ReaderT</span> c <span class="dt">IO</span>)</a></code></pre></div>
<p>In order to run it I wrote a function corresponding to <code>scotty</code>:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">chell ::</span> c <span class="ot">-&gt;</span> <span class="dt">Port</span> <span class="ot">-&gt;</span> <span class="dt">ChellM</span> () <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">chell cfg port a <span class="fu">=</span> scottyOptsT opts (flip runReaderT cfg) a</a>
<a class="sourceLine" id="cb2-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4">    opts <span class="fu">=</span> def { verbose <span class="fu">=</span> <span class="dv">0</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">               , settings <span class="fu">=</span> (settings def) { settingsPort <span class="fu">=</span> port }</a>
<a class="sourceLine" id="cb2-6" data-line-number="6">               }</a></code></pre></div>
<h2 id="correlation-id">Correlation ID</h2>
<p>To deal with the correlation ID each incoming request should be checked for the HTTP header <code>X-Correlation-Id</code> and if present it should be used during logging. If no such header is present then a new correlation ID should be created. Since it’s per request it feels natural to create a WAI middleware for this.</p>
<p>The easiest way I could come up with was to push the correlation ID into the request’s headers before it’s passed on:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">requestHeaderCorrelationId ::</span> <span class="dt">Request</span> <span class="ot">-&gt;</span> <span class="dt">Maybe</span> <span class="dt">ByteString</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2">requestHeaderCorrelationId <span class="fu">=</span> lookup <span class="st">&quot;X-Correlation-Id&quot;</span> <span class="fu">.</span> requestHeaders</a>
<a class="sourceLine" id="cb3-3" data-line-number="3"></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="ot">correlationId ::</span>  <span class="dt">Middleware</span></a>
<a class="sourceLine" id="cb3-5" data-line-number="5">correlationId app req sendResponse <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-6" data-line-number="6">  u <span class="ot">&lt;-</span> (<span class="ot">randomIO ::</span> <span class="dt">IO</span> <span class="dt">UUID</span>)</a>
<a class="sourceLine" id="cb3-7" data-line-number="7">  <span class="kw">let</span> corrId <span class="fu">=</span> maybe (toASCIIBytes u) id (requestHeaderCorrelationId req)</a>
<a class="sourceLine" id="cb3-8" data-line-number="8">      newHeaders <span class="fu">=</span> (<span class="st">&quot;X-Correlation-Id&quot;</span>, corrId) <span class="fu">:</span> (requestHeaders req)</a>
<a class="sourceLine" id="cb3-9" data-line-number="9">  app (req { requestHeaders <span class="fu">=</span> newHeaders }) <span class="fu">$</span> \ res <span class="ot">-&gt;</span> sendResponse res</a></code></pre></div>
<p>It also turns out to be useful to have both a default correlation ID and a function for pulling it out of the headers:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">defaultCorrelationString ::</span> <span class="dt">ByteString</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">defaultCorrelationString <span class="fu">=</span> <span class="st">&quot;no-correlation-id&quot;</span></a>
<a class="sourceLine" id="cb4-3" data-line-number="3"></a>
<a class="sourceLine" id="cb4-4" data-line-number="4"><span class="ot">getCorrelationId ::</span> <span class="dt">Request</span> <span class="ot">-&gt;</span> <span class="dt">ByteString</span></a>
<a class="sourceLine" id="cb4-5" data-line-number="5">getCorrelationId r <span class="fu">=</span> maybe defaultCorrelationString id (requestHeaderCorrelationId r)</a></code></pre></div>
<h2 id="getting-the-correlation-id-into-the-configuration">Getting the correlation ID into the configuration</h2>
<p>Since the correlation ID should be picked out of the request on handling of every request it’s useful to have it the configuration when running the <code>ChellActionM</code> actions. However, since the correlation ID isn’t available when running the reader (the call to <code>runReaderT</code> in <code>chell</code>) something else is called for. When looking around I found <code>local</code> (and later I was pointed to the more general <code>withReaderT</code>) but it doesn’t have a suitable type. After some <a href="https://twitter.com/EyalL/status/1046696148218580993">help on Twitter</a> I arrived at <code>withConfig</code> which allows me to run an action in a modified configuration:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">withConfig ::</span> (c <span class="ot">-&gt;</span> c&#39;) <span class="ot">-&gt;</span> <span class="dt">ChellActionM</span> c&#39; () <span class="ot">-&gt;</span> <span class="dt">ChellActionM</span> c ()</a>
<a class="sourceLine" id="cb5-2" data-line-number="2">withConfig <span class="fu">=</span> mapActionT <span class="fu">.</span> withReaderT</a>
<a class="sourceLine" id="cb5-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-4" data-line-number="4">    mapActionT f (<span class="dt">ActionT</span> a) <span class="fu">=</span> <span class="dt">ActionT</span> <span class="fu">$</span> (mapExceptT <span class="fu">.</span> mapReaderT <span class="fu">.</span> mapStateT) f a</a></code></pre></div>
<h2 id="making-it-handy-to-use">Making it handy to use</h2>
<p>Armed with this I can put together some functions to replace Scotty’s <code>get</code>, <code>post</code>, etc. With a configuration type like this:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="kw">data</span> <span class="dt">Config</span> <span class="fu">=</span> <span class="dt">Cfg</span> <span class="dt">LoggerSet</span> <span class="dt">ByteString</span></a></code></pre></div>
<p>The modified <code>get</code> looks like this (Scotty’s original is <code>S.get</code>)</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="ot">get ::</span> <span class="dt">RoutePattern</span> <span class="ot">-&gt;</span> <span class="dt">ChellActionM</span> <span class="dt">Config</span> () <span class="ot">-&gt;</span> <span class="dt">ChellM</span> <span class="dt">Config</span> ()</a>
<a class="sourceLine" id="cb7-2" data-line-number="2">get p a <span class="fu">=</span> S.get p <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb7-3" data-line-number="3">  r <span class="ot">&lt;-</span> request</a>
<a class="sourceLine" id="cb7-4" data-line-number="4">  <span class="kw">let</span> corrId <span class="fu">=</span> getCorrelationId r</a>
<a class="sourceLine" id="cb7-5" data-line-number="5">  withConfig (\ (<span class="dt">Cfg</span> l _) <span class="ot">-&gt;</span> <span class="dt">Cfg</span> l corrId) a</a></code></pre></div>
<p>With this in place I can use the simpler <code>ReaderT Config IO</code> for inner functions that need to log.</p>]]></summary>
</entry>
<entry>
    <title>QuickCheck on a REST API</title>
    <link href="http://magnus.therning.org/posts/2018-07-14-000-quickcheck-on-a-rest-api.html" />
    <id>http://magnus.therning.org/posts/2018-07-14-000-quickcheck-on-a-rest-api.html</id>
    <published>2018-07-14T00:00:00Z</published>
    <updated>2018-07-14T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Since I’m working with web stuff nowadays I thought I’d play a little with translating my old post on <a href="/posts/2015-06-15-000-using-quickcheck-to-test-c-apis.html">using QuickCheck to test C APIs</a> to the web.</p>
<h2 id="the-goal-and-how-to-reach-it">The goal and how to reach it</h2>
<p>I want to use <a href="https://hackage.haskell.org/package/QuickCheck">QuickCheck</a> to test a REST API, just like in the case of the C API the idea is to</p>
<ol type="1">
<li>generate a sequence of API calls (a <em>program</em>), then</li>
<li>run the sequence against a model, as well as</li>
<li>run the sequence against the web service, and finally</li>
<li>compare the resulting model against reality.</li>
</ol>
<h2 id="the-rest-api">The REST API</h2>
<p>I’ll use a small web service I’m working on, and then concentrate on only a small part of the API to begin with.</p>
<p>The parts of the API I’ll use for the programs at this stage are</p>
<table>
<colgroup>
<col style="width: 7%" />
<col style="width: 13%" />
<col style="width: 39%" />
<col style="width: 40%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: left;">Method</th>
<th style="text-align: left;">Route</th>
<th style="text-align: left;">Example in</th>
<th style="text-align: left;">Example out</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>POST</code></td>
<td style="text-align: left;"><code>/users</code></td>
<td style="text-align: left;"><code>{&quot;userId&quot;: 0, &quot;userName&quot;: &quot;Yogi Berra&quot;}</code></td>
<td style="text-align: left;"><code>{&quot;userId&quot;: 42, &quot;userName&quot;: &quot;Yogi Berra&quot;}</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>DELETE</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<p>The following API calls will also be used, but not in the programs</p>
<table>
<thead>
<tr class="header">
<th style="text-align: left;">Method</th>
<th style="text-align: left;">Route</th>
<th style="text-align: left;">Example in</th>
<th style="text-align: left;">Example out</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>[0,3,7]</code></td>
</tr>
<tr class="even">
<td style="text-align: left;"><code>GET</code></td>
<td style="text-align: left;"><code>/users/:id</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"><code>{&quot;userId&quot;: 42, &quot;userName&quot;: &quot;Yogi Berra&quot;}</code></td>
</tr>
<tr class="odd">
<td style="text-align: left;"><code>POST</code></td>
<td style="text-align: left;"><code>/reset</code></td>
<td style="text-align: left;"></td>
<td style="text-align: left;"></td>
</tr>
</tbody>
</table>
<h2 id="representing-api-calls">Representing API calls</h2>
<p>Given the information about the API above it seems the following is enough to represent the two calls of interest together with a constructor representing the end of a program</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">ApiCall</span> <span class="fu">=</span> <span class="dt">AddUser</span> <span class="dt">Text</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">             <span class="fu">|</span> <span class="dt">DeleteUser</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">             <span class="fu">|</span> <span class="dt">EndProgram</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">             <span class="kw">deriving</span> (<span class="dt">Show</span>)</a></code></pre></div>
<p>and a program is just a sequence of calls, so list of <code>ApiCall</code> will do. However, since I want to generate sequences of calls, i.e. implement <code>Arbitrary</code>, I’ll wrap it in a <code>newtype</code></p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">newtype</span> <span class="dt">Program</span> <span class="fu">=</span> <span class="dt">Prog</span> [<span class="dt">ApiCall</span>]</a></code></pre></div>
<h2 id="running-against-a-model-simulation">Running against a model (simulation)</h2>
<p>First of all I need to decide what model to use. Based on the part of the API I’m using I’ll use an ordinary dictionary of <code>Int</code> and <code>Text</code></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">type</span> <span class="dt">Model</span> <span class="fu">=</span> <span class="dt">M.Map</span> <span class="dt">Int</span> <span class="dt">Text</span></a></code></pre></div>
<p>Simulating execution of a program is simulating each call against a model that’s updated with each step. I expect the final model to correspond to the state of the real service after the program is run for real. The simulation begins with an empty dictionary.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">simulateProgram ::</span> <span class="dt">Program</span> <span class="ot">-&gt;</span> <span class="dt">Model</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">simulateProgram (<span class="dt">Prog</span> cs) <span class="fu">=</span> foldl simulateCall M.empty cs</a></code></pre></div>
<p>The simulation of the API calls must then be a function taking a model and a call, returning an updated model</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">simulateCall ::</span> <span class="dt">Model</span> <span class="ot">-&gt;</span> <span class="dt">ApiCall</span> <span class="ot">-&gt;</span> <span class="dt">Model</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2">simulateCall m (<span class="dt">AddUser</span> t) <span class="fu">=</span> M.insert k t m</a>
<a class="sourceLine" id="cb5-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-4" data-line-number="4">    k <span class="fu">=</span> succ <span class="fu">$</span> foldl max <span class="dv">0</span> (M.keys m)</a>
<a class="sourceLine" id="cb5-5" data-line-number="5">simulateCall m (<span class="dt">DeleteUser</span> k) <span class="fu">=</span> M.delete k m</a>
<a class="sourceLine" id="cb5-6" data-line-number="6">simulateCall m <span class="dt">EndProgram</span> <span class="fu">=</span> m</a></code></pre></div>
<p>Here I have to make a few assumptions. First, I assume the indeces for the users start on <code>1</code>. Second, that the next index used always is the successor of highest currently used index. We’ll see how well this holds up to reality later on.</p>
<h2 id="running-against-the-web-service">Running against the web service</h2>
<p>Running the program against the actual web service follows the same pattern, but here I’m dealing with the real world, so it’s a little more messy, i.e. <code>IO</code> is involved. First the running of a single call</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="ot">runCall ::</span> <span class="dt">Manager</span> <span class="ot">-&gt;</span> <span class="dt">ApiCall</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb6-2" data-line-number="2">runCall mgr (<span class="dt">AddUser</span> t) <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb6-3" data-line-number="3">  ireq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/users&quot;</span></a>
<a class="sourceLine" id="cb6-4" data-line-number="4">  <span class="kw">let</span> req <span class="fu">=</span> ireq { requestBody <span class="fu">=</span> <span class="dt">RequestBodyLBS</span> (encode <span class="fu">$</span> <span class="dt">User</span> <span class="dv">0</span> t)}</a>
<a class="sourceLine" id="cb6-5" data-line-number="5">  resp <span class="ot">&lt;-</span> httpLbs req mgr</a>
<a class="sourceLine" id="cb6-6" data-line-number="6">  guard (status201 <span class="fu">==</span> responseStatus resp)</a>
<a class="sourceLine" id="cb6-7" data-line-number="7"></a>
<a class="sourceLine" id="cb6-8" data-line-number="8">runCall mgr (<span class="dt">DeleteUser</span> k) <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb6-9" data-line-number="9">  req <span class="ot">&lt;-</span> parseRequest <span class="fu">$</span> <span class="st">&quot;DELETE http://localhost:3000/users/&quot;</span> <span class="fu">++</span> show k</a>
<a class="sourceLine" id="cb6-10" data-line-number="10">  resp <span class="ot">&lt;-</span> httpNoBody req mgr</a>
<a class="sourceLine" id="cb6-11" data-line-number="11">  guard (status200 <span class="fu">==</span> responseStatus resp)</a>
<a class="sourceLine" id="cb6-12" data-line-number="12"></a>
<a class="sourceLine" id="cb6-13" data-line-number="13">runCall _ <span class="dt">EndProgram</span> <span class="fu">=</span> return ()</a></code></pre></div>
<p>The running of a program is slightly more involved. Of course I have to set up the <code>Manager</code> needed for the HTTP calls, but I also need to</p>
<ol type="1">
<li>ensure that the web service is in a well-known state before starting, and</li>
<li>extract the state of the web service after running the program, so I can compare it to the model</li>
</ol>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="ot">runProgram ::</span> <span class="dt">Program</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">Model</span></a>
<a class="sourceLine" id="cb7-2" data-line-number="2">runProgram (<span class="dt">Prog</span> cs) <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb7-3" data-line-number="3">  mgr <span class="ot">&lt;-</span> newManager defaultManagerSettings</a>
<a class="sourceLine" id="cb7-4" data-line-number="4">  resetReq <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;POST http://localhost:3000/reset&quot;</span></a>
<a class="sourceLine" id="cb7-5" data-line-number="5">  httpNoBody resetReq mgr</a>
<a class="sourceLine" id="cb7-6" data-line-number="6">  mapM_ (runCall mgr) cs</a>
<a class="sourceLine" id="cb7-7" data-line-number="7">  model <span class="ot">&lt;-</span> extractModel mgr</a>
<a class="sourceLine" id="cb7-8" data-line-number="8">  return model</a></code></pre></div>
<p>The call to <code>POST /reset</code> resets the web service. I would have liked to simply restart the service completely, but I failed in automating it. I think I’ll have to take a closer look at the implementation of <a href="http://hackage.haskell.org/package/scotty">scotty</a> to find a way.</p>
<p>Extracting the web service state and packaging it in a <code>Model</code> is a matter of calling <code>GET /users</code> and then repeatedly calling <code>GET /users/:id</code> with each <code>id</code> gotten from the first call</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="ot">extractModel ::</span> <span class="dt">Manager</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">Model</span></a>
<a class="sourceLine" id="cb8-2" data-line-number="2">extractModel mgr <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb8-3" data-line-number="3">  req <span class="ot">&lt;-</span> parseRequest <span class="st">&quot;http://localhost:3000/users&quot;</span></a>
<a class="sourceLine" id="cb8-4" data-line-number="4">  resp <span class="ot">&lt;-</span> httpLbs req mgr</a>
<a class="sourceLine" id="cb8-5" data-line-number="5">  <span class="kw">let</span> (<span class="dt">Just</span> ids) <span class="fu">=</span> decode (responseBody resp)<span class="ot"> ::</span> <span class="dt">Maybe</span> [<span class="dt">Int</span>]</a>
<a class="sourceLine" id="cb8-6" data-line-number="6">  users <span class="ot">&lt;-</span> forM ids <span class="fu">$</span> \ id <span class="ot">-&gt;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb8-7" data-line-number="7">    req <span class="ot">&lt;-</span> parseRequest <span class="fu">$</span> <span class="st">&quot;http://localhost:3000/users/&quot;</span> <span class="fu">++</span> show id</a>
<a class="sourceLine" id="cb8-8" data-line-number="8">    resp <span class="ot">&lt;-</span> httpLbs req mgr</a>
<a class="sourceLine" id="cb8-9" data-line-number="9">    <span class="kw">let</span> (<span class="dt">Just</span> (user<span class="fu">:</span>_)) <span class="fu">=</span> decode (responseBody resp)<span class="ot"> ::</span> <span class="dt">Maybe</span> [<span class="dt">User</span>]</a>
<a class="sourceLine" id="cb8-10" data-line-number="10">    return user</a>
<a class="sourceLine" id="cb8-11" data-line-number="11">  return <span class="fu">$</span> foldl (\ map (<span class="dt">User</span> id name) <span class="ot">-&gt;</span> M.insert id name map) M.empty users</a></code></pre></div>
<h2 id="generating-programs">Generating programs</h2>
<p>My approach to generating a program is based on the idea that given a certain state there is only a limited number of possible calls that make sense. Given a model <code>m</code> it makes sense to make one of the following calls:</p>
<ul>
<li>add a new user</li>
<li>delete an existing user</li>
<li>end the program</li>
</ul>
<p>Based on this writing <code>genProgram</code> is rather straight forward</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="ot">genProgram ::</span> <span class="dt">Gen</span> <span class="dt">Program</span></a>
<a class="sourceLine" id="cb9-2" data-line-number="2">genProgram <span class="fu">=</span> <span class="dt">Prog</span> <span class="fu">&lt;$&gt;</span> go M.empty</a>
<a class="sourceLine" id="cb9-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb9-4" data-line-number="4">    possibleAddUser _ <span class="fu">=</span> [<span class="dt">AddUser</span> <span class="fu">&lt;$&gt;</span> arbitrary]</a>
<a class="sourceLine" id="cb9-5" data-line-number="5">    possibleDeleteUser m <span class="fu">=</span> map (return <span class="fu">.</span> <span class="dt">DeleteUser</span>) (M.keys m)</a>
<a class="sourceLine" id="cb9-6" data-line-number="6">    possibleEndProgram _ <span class="fu">=</span> [return <span class="dt">EndProgram</span>]</a>
<a class="sourceLine" id="cb9-7" data-line-number="7"></a>
<a class="sourceLine" id="cb9-8" data-line-number="8">    go m <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-9" data-line-number="9">      <span class="kw">let</span> possibles <span class="fu">=</span> possibleDeleteUser m <span class="fu">++</span> possibleAddUser m <span class="fu">++</span> possibleEndProgram m</a>
<a class="sourceLine" id="cb9-10" data-line-number="10">      s <span class="ot">&lt;-</span> oneof possibles</a>
<a class="sourceLine" id="cb9-11" data-line-number="11">      <span class="kw">let</span> m&#39; <span class="fu">=</span> simulateCall m s</a>
<a class="sourceLine" id="cb9-12" data-line-number="12">      <span class="kw">case</span> s <span class="kw">of</span></a>
<a class="sourceLine" id="cb9-13" data-line-number="13">        <span class="dt">EndProgram</span> <span class="ot">-&gt;</span> return []</a>
<a class="sourceLine" id="cb9-14" data-line-number="14">        _ <span class="ot">-&gt;</span> (s<span class="fu">:</span>) <span class="fu">&lt;$&gt;</span> go m&#39;</a></code></pre></div>
<p>Armed with that the <code>Arbitrary</code> instance for <code>Program</code> can be implemented as<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a></p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb10-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">Arbitrary</span> <span class="dt">Program</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb10-2" data-line-number="2">  arbitrary <span class="fu">=</span> genProgram</a>
<a class="sourceLine" id="cb10-3" data-line-number="3">  shrink p <span class="fu">=</span> []</a></code></pre></div>
<h2 id="the-property-of-an-api">The property of an API</h2>
<p>The steps in the <a href="#the-goal-and-how-to-reach-it">first section</a> can be used as a recipe for writing the property</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb11-1" data-line-number="1"><span class="ot">prop_progCorrectness ::</span> <span class="dt">Program</span> <span class="ot">-&gt;</span> <span class="dt">Property</span></a>
<a class="sourceLine" id="cb11-2" data-line-number="2">prop_progCorrectness program <span class="fu">=</span> monadicIO <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb11-3" data-line-number="3">  <span class="kw">let</span> simulatedModel <span class="fu">=</span> simulateProgram program</a>
<a class="sourceLine" id="cb11-4" data-line-number="4">  runModel <span class="ot">&lt;-</span> run <span class="fu">$</span> runProgram program</a>
<a class="sourceLine" id="cb11-5" data-line-number="5">  assert <span class="fu">$</span> simulatedModel <span class="fu">==</span> runModel</a></code></pre></div>
<h2 id="what-next">What next?</h2>
<p>There are some improvements that I’d like to make:</p>
<ul>
<li>Make the generation of <code>Program</code> better in the sense that the programs become longer. I think this is important as I start tackling larger APIs.</li>
<li>Write an implementation of <code>shrink</code> for <code>Program</code>. With longer programs it’s of course more important to actually implement <code>shrink</code>.</li>
</ul>
<p>I’d love to hear if others are using <a href="https://hackage.haskell.org/package/QuickCheck">QuickCheck</a> to test REST APIs in some way, if anyone has suggestions for improvements, and of course ideas for how to implement <code>shrink</code> in a nice way.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>Yes, I completely skip the issue of shrinking programs at this point. This is OK at this point though, because the generated <code>Programs</code>s do end up to be very short indeed.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>A simple zaw widget for jumping to git projects</title>
    <link href="http://magnus.therning.org/posts/2018-06-19-000-a-simple-zaw-widget.html" />
    <id>http://magnus.therning.org/posts/2018-06-19-000-a-simple-zaw-widget.html</id>
    <published>2018-06-19T00:00:00Z</published>
    <updated>2018-06-19T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>A colleague at work showed me a script he put together to quickly jump to the numerous git projects we work with. It’s based on <code>dmenu</code> and looks rather nice. However, I’d rather have something based on <code>zsh</code>, but when looking around I didn’t find anything that really fit. So, instead I ended up writing a simple widget for <a href="https://github.com/zsh-users/zaw">zaw</a>.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode zsh"><code class="sourceCode zsh"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">function</span><span class="fu"> magus-git-projs()</span> <span class="kw">{</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">    <span class="ot">projs=</span><span class="st">&quot;</span><span class="ot">$(</span><span class="kw">find</span> ~/projs -maxdepth 2 -name .git -exec <span class="kw">dirname</span> <span class="dt">{}</span> <span class="dt">\;</span><span class="ot">)</span><span class="st">&quot;</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    <span class="ot">candidates=(</span>$<span class="dt">{(f)projs}</span><span class="ot">)</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    <span class="ot">actions=(</span>magus-git-projs-cd</a>
<a class="sourceLine" id="cb1-5" data-line-number="5">             magus-git-projs-edit-first)</a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="kw">}</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="kw">function</span><span class="fu"> magus-git-projs-cd()</span> <span class="kw">{</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">    zaw-callback-execute <span class="st">&quot;cd </span><span class="ot">${1}</span><span class="st">&quot;</span></a>
<a class="sourceLine" id="cb1-10" data-line-number="10"><span class="kw">}</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11"></a>
<a class="sourceLine" id="cb1-12" data-line-number="12"><span class="kw">function</span><span class="fu"> magus-git-projs-edit-first()</span> <span class="kw">{</span></a>
<a class="sourceLine" id="cb1-13" data-line-number="13">    zaw-callback-replace-buffer <span class="st">&quot;cd </span><span class="ot">${1}</span><span class="st">&quot;</span></a>
<a class="sourceLine" id="cb1-14" data-line-number="14"><span class="kw">}</span></a>
<a class="sourceLine" id="cb1-15" data-line-number="15"></a>
<a class="sourceLine" id="cb1-16" data-line-number="16">zaw-register-src -n git-projs magus-git-projs</a></code></pre></div>
<p>I then attached bound it like this</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode zsh"><code class="sourceCode zsh"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">bindkey</span> -M vicmd <span class="st">&#39;zgp&#39;</span> zaw-git-projs</a></code></pre></div>]]></summary>
</entry>
<entry>
    <title>On mocks and stubs in python (free monad or interpreter pattern)</title>
    <link href="http://magnus.therning.org/posts/2017-01-31-000-on-mocks-and-stubs-in-python--free-monad-or-interpreter-pattern-.html" />
    <id>http://magnus.therning.org/posts/2017-01-31-000-on-mocks-and-stubs-in-python--free-monad-or-interpreter-pattern-.html</id>
    <published>2017-01-31T00:00:00Z</published>
    <updated>2017-01-31T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>A few weeks ago I watched a video where <a href="https://youtu.be/EaxDl5NPuCA">Ken Scambler talks about mocks and stubs</a>. In particular he talks about how to get rid of them.</p>
<p>One part is about coding IO operatioins as data and using the GoF interpreter pattern to</p>
<p>What he’s talking about is of course free monads, but I feel he’s glossing over a lot of details. Based on some of the questions asked during the talk I think I share that feeling with some people in the audience. Specifically I feel he skipped over the following:</p>
<ul>
<li>How does one <em>actually</em> write such code in a mainstream OO/imperative language?</li>
<li>What’s required of the language in order to allow using the techniques he’s talking about?</li>
<li>Errors tend to break abstractions, so how does one deal with error (i.e. exceptions)?</li>
</ul>
<p>Every time I’ve used mocks and stubs for unit testing I’ve had a feeling that “this can’t be how it’s supposed to be done!” So to me, Ken’s talk offered some hope, and I <em>really</em> want to know how applicable the ideas are in mainstream OO/imperative languages.</p>
<h3 id="the-example">The example</h3>
<p>To play around with this I picked the following function (in Python):</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">def</span> count_chars_of_file(fn):</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">    fd <span class="op">=</span> os.<span class="bu">open</span>(fn, os.O_RDONLY)</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    text <span class="op">=</span> os.read(fd, <span class="dv">10000</span>)</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    n <span class="op">=</span> <span class="bu">len</span>(text)</a>
<a class="sourceLine" id="cb1-5" data-line-number="5">    os.close(fd)</a>
<a class="sourceLine" id="cb1-6" data-line-number="6">    <span class="cf">return</span> n</a></code></pre></div>
<p>It’s small and simple, but I think it suffices to highlight a few important points. So the goal is to rewrite this function such that calls to IO operations (actions) (e.g. <code>os.read</code>) are replaced by data (an instance of some data type) conveying the intent of the operation. This data can later be passed to an <em>interpreter of actions</em>.</p>
<h3 id="thoughts-on-the-execution-of-actions-and-the-interpreter-pattern">Thoughts on the execution of actions and the interpreter pattern</h3>
<p>When reading the examples in the <a href="http://www.oodesign.com/interpreter-pattern.html">description of the interpreter pattern</a> what stands out to me is that they are either</p>
<ol type="1">
<li>a list of expressions, or</li>
<li>a tree of expressions</li>
</ol>
<p>that is passed to an interpreter. Will this do for us when trying to rewrite <code>count_chars_of_file</code>?</p>
<p>No, it won’t! Here’s why:</p>
<ul>
<li>A tree of actions doesn’t really make sense. Our actions are small and simple, they encode the intent of a single IO operation.</li>
<li>A list of actions can’t deal with interspersed non-actions, in this case it’s the line <code>n = len(text)</code> that causes a problem.</li>
</ul>
<p>The interpreter pattern misses something that is crucial in this case: the running of the interpreter must be intermingled with running non-interpreted code. The way I think of it is that not only the action needs to be present and dealt with, but also <em>the rest of the program</em>, that latter thing is commonly called a <em>continuation</em>.</p>
<p>So, can we introduce actions and rewrite <code>count_chars_of_file</code> such that we pause the program when interpretation of an action is required, interpret it, and then resume where we left off?</p>
<p>Sure, but it’s not really idiomatic Python code!</p>
<h3 id="actions-and-continuations">Actions and continuations</h3>
<p>The IO operations (actions) are represented as a named tuple:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb2-1" data-line-number="1">Op <span class="op">=</span> collections.namedtuple(<span class="st">&#39;Op&#39;</span>, [<span class="st">&#39;op&#39;</span>, <span class="st">&#39;args&#39;</span>, <span class="st">&#39;k&#39;</span>])</a></code></pre></div>
<p>and the functions returning actions can then be written as</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">def</span> cps_open(fn, k):</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">    <span class="cf">return</span> Op(<span class="st">&#39;open&#39;</span>, [fn], k)</a>
<a class="sourceLine" id="cb3-3" data-line-number="3"></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="kw">def</span> cps_read(fd, k):</a>
<a class="sourceLine" id="cb3-5" data-line-number="5">    <span class="cf">return</span> Op(<span class="st">&#39;read&#39;</span>, [fd], k)</a>
<a class="sourceLine" id="cb3-6" data-line-number="6"></a>
<a class="sourceLine" id="cb3-7" data-line-number="7"><span class="kw">def</span> cps_close(fd, k):</a>
<a class="sourceLine" id="cb3-8" data-line-number="8">    <span class="cf">return</span> Op(<span class="st">&#39;close&#39;</span>, [fd], k)</a></code></pre></div>
<p>The interpreter is then an <em>if</em> statement checking the value of <code>op.op</code> with each branch executing the IO operation and passing the result to the rest of the program. I decided to wrap it directly in the program runner:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">def</span> runProgram(prog):</a>
<a class="sourceLine" id="cb4-2" data-line-number="2">    <span class="kw">def</span> runOp(op):</a>
<a class="sourceLine" id="cb4-3" data-line-number="3">        <span class="cf">if</span> op.op <span class="op">==</span> <span class="st">&#39;open&#39;</span>:</a>
<a class="sourceLine" id="cb4-4" data-line-number="4">            fd <span class="op">=</span> os.<span class="bu">open</span>(<span class="op">*</span>op.args, os.O_RDONLY)</a>
<a class="sourceLine" id="cb4-5" data-line-number="5">            <span class="cf">return</span> op.k(fd)</a>
<a class="sourceLine" id="cb4-6" data-line-number="6">        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;read&#39;</span>:</a>
<a class="sourceLine" id="cb4-7" data-line-number="7">            text <span class="op">=</span> os.read(<span class="op">*</span>op.args, <span class="dv">10000</span>)</a>
<a class="sourceLine" id="cb4-8" data-line-number="8">            <span class="cf">return</span> op.k(text)</a>
<a class="sourceLine" id="cb4-9" data-line-number="9">        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;close&#39;</span>:</a>
<a class="sourceLine" id="cb4-10" data-line-number="10">            os.close(<span class="op">*</span>op.args)</a>
<a class="sourceLine" id="cb4-11" data-line-number="11">            <span class="cf">return</span> op.k()</a>
<a class="sourceLine" id="cb4-12" data-line-number="12"></a>
<a class="sourceLine" id="cb4-13" data-line-number="13">    <span class="cf">while</span> <span class="bu">isinstance</span>(prog, Op):</a>
<a class="sourceLine" id="cb4-14" data-line-number="14">        prog <span class="op">=</span> runOp(prog)</a>
<a class="sourceLine" id="cb4-15" data-line-number="15"></a>
<a class="sourceLine" id="cb4-16" data-line-number="16">    <span class="cf">return</span> prog</a></code></pre></div>
<p>So far so good, but what will <code>count_char_of_file</code> all of this do to <code>count_chars_of_file</code>?</p>
<p>Well, it’s not quite as easy to read any more (basically it’s rewritten in <a href="https://en.wikipedia.org/wiki/Continuation-passing_style">CPS</a>):</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="kw">def</span> count_chars_of_file(fn):</a>
<a class="sourceLine" id="cb5-2" data-line-number="2"></a>
<a class="sourceLine" id="cb5-3" data-line-number="3">    <span class="kw">def</span> cont_1(text, fd):</a>
<a class="sourceLine" id="cb5-4" data-line-number="4">        n <span class="op">=</span> <span class="bu">len</span>(text)</a>
<a class="sourceLine" id="cb5-5" data-line-number="5">        <span class="cf">return</span> cps_close(fd, <span class="kw">lambda</span> n<span class="op">=</span>n: n)</a>
<a class="sourceLine" id="cb5-6" data-line-number="6"></a>
<a class="sourceLine" id="cb5-7" data-line-number="7">    <span class="kw">def</span> cont_0(fd):</a>
<a class="sourceLine" id="cb5-8" data-line-number="8">        <span class="cf">return</span> cps_read(fd, <span class="kw">lambda</span> text, fd<span class="op">=</span>fd: cont_1(text, fd))</a>
<a class="sourceLine" id="cb5-9" data-line-number="9"></a>
<a class="sourceLine" id="cb5-10" data-line-number="10">    <span class="cf">return</span> cps_open(fn, cont_0)</a></code></pre></div>
<h3 id="generators-to-the-rescue">Generators to the rescue</h3>
<p>Python does have a notion of continuations in the form of generators.<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a> By making <code>count_char_of_file</code> into a generator it’s possible to remove the explicit continuations and the program actually resembles the original one again.</p>
<p>The type for the actions loses one member, and the functions creating them lose an argument:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb6-1" data-line-number="1">Op <span class="op">=</span> collections.namedtuple(<span class="st">&#39;Op&#39;</span>, [<span class="st">&#39;op&#39;</span>, <span class="st">&#39;args&#39;</span>])</a>
<a class="sourceLine" id="cb6-2" data-line-number="2"></a>
<a class="sourceLine" id="cb6-3" data-line-number="3"><span class="kw">def</span> gen_open(fn):</a>
<a class="sourceLine" id="cb6-4" data-line-number="4">    <span class="cf">return</span> Op(<span class="st">&#39;open&#39;</span>, [fn])</a>
<a class="sourceLine" id="cb6-5" data-line-number="5"></a>
<a class="sourceLine" id="cb6-6" data-line-number="6"><span class="kw">def</span> gen_read(fd):</a>
<a class="sourceLine" id="cb6-7" data-line-number="7">    <span class="cf">return</span> Op(<span class="st">&#39;read&#39;</span>, [fd])</a>
<a class="sourceLine" id="cb6-8" data-line-number="8"></a>
<a class="sourceLine" id="cb6-9" data-line-number="9"><span class="kw">def</span> gen_close(fd):</a>
<a class="sourceLine" id="cb6-10" data-line-number="10">    <span class="cf">return</span> Op(<span class="st">&#39;close&#39;</span>, [fd])</a></code></pre></div>
<p>The interpreter and program runner must be modified to step the generator until its end:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="kw">def</span> runProgram(prog):</a>
<a class="sourceLine" id="cb7-2" data-line-number="2">    <span class="kw">def</span> runOp(op):</a>
<a class="sourceLine" id="cb7-3" data-line-number="3">        <span class="cf">if</span> op.op <span class="op">==</span> <span class="st">&#39;open&#39;</span>:</a>
<a class="sourceLine" id="cb7-4" data-line-number="4">            fd <span class="op">=</span> os.<span class="bu">open</span>(op.args[<span class="dv">0</span>], os.O_RDONLY)</a>
<a class="sourceLine" id="cb7-5" data-line-number="5">            <span class="cf">return</span> fd</a>
<a class="sourceLine" id="cb7-6" data-line-number="6">        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;read&#39;</span>:</a>
<a class="sourceLine" id="cb7-7" data-line-number="7">            text <span class="op">=</span> os.read(op.args[<span class="dv">0</span>], <span class="dv">10000</span>)</a>
<a class="sourceLine" id="cb7-8" data-line-number="8">            <span class="cf">return</span> text</a>
<a class="sourceLine" id="cb7-9" data-line-number="9">        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;close&#39;</span>:</a>
<a class="sourceLine" id="cb7-10" data-line-number="10">            os.close(op.args[<span class="dv">0</span>])</a>
<a class="sourceLine" id="cb7-11" data-line-number="11">            <span class="cf">return</span> <span class="va">None</span></a>
<a class="sourceLine" id="cb7-12" data-line-number="12"></a>
<a class="sourceLine" id="cb7-13" data-line-number="13">    <span class="cf">try</span>:</a>
<a class="sourceLine" id="cb7-14" data-line-number="14">        op <span class="op">=</span> prog.send(<span class="va">None</span>)</a>
<a class="sourceLine" id="cb7-15" data-line-number="15">        <span class="cf">while</span> <span class="va">True</span>:</a>
<a class="sourceLine" id="cb7-16" data-line-number="16">            r <span class="op">=</span> runOp(op)</a>
<a class="sourceLine" id="cb7-17" data-line-number="17">            op <span class="op">=</span> prog.send(r)</a>
<a class="sourceLine" id="cb7-18" data-line-number="18">    <span class="cf">except</span> <span class="pp">StopIteration</span> <span class="im">as</span> e:</a>
<a class="sourceLine" id="cb7-19" data-line-number="19">        <span class="cf">return</span> e.value</a></code></pre></div>
<p>Finally, the generator-version of <code>count_chars_of_file</code> goes back to being a bit more readable:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="kw">def</span> count_chars_of_file(fn):</a>
<a class="sourceLine" id="cb8-2" data-line-number="2">    fd <span class="op">=</span> <span class="cf">yield</span> gen_open(fn)</a>
<a class="sourceLine" id="cb8-3" data-line-number="3">    text <span class="op">=</span> <span class="cf">yield</span> gen_read(fd)</a>
<a class="sourceLine" id="cb8-4" data-line-number="4">    n <span class="op">=</span> <span class="bu">len</span>(text)</a>
<a class="sourceLine" id="cb8-5" data-line-number="5">    <span class="cf">yield</span> gen_close(fd)</a>
<a class="sourceLine" id="cb8-6" data-line-number="6">    <span class="cf">return</span> n</a></code></pre></div>
<h3 id="generators-all-the-way">Generators all the way</h3>
<p>Limitations of Python generators mean that we have either have to push the interpreter (<code>runProgram</code>) down to where <code>count_char_of_file</code> is used, or make all intermediate layers into generators and rewrite the interpreter to deal with this. It could look something like this then:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="kw">def</span> runProgram(prog):</a>
<a class="sourceLine" id="cb9-2" data-line-number="2">    <span class="kw">def</span> runOp(op):</a>
<a class="sourceLine" id="cb9-3" data-line-number="3">        <span class="cf">if</span> op.op <span class="op">==</span> <span class="st">&#39;open&#39;</span>:</a>
<a class="sourceLine" id="cb9-4" data-line-number="4">            fd <span class="op">=</span> os.<span class="bu">open</span>(op.args[<span class="dv">0</span>], os.O_RDONLY)</a>
<a class="sourceLine" id="cb9-5" data-line-number="5">            <span class="cf">return</span> fd</a>
<a class="sourceLine" id="cb9-6" data-line-number="6">        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;read&#39;</span>:</a>
<a class="sourceLine" id="cb9-7" data-line-number="7">            text <span class="op">=</span> os.read(op.args[<span class="dv">0</span>], <span class="dv">10000</span>)</a>
<a class="sourceLine" id="cb9-8" data-line-number="8">            <span class="cf">return</span> text</a>
<a class="sourceLine" id="cb9-9" data-line-number="9">        <span class="cf">elif</span> op.op <span class="op">==</span> <span class="st">&#39;close&#39;</span>:</a>
<a class="sourceLine" id="cb9-10" data-line-number="10">            os.close(op.args[<span class="dv">0</span>])</a>
<a class="sourceLine" id="cb9-11" data-line-number="11">            <span class="cf">return</span> <span class="va">None</span></a>
<a class="sourceLine" id="cb9-12" data-line-number="12"></a>
<a class="sourceLine" id="cb9-13" data-line-number="13">    <span class="cf">try</span>:</a>
<a class="sourceLine" id="cb9-14" data-line-number="14">        op <span class="op">=</span> prog.send(<span class="va">None</span>)</a>
<a class="sourceLine" id="cb9-15" data-line-number="15">        <span class="cf">while</span> <span class="va">True</span>:</a>
<a class="sourceLine" id="cb9-16" data-line-number="16">            <span class="cf">if</span> <span class="bu">isinstance</span>(op, Op):</a>
<a class="sourceLine" id="cb9-17" data-line-number="17">                r <span class="op">=</span> runOp(op)</a>
<a class="sourceLine" id="cb9-18" data-line-number="18">                op <span class="op">=</span> prog.send(r)</a>
<a class="sourceLine" id="cb9-19" data-line-number="19">            <span class="cf">elif</span> <span class="bu">isinstance</span>(op, types.GeneratorType):</a>
<a class="sourceLine" id="cb9-20" data-line-number="20">                r <span class="op">=</span> runProgram(op)</a>
<a class="sourceLine" id="cb9-21" data-line-number="21">                op <span class="op">=</span> prog.send(r)</a>
<a class="sourceLine" id="cb9-22" data-line-number="22"></a>
<a class="sourceLine" id="cb9-23" data-line-number="23">    <span class="cf">except</span> <span class="pp">StopIteration</span> <span class="im">as</span> e:</a>
<a class="sourceLine" id="cb9-24" data-line-number="24">        <span class="cf">return</span> e.value</a></code></pre></div>
<h3 id="final-thoughts">Final thoughts</h3>
<p>I think I’ve shown one way to achieve, at least parts of, what Ken talks about. The resulting code looks almost like “normal Python”. There are some things to note:</p>
<ol type="1">
<li>Exception handling is missing. I know of no way to inject an exception into a generator in Python so I’m guessing that exceptions from running the IO operations would have to be passed in via <code>generator.send</code> as a normal value, which means that exception handling code would have to look decidedly non-Pythonic.</li>
<li>Using this approach means the language must have support for generators (or some other way to represent <em>the rest of the program</em>). I think this rules out Java, but probably it can be done in C#.</li>
<li>I’ve only used a single interpreter here, but I see no issues with combining interpreters (to combine domains of operations like <em>file operations</em> and <em>network operations</em>). I also think it’d be possible to use it to realize what De Goes calls <a href="http://degoes.net/articles/modern-fp-part-2#onion-architecture">Onion Architecture</a>.</li>
</ol>
<p>Would I ever advocate this approach for a larger Python project, or even any project in an OO/imperative language?</p>
<p>I’m not sure! I think that testing using mocks and stubs has lead to a smelly code base each an every time I’ve used it, but this approach feels a little too far from how OO/imperative code usually is written. I would love to try it out and see what the implications really are.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>I know, I know, <em>coroutines</em>! I’m simplifying and brushing over details here, but I don’t think I’m brushing over any details that are <em>important</em> for this example.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>That's a large project</title>
    <link href="http://magnus.therning.org/posts/2016-10-21-000-large-project.html" />
    <id>http://magnus.therning.org/posts/2016-10-21-000-large-project.html</id>
    <published>2016-10-21T00:00:00Z</published>
    <updated>2016-10-21T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>From <a href="http://lwn.net/Articles/702177/">LWN October 6, 2016</a>:</p>
<blockquote>
<p>Over the course of the last year, the project accepted about eight changes per hour — every hour — from over 4,000 developers sponsored by over 400 companies.</p>
</blockquote>]]></summary>
</entry>
<entry>
    <title>Free, take 2</title>
    <link href="http://magnus.therning.org/posts/2016-06-18-free--take-2.html" />
    <id>http://magnus.therning.org/posts/2016-06-18-free--take-2.html</id>
    <published>2016-06-18T00:00:00Z</published>
    <updated>2016-06-18T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The other day I read <a href="https://ocharles.org.uk/blog/posts/2016-01-26-transformers-free-monads-mtl-laws.html">a blog post on monads and stuff</a> after which I felt rather silly about my <a href="/posts/2016-01-13-000-free-play--part-one.html">earlier</a> <a href="/posts/2016-01-15-000-free-play--part-two.html">posts</a> on <a href="/posts/2016-01-17-000-free-play--part-three.html">Free</a>.</p>
<p>I think this is probably the post I should have written instead :)</p>
<p>I’ll use three pieces of code, each one builds on the one before:</p>
<ul>
<li><code>Free1.hs</code> - Uses a free monad for a single algebra/API (full code <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free1-hs">here</a>).</li>
<li><code>Free2.hs</code> - Uses a free monad for a two algebras/APIs, where one decorates the other (full code <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free2-hs">here</a>).</li>
<li><code>Free3.hs</code> - Uses a free monad for a three algebras/APIs, where two are used in the program and the remaing one decorates the other two (full code <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free3-hs">here</a>).</li>
</ul>
<h3 id="the-first---one-algebra">The first - one algebra</h3>
<p>I’m re-using the algebras from my previous posts, but I believe it makes it easier to follow along if the amount of jumping between posts is minimized so here is the first one once again:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">SimpleFileF</span> a</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">  <span class="fu">=</span> <span class="dt">LoadFile</span> FilePath (<span class="dt">String</span> <span class="ot">-&gt;</span> a)</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">  <span class="fu">|</span> <span class="dt">SaveFile</span> FilePath <span class="dt">String</span> a</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">  <span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="kw">type</span> <span class="dt">SimpleFileAPI</span> <span class="fu">=</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="ot">loadFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">loadFile fp <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">LoadFile</span> fp id</a>
<a class="sourceLine" id="cb1-10" data-line-number="10"></a>
<a class="sourceLine" id="cb1-11" data-line-number="11"><span class="ot">saveFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> ()</a>
<a class="sourceLine" id="cb1-12" data-line-number="12">saveFile fp d <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">SaveFile</span> fp d ()</a></code></pre></div>
<p>It’s a ridiculously small one, but I believe it’s good enough to work with. In the previous posts I implemented the <code>Functor</code> instances manually. I couldn’t be bothered this time around; I think I pretty much know how to do that for this kind of types now.</p>
<p>Having a type for the algebra, <code>SimpleFileAPI</code>, is convenient already now, even more so in the other examples.</p>
<p>The two convenience functions on the end makes it straight forward to write functions using the algebra:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">withSimpleFile ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> FilePath <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> ()</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">withSimpleFile f fp <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3">  d <span class="ot">&lt;-</span> loadFile fp</a>
<a class="sourceLine" id="cb2-4" data-line-number="4">  <span class="kw">let</span> result <span class="fu">=</span> f d</a>
<a class="sourceLine" id="cb2-5" data-line-number="5">  saveFile (fp <span class="fu">++</span> <span class="st">&quot;_new&quot;</span>) result</a></code></pre></div>
<p>This is simple, straight forward monadic code. Easy to read and work with. Of course it doesn’t actually do anything at all yet. For that I need need an interpreter, something that translates (reduces) the algebra, the API, the commands, call them what you will, into the (side-)effects I want. For <a href="http://hackage.haskell.org/package/free-4.12.4/docs/Control-Monad-Free.html#t:Free"><code>Free</code></a> that is <a href="http://hackage.haskell.org/package/free-4.12.4/docs/Control-Monad-Free.html#v:foldFree"><code>foldFree</code></a> together with a suitable function <code class="sourceCode haskell"><span class="ot">f ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</code>.</p>
<p>I want <code>LoadFile</code> to translate to a file being read and <code>SaveFile</code> to some data being saved to a file. That makes it pretty obvious how that <code>f</code> needs to be written:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">runSimpleFile ::</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">runSimpleFile <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb3-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-4" data-line-number="4">    f (<span class="dt">LoadFile</span> fp f&#39;) <span class="fu">=</span> f&#39; <span class="fu">&lt;$&gt;</span> readFile fp</a>
<a class="sourceLine" id="cb3-5" data-line-number="5">    f (<span class="dt">SaveFile</span> fp d r) <span class="fu">=</span> writeFile fp d <span class="fu">&gt;&gt;</span> return r</a></code></pre></div>
<p>At this point it might be good to explain the constructors of <code>SimpleFileF</code> a bit more. At first I thought they looked a bit funny. I mean, why does <code>SaveFile</code> have an <code>a</code> at all since it obviously always should result in <code>()</code>? And what’s up with that function in <code>Loadfile</code>?</p>
<p>It did become a little clearer to me after some thought and having a look at <code>Free</code>:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">data</span> <span class="dt">Free</span> f a <span class="fu">=</span> <span class="dt">Pure</span> a <span class="fu">|</span> <span class="dt">Free</span> (f (<span class="dt">Free</span> f a))</a></code></pre></div>
<p>I personally find the latter constructor a bit mind-bending. I can handle recursive functions fairly well, but recursive types have a tendency to confuse me. From what I understand one can think of <code>Free</code> as similar to a list. <code>Pure</code> ends the list (<code>Nil</code>) and <code>Free</code> one instance of <code>f</code> to the rest of the list (<code>Cons</code>). Since <code>Free f a</code> is a monad one can think of <code>a</code> as the result of the command.</p>
<p>If I were to write <code>saveFile</code> explicitly it’d look like this</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1">saveFile fp d <span class="fu">=</span> <span class="dt">Free</span> (<span class="dt">SaveFile</span> fp d (<span class="dt">Pure</span> ()))</a></code></pre></div>
<p>and for <code>loadFile</code>:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1">loadFile fp <span class="fu">=</span> <span class="dt">Free</span> (<span class="dt">LoadFile</span> fp (\ s <span class="ot">-&gt;</span> <span class="dt">Pure</span> s))</a></code></pre></div>
<p>But let’s get back to the type and why ‘a’ occurs like it does in the two constructors. As Gabriel G shows in his post <a href="http://www.haskellforall.com/2012/06/you-could-have-invented-free-monads.html">Why free monads matter</a> a constructor without <code>a</code> would result in termination. In other words, if <code>SaveFile</code> didn’t hold an <code>a</code> I’d not be able to write, in a natural way, a function that saves two files.</p>
<p>Another limiting factor is that <code>foldFree</code> of the <code>Free</code> implementation I’m using has the type <code class="sourceCode haskell"><span class="dt">Monad</span> m <span class="ot">=&gt;</span> (forall x<span class="fu">.</span> f x <span class="ot">-&gt;</span> m x) <span class="ot">-&gt;</span> <span class="dt">Free</span> f a <span class="ot">-&gt;</span> m a</code>. This sets a requirement on what the function translating from my API into effects may look like, i.e. what <code>f</code> in <code>runSimpleFile</code> may look like. If <code>SaveFile</code> had no <code>a</code> to return what would <code>f (SaveFile {})</code> return, how could it ever satisfy the required type?</p>
<p>The reason for <code>LoadFile</code> having a function <code>String -&gt; a</code> is simply that there is no data yet, but I still want to be able to manipulate it. Using a function and function composition is the ticket then.</p>
<p>I think that’s all there is to say about to the first piece of code. To run it take a look at the comment at the end of <a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free1-hs">the file</a> and then play with it. If you want to turn all characters of a file <code>foo</code> into upper case you can use</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1">runSimpleFile <span class="fu">$</span> withSimpleFile (map toUpper) <span class="st">&quot;foo&quot;</span></a></code></pre></div>
<h3 id="the-second---two-algebras-one-decorating-the-other">The second - two algebras, one decorating the other</h3>
<p>The second piece of code almost only adds to the first one. There is one exception though, the function <code>runSimpleFile</code> is removed. Instead I’ve taken the transformation function, which used to be called <code>f</code> and was internal to <code>runSimpleFile</code> and moved it out. It’s called <code>stepSimpleFile</code>:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="ot">stepSimpleFile ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb8-2" data-line-number="2">stepSimpleFile (<span class="dt">LoadFile</span> fp f&#39;) <span class="fu">=</span> f&#39; <span class="fu">&lt;$&gt;</span> readFile fp</a>
<a class="sourceLine" id="cb8-3" data-line-number="3">stepSimpleFile (<span class="dt">SaveFile</span> fp d r) <span class="fu">=</span> writeFile fp d <span class="fu">&gt;&gt;</span> return r</a></code></pre></div>
<p>The logging API, <code>LogAPI</code>, follows the same pattern as <code>SimpleFileAPI</code> and I’m counting on the description above being clear enough to not have to repeat myself. For completeness I include the code:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="kw">data</span> <span class="dt">LogF</span> a <span class="fu">=</span> <span class="dt">Log</span> <span class="dt">String</span> a</a>
<a class="sourceLine" id="cb9-2" data-line-number="2">          <span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb9-3" data-line-number="3"></a>
<a class="sourceLine" id="cb9-4" data-line-number="4"><span class="kw">type</span> <span class="dt">LogAPI</span> <span class="fu">=</span> <span class="dt">Free</span> <span class="dt">LogF</span></a>
<a class="sourceLine" id="cb9-5" data-line-number="5"></a>
<a class="sourceLine" id="cb9-6" data-line-number="6"><span class="ot">stepLog ::</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb9-7" data-line-number="7">stepLog (<span class="dt">Log</span> s r) <span class="fu">=</span> putStrLn s <span class="fu">&gt;&gt;</span> return r</a></code></pre></div>
<p>I intend the <code>LogAPI</code> to be used as embellishments on the <code>SimpleFileAPI</code>, in other words I somehow have to turn an operation of <code>SimpleFileAPI</code> into an operation of <code>LogAPI</code>, i.e. I need a transformation. I called it <code>logSimpleFileT</code> and let it turn operations in <code>SimpleFileF</code> (i.e. not exactly <code>SimpleFileAPI</code>) into <code>LogAPI</code> (if you are wondering about my choice of type I hope it’ll become clear below, just trust me for now that this is a good choice):</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb10-1" data-line-number="1"><span class="ot">logSimpleFileT ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">LogAPI</span> ()</a>
<a class="sourceLine" id="cb10-2" data-line-number="2">logSimpleFileT (<span class="dt">LoadFile</span> fp _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** load file &quot;</span> <span class="fu">++</span> fp) ()</a>
<a class="sourceLine" id="cb10-3" data-line-number="3">logSimpleFileT (<span class="dt">SaveFile</span> fp _ _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** save file &quot;</span> <span class="fu">++</span> fp) ()</a></code></pre></div>
<p>So far everything is hopefully very straight forward and unsurprising. Now I need to combine the two APIs, I need to add them, in other words, I need a sum type:</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb11-1" data-line-number="1"><span class="kw">data</span> <span class="dt">S</span> a1 a2 t <span class="fu">=</span> <span class="dt">A1</span> (a1 t) <span class="fu">|</span> <span class="dt">A2</span> (a2 t)</a>
<a class="sourceLine" id="cb11-2" data-line-number="2">              <span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb11-3" data-line-number="3"></a>
<a class="sourceLine" id="cb11-4" data-line-number="4"><span class="kw">type</span> <span class="dt">SumAPI</span> <span class="fu">=</span> <span class="dt">Free</span> (<span class="dt">S</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>)</a></code></pre></div>
<p>The next question is how to turn my two original APIs, <code>SimpleFileAPI</code> and <code>LogAPI</code>, into <code>SumAPI</code>. Luckily that’s already solved by the function <a href="http://hackage.haskell.org/package/free-4.12.4/docs/Control-Monad-Free.html#v:hoistFree"><code>hoistFree</code></a>:</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb12-1" data-line-number="1"><span class="ot">hoistFree ::</span> <span class="dt">Functor</span> g <span class="ot">=&gt;</span> (forall a<span class="fu">.</span> f a <span class="ot">-&gt;</span> g a) <span class="ot">-&gt;</span> <span class="dt">Free</span> f b <span class="ot">-&gt;</span> <span class="dt">Free</span> g b </a></code></pre></div>
<p>With this and <code>logSimpleFileT</code> from above I can use <code>foldFree</code> to decorate each operation with a logging operation like this:</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb13-1" data-line-number="1"><span class="ot">logSimpleFile ::</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> a</a>
<a class="sourceLine" id="cb13-2" data-line-number="2">logSimpleFile <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb13-3" data-line-number="3"><span class="kw">where</span></a>
<a class="sourceLine" id="cb13-4" data-line-number="4">  f op <span class="fu">=</span> hoistFree <span class="dt">A1</span> (logSimpleFileT op) <span class="fu">*&gt;</span> hoistFree <span class="dt">A2</span> (liftF op)</a></code></pre></div>
<p>This is where the type of <code>logSimpleFileT</code> hopefully makes sense!</p>
<p>Just as in the first section of this post, I need an interpreter for my API (<code>SumAPI</code> this time). Once again it’s written using <code>foldFree</code>, but this time I provide the interpreters for the sub-algegras (what I’ve chosen to call step functions):</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb14-1" data-line-number="1"><span class="ot">runSum ::</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> (forall a<span class="fu">.</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> m a) <span class="ot">-&gt;</span> (forall a<span class="fu">.</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> m a) <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> b <span class="ot">-&gt;</span> m b</a>
<a class="sourceLine" id="cb14-2" data-line-number="2">runSum f1 f2 <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb14-3" data-line-number="3"><span class="kw">where</span></a>
<a class="sourceLine" id="cb14-4" data-line-number="4">  f (<span class="dt">A1</span> op) <span class="fu">=</span> f1 op</a>
<a class="sourceLine" id="cb14-5" data-line-number="5">  f (<span class="dt">A2</span> op) <span class="fu">=</span> f2 op</a></code></pre></div>
<p><a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free2-hs">The file</a> has a comment at the end for how to run it. The same example as in the previous section, but now with logging, looks like this</p>
<div class="sourceCode" id="cb15"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb15-1" data-line-number="1">runSum stepLog stepSimpleFile <span class="fu">$</span> logSimpleFile <span class="fu">$</span> withSimpleFile (map toUpper) <span class="st">&quot;foo&quot;</span></a></code></pre></div>
<h3 id="the-third---three-algebras-one-decorating-the-other-two">The third - three algebras, one decorating the other two</h3>
<p>To combine three algebras I simply take what’s in the previous section and extend it, i.e. a sum type with three constructors:</p>
<div class="sourceCode" id="cb16"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb16-1" data-line-number="1"><span class="kw">data</span> <span class="dt">S</span> a1 a2 a3 t <span class="fu">=</span> <span class="dt">A1</span> (a1 t) <span class="fu">|</span> <span class="dt">A2</span> (a2 t) <span class="fu">|</span> <span class="dt">A3</span> (a3 t)</a>
<a class="sourceLine" id="cb16-2" data-line-number="2">                <span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb16-3" data-line-number="3"></a>
<a class="sourceLine" id="cb16-4" data-line-number="4"><span class="kw">type</span> <span class="dt">SumAPI</span> <span class="fu">=</span> <span class="dt">Free</span> (<span class="dt">S</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span> <span class="dt">StdIoF</span>)</a>
<a class="sourceLine" id="cb16-5" data-line-number="5"></a>
<a class="sourceLine" id="cb16-6" data-line-number="6"><span class="ot">runSum ::</span> <span class="dt">Monad</span> m <span class="ot">=&gt;</span> (forall a<span class="fu">.</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> m a)</a>
<a class="sourceLine" id="cb16-7" data-line-number="7">      <span class="ot">-&gt;</span> (forall a<span class="fu">.</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> m a)</a>
<a class="sourceLine" id="cb16-8" data-line-number="8">      <span class="ot">-&gt;</span> (forall a<span class="fu">.</span> <span class="dt">StdIoF</span> a <span class="ot">-&gt;</span> m a)</a>
<a class="sourceLine" id="cb16-9" data-line-number="9">      <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> b <span class="ot">-&gt;</span> m b</a>
<a class="sourceLine" id="cb16-10" data-line-number="10">runSum f1 f2 f3 <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb16-11" data-line-number="11"><span class="kw">where</span></a>
<a class="sourceLine" id="cb16-12" data-line-number="12">  f (<span class="dt">A1</span> op) <span class="fu">=</span> f1 op</a>
<a class="sourceLine" id="cb16-13" data-line-number="13">  f (<span class="dt">A2</span> op) <span class="fu">=</span> f2 op</a>
<a class="sourceLine" id="cb16-14" data-line-number="14">  f (<span class="dt">A3</span> op) <span class="fu">=</span> f3 op</a></code></pre></div>
<p>With this I’ve already revealed that my three APIs are the two from previous sections, <code>LogAPI</code> (for decorating the other APIs), <code>SimpleFileAPI</code> and a new one, <code>StdIoAPI</code>.</p>
<p>I want to combine them in such a wat that I can write functions using both APIs at the same time. Then I modify <code>withSimpleFile</code> into</p>
<div class="sourceCode" id="cb17"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb17-1" data-line-number="1"><span class="ot">withSimpleFile ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> FilePath <span class="ot">-&gt;</span>  <span class="dt">SumAPI</span> ()</a>
<a class="sourceLine" id="cb17-2" data-line-number="2">withSimpleFile f fp <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb17-3" data-line-number="3">  d <span class="ot">&lt;-</span> loadFile fp</a>
<a class="sourceLine" id="cb17-4" data-line-number="4">  <span class="kw">let</span> result <span class="fu">=</span> f d</a>
<a class="sourceLine" id="cb17-5" data-line-number="5">  saveFile (fp <span class="fu">++</span> <span class="st">&quot;_new&quot;</span>) result</a></code></pre></div>
<p>and I can add another function that uses it with <code>StdIoAPI</code>:</p>
<div class="sourceCode" id="cb18"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb18-1" data-line-number="1"><span class="ot">prog ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> ()</a>
<a class="sourceLine" id="cb18-2" data-line-number="2">prog fn <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb18-3" data-line-number="3">  stdioPut <span class="st">&quot;About to start&quot;</span></a>
<a class="sourceLine" id="cb18-4" data-line-number="4">  withSimpleFile (map toUpper) fn</a>
<a class="sourceLine" id="cb18-5" data-line-number="5">  stdioPut <span class="st">&quot;Done!&quot;</span></a></code></pre></div>
<p>The way to allow the APIs to be combined this way is to bake in <code>S</code> already in the convenience functions. This means the code for <code>SimpleFileAPI</code> has to change slightly (note the use of <code>A2</code> in <code>loadFile</code> and <code>saveFile</code>):</p>
<div class="sourceCode" id="cb19"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb19-1" data-line-number="1"><span class="kw">data</span> <span class="dt">SimpleFileF</span> a</a>
<a class="sourceLine" id="cb19-2" data-line-number="2"><span class="fu">=</span> <span class="dt">LoadFile</span> FilePath (<span class="dt">String</span> <span class="ot">-&gt;</span> a)</a>
<a class="sourceLine" id="cb19-3" data-line-number="3"><span class="fu">|</span> <span class="dt">SaveFile</span> FilePath <span class="dt">String</span> a</a>
<a class="sourceLine" id="cb19-4" data-line-number="4"><span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb19-5" data-line-number="5"></a>
<a class="sourceLine" id="cb19-6" data-line-number="6"><span class="ot">loadFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb19-7" data-line-number="7">loadFile fp <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">A2</span> <span class="fu">$</span> <span class="dt">LoadFile</span> fp id</a>
<a class="sourceLine" id="cb19-8" data-line-number="8"></a>
<a class="sourceLine" id="cb19-9" data-line-number="9"><span class="ot">saveFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> ()</a>
<a class="sourceLine" id="cb19-10" data-line-number="10">saveFile fp d <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">A2</span> <span class="fu">$</span> <span class="dt">SaveFile</span> fp d ()</a>
<a class="sourceLine" id="cb19-11" data-line-number="11"></a>
<a class="sourceLine" id="cb19-12" data-line-number="12"><span class="ot">stepSimpleFile ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb19-13" data-line-number="13">stepSimpleFile (<span class="dt">LoadFile</span> fp f&#39;) <span class="fu">=</span> f&#39; <span class="fu">&lt;$&gt;</span> readFile fp</a>
<a class="sourceLine" id="cb19-14" data-line-number="14">stepSimpleFile (<span class="dt">SaveFile</span> fp d r) <span class="fu">=</span> writeFile fp d <span class="fu">&gt;&gt;</span> return r</a></code></pre></div>
<p>The new API, <code>StdIoAPI</code>, has only one operation:</p>
<div class="sourceCode" id="cb20"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb20-1" data-line-number="1"><span class="kw">data</span> <span class="dt">StdIoF</span> a <span class="fu">=</span> <span class="dt">PutStrLn</span> <span class="dt">String</span> a</a>
<a class="sourceLine" id="cb20-2" data-line-number="2">              <span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb20-3" data-line-number="3"></a>
<a class="sourceLine" id="cb20-4" data-line-number="4"><span class="ot">stdioPut ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> ()</a>
<a class="sourceLine" id="cb20-5" data-line-number="5">stdioPut s <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">A3</span> <span class="fu">$</span> <span class="dt">PutStrLn</span> s ()</a>
<a class="sourceLine" id="cb20-6" data-line-number="6"></a>
<a class="sourceLine" id="cb20-7" data-line-number="7"><span class="ot">stepStdIo ::</span> <span class="dt">StdIoF</span> b <span class="ot">-&gt;</span> <span class="dt">IO</span> b</a>
<a class="sourceLine" id="cb20-8" data-line-number="8">stepStdIo (<span class="dt">PutStrLn</span> s a) <span class="fu">=</span> putStrLn s <span class="fu">&gt;&gt;</span> return a</a></code></pre></div>
<p>The logging API, <code>LogAPI</code>, looks exactly the same but I now need two transformation functions, one for <code>SimpleFileAPI</code> and one for <code>StdIoAPI</code>.</p>
<div class="sourceCode" id="cb21"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb21-1" data-line-number="1"><span class="kw">data</span> <span class="dt">LogF</span> a <span class="fu">=</span> <span class="dt">Log</span> <span class="dt">String</span> a</a>
<a class="sourceLine" id="cb21-2" data-line-number="2">            <span class="kw">deriving</span>(<span class="dt">Functor</span>)</a>
<a class="sourceLine" id="cb21-3" data-line-number="3"></a>
<a class="sourceLine" id="cb21-4" data-line-number="4"><span class="kw">type</span> <span class="dt">LogAPI</span> <span class="fu">=</span> <span class="dt">Free</span> <span class="dt">LogF</span></a>
<a class="sourceLine" id="cb21-5" data-line-number="5"></a>
<a class="sourceLine" id="cb21-6" data-line-number="6"><span class="ot">stepLog ::</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb21-7" data-line-number="7">stepLog (<span class="dt">Log</span> s r) <span class="fu">=</span> putStrLn s <span class="fu">&gt;&gt;</span> return r</a>
<a class="sourceLine" id="cb21-8" data-line-number="8"></a>
<a class="sourceLine" id="cb21-9" data-line-number="9"><span class="ot">logSimpleFileT ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">LogAPI</span> ()</a>
<a class="sourceLine" id="cb21-10" data-line-number="10">logSimpleFileT (<span class="dt">LoadFile</span> fp _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** load file &quot;</span> <span class="fu">++</span> fp) ()</a>
<a class="sourceLine" id="cb21-11" data-line-number="11">logSimpleFileT (<span class="dt">SaveFile</span> fp _ _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** save file &quot;</span> <span class="fu">++</span> fp) ()</a>
<a class="sourceLine" id="cb21-12" data-line-number="12"></a>
<a class="sourceLine" id="cb21-13" data-line-number="13"><span class="ot">logStdIoT ::</span> <span class="dt">StdIoF</span> a <span class="ot">-&gt;</span> <span class="dt">LogAPI</span> ()</a>
<a class="sourceLine" id="cb21-14" data-line-number="14">logStdIoT (<span class="dt">PutStrLn</span> s _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** on stdio &quot;</span> <span class="fu">++</span> s) ()</a></code></pre></div>
<p>The new version of <code>logT</code> needs to operate on <code>S</code> in order to decorate both APIs.</p>
<div class="sourceCode" id="cb22"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb22-1" data-line-number="1"><span class="ot">logT ::</span> <span class="dt">SumAPI</span> a <span class="ot">-&gt;</span> <span class="dt">SumAPI</span> a</a>
<a class="sourceLine" id="cb22-2" data-line-number="2">logT <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb22-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb22-4" data-line-number="4">    f (<span class="dt">A2</span> op) <span class="fu">=</span> hoistFree <span class="dt">A1</span> (logSimpleFileT op) <span class="fu">*&gt;</span> hoistFree <span class="dt">A2</span> (liftF op)</a>
<a class="sourceLine" id="cb22-5" data-line-number="5">    f (<span class="dt">A3</span> op) <span class="fu">=</span> hoistFree <span class="dt">A1</span> (logStdIoT op) <span class="fu">*&gt;</span> hoistFree <span class="dt">A3</span> (liftF op)</a>
<a class="sourceLine" id="cb22-6" data-line-number="6">    f a<span class="fu">@</span>(<span class="dt">A1</span> _) <span class="fu">=</span> liftF a</a></code></pre></div>
<p><a href="https://gist.github.com/magthe/9fd7fa6c0a5a5b63cdc84d5c81cc50b5#file-free3-hs">This file</a> also has comments on how to run it at the end. This time there are two examples, one on how to run it without logging</p>
<div class="sourceCode" id="cb23"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb23-1" data-line-number="1">runSum undefined stepSimpleFile stepStdIo <span class="fu">$</span> prog <span class="st">&quot;foo&quot;</span></a></code></pre></div>
<p>and one with logging</p>
<div class="sourceCode" id="cb24"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb24-1" data-line-number="1">runSum stepLog stepSimpleFile stepStdIo (logT <span class="fu">$</span> prog <span class="st">&quot;foo&quot;</span>)</a></code></pre></div>]]></summary>
</entry>
<entry>
    <title>CMake, ExternalData, and custom fetch script</title>
    <link href="http://magnus.therning.org/posts/2016-05-17-000-cmake--externaldata--and-custom-fetch-script.html" />
    <id>http://magnus.therning.org/posts/2016-05-17-000-cmake--externaldata--and-custom-fetch-script.html</id>
    <published>2016-05-17T00:00:00Z</published>
    <updated>2016-05-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I failed to find a concrete example on how to use the <a href="https://cmake.org/documentation/">CMake</a> module <a href="https://cmake.org/cmake/help/v3.5/module/ExternalData.html">ExternalData</a> with a custom fetch script. Since I finally manage to work out how to use it I thought I’d try to help out the next person who needs to go down this route.</p>
<h3 id="why-externaldata">Why ExternalData?</h3>
<p>I thought I’d start with a short justification of why I was looking at the module at all.</p>
<p>At work I work with a product that processes images and video. When writing tests we often need some rather large files (from MiB to GiB) as input. The two obvious options are:</p>
<ol type="1">
<li>Check the files into our Git repo, or</li>
<li>Put them on shared storage</li>
</ol>
<p>Neither of these are very appealing. The former just doesn’t feel quite right, these are large binary files that rarely, if ever, change, why place them under version control at all? And if they do change the Git repo is likely to balloon in size and impact cloning times negatively. The latter makes it difficult to run our tests on a machine that isn’t on the office network and any changes to the files will break older tests, unless we always only add files, never modify any in place. On the other hand, the former guarantees that the files needed for testing are always available and it is possible to modify the files without breaking older tests. The pro of the latter is that we only download the files needed for the current tests.</p>
<p>ExternalData is <em>one</em> option to address this. On some level it feels like it offers a combination of both options above:</p>
<ul>
<li>It’s possible to use the shared storage</li>
<li>When the shared storage isn’t available it’s possible to fall back on downloading the files via other means</li>
<li>The layout of the storage is such that modifying in place is much less likely</li>
<li>Only the files needed for the currest tests will be downloaded when building off-site</li>
</ul>
<h3 id="the-object-store">The object store</h3>
<p>We do our building in <a href="https://www.docker.com/">docker</a> images that do have our shared storage mapped in, so I’d like them to take advantage of that. At the same time I want the builds performed off-site to download the files. To get this behaviour I defined two object stores:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode cmake"><code class="sourceCode cmake"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">set</span>(<span class="dv">ExternalData_OBJECT_STORES</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">  <span class="dv">${CMAKE_BINARY_DIR}</span>/ExternalData/Objects</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">  /mnt/shared/over/nfs/Objects</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">  )</a></code></pre></div>
<p>The module will search each of these for the required files and download only if they aren’t found. Downloaded files will be put into the first of the stores. Oh, and it’s <em>very</em> important that the first store is given with an absolute path!</p>
<p>The store on the shared storage looks something like this:</p>
<pre><code>/mnt/share/over/nfs/Objects
└── MD5
    ├── 94ed17f9b6c74a732fba7b243ab945ff
    └── a2036177b190fbee6e9e038b718f1c20</code></pre>
<p>I can then drop a file <code>MyInput.avi.md5</code> in my source tree with the md5 of the real file (e.g. <code>a2036177b190fbee6e9e038b718f1c20</code>) as the content. Once that is done I can follow the example found in the <a href="https://cmake.org/cmake/help/v3.5/module/ExternalData.html#introduction">introduction</a> of the reference documentation.</p>
<h3 id="curl-vs-sftp">curl vs sftp</h3>
<p>So far so good. This now works on-site, but for off-site use I need to fetch the needed files. The last section of the reference documentation is called <a href="https://cmake.org/cmake/help/v3.5/module/ExternalData.html#custom-fetch-scripts">Custom Fetch Scripts</a>. It mentions that files are normally downloaded using <code>file(DOWNLOAD)</code>. Neither there, nor in the <a href="https://cmake.org/cmake/help/v3.5/command/file.html#command:file">documentation for file</a> is there a mention of what is used under the hood to fetch the files. After asking on in <code>#cmake</code> I found out that it’s <a href="https://curl.haxx.se/">curl</a>. While <code>curl</code> does handle SFTP I didn’t get it to work with my <code>known_hosts</code> file, nor with my SSH agent (both from <a href="http://www.openssh.com/">OpenSSH</a>). On the other hand it was rather easy to configure <code>sftp</code> to fetch a file from the internet-facing SSH server we have. Now I just had to hook it into CMake somehow.</p>
<h3 id="custom-fetch-script">Custom fetch script</h3>
<p>As the section on “Custom Fetch Scripts” mention three things are needed:</p>
<ol type="1">
<li>Specify the script via the <code>ExternalDataCustomScript://</code> protocol.</li>
<li>Tell CMake where it can find the fetch script.</li>
<li>The fetch script itself.</li>
</ol>
<p>The first two steps are done by providing a URL template and pointing to the script via a special variable:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode cmake"><code class="sourceCode cmake"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">set</span>(<span class="dv">ExternalData_URL_TEMPLATES</span> <span class="st">&quot;ExternalDataCustomScript://sftp/mnt/shared/over/nfs/Objects/%(algo)/%(hash)&quot;</span>)</a>
<a class="sourceLine" id="cb3-2" data-line-number="2"><span class="kw">set</span>(<span class="dv">ExternalData_CUSTOM_SCRIPT_sftp</span> <span class="dv">${CMAKE_SOURCE_DIR}</span>/cmake/FetchFromSftp.cmake)</a></code></pre></div>
<p>It took me a ridiculous amount of time to work out how to write a script that turns out to be rather short. This is an experience that seems to repeat itself when using CMake; it could say something about me, or something about CMake.</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode cmake"><code class="sourceCode cmake"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">get_filename_component</span>(FFS_ObjStoreDir <span class="dv">${ExternalData_CUSTOM_FILE}</span> <span class="ot">DIRECTORY</span>)</a>
<a class="sourceLine" id="cb4-2" data-line-number="2"><span class="kw">get_filename_component</span>(FFS_InputFilename <span class="dv">${ExternalData_CUSTOM_LOCATION}</span> <span class="ot">NAME</span>)</a>
<a class="sourceLine" id="cb4-3" data-line-number="3"><span class="kw">get_filename_component</span>(FFS_OutputFilename <span class="dv">${ExternalData_CUSTOM_FILE}</span> <span class="ot">NAME</span>)</a>
<a class="sourceLine" id="cb4-4" data-line-number="4"></a>
<a class="sourceLine" id="cb4-5" data-line-number="5"><span class="kw">execute_process</span>(<span class="ot">COMMAND</span> sftp sftp.company.com:/<span class="dv">${ExternalData_CUSTOM_LOCATION}</span></a>
<a class="sourceLine" id="cb4-6" data-line-number="6">  <span class="ot">RESULT_VARIABLE</span> FFS_SftpResult</a>
<a class="sourceLine" id="cb4-7" data-line-number="7">  <span class="ot">OUTPUT_QUIET</span></a>
<a class="sourceLine" id="cb4-8" data-line-number="8">  <span class="ot">ERROR_VARIABLE</span> FFS_SftpErr</a>
<a class="sourceLine" id="cb4-9" data-line-number="9">  )</a>
<a class="sourceLine" id="cb4-10" data-line-number="10"></a>
<a class="sourceLine" id="cb4-11" data-line-number="11"><span class="kw">if</span>(FFS_SftpResult)</a>
<a class="sourceLine" id="cb4-12" data-line-number="12">  <span class="kw">set</span>(<span class="dv">ExternalData_CUSTOM_ERROR</span> <span class="st">&quot;Failed to fetch from SFTP - </span><span class="dv">${</span>FFS_SftpErr<span class="dv">}</span><span class="st">&quot;</span>)</a>
<a class="sourceLine" id="cb4-13" data-line-number="13"><span class="kw">else</span>(FFS_SftpResult)</a>
<a class="sourceLine" id="cb4-14" data-line-number="14">  <span class="kw">file</span>(<span class="ot">MAKE_DIRECTORY</span> <span class="dv">${</span>FFS_ObjStoreDir<span class="dv">}</span>)</a>
<a class="sourceLine" id="cb4-15" data-line-number="15">  <span class="kw">file</span>(<span class="ot">RENAME</span> <span class="dv">${</span>FFS_InputFilename<span class="dv">}</span> <span class="dv">${</span>FFS_ObjStoreDir<span class="dv">}</span>/<span class="dv">${</span>FFS_OutputFilename<span class="dv">}</span>)</a>
<a class="sourceLine" id="cb4-16" data-line-number="16"><span class="kw">endif</span>(FFS_SftpResult)</a></code></pre></div>
<p>This script is run with <code>cmake -P</code> in the binary dir of the <code>CMakeLists.txt</code> where the test is defined, which means it’s oblivious about the project it’s part of. <code>PROJECT_BINARY_DIR</code> is empty and <code>CMAKE_BINARY_DIR</code> is the same as <code>CMAKE_CURRENT_BINARY_DIR</code>. This is the reason why the first store in <code>ExternalData_OBJECT_STORES</code> has to be an absolute path – it’s very difficult, if not impossible, to find the correct placement of the object store otherwise.</p>]]></summary>
</entry>
<entry>
    <title>Qt5+D-Bus+CMake, a complete example</title>
    <link href="http://magnus.therning.org/posts/2016-04-07-000-qt5%2Bdbus%2Bcmake--a-complete-example.html" />
    <id>http://magnus.therning.org/posts/2016-04-07-000-qt5%2Bdbus%2Bcmake--a-complete-example.html</id>
    <published>2016-04-07T00:00:00Z</published>
    <updated>2016-04-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Yesterday I started digging into Qt5 and D-Bus. I never found a complete example, so I put one together myself: <a href="https://gist.github.com/magthe/2cf7220655bd8bf431259cc7dee99f64" class="uri">https://gist.github.com/magthe/2cf7220655bd8bf431259cc7dee99f64</a>.</p>]]></summary>
</entry>
<entry>
    <title>Final version of JSON to sum type</title>
    <link href="http://magnus.therning.org/posts/2016-03-08-000-final-version-of-json-to-sum-type.html" />
    <id>http://magnus.therning.org/posts/2016-03-08-000-final-version-of-json-to-sum-type.html</id>
    <published>2016-03-08T00:00:00Z</published>
    <updated>2016-03-08T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>After some feedback on my <a href="/posts/2016-03-06-000-from-json-to-sum-type.html">previous post</a> I ended up with the following implementation.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">FromJSON</span> <span class="dt">V.VersionRange</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">  parseJSON <span class="fu">=</span> withObject <span class="st">&quot;VersionRange&quot;</span> go</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">      go o <span class="fu">=</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">        V.thisVersion <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;ThisVersion&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">        V.laterVersion <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;LaterVersion&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7">        V.earlierVersion <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;EarlierVersion&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8">        <span class="dt">V.WildcardVersion</span> <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;WildcardVersion&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">        nullaryOp V.anyVersion <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;AnyVersion&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-10" data-line-number="10">        binaryOp V.unionVersionRanges <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;UnionVersionRanges&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11">        binaryOp V.intersectVersionRanges <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;IntersectVersionRanges&quot;</span> <span class="fu">&lt;|&gt;</span></a>
<a class="sourceLine" id="cb1-12" data-line-number="12">        <span class="dt">V.VersionRangeParens</span> <span class="fu">&lt;$&gt;</span> o <span class="fu">.:</span> <span class="st">&quot;VersionRangeParens&quot;</span></a>
<a class="sourceLine" id="cb1-13" data-line-number="13"></a>
<a class="sourceLine" id="cb1-14" data-line-number="14"><span class="ot">      nullaryOp ::</span> a <span class="ot">-&gt;</span> <span class="dt">Value</span> <span class="ot">-&gt;</span> a</a>
<a class="sourceLine" id="cb1-15" data-line-number="15">      nullaryOp <span class="fu">=</span> const</a>
<a class="sourceLine" id="cb1-16" data-line-number="16"></a>
<a class="sourceLine" id="cb1-17" data-line-number="17">      binaryOp f [a, b] <span class="fu">=</span> f a b</a></code></pre></div>
<p>Thanks David for your suggestions.</p>]]></summary>
</entry>
<entry>
    <title>From JSON to sum type</title>
    <link href="http://magnus.therning.org/posts/2016-03-06-000-from-json-to-sum-type.html" />
    <id>http://magnus.therning.org/posts/2016-03-06-000-from-json-to-sum-type.html</id>
    <published>2016-03-06T00:00:00Z</published>
    <updated>2016-03-06T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>For a while I’ve been planning to take full ownership of the JSON serialisation and parsing in <a href="https://github.com/magthe/cblrepo/">cblrepo</a>. The recent inclusion of instances of <code>ToJSON</code> and <code>FromJSON</code> for <a href="http://hackage.haskell.org/package/base-4.8.2.0/docs/Data-Version.html#t:Version"><code>Version</code></a> pushed me to take the first step by writing my own instances for all external types.</p>
<p>When doing this I noticed that all examples in the <a href="http://hackage.haskell.org/package/aeson">aeson</a> docs use a product</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">Person</span> <span class="fu">=</span> <span class="dt">Person</span> {</a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="ot">      name ::</span> <span class="dt">Text</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    ,<span class="ot"> age  ::</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    }</a></code></pre></div>
<p>whereas I had to deal with quite a few sums, e.g. <a href="http://hackage.haskell.org/package/Cabal-1.22.8.0/docs/Distribution-Version.html#t:VersionRange"><code>VersionRange</code></a>. At first I struggled a little with how to write an instance of <code>FromJSON</code>. After quite a bit of thinking I came up with the following, which I think is fairly nice, but I’d really like to hear what others think about it. Maybe I’ve just missed a much simpler way of implementing <code>parseJSON</code>:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">FromJSON</span> <span class="dt">V.VersionRange</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2">  parseJSON <span class="fu">=</span> withObject <span class="st">&quot;VersionRange&quot;</span> go</a>
<a class="sourceLine" id="cb2-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4">      go o <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">        lv <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;LaterVersion&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap V.laterVersion</a>
<a class="sourceLine" id="cb2-6" data-line-number="6">        tv <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;ThisVersion&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap V.thisVersion</a>
<a class="sourceLine" id="cb2-7" data-line-number="7">        ev <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;EarlierVersion&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap V.earlierVersion</a>
<a class="sourceLine" id="cb2-8" data-line-number="8">        av <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;AnyVersion&quot;</span>) <span class="fu">&gt;&gt;=</span> \ (<span class="ot">_::</span><span class="dt">Maybe</span> [(<span class="dt">Int</span>,<span class="dt">Int</span>)]) <span class="ot">-&gt;</span> return <span class="fu">$</span> <span class="dt">Just</span> V.anyVersion</a>
<a class="sourceLine" id="cb2-9" data-line-number="9">        wv <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;WildcardVersion&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap <span class="dt">V.WildcardVersion</span></a>
<a class="sourceLine" id="cb2-10" data-line-number="10">        uvr <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;UnionVersionRanges&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap toUvr</a>
<a class="sourceLine" id="cb2-11" data-line-number="11">        ivr <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;IntersectVersionRanges&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap toIvr</a>
<a class="sourceLine" id="cb2-12" data-line-number="12">        vrp <span class="ot">&lt;-</span> (o <span class="fu">.:?</span> <span class="st">&quot;VersionRangeParens&quot;</span>) <span class="fu">&gt;&gt;=</span> return <span class="fu">.</span> fmap <span class="dt">V.VersionRangeParens</span></a>
<a class="sourceLine" id="cb2-13" data-line-number="13">        maybe (typeMismatch <span class="st">&quot;VersionRange&quot;</span> <span class="fu">$</span> <span class="dt">Object</span> o)</a>
<a class="sourceLine" id="cb2-14" data-line-number="14">          return</a>
<a class="sourceLine" id="cb2-15" data-line-number="15">          (lv <span class="fu">&lt;|&gt;</span> tv <span class="fu">&lt;|&gt;</span> ev <span class="fu">&lt;|&gt;</span> uvr <span class="fu">&lt;|&gt;</span> ivr <span class="fu">&lt;|&gt;</span> wv <span class="fu">&lt;|&gt;</span> vrp <span class="fu">&lt;|&gt;</span> av)</a>
<a class="sourceLine" id="cb2-16" data-line-number="16"></a>
<a class="sourceLine" id="cb2-17" data-line-number="17">      toUvr [v0, v1] <span class="fu">=</span> V.unionVersionRanges v0 v1</a>
<a class="sourceLine" id="cb2-18" data-line-number="18">      toIvr [v0, v1] <span class="fu">=</span> V.intersectVersionRanges v0 v1</a></code></pre></div>
<p>Any and all comments and suggestions are more than welcome!</p>]]></summary>
</entry>
<entry>
    <title>Freer play with effects</title>
    <link href="http://magnus.therning.org/posts/2016-01-24-000-freer-play-with-effects.html" />
    <id>http://magnus.therning.org/posts/2016-01-24-000-freer-play-with-effects.html</id>
    <published>2016-01-24T00:00:00Z</published>
    <updated>2016-01-24T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In the previous posts on my <a href="/posts/2016-01-13-000-free-play--part-one.html">playing</a> <a href="/posts/2016-01-15-000-free-play--part-two.html">with</a> <a href="/posts/2016-01-17-000-free-play--part-three.html">free</a> I got stuck at combining APIs. I recalled reading a <a href="http://okmij.org/ftp/Haskell/extensible/exteff.pdf">paper on extensible effects</a> as an alternatve to monad transformers. I have to admit to not having finished the paper, and not quite understanding the part I did read. When looking it up again I found that the work had continued and that there is a <a href="http://okmij.org/ftp/Haskell/extensible/more.pdf">paper on more extensible effects</a>. (I got to it via <a href="http://okmij.org/ftp/Haskell/extensible/" class="uri">http://okmij.org/ftp/Haskell/extensible/</a>.)</p>
<p>A quick search of <a href="http://hackage.haskell.org/">Hackage</a> revealed the package <a href="http://hackage.haskell.org/package/extensible-effects">extensible-effects</a> with an implementation of the ideas, including the stuff in the latter paper. So, what would the examples from my previous posts look like using extensible effects?</p>
<h3 id="opening">Opening</h3>
<p>The examples require a few extensions and modules:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">{-# LANGUAGE FlexibleContexts #-}</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="ot">{-# LANGUAGE GADTs #-}</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="ot">{-# LANGUAGE TypeOperators #-}</span></a></code></pre></div>
<p>and</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">import</span> <span class="dt">Control.Eff</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2"><span class="kw">import</span> <span class="dt">Control.Eff.Lift</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3"><span class="kw">import</span> <span class="dt">Control.Eff.Operational</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4"><span class="kw">import</span> <span class="dt">Data.Typeable</span></a></code></pre></div>
<h3 id="just-an-api">Just an API</h3>
<p>This part was greatly helped by the fact that there is a <a href="http://hackage.haskell.org/package/extensible-effects-1.11.0.1/docs/Control-Eff-Operational-Example.html">example in extensible-effects</a>.</p>
<p>I start with defining the <code>SimpleFile</code> API using GADTs</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">data</span> <span class="dt">SimpleFileAPI</span> a <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2">  <span class="dt">LoadFile</span><span class="ot"> ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb3-3" data-line-number="3">  <span class="dt">SaveFile</span><span class="ot"> ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">SimpleFileAPI</span> ()</a></code></pre></div>
<p>The usage of the constructors need to be wrapped up in <code>singleton</code>. To remember that I create two convenience functions</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">loadFile ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r <span class="ot">=&gt;</span> FilePath <span class="ot">-&gt;</span> <span class="dt">Eff</span> r <span class="dt">String</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">loadFile <span class="fu">=</span> singleton <span class="fu">.</span> <span class="dt">LoadFile</span></a>
<a class="sourceLine" id="cb4-3" data-line-number="3"></a>
<a class="sourceLine" id="cb4-4" data-line-number="4"><span class="ot">saveFile ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r <span class="ot">=&gt;</span> FilePath <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</a>
<a class="sourceLine" id="cb4-5" data-line-number="5">saveFile fp <span class="fu">=</span> singleton <span class="fu">.</span> <span class="dt">SaveFile</span> fp</a></code></pre></div>
<p>For <code>withSimpleFile</code> I only have to modify the type</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">withSimpleFile ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r <span class="ot">=&gt;</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> FilePath <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</a>
<a class="sourceLine" id="cb5-2" data-line-number="2">withSimpleFile f fp <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb5-3" data-line-number="3">  d <span class="ot">&lt;-</span> loadFile fp</a>
<a class="sourceLine" id="cb5-4" data-line-number="4">  <span class="kw">let</span> result <span class="fu">=</span> f d</a>
<a class="sourceLine" id="cb5-5" data-line-number="5">  saveFile (fp <span class="fu">++</span> <span class="st">&quot;_new&quot;</span>) result</a></code></pre></div>
<p>Now for the gut of it, the interpreter.</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="ot">runSimpleFile ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">Eff</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span> <span class="fu">:&gt;</span> r) a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb6-2" data-line-number="2">runSimpleFile <span class="fu">=</span> runProgram f</a>
<a class="sourceLine" id="cb6-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb6-4" data-line-number="4"><span class="ot">    f ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb6-5" data-line-number="5">    f (<span class="dt">LoadFile</span> fp) <span class="fu">=</span> lift <span class="fu">$</span> readFile fp</a>
<a class="sourceLine" id="cb6-6" data-line-number="6">    f (<span class="dt">SaveFile</span> fp s) <span class="fu">=</span> lift <span class="fu">$</span> writeFile fp s</a></code></pre></div>
<p>Runnnig it is fairly simple after this</p>
<pre><code>&gt; :! cat test.txt 
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runLift $ runSimpleFile $ withSimpleFile (map toUpper) &quot;test.txt&quot;
&gt; :! cat test.txt_new 
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.</code></pre>
<p>Now, that was pretty easy. It looks almost exactly like when using <code>Free</code>, only without the <code>Functor</code> instance and rather more complicated types.</p>
<h3 id="combining-two-apis">Combining two APIs</h3>
<p>Now I get to the stuff that I didn’t manage to do using <code>Free</code>; combining two different APIs.</p>
<p>I start with defining another API. This one is truly a play example, sorry for that, but it doesn’t really matter. The type with convenience function looks like this</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="kw">data</span> <span class="dt">StdIoAPI</span> a <span class="kw">where</span></a>
<a class="sourceLine" id="cb8-2" data-line-number="2">  <span class="dt">WriteStrLn</span><span class="ot"> ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">StdIoAPI</span> ()</a>
<a class="sourceLine" id="cb8-3" data-line-number="3"></a>
<a class="sourceLine" id="cb8-4" data-line-number="4"><span class="ot">writeStrLn ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">StdIoAPI</span>) r <span class="ot">=&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</a>
<a class="sourceLine" id="cb8-5" data-line-number="5">writeStrLn <span class="fu">=</span> singleton <span class="fu">.</span> <span class="dt">WriteStrLn</span></a></code></pre></div>
<p>The interpreter then is straight forward</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="ot">runStdIo ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">Eff</span> (<span class="dt">Program</span> <span class="dt">StdIoAPI</span> <span class="fu">:&gt;</span> r) a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb9-2" data-line-number="2">runStdIo <span class="fu">=</span> runProgram f</a>
<a class="sourceLine" id="cb9-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb9-4" data-line-number="4"><span class="ot">    f ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">StdIoAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb9-5" data-line-number="5">    f (<span class="dt">WriteStrLn</span> s) <span class="fu">=</span> lift <span class="fu">$</span> putStrLn s</a></code></pre></div>
<p>Now I just need a program that combines the two APIs</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb10-1" data-line-number="1"><span class="ot">verboseWithSimpleFile ::</span> (<span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">StdIoAPI</span>) r, <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r) <span class="ot">=&gt;</span></a>
<a class="sourceLine" id="cb10-2" data-line-number="2">                         (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</a>
<a class="sourceLine" id="cb10-3" data-line-number="3">verboseWithSimpleFile f fp <span class="fu">=</span> writeStrLn (<span class="st">&quot;verboseWithSimpleFile on &quot;</span> <span class="fu">++</span> fp) <span class="fu">&gt;&gt;</span> withSimpleFile f fp</a></code></pre></div>
<p>That type is surprisingly clear I find, albeit a bit on the long side. Running it is just a matter of combining <code>runStdIo</code> and <code>runSimpleFile</code>.</p>
<pre><code>&gt; :! cat test.txt 
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runLift $ runSimpleFile $ runStdIo $ verboseWithSimpleFile (map toUpper) &quot;test.txt&quot;
verboseWithSimpleFile on test.txt
&gt; :! cat test.txt_new 
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.</code></pre>
<p>Oh, and it doesn’t matter in what order the interpreters are run!</p>
<p>At this point I got really excited about <code>Eff</code> because now it’s obvious that I’ll be able to write the logging “decorator”, in fact it’s clear that it’ll be rather simple too.</p>
<h3 id="the-logging">The logging</h3>
<p>As before I start with a data type and a convenience function</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb12-1" data-line-number="1"><span class="kw">data</span> <span class="dt">LoggerAPI</span> a <span class="kw">where</span></a>
<a class="sourceLine" id="cb12-2" data-line-number="2">  <span class="dt">Log</span><span class="ot"> ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">LoggerAPI</span> ()</a>
<a class="sourceLine" id="cb12-3" data-line-number="3"></a>
<a class="sourceLine" id="cb12-4" data-line-number="4"><span class="ot">logStr ::</span> <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">LoggerAPI</span>) r <span class="ot">=&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Eff</span> r ()</a>
<a class="sourceLine" id="cb12-5" data-line-number="5">logStr <span class="fu">=</span> singleton <span class="fu">.</span> <span class="dt">Log</span></a></code></pre></div>
<p>For the decorating I can make use of the fact that APIs can be combined like I did above. That is, I don’t need to bother with any <em>coproduct</em> (<code>Sum</code>) or anything like that, I can simply just push in a call to <code>logStr</code> before each use of <code>SimpleFileAPI</code></p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb13-1" data-line-number="1"><span class="ot">logSimpleFileOp ::</span> (<span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">SimpleFileAPI</span>) r, <span class="dt">Member</span> (<span class="dt">Program</span> <span class="dt">LoggerAPI</span>) r) <span class="ot">=&gt;</span> <span class="dt">SimpleFileAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb13-2" data-line-number="2">logSimpleFileOp op<span class="fu">@</span>(<span class="dt">LoadFile</span> fp) <span class="fu">=</span> logStr (<span class="st">&quot;LoadFile &quot;</span> <span class="fu">++</span> fp) <span class="fu">*&gt;</span> singleton op</a>
<a class="sourceLine" id="cb13-3" data-line-number="3">logSimpleFileOp op<span class="fu">@</span>(<span class="dt">SaveFile</span> fp _) <span class="fu">=</span> logStr (<span class="st">&quot;SaveFile &quot;</span> <span class="fu">++</span> fp) <span class="fu">*&gt;</span> singleton op</a></code></pre></div>
<p>Of course an interpreter is needed as well</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb14-1" data-line-number="1"><span class="ot">runLogger ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">Eff</span> (<span class="dt">Program</span> <span class="dt">LoggerAPI</span> <span class="fu">:&gt;</span> r) a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb14-2" data-line-number="2">runLogger <span class="fu">=</span> runProgram f</a>
<a class="sourceLine" id="cb14-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb14-4" data-line-number="4"><span class="ot">    f ::</span> (<span class="dt">Member</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r, <span class="dt">SetMember</span> <span class="dt">Lift</span> (<span class="dt">Lift</span> <span class="dt">IO</span>) r) <span class="ot">=&gt;</span> <span class="dt">LoggerAPI</span> a <span class="ot">-&gt;</span> <span class="dt">Eff</span> r a</a>
<a class="sourceLine" id="cb14-5" data-line-number="5">    f (<span class="dt">Log</span> s) <span class="fu">=</span> lift <span class="fu">$</span> putStrLn s</a></code></pre></div>
<p>Running is, once again, a matter of stacking interpreters</p>
<pre><code>&gt; :! cat test.txt
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runLift $ runLogger $ runSimpleFile $ runProgram logSimpleFileOp $ withSimpleFile (map toUpper) &quot;test.txt&quot;
LoadFile test.txt
SaveFile test.txt_new
&gt; :! cat test.txt_new 
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.</code></pre>
<h3 id="closing-thoughts">Closing thoughts</h3>
<p>With <code>Eff</code> I’ve pretty much arrived where I wanted, I can</p>
<ul>
<li>define APIs of operations in a simple way (simpler than when using <code>Free</code> even).</li>
<li>write a definitional interpreter for the operations.</li>
<li>combine two different APIs in the same function.</li>
<li>translate from one API to another (or even to a set of other APIs).</li>
</ul>
<p>On top, I can do this without having to write a ridiculous amount of code.</p>
<p>I’m sure there are drawbacks as well. There’s a mention of some of them in the paper. However, for my typical uses of Haskell I haven’t read anything that would be a deal breaker.</p>]]></summary>
</entry>
<entry>
    <title>Free play, part three</title>
    <link href="http://magnus.therning.org/posts/2016-01-17-000-free-play--part-three.html" />
    <id>http://magnus.therning.org/posts/2016-01-17-000-free-play--part-three.html</id>
    <published>2016-01-17T00:00:00Z</published>
    <updated>2016-01-17T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The code in the <a href="/posts/2016-01-15-000-free-play--part-two.html">previous post</a> can do with a bit of cleaning up. I start with introducing a type class for an API that can be run</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">class</span> <span class="dt">RunnableF</span> f <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="ot">  runOp ::</span> f a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a></code></pre></div>
<p>and a function that actually runs iteFile</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">runF ::</span> (<span class="dt">RunnableF</span> o) <span class="ot">=&gt;</span> <span class="dt">Free</span> o a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">runF <span class="fu">=</span> foldFree runOp</a></code></pre></div>
<p>A coproduct (<code>Sum</code>) of two runnable APIs can itself be runnable</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">instance</span> (<span class="dt">RunnableF</span> f, <span class="dt">RunnableF</span> g) <span class="ot">=&gt;</span> <span class="dt">RunnableF</span> (<span class="dt">Sum</span> f g) <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2">  runOp (<span class="dt">InL</span> op) <span class="fu">=</span> runOp op</a>
<a class="sourceLine" id="cb3-3" data-line-number="3">  runOp (<span class="dt">InR</span> op) <span class="fu">=</span> runOp op</a></code></pre></div>
<p>After this all I have to do is to move the guts of <code>runSimpleF</code> and <code>runLogging</code> into implementations for <code>SimpleFil</code> and <code>LogF</code> respectively</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">RunnableF</span> <span class="dt">SimpleFileF</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">  runOp (<span class="dt">LoadFile</span> fp f&#39;) <span class="fu">=</span> liftM f&#39; <span class="fu">$</span> readFile fp</a>
<a class="sourceLine" id="cb4-3" data-line-number="3">  runOp (<span class="dt">SaveFile</span> fp d r) <span class="fu">=</span> writeFile fp d <span class="fu">&gt;&gt;</span> return r</a>
<a class="sourceLine" id="cb4-4" data-line-number="4"></a>
<a class="sourceLine" id="cb4-5" data-line-number="5"><span class="kw">instance</span> <span class="dt">RunnableF</span> <span class="dt">LogF</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb4-6" data-line-number="6">  runOp (<span class="dt">Log</span> s a)<span class="fu">=</span> putStrLn s <span class="fu">&gt;&gt;</span> return a</a></code></pre></div>
<p>The rest is left as is. Running now looks like this</p>
<pre><code>&gt; :! cat test.txt
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Donec a diam lectus.
Sed sit amet ipsum mauris. Maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.
&gt; runF $ withSimpleFile (map toUpper) &quot;test.txt&quot;
&gt; :! cat test.txt_new
LOREM IPSUM DOLOR SIT AMET, CONSECTETUR ADIPISCING ELIT. DONEC A DIAM LECTUS.
SED SIT AMET IPSUM MAURIS. MAECENAS CONGUE LIGULA AC QUAM VIVERRA NEC
CONSECTETUR ANTE HENDRERIT.
&gt; runF $ foldFree loggingSimpleFileI $ withSimpleFile (map toLower) &quot;test.txt&quot;
** load file test.txt
** save file test.txt_new
&gt; :! cat test.txt_new
lorem ipsum dolor sit amet, consectetur adipiscing elit. donec a diam lectus.
sed sit amet ipsum mauris. maecenas congue ligula ac quam viverra nec
consectetur ante hendrerit.</code></pre>
<h3 id="what-else">What else?</h3>
<p>With these changes it becomes slightly esier to create new basic API types. but there are still a few things I think could be useful:</p>
<ul>
<li>More generic decorators. <code>logSimpleFileI</code> is tied to decorating only <code>SimpleFileF</code>. I’m fairly sure this could be dealt with by a type class <code>LoggableF</code> and have APIs implement it. I also think there’s a rather natural implementation of <code>LoggableF</code> for <code>Sum f g</code>.</li>
<li>Combining decorators (and other interpreters). I’m guessing this is what John refers to as “generalizing interpreters.”</li>
<li>Combining APIs. I’d prefer making small APIs of related operations, but then that also means I need a nice way of combining APIs. I had a go at using <a href="http://hackage.haskell.org/package/free-4.12.1/docs/Control-Monad-Trans-Free.html"><code>FreeT</code></a> but simply gave up on bending the types to my will. In any case I’m not completely sure that a stack of various <code>FreeT</code> is a good direction to go.</li>
</ul>]]></summary>
</entry>
<entry>
    <title>Free play, part two</title>
    <link href="http://magnus.therning.org/posts/2016-01-15-000-free-play--part-two.html" />
    <id>http://magnus.therning.org/posts/2016-01-15-000-free-play--part-two.html</id>
    <published>2016-01-15T00:00:00Z</published>
    <updated>2016-01-15T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>This post on <code>Free</code> play builds on my <a href="/posts/2016-01-13-000-free-play--part-one.html">previous one</a>. It was <a href="http://degoes.net/articles/modern-fp">John A De Goes’ post</a> that finally got me started on playing with <code>Free</code> and his post contian the following:</p>
<div class="line-block">Moreover, not only do algebras compose (that is, if you have algebras f and g, you can compose them into a composite algebra Coproduct f g for some suitable definition of Coproduct), but interpreters also compose — both horizontally and vertically.</div>
<p>And a little bit later he offers a few types and some details, but not <em>all</em> details. How could something like that look in Haskell?</p>
<p>Starting from the code in the previous post I first created a new type for a logging action</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">LogF</span> a <span class="fu">=</span> <span class="dt">Log</span> <span class="dt">String</span> a</a></code></pre></div>
<p>This type has to be a <code>Functor</code></p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">Functor</span> <span class="dt">LogF</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2">  fmap f (<span class="dt">Log</span> s a) <span class="fu">=</span> <span class="dt">Log</span> s (f a)</a></code></pre></div>
<p>The logging should basically decorate a <code>SimpleFileF a</code> action, so I need a function to map one into a <code>Free LogF a</code></p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">logSimpleFileI ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">LogF</span> ()</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">logSimpleFileI (<span class="dt">LoadFile</span> fp _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** load file &quot;</span> <span class="fu">++</span> fp) ()</a>
<a class="sourceLine" id="cb3-3" data-line-number="3">logSimpleFileI (<span class="dt">SaveFile</span> fp _ _) <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">Log</span> (<span class="st">&quot;** save file &quot;</span> <span class="fu">++</span> fp) ()</a></code></pre></div>
<p>Now I needed a <code>Coproduct</code> for <code>Functor</code>. Searching <a href="http://hackage.haskell.org/">hackage</a> only offered up one for <code>Monoid</code> (in <a href="http://hackage.haskell.org/package/monoid-extras">monoid-extras</a>) so I first translated one from PureScript, but later I got some help via Twitter and was pointed to two in Haskell, <a href="http://hackage.haskell.org/package/comonad-4.2.7.2/docs/Data-Functor-Coproduct.html"><code>Data.Functor.Coproduct</code></a> from comonad and <a href="http://hackage.haskell.org/package/transformers-0.5.0.0/docs/Data-Functor-Sum.html"><code>Data.Functor.Sum</code></a> from transformers, I decided on the one from transformers because of its shorter name and the fact that it was very different from my translated-from-PureScript version.</p>
<p>Following John’s example I use <code>Applicative</code> to combine the logging with the file action</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">loggingSimpleFileI ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">Free</span> (<span class="dt">Sum</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>) a</a>
<a class="sourceLine" id="cb4-2" data-line-number="2">loggingSimpleFileI op <span class="fu">=</span> toLeft (logSimpleFileI op) <span class="fu">*&gt;</span> toRight (liftF op)</a></code></pre></div>
<p>with <code>toLeft</code> and <code>toRight</code> defined like this</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">toLeft ::</span> (<span class="dt">Functor</span> f, <span class="dt">Functor</span> g) <span class="ot">=&gt;</span> <span class="dt">Free</span> f a <span class="ot">-&gt;</span> <span class="dt">Free</span> (<span class="dt">Sum</span> f g) a</a>
<a class="sourceLine" id="cb5-2" data-line-number="2">toLeft <span class="fu">=</span> hoistFree <span class="dt">InL</span></a>
<a class="sourceLine" id="cb5-3" data-line-number="3"><span class="ot">toRight ::</span> (<span class="dt">Functor</span> f, <span class="dt">Functor</span> g) <span class="ot">=&gt;</span> <span class="dt">Free</span> g a <span class="ot">-&gt;</span> <span class="dt">Free</span> (<span class="dt">Sum</span> f g) a</a>
<a class="sourceLine" id="cb5-4" data-line-number="4">toRight <span class="fu">=</span> hoistFree <span class="dt">InR</span></a></code></pre></div>
<p>With all of this in place I can decorate the program from the last post like this <code>foldFree loggingSimpleFileI (withSimpleF toUpper &quot;FreePlay.hs&quot;)</code>. What’s left is a way to run it. The function for that is a natural extension of <code>runsimpleFile</code></p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"></a>
<a class="sourceLine" id="cb6-2" data-line-number="2"><span class="ot">runLogging ::</span> <span class="dt">Free</span> (<span class="dt">Sum</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>) a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb6-3" data-line-number="3">runLogging <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb6-4" data-line-number="4">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb6-5" data-line-number="5"><span class="ot">    f ::</span> (<span class="dt">Sum</span> <span class="dt">LogF</span> <span class="dt">SimpleFileF</span>) a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb6-6" data-line-number="6">    f (<span class="dt">InL</span> op) <span class="fu">=</span> g op</a>
<a class="sourceLine" id="cb6-7" data-line-number="7">    f (<span class="dt">InR</span> op) <span class="fu">=</span> h op</a>
<a class="sourceLine" id="cb6-8" data-line-number="8"></a>
<a class="sourceLine" id="cb6-9" data-line-number="9"><span class="ot">    g ::</span> <span class="dt">LogF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb6-10" data-line-number="10">    g (<span class="dt">Log</span> s a)<span class="fu">=</span> putStrLn s <span class="fu">&gt;&gt;</span> return a</a>
<a class="sourceLine" id="cb6-11" data-line-number="11"></a>
<a class="sourceLine" id="cb6-12" data-line-number="12"><span class="ot">    h ::</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb6-13" data-line-number="13">    h (<span class="dt">LoadFile</span> fp f&#39;) <span class="fu">=</span> liftM f&#39; <span class="fu">$</span> readFile fp</a>
<a class="sourceLine" id="cb6-14" data-line-number="14">    h (<span class="dt">SaveFile</span> fp d r) <span class="fu">=</span> writeFile fp d <span class="fu">&gt;&gt;</span> return r</a></code></pre></div>
<p>Running the decorated program</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1">runLogging <span class="fu">$</span> foldFree loggingSimpleFileI (withSimpleF toUpper <span class="st">&quot;FreePlay.hs&quot;</span>)</a></code></pre></div>
<p>does indeed result in the expected output</p>
<pre><code>** load file FreePlay.hs
** save file FreePlay.hs_new</code></pre>
<p>and the file <code>FreePlay.hs_new</code> contains only uppercase letters.</p>
<h3 id="my-thoughts">My thoughts</h3>
<p>This ability to decorate actions (or compose algebras) is very nice. There’s probably value in the “multiple interpreters for a program” in some domains, but I have a feeling that it could be a hard sell. However, combining it with this kind of composability adds quite a bit of value in my opinion. I must say I don’t think <em>my</em> code scales very well for adding more decorators (composing more algebras), but hopefully some type wizard can show me a way to improve on that.</p>
<p>The code above is rather crude though, and I have another version that cleans it up quite a bit. That’ll be in the next post.</p>]]></summary>
</entry>
<entry>
    <title>Free play, part one</title>
    <link href="http://magnus.therning.org/posts/2016-01-13-000-free-play--part-one.html" />
    <id>http://magnus.therning.org/posts/2016-01-13-000-free-play--part-one.html</id>
    <published>2016-01-13T00:00:00Z</published>
    <updated>2016-01-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When I read John A De Goes post <a href="http://degoes.net/articles/modern-fp">A Modern Architecture for FP</a> I found it to be a bit too vague for me, but the topic was just interesting enough to finally push me to play a little with <em>free monads</em>. It’s not the first post I’ve read on the topic, there have <a href="http://www.haskellforall.com/2012/06/you-could-have-invented-free-monads.html">been</a> <a href="https://www.schoolofhaskell.com/user/dolio/many-roads-to-free-monads">many</a> <a href="http://okmij.org/ftp/Computation/free-monad.html">before</a>. None have quite managed to push me into actually doing something though!</p>
<h3 id="a-file-api">A file API</h3>
<p>To make it concrete but still short enough to not bore readers I came up with a small API for working with files:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">SimpleFileF</span> a <span class="fu">=</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">  <span class="dt">LoadFile</span> FilePath (<span class="dt">String</span> <span class="ot">-&gt;</span> a)</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">  <span class="fu">|</span> <span class="dt">SaveFile</span> FilePath <span class="dt">String</span> a</a></code></pre></div>
<p>The free monad wraps a <em>functor</em>, so here’s an implementation of that</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">Functor</span> <span class="dt">SimpleFileF</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2">  fmap f (<span class="dt">LoadFile</span> fp f&#39;)<span class="fu">=</span> <span class="dt">LoadFile</span> fp (f <span class="fu">.</span> f&#39;)</a>
<a class="sourceLine" id="cb2-3" data-line-number="3">  fmap f (<span class="dt">SaveFile</span> fp d a) <span class="fu">=</span> <span class="dt">SaveFile</span> fp d (f a)</a></code></pre></div>
<p>Now for some convenient functions to work with the API type</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">loadFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2">loadFile fp <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">LoadFile</span> fp id</a>
<a class="sourceLine" id="cb3-3" data-line-number="3"></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="ot">saveFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> ()</a>
<a class="sourceLine" id="cb3-5" data-line-number="5">saveFile fp d <span class="fu">=</span> liftF <span class="fu">$</span> <span class="dt">SaveFile</span> fp d ()</a></code></pre></div>
<p>With this in place I can write a somewhat more complex one</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">withSimpleFile ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> FilePath <span class="ot">-&gt;</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> ()</a>
<a class="sourceLine" id="cb4-2" data-line-number="2">withSimpleFile f fp <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb4-3" data-line-number="3">  d <span class="ot">&lt;-</span> loadFile fp</a>
<a class="sourceLine" id="cb4-4" data-line-number="4">  <span class="kw">let</span> result <span class="fu">=</span> f d</a>
<a class="sourceLine" id="cb4-5" data-line-number="5">  saveFile (fp <span class="fu">++</span> <span class="st">&quot;_new&quot;</span>) result</a></code></pre></div>
<p>Now I need a way to run programs using the API</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">runSimpleFile ::</span> <span class="dt">Free</span> <span class="dt">SimpleFileF</span> a <span class="ot">-&gt;</span> <span class="dt">IO</span> a</a>
<a class="sourceLine" id="cb5-2" data-line-number="2">runSimpleFile <span class="fu">=</span> foldFree f</a>
<a class="sourceLine" id="cb5-3" data-line-number="3">  <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-4" data-line-number="4">    f (<span class="dt">LoadFile</span> fp f&#39;) <span class="fu">=</span> liftM f&#39; <span class="fu">$</span> readFile fp</a>
<a class="sourceLine" id="cb5-5" data-line-number="5">    f (<span class="dt">SaveFile</span> fp d r) <span class="fu">=</span> writeFile fp d <span class="fu">&gt;&gt;</span> return r</a></code></pre></div>
<p>If this code was save in the file <code>FreePlay.hs</code> I can now convert it all to upper case by using</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1">runSimpleFile <span class="fu">$</span> withSimpleFile toUpper <span class="st">&quot;FreePlay.hs&quot;</span></a></code></pre></div>
<p>which of course will create the file <code>FreePlay.hs_new</code>.</p>
<h3 id="what-have-i-bought-so-far">What have I bought so far?</h3>
<p>Well, not much really.</p>
<p>So far it’s not much more than a rather roundabout way to limit what IO actions are available. In other words it’s not much more than what can be done by creating a limited IO, as in my earlier posts <a href="/posts/2009-09-07-738-wrapping-io-part-1.html">here</a> and <a href="/posts/2009-09-07-742-wrapping-io-part-2.html">here</a>.</p>
<p>Of course it would be possible to write another <em>run</em> function, e.g. one that doesn’t actually perform the file actions but just says what it <em>would</em> do. The nice thing though is, to use that same metaphor as John does, that I can add a layer to the onion. In other words, I can decorate each use of my <code>SimpleFileF</code> API with some other API. I think it was John’s description of this layering that pushed me to play with <code>Free</code>.</p>]]></summary>
</entry>
<entry>
    <title>Trick for pre-processing source in CMake</title>
    <link href="http://magnus.therning.org/posts/2015-11-26-001-trick-for-pre-processing-source-in-cmake.html" />
    <id>http://magnus.therning.org/posts/2015-11-26-001-trick-for-pre-processing-source-in-cmake.html</id>
    <published>2015-11-26T00:00:00Z</published>
    <updated>2015-11-26T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When using <a href="https://cmake.org/cmake/help/v3.4/prop_tgt/LANG_COMPILER_LAUNCHER.html"><code>&lt;LANG&gt;_COMPILER_LAUCHER</code></a> or <a href="https://cmake.org/cmake/help/v3.3/prop_gbl/RULE_LAUNCH_COMPILE.html"><code>RULE_LAUNCH_COMPILER</code></a> the following is a nice little pattern to deal with the arguments.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode python"><code class="sourceCode python"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="co">#! /usr/bin/python3</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="im">import</span> argparse</a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="im">import</span> subprocess</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="im">import</span> sys</a>
<a class="sourceLine" id="cb1-6" data-line-number="6"></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"><span class="co"># create an argumentparser for the stuff the launcher script needs, some</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="co"># specific to it and some of the compiler arguments</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">parser <span class="op">=</span> argparse.ArgumentParser(description<span class="op">=</span><span class="st">&#39;Compiler launcher for C.&#39;</span>)</a>
<a class="sourceLine" id="cb1-10" data-line-number="10">parser.add_argument(<span class="st">&#39;--flag-for-wrapper&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</a>
<a class="sourceLine" id="cb1-11" data-line-number="11">parser.add_argument(<span class="st">&#39;-I&#39;</span>, dest<span class="op">=</span><span class="st">&#39;includes&#39;</span>, action<span class="op">=</span><span class="st">&#39;append&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>, default<span class="op">=</span>[])</a>
<a class="sourceLine" id="cb1-12" data-line-number="12">parser.add_argument(<span class="st">&#39;-D&#39;</span>, dest<span class="op">=</span><span class="st">&#39;defines&#39;</span>, action<span class="op">=</span><span class="st">&#39;append&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>, default<span class="op">=</span>[])</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">parser.add_argument(<span class="st">&#39;-o&#39;</span>, dest<span class="op">=</span><span class="st">&#39;output&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</a>
<a class="sourceLine" id="cb1-14" data-line-number="14">parser.add_argument(<span class="st">&#39;-c&#39;</span>, dest<span class="op">=</span><span class="st">&#39;input&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</a>
<a class="sourceLine" id="cb1-15" data-line-number="15"></a>
<a class="sourceLine" id="cb1-16" data-line-number="16">args, rest <span class="op">=</span> parser.parse_known_args(sys.argv)</a>
<a class="sourceLine" id="cb1-17" data-line-number="17"></a>
<a class="sourceLine" id="cb1-18" data-line-number="18"><span class="co"># do stuff with the source, e.g. pass it through the C pre-processor (using the</span></a>
<a class="sourceLine" id="cb1-19" data-line-number="19"><span class="co"># arguments picked out above) and then process the output</span></a>
<a class="sourceLine" id="cb1-20" data-line-number="20"></a>
<a class="sourceLine" id="cb1-21" data-line-number="21"><span class="co"># build another argument parser to prepare the call to the compiler</span></a>
<a class="sourceLine" id="cb1-22" data-line-number="22">cc_parser <span class="op">=</span> argparse.ArgumentParser(description<span class="op">=</span><span class="st">&#39;Parser for removing launcher-specific arguments.&#39;</span>)</a>
<a class="sourceLine" id="cb1-23" data-line-number="23">cc_parser.add_argument(<span class="st">&#39;--flag-for-wrapper&#39;</span>, <span class="bu">type</span><span class="op">=</span><span class="bu">str</span>)</a>
<a class="sourceLine" id="cb1-24" data-line-number="24">cc_cmd <span class="op">=</span> cc_parser.parse_known_args(sys.argv)[<span class="dv">1</span>][<span class="dv">1</span>:]</a>
<a class="sourceLine" id="cb1-25" data-line-number="25"></a>
<a class="sourceLine" id="cb1-26" data-line-number="26">subprocess.call(cc_cmd)</a></code></pre></div>
<p>That is, first create a parser for launcher-specific arguments and the compiler arguments<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a> that are of interest to the launcher. Then perform the magic of the launcher. Finally, create another parser and use it to remove the launcher-specific arguments and use the remainder to perform the compilation.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>Here I’m relying on CMake always putting <em>-c</em> just before the source file, which seems to hold most of the time.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>Elm 0.16 with stack</title>
    <link href="http://magnus.therning.org/posts/2015-11-23-000-elm-0-16-with-stack.html" />
    <id>http://magnus.therning.org/posts/2015-11-23-000-elm-0-16-with-stack.html</id>
    <published>2015-11-23T00:00:00Z</published>
    <updated>2015-11-23T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I just noticed that there’s been a new release of the <a href="http://elm-lang.org">elm tools</a>. The previous release was fairly easy to <a href="/posts/2015-10-13-000-building-elm-with-stack.html">build using stack</a>, but this release made it a little bit easier still.</p>
<p>The script for cloning should be modified to look like this:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="co">#! /usr/bin/zsh</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="va">repos=(</span><span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-compiler.git&quot;</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-package.git&quot;</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-make.git&quot;</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-reactor.git&quot;</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7">       <span class="st">&quot;git clone -b 0.16 https://github.com/elm-lang/elm-repl.git&quot;</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8">      )</a>
<a class="sourceLine" id="cb1-9" data-line-number="9"></a>
<a class="sourceLine" id="cb1-10" data-line-number="10"><span class="kw">for</span> <span class="ex">r</span> in <span class="va">${repos[@]}</span><span class="kw">;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11">    <span class="bu">eval</span> <span class="va">${r}</span></a>
<a class="sourceLine" id="cb1-12" data-line-number="12"><span class="kw">done</span></a></code></pre></div>
<p>When creating the inital <code>stack.yaml</code> tell <em>stack</em> to use release 7.10.2 of <em>ghc</em>:</p>
<pre><code>% stack init --resolver ghc-7.10.2</code></pre>
<p>That makes it possible to use the installed version of <em>ghc</em>. It’s also unecessary to provide any flag for <em>aeson</em>, which means there’s no need for any manual changed to <code>stack.yaml</code> prior to running (add <code>--install-ghc</code> if you don’t have it on your system already)</p>
<pre><code>% stack solver --modify-stack-yaml</code></pre>
<p>Now just build and install as before:</p>
<pre><code>% stack install elm-{compiler,make,package,repl}
% stack install elm-reactor</code></pre>
<p>That’s it!</p>]]></summary>
</entry>
<entry>
    <title>How can I unit test failure cases?</title>
    <link href="http://magnus.therning.org/posts/2015-11-13-000-unit-testing-failure-cases.html" />
    <id>http://magnus.therning.org/posts/2015-11-13-000-unit-testing-failure-cases.html</id>
    <published>2015-11-13T00:00:00Z</published>
    <updated>2015-11-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>When writing unit tests, especially when using mock objects, there’s always a risk of falling into the trap of writing tests for the implementation rather than for the API. In my experience the most obvious indication of that is that any refactoring of the code requires major rework of the tests.</p>
<p>Right now I’m sitting with the task of writing tests for a function that promises to return <code>NULL</code> on failure. The function in question allocates memory up to three times, opens two message queues, sends a message on one and expects a reponse on the other. If all goes well it returns the pointer to one of the memory areas allocated. And if it fails, for whatever reason, it returns <code>NULL</code>.</p>
<p><em>Is it even possible to write a set of tests for it without testing the specific implementation?</em></p>
<p>I know some of the calls can be re-arranged, e.g. moving all memory allocation to the start of the function, without affecting the caller of the function. However, if I use mock objects to simulate failure in the external libraries and system calls I’d be forced to modify the tests on each such re-arranging of calls. In other words I’m testing the implementation!</p>
<p>Should I think differently about testing failure cases?</p>
<p>Should the API be changed in some way to make unit tests a better fit for testing failures?</p>]]></summary>
</entry>
<entry>
    <title>Docker container with ip address on local network, p2</title>
    <link href="http://magnus.therning.org/posts/2015-11-01-000-docker-container-with-ip-address-on-local-network--p2.html" />
    <id>http://magnus.therning.org/posts/2015-11-01-000-docker-container-with-ip-address-on-local-network--p2.html</id>
    <published>2015-11-01T00:00:00Z</published>
    <updated>2015-11-01T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In my <a href="/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html">latest post</a> I described described a way to give a docker container an address on the local network. That’s a general way of achieving it, but it’s also rather complicated. If you know exactly what ports need to be exposed there is a simpler way.</p>
<h2 id="add-a-second-ip-to-the-system-interface">Add a second IP to the system interface</h2>
<p>There’s a good description on how to give an interface multiple in Debian at the <a href="https://wiki.debian.org/NetworkConfiguration#Multiple_IP_addresses_on_one_Interface">Debian Wiki</a>. The completely manual way is</p>
<pre><code>ip addr add 192.168.1.199/24 dev eth0 label eth0:0</code></pre>
<p>The configuration in Debian is</p>
<pre><code>auto eth0:0
iface eth0:0 inet static
  address 192.168.1.199
  netmask 255.255.255.0</code></pre>
<p>If you’re using Systemd it seems to be enough to just add extra <code>Address=...</code> statements to the <code>.network</code> file:</p>
<pre><code>[Match]
Name=eth0

[Network]
Address=192.168.1.2/24
Address=192.168.1.199/24
Gateway=192.168.1.1</code></pre>
<h2 id="bind-ports-of-the-container-to-a-specific-ip">Bind ports of the container to a specific IP</h2>
<p>Using the image from my <a href="/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html">latest post</a> it’s just to run it using the <code>--publish</code> option:</p>
<pre><code>docker --rm --interactive --tty --name=echo --publish=192.168.1.199:2048:2048 nw /home/myusre/echosrv</code></pre>
<p>Just like the last time it’s easy to verify everything’s working using <code>netcat</code>.</p>]]></summary>
</entry>
<entry>
    <title>Docker container with ip address on local network</title>
    <link href="http://magnus.therning.org/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html" />
    <id>http://magnus.therning.org/posts/2015-10-22-000-docker-container-with-ip-address-on-local-network.html</id>
    <published>2015-10-22T00:00:00Z</published>
    <updated>2015-10-22T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Shortly after I started my new job in August I put an instance of gitlab on a server and started promoting using it over the git-repos-on-an-NFS-share that had been used thus far. Thanks to the <a href="https://hub.docker.com/r/gitlab/gitlab-ce/">Docker image of Gitlab CE</a> it only took about 5 minutes to have a running instance – setting up authentication via LDAP only took slightly longer. So far I’ve been running it on non-standard ports, just like the <a href="http://doc.gitlab.com/omnibus/docker/">guide</a> suggests, but I’ve gotten to the point where I’d like to make it a bit more official. That means I’d like to get it off non-standard ports, but I do really like running it in Docker. In short, I need to give the Docker image an ip address on the local network.</p>
<p>Unfortunately Docker doesn’t make it easy to do that, but I found an article on <a href="http://blog.oddbit.com/2014/08/11/four-ways-to-connect-a-docker/">four ways to connect a docker container to a local network</a>. What follows is my take on the instruction for how to use a <code>macvlan</code> device to achieve it.</p>
<h2 id="an-image-for-testing">An image for testing</h2>
<p>First I looked around for a simple echo server to run in my testing image:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">module</span> <span class="dt">Main</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">       <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="kw">import</span> <span class="dt">Control.Concurrent</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="kw">import</span> <span class="dt">Control.Monad</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="kw">import</span> <span class="dt">Network.Socket</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"><span class="kw">import</span> <span class="dt">System.IO</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"></a>
<a class="sourceLine" id="cb1-9" data-line-number="9"><span class="ot">srvPort ::</span> <span class="dt">PortNumber</span></a>
<a class="sourceLine" id="cb1-10" data-line-number="10">srvPort <span class="fu">=</span> <span class="dv">2048</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11"></a>
<a class="sourceLine" id="cb1-12" data-line-number="12"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">main <span class="fu">=</span> withSocketsDo <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-14" data-line-number="14">  newSocket <span class="ot">&lt;-</span> socket <span class="dt">AF_INET</span> <span class="dt">Stream</span> defaultProtocol</a>
<a class="sourceLine" id="cb1-15" data-line-number="15">  setSocketOption newSocket <span class="dt">ReuseAddr</span> <span class="dv">1</span></a>
<a class="sourceLine" id="cb1-16" data-line-number="16">  bindSocket newSocket <span class="fu">$</span> <span class="dt">SockAddrInet</span> srvPort iNADDR_ANY</a>
<a class="sourceLine" id="cb1-17" data-line-number="17">  listen newSocket <span class="dv">2</span></a>
<a class="sourceLine" id="cb1-18" data-line-number="18">  runServer echo newSocket</a>
<a class="sourceLine" id="cb1-19" data-line-number="19"></a>
<a class="sourceLine" id="cb1-20" data-line-number="20"><span class="ot">runServer ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">Socket</span> <span class="ot">-&gt;</span> <span class="dt">IO</span>()</a>
<a class="sourceLine" id="cb1-21" data-line-number="21">runServer f s <span class="fu">=</span> forever <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-22" data-line-number="22">  (usableSocket,_) <span class="ot">&lt;-</span> accept s</a>
<a class="sourceLine" id="cb1-23" data-line-number="23">  forkIO <span class="fu">$</span> interactWithSocket f usableSocket</a>
<a class="sourceLine" id="cb1-24" data-line-number="24"></a>
<a class="sourceLine" id="cb1-25" data-line-number="25"><span class="ot">interactWithSocket ::</span> (<span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">Socket</span> <span class="ot">-&gt;</span> <span class="dt">IO</span>()</a>
<a class="sourceLine" id="cb1-26" data-line-number="26">interactWithSocket f s <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-27" data-line-number="27">  handle <span class="ot">&lt;-</span> socketToHandle s <span class="dt">ReadWriteMode</span></a>
<a class="sourceLine" id="cb1-28" data-line-number="28">  forever <span class="fu">$</span> f <span class="fu">&lt;$&gt;</span> hGetLine handle <span class="fu">&gt;&gt;=</span> hPutStrLn handle</a>
<a class="sourceLine" id="cb1-29" data-line-number="29"></a>
<a class="sourceLine" id="cb1-30" data-line-number="30"><span class="ot">echo ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb1-31" data-line-number="31">echo <span class="fu">=</span> (<span class="st">&quot;R: &quot;</span> <span class="fu">++</span>)</a></code></pre></div>
<p>Run it and test it by pointing <em>netcat</em> to port 2048.</p>
<p>Once that was built I put together a <em>Dockerfile</em> that uses Debian Jessie as a base and copies in the echo server (I based it on another image):</p>
<pre><code>FROM debian:8.2
MAINTAINER Magnus Therning &lt;magnus@therning.org&gt;

RUN TERM=vt220 apt-get update &amp;&amp; \
    TERM=vt220 DEBIAN_FRONTEND=noninteractive apt-get -y install \
        apt-utils \
        dialog \
        libgmp10 \
    &amp;&amp; true

RUN useradd -G users -m -s /bin/bash myuser &amp;&amp; \
    echo &quot;root:root&quot; | chpasswd

USER myuser
COPY echosrv /home/myuser/
CMD bash --login</code></pre>
<p>I popped the <em>Dockerfile</em> and the binary into a folder <code>srv/</code> so I can build the image using</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb3-1" data-line-number="1">$ <span class="ex">docker</span> build --rm --tag=nw srv</a></code></pre></div>
<p>and then started using</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb4-1" data-line-number="1">$ <span class="ex">docker</span> run --rm --interactive --tty --name=echo nw /home/myuser/echosrv</a></code></pre></div>
<h2 id="a-handy-alias">A handy alias</h2>
<p>The following alias will turn out to be very handy indeed</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb5-1" data-line-number="1">$ <span class="bu">alias</span> docker-pid=<span class="st">&quot;docker inspect --format &#39;{{ .State.Pid }}&#39;&quot;</span></a></code></pre></div>
<h2 id="the-network-setup">The network setup</h2>
<p>The local network is <code>192.168.1.0/24</code> and I found an unused address, <code>192.168.1.199</code> that I decided to use for my experiment. The gateway is on <code>192.168.1.1</code>.</p>
<p>First create the <code>macvlan</code> device:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb6-1" data-line-number="1">$ <span class="fu">sudo</span> ip link add mybridge link enp0s25 type macvlan mode bridge</a></code></pre></div>
<p>Then put it into the network namespace of the running container, and bring it up:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb7-1" data-line-number="1">$ <span class="fu">sudo</span> ip link set netns <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> mybridge</a>
<a class="sourceLine" id="cb7-2" data-line-number="2">$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip link set mybridge up</a></code></pre></div>
<p>Now the device need its address and the routing has to be set up:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb8-1" data-line-number="1">$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip route del default</a>
<a class="sourceLine" id="cb8-2" data-line-number="2">$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip addr add 192.168.1.199/24 dev mybridge</a>
<a class="sourceLine" id="cb8-3" data-line-number="3">$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n ip route add default via 192.168.1.1 dev mybridge</a></code></pre></div>
<p>That’s it, now the docker container is reachable on <code>192.168.1.199</code>. Well, it’s reachable on that ip from any computer on the network except for the host. In order for the host to be able to reach it add another route:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb9-1" data-line-number="1">$ <span class="fu">sudo</span> ip route add 192.168.1.199 dev docker0</a></code></pre></div>
<h2 id="dhcp">DHCP</h2>
<p>If you’d rather have the container on an ip handed out by a DHCP server, then you replace the three commands for setting ip and route with:</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb10-1" data-line-number="1">$ <span class="fu">sudo</span> nsenter -t <span class="va">$(</span><span class="ex">docker-pid</span> echo<span class="va">)</span> -n -- dhclient -d maclan</a></code></pre></div>]]></summary>
</entry>
<entry>
    <title>Building elm with stack</title>
    <link href="http://magnus.therning.org/posts/2015-10-13-000-building-elm-with-stack.html" />
    <id>http://magnus.therning.org/posts/2015-10-13-000-building-elm-with-stack.html</id>
    <published>2015-10-13T00:00:00Z</published>
    <updated>2015-10-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Building <a href="http://elm-lang.org/">elm</a> is a slightly invasive procedure if your distro doesn’t have any pre-built packages for it and has a non-ancient version of <em>ghc</em>. Removing all haskell packages and installing <a href="https://www.haskell.org/platform/">HaskellPlatform</a> didn’t really appeal to me. Then it occured to me that <a href="http://www.stackage.org/">stack</a> ought to be perfectly suited for this task. It took me a while to sort it out, but here’s how I got a working build of the elm tools installed on Archlinux.</p>
<h2 id="installing-stack-and-cabal">Installing <em>stack</em> and <em>cabal</em></h2>
<p>Add the <em>ArchHaskell</em> repo by following the instructions <a href="https://wiki.archlinux.org/index.php/ArchHaskell">on the wiki</a>. Then install the <code>haskell-stack-bin</code> package. For <em>stack</em> to work fully one also needs the <a href="https://aur.archlinux.org/packages/libtinfo/">libtinfo package from AUR</a>.</p>
<p>If you aren’t using Archlinux there are instructions on the <em>stack</em> page.</p>
<p>Now build <em>cabal-install</em> using stack:</p>
<pre><code>% stack --install-ghc install cabal-install</code></pre>
<p>After this make sure <code>~/.local/bin</code> is in your <code>$PATH</code>.</p>
<h2 id="cloning-the-elm-tool-repos">Cloning the elm tool repos</h2>
<p>The elm developers have decided to not use Hackage so to get the tools one has to clone the repos from GitHub. I used the following shell script to get them all cloned in one go:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode bash"><code class="sourceCode bash"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="co">#! /usr/bin/zsh</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2"></a>
<a class="sourceLine" id="cb2-3" data-line-number="3"><span class="va">repos=(</span><span class="st">&quot;git clone https://github.com/elm-lang/elm-compiler.git -b 0.15.1&quot;</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4">       <span class="st">&quot;git clone https://github.com/elm-lang/elm-package.git -b 0.5.1&quot;</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">       <span class="st">&quot;git clone https://github.com/elm-lang/elm-make.git -b 0.2&quot;</span></a>
<a class="sourceLine" id="cb2-6" data-line-number="6">       <span class="st">&quot;git clone https://github.com/elm-lang/elm-reactor.git -b 0.3.2&quot;</span></a>
<a class="sourceLine" id="cb2-7" data-line-number="7">       <span class="st">&quot;git clone https://github.com/elm-lang/elm-repl.git -b 0.4.2&quot;</span></a>
<a class="sourceLine" id="cb2-8" data-line-number="8">      )</a>
<a class="sourceLine" id="cb2-9" data-line-number="9"></a>
<a class="sourceLine" id="cb2-10" data-line-number="10"><span class="kw">for</span> <span class="ex">r</span> in <span class="va">${repos[@]}</span><span class="kw">;</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-11" data-line-number="11">    <span class="bu">eval</span> <span class="va">${r}</span></a>
<a class="sourceLine" id="cb2-12" data-line-number="12"><span class="kw">done</span></a></code></pre></div>
<h2 id="creating-stack.yaml">Creating <code>stack.yaml</code></h2>
<p>To create the initial <code>stack.yalm</code> run</p>
<pre><code>% stack init --resolver ghc-7.8.4</code></pre>
<p>Now open it and make a couple of modifications. First add the line</p>
<pre><code>system-ghc: false</code></pre>
<p>This is to make sure <em>stack</em> won’t try to use any <em>ghc</em> version already installed on the system. Then add a flag for <code>aeson</code>:</p>
<pre><code>flags:
  aeson:
    old-locale: true</code></pre>
<p>Now use <em>stack</em> to create the full <code>stack.yaml</code>:</p>
<pre><code>% stack --install-ghc solver --modify-stack-yaml</code></pre>
<h2 id="patch-elm-reactor">Patch <em>elm-reactor</em></h2>
<p>In order to build <em>elm-reactor</em> it first needs a tiny patch:</p>
<pre><code>diff --git a/backend/Socket.hs b/backend/Socket.hs
index b80a98f..69fe488 100644
--- a/backend/Socket.hs
+++ b/backend/Socket.hs
@@ -18,7 +18,7 @@ fileChangeApp :: FilePath -&gt; WS.ServerApp
 fileChangeApp watchedFile pendingConnection =
   do  connection &lt;- WS.acceptRequest pendingConnection
       Notify.withManager $ \notifyManager -&gt;
-        do  _ &lt;- NDevel.treeExtExists notifyManager &quot;.&quot; &quot;elm&quot; (sendHotSwap watchedFile connection)
+        do  _ &lt;- NDevel.treeExtExists notifyManager &quot;.&quot; &quot;elm&quot; (sendHotSwap watchedFile connection . FP.decodeString)
             keepAlive connection</code></pre>
<h2 id="building-the-tools">Building the tools</h2>
<p>Now build the elm tools (it’s important that <em>elm-reactor</em> is built after <em>elm-make</em>):</p>
<pre><code>% stack install elm-{compiler,make,package,repl}
% stack install elm-reactor</code></pre>
<p>Now just sit back and wait for the building to finish.</p>
<h2 id="verify-that-it-works">Verify that it works</h2>
<p>Follow the instructions for running <a href="https://github.com/evancz/elm-architecture-tutorial/tree/master/examples/1">example 1</a> of the <a href="https://github.com/evancz/elm-architecture-tutorial/">elm architecture tutorial</a>. I had to use the following two commands to get it running properly:</p>
<pre><code>% elm make
% elm reactor</code></pre>
<p>Then I can point my browser at <a href="http://localhost:8000/Main.elm?debug" class="uri">http://localhost:8000/Main.elm?debug</a> and play with the counter.</p>]]></summary>
</entry>
<entry>
    <title>Extracting titles and links from atom feed</title>
    <link href="http://magnus.therning.org/posts/2015-09-29-000-extracting-titles-and-links-from-atom-feed.html" />
    <id>http://magnus.therning.org/posts/2015-09-29-000-extracting-titles-and-links-from-atom-feed.html</id>
    <published>2015-09-29T00:00:00Z</published>
    <updated>2015-09-29T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The code I used to extract the titles and links for the <a href="/posts/2015-09-28-000-systemd-watchdog.html">previous post</a>:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">#! /usr/bin/runhaskell</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="kw">import</span> <span class="dt">System.Environment</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="kw">import</span> <span class="dt">Text.HTML.TagSoup</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="kw">import</span> <span class="dt">Text.Printf</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-8" data-line-number="8">main <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">    [fn] <span class="ot">&lt;-</span> getArgs</a>
<a class="sourceLine" id="cb1-10" data-line-number="10">    readAtomFile fn <span class="fu">&gt;&gt;=</span> writeList</a>
<a class="sourceLine" id="cb1-11" data-line-number="11"></a>
<a class="sourceLine" id="cb1-12" data-line-number="12"><span class="ot">readAtomFile ::</span> FilePath <span class="ot">-&gt;</span> <span class="dt">IO</span> [(<span class="dt">String</span>, <span class="dt">String</span>)]</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">readAtomFile fn <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-14" data-line-number="14">    xml <span class="ot">&lt;-</span> readFile fn</a>
<a class="sourceLine" id="cb1-15" data-line-number="15">    <span class="kw">let</span> xmlTags <span class="fu">=</span> parseTags xml</a>
<a class="sourceLine" id="cb1-16" data-line-number="16">        es <span class="fu">=</span> map parseEntry (getAllEntries xmlTags)</a>
<a class="sourceLine" id="cb1-17" data-line-number="17">    return es</a>
<a class="sourceLine" id="cb1-18" data-line-number="18"></a>
<a class="sourceLine" id="cb1-19" data-line-number="19"><span class="ot">getAllEntries ::</span> [<span class="dt">Tag</span> <span class="dt">String</span>] <span class="ot">-&gt;</span> [[<span class="dt">Tag</span> <span class="dt">String</span>]]</a>
<a class="sourceLine" id="cb1-20" data-line-number="20">getAllEntries <span class="fu">=</span> partitions (<span class="fu">~==</span> <span class="dt">TagOpen</span> <span class="st">&quot;entry&quot;</span> [])</a>
<a class="sourceLine" id="cb1-21" data-line-number="21"></a>
<a class="sourceLine" id="cb1-22" data-line-number="22"><span class="ot">getElemText ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> [<span class="dt">Tag</span> <span class="dt">String</span>] <span class="ot">-&gt;</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb1-23" data-line-number="23">getElemText n <span class="fu">=</span> fromTagText <span class="fu">.</span> (<span class="fu">!!</span> <span class="dv">1</span>) <span class="fu">.</span> dropWhile (<span class="fu">~/=</span> <span class="dt">TagOpen</span> n [])</a>
<a class="sourceLine" id="cb1-24" data-line-number="24"></a>
<a class="sourceLine" id="cb1-25" data-line-number="25">getEntryTitle,<span class="ot"> getEntryLink ::</span> [<span class="dt">Tag</span> <span class="dt">String</span>] <span class="ot">-&gt;</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb1-26" data-line-number="26">getEntryTitle <span class="fu">=</span> getElemText <span class="st">&quot;title&quot;</span></a>
<a class="sourceLine" id="cb1-27" data-line-number="27">getEntryLink <span class="fu">=</span> fromAttrib <span class="st">&quot;href&quot;</span> <span class="fu">.</span> (<span class="fu">!!</span> <span class="dv">0</span>) <span class="fu">.</span> dropWhile (<span class="fu">~/=</span> <span class="dt">TagOpen</span> <span class="st">&quot;link&quot;</span> [])</a>
<a class="sourceLine" id="cb1-28" data-line-number="28"></a>
<a class="sourceLine" id="cb1-29" data-line-number="29"><span class="ot">parseEntry ::</span> [<span class="dt">Tag</span> <span class="dt">String</span>] <span class="ot">-&gt;</span> (<span class="dt">String</span>, <span class="dt">String</span>)</a>
<a class="sourceLine" id="cb1-30" data-line-number="30">parseEntry e <span class="fu">=</span> (title, link)</a>
<a class="sourceLine" id="cb1-31" data-line-number="31">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-32" data-line-number="32">        title <span class="fu">=</span> getEntryTitle e</a>
<a class="sourceLine" id="cb1-33" data-line-number="33">        link <span class="fu">=</span> getEntryLink e</a>
<a class="sourceLine" id="cb1-34" data-line-number="34"></a>
<a class="sourceLine" id="cb1-35" data-line-number="35"><span class="ot">writeList ::</span> [(<span class="dt">String</span>, <span class="dt">String</span>)] <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-36" data-line-number="36">writeList posts <span class="fu">=</span> putStrLn items</a>
<a class="sourceLine" id="cb1-37" data-line-number="37">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-38" data-line-number="38">        items <span class="fu">=</span> unlines <span class="fu">$</span> fmap writeListItem posts</a>
<a class="sourceLine" id="cb1-39" data-line-number="39"></a>
<a class="sourceLine" id="cb1-40" data-line-number="40"><span class="ot">writeListItem ::</span> (<span class="dt">String</span>, <span class="dt">String</span>) <span class="ot">-&gt;</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb1-41" data-line-number="41">writeListItem (t, l) <span class="fu">=</span> printf <span class="st">&quot;- [%s](%s)&quot;</span> t l</a></code></pre></div>
<p>It can also be found as a snippet <a href="https://gitlab.com/snippets/8968">here</a>.</p>]]></summary>
</entry>
<entry>
    <title>Systemd watchdog</title>
    <link href="http://magnus.therning.org/posts/2015-09-28-000-systemd-watchdog.html" />
    <id>http://magnus.therning.org/posts/2015-09-28-000-systemd-watchdog.html</id>
    <published>2015-09-28T00:00:00Z</published>
    <updated>2015-09-28T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I haven’t been posting much lately, since last time I’ve switched jobs. My silence is partly because this new job is more fun than the previous one – hence less need to escape into the sanity offered by Linux, FP (Haskell, OCaml, …), and the other stuff I like to fill my free time with. Now I’ve started to get into the job, and I hope I’ll find stuff to write about and time to do it.</p>
<p>Lately I’ve spent a lot of time reading up on, and playing with <em>systemd</em>. It’s really a very impressive piece of software. I found the series “systemd for Administrators” by Lennart Poettering a very good introduction:</p>
<ul>
<li><a href="http://0pointer.net/blog/projects/systemd-for-admins-1.html">systemd for Administrators, Part 1</a></li>
<li><a href="http://0pointer.net/blog/projects/systemd-for-admins-2.html">systemd for Administrators, Part II</a></li>
<li><a href="http://0pointer.net/blog/projects/systemd-for-admins-3.html">systemd for Administrators, Part III</a></li>
<li><a href="http://0pointer.net/blog/projects/systemd-for-admins-4.html">systemd for Administrators, Part IV</a></li>
<li><a href="http://0pointer.net/blog/projects/three-levels-of-off.html">systemd for Administrators, Part V</a></li>
<li><a href="http://0pointer.net/blog/projects/changing-roots.html">systemd for Administrators, Part VI</a></li>
<li><a href="http://0pointer.net/blog/projects/blame-game.html">systemd for Administrators, Part VII</a></li>
<li><a href="http://0pointer.net/blog/projects/the-new-configuration-files.html">systemd for Administrators, Part VIII</a></li>
<li><a href="http://0pointer.net/blog/projects/on-etc-sysinit.html">systemd for Administrators, Part IX</a></li>
<li><a href="http://0pointer.net/blog/projects/instances.html">systemd for Administrators, Part X</a></li>
<li><a href="http://0pointer.net/blog/projects/inetd.html">systemd for Administrators, Part XI</a></li>
<li><a href="http://0pointer.net/blog/projects/security.html">systemd for Administrators, Part XII</a></li>
<li><a href="http://0pointer.net/blog/projects/systemctl-journal.html">systemd for Administrators, Part XIII</a></li>
<li><a href="http://0pointer.net/blog/projects/self-documented-boot.html">systemd for Administrators, Part XIV</a></li>
<li><a href="http://0pointer.net/blog/projects/watchdog.html">systemd for Administrators, Part XV</a></li>
<li><a href="http://0pointer.net/blog/projects/serial-console.html">systemd for Administrators, Part XVI</a></li>
<li><a href="http://0pointer.net/blog/projects/journalctl.html">systemd for Administrators, Part XVII</a></li>
<li><a href="http://0pointer.net/blog/projects/resources.html">systemd for Administrators, Part XVIII</a></li>
<li><a href="http://0pointer.net/blog/projects/detect-virt.html">systemd for Administrators, Part XIX</a></li>
<li><a href="http://0pointer.net/blog/projects/socket-activated-containers.html">systemd for Administrators, Part XX</a></li>
<li><a href="http://0pointer.net/blog/systemd-for-administrators-part-xxi.html">systemd For Administrators, Part XXI</a></li>
</ul>
<p>There’s also a (much shorter) “systemd for Developers”</p>
<ul>
<li><a href="http://0pointer.net/blog/projects/socket-activation.html">systemd for Developers I</a></li>
<li><a href="http://0pointer.net/blog/projects/socket-activation2.html">systemd for Developers II</a></li>
<li><a href="http://0pointer.net/blog/projects/journal-submit.html">systemd for Developers III</a></li>
</ul>
<h2 id="watchdog-in-systemd">Watchdog in systemd</h2>
<p>One of the nice things offered by <em>systemd</em> is watchdog functionality. For work I had a need to try it out, partly for my own sake (I’ve never really had a need to play with watchdog functinality in any way before) and partly to communicate to the rest of the team how they can modify their parts to integrate with <em>systemd</em>.</p>
<p>As is described in <a href="http://0pointer.net/blog/projects/watchdog.html">part 15</a> the watchdog period is communicated via the environment variable <code>WATCHDOG_USEC</code>. As the name suggests it holds the period in micro seconds. To then tickle the watchdog one uses <code>sd_notify(0, &quot;WATCHDOG=1&quot;)</code>. To test this all I wrote the follwoing bit of C (<code>sd-watch.c</code>):</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode c"><code class="sourceCode c"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="pp">#include </span><span class="im">&lt;stdbool.h&gt;</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="pp">#include </span><span class="im">&lt;stdio.h&gt;</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="pp">#include </span><span class="im">&lt;stdlib.h&gt;</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="pp">#include </span><span class="im">&lt;sys/stat.h&gt;</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5"><span class="pp">#include </span><span class="im">&lt;unistd.h&gt;</span></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="pp">#include </span><span class="im">&lt;systemd/sd-daemon.h&gt;</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="dt">int</span> main(<span class="dt">int</span> ac, <span class="dt">char</span> **av)</a>
<a class="sourceLine" id="cb1-9" data-line-number="9">{</a>
<a class="sourceLine" id="cb1-10" data-line-number="10">  (<span class="dt">void</span>)ac;</a>
<a class="sourceLine" id="cb1-11" data-line-number="11"></a>
<a class="sourceLine" id="cb1-12" data-line-number="12">  <span class="dt">char</span> *e = getenv(<span class="st">&quot;WATCHDOG_USEC&quot;</span>);</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">  <span class="cf">if</span>(!e) {</a>
<a class="sourceLine" id="cb1-14" data-line-number="14">    printf(<span class="st">&quot;No WATCHDOG_USEC set!</span><span class="sc">\n</span><span class="st">&quot;</span>);</a>
<a class="sourceLine" id="cb1-15" data-line-number="15">    exit(<span class="dv">1</span>);</a>
<a class="sourceLine" id="cb1-16" data-line-number="16">  } <span class="cf">else</span></a>
<a class="sourceLine" id="cb1-17" data-line-number="17">    printf(<span class="st">&quot;WATCHDOG_USEC: %s</span><span class="sc">\n</span><span class="st">&quot;</span>, e);</a>
<a class="sourceLine" id="cb1-18" data-line-number="18"></a>
<a class="sourceLine" id="cb1-19" data-line-number="19">  <span class="dt">int</span> i = atoi(e);</a>
<a class="sourceLine" id="cb1-20" data-line-number="20">  i /= <span class="dv">2</span>;</a>
<a class="sourceLine" id="cb1-21" data-line-number="21">  printf(<span class="st">&quot;* Barking every %i usec.</span><span class="sc">\n</span><span class="st">&quot;</span>, i);</a>
<a class="sourceLine" id="cb1-22" data-line-number="22"></a>
<a class="sourceLine" id="cb1-23" data-line-number="23">  <span class="dt">bool</span> cont = true;</a>
<a class="sourceLine" id="cb1-24" data-line-number="24">  <span class="cf">while</span>(cont) {</a>
<a class="sourceLine" id="cb1-25" data-line-number="25">    usleep(i);</a>
<a class="sourceLine" id="cb1-26" data-line-number="26"></a>
<a class="sourceLine" id="cb1-27" data-line-number="27">    <span class="kw">struct</span> stat b;</a>
<a class="sourceLine" id="cb1-28" data-line-number="28">    <span class="dt">int</span> r = stat(av[<span class="dv">1</span>], &amp;b);</a>
<a class="sourceLine" id="cb1-29" data-line-number="29">    <span class="cf">if</span>(<span class="dv">0</span> == r) {</a>
<a class="sourceLine" id="cb1-30" data-line-number="30">      <span class="cf">continue</span>;</a>
<a class="sourceLine" id="cb1-31" data-line-number="31">    } <span class="cf">else</span> {</a>
<a class="sourceLine" id="cb1-32" data-line-number="32">      r = sd_notify(<span class="dv">0</span>, <span class="st">&quot;WATCHDOG=1&quot;</span>);</a>
<a class="sourceLine" id="cb1-33" data-line-number="33">      printf(<span class="st">&quot;Barked!!! (%i)</span><span class="sc">\n</span><span class="st">&quot;</span>, r);</a>
<a class="sourceLine" id="cb1-34" data-line-number="34">    }</a>
<a class="sourceLine" id="cb1-35" data-line-number="35">  }</a>
<a class="sourceLine" id="cb1-36" data-line-number="36"></a>
<a class="sourceLine" id="cb1-37" data-line-number="37">  <span class="cf">return</span> <span class="dv">0</span>;</a>
<a class="sourceLine" id="cb1-38" data-line-number="38">}</a></code></pre></div>
<p>Compile it with <code>gcc -o sd-watch sd-watch.c $(pkg-config --libs libsystemd)</code>.</p>
<p>Together with this comes a service description:</p>
<pre><code>[Unit]
Description=Test for systemd watchdog

[Service]
ExecStart=/usr/local/bin/sd-watch /tmp/foobar
WatchdogSec=5s
Restart=on-failure
StartLimitInterval=1min
StartLimitBurst=2
StartLimitAction=reboot</code></pre>
<p>As you can see I placed the binary in <code>/usr/local/bin</code>, then I placed the service description in <code>/usr/local/lib/systemd/system/sd-watch.service</code>. A modification to <code>/etc/systemd/systemd.con</code> is the last piece, uncomment <code>RuntimeWatchdogSec</code> and set it to a suitable value, I chose <code>60</code>:</p>
<pre><code>RunimeWatchdogSec=60</code></pre>
<p>Just reboot,log in and start the service and follow the log:</p>
<pre><code># systemctl start sd-watch
# journalctl -f</code></pre>
<p>In another terminal create the file, <code>touch /tmp/foobar</code>, and watch what’s written to the log, and after a little while the system should reboot.</p>]]></summary>
</entry>
<entry>
    <title>Using QuickCheck to test C APIs</title>
    <link href="http://magnus.therning.org/posts/2015-06-15-000-using-quickcheck-to-test-c-apis.html" />
    <id>http://magnus.therning.org/posts/2015-06-15-000-using-quickcheck-to-test-c-apis.html</id>
    <published>2015-06-15T00:00:00Z</published>
    <updated>2015-06-15T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Last year at ICFP I attended the <a href="http://cufp.org/2014/t12-john-hughes-introduction-to-testing-with-quickcheck.html">tutorial on QuickCheck</a> with John Hughes. We got to use the Erlang implementation of QuickCheck to test a C API. Ever since I’ve been planning to do the same thing using Haskell. I’ve put it off for the better part of a year now, but then Francesco Mazzoli wrote about <a href="http://hackage.haskell.org/package/inline-c">inline-c</a> (<a href="https://www.fpcomplete.com/blog/2015/05/inline-c">Call C functions from Haskell without bindings</a> and I found the motivation to actually start writing some code.</p>
<h2 id="the-general-idea">The general idea</h2>
<p>Many C APIs are rather stateful beasts so to test it I</p>
<ol type="1">
<li>generate a sequence of API calls (a program of sorts),</li>
<li>run the sequence against a model,</li>
<li>run the sequence against the real implementation, and</li>
<li>compare the model against the real state each step of the way.</li>
</ol>
<h2 id="the-c-api">The C API</h2>
<p>To begin with I hacked up a simple implementation of a stack in C. The “specification” is</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode c"><code class="sourceCode c"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="co">/**</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"><span class="co"> * Create a stack.</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="co"> */</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="dt">void</span> *create();</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="co">/**</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7"><span class="co"> * Push a value onto an existing stack.</span></a>
<a class="sourceLine" id="cb1-8" data-line-number="8"><span class="co"> */</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9"><span class="dt">void</span> push (<span class="dt">void</span> *, <span class="dt">int</span>);</a>
<a class="sourceLine" id="cb1-10" data-line-number="10"></a>
<a class="sourceLine" id="cb1-11" data-line-number="11"><span class="co">/**</span></a>
<a class="sourceLine" id="cb1-12" data-line-number="12"><span class="co"> * Pop a value off an existing stack.</span></a>
<a class="sourceLine" id="cb1-13" data-line-number="13"><span class="co"> */</span></a>
<a class="sourceLine" id="cb1-14" data-line-number="14"><span class="dt">int</span> pop(<span class="dt">void</span> *);</a></code></pre></div>
<p>Using <code>inline-c</code> to create bindings for it is amazingly simple:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">{-# LANGUAGE QuasiQuotes #-}</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2"><span class="ot">{-# LANGUAGE TemplateHaskell #-}</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3"></a>
<a class="sourceLine" id="cb2-4" data-line-number="4"><span class="kw">module</span> <span class="dt">CApi</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-6" data-line-number="6"></a>
<a class="sourceLine" id="cb2-7" data-line-number="7"><span class="kw">import</span> <span class="kw">qualified</span> <span class="dt">Language.C.Inline</span> <span class="kw">as</span> <span class="dt">C</span></a>
<a class="sourceLine" id="cb2-8" data-line-number="8"><span class="kw">import</span> <span class="dt">Foreign.Ptr</span></a>
<a class="sourceLine" id="cb2-9" data-line-number="9"></a>
<a class="sourceLine" id="cb2-10" data-line-number="10">C.include <span class="st">&quot;stack.h&quot;</span></a>
<a class="sourceLine" id="cb2-11" data-line-number="11"></a>
<a class="sourceLine" id="cb2-12" data-line-number="12"><span class="ot">create ::</span> <span class="dt">IO</span> (<span class="dt">Ptr</span> ())</a>
<a class="sourceLine" id="cb2-13" data-line-number="13">create <span class="fu">=</span> [C.exp<span class="fu">|</span> void <span class="fu">*</span> { create() } <span class="fu">|</span>]</a>
<a class="sourceLine" id="cb2-14" data-line-number="14"></a>
<a class="sourceLine" id="cb2-15" data-line-number="15"><span class="ot">push ::</span> <span class="dt">Ptr</span> () <span class="ot">-&gt;</span> <span class="dt">C.CInt</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb2-16" data-line-number="16">push s i <span class="fu">=</span> [C.exp<span class="fu">|</span> void { push(<span class="fu">$</span>(void <span class="fu">*</span>s), <span class="fu">$</span>(int i)) } <span class="fu">|</span>]</a>
<a class="sourceLine" id="cb2-17" data-line-number="17"></a>
<a class="sourceLine" id="cb2-18" data-line-number="18"><span class="ot">pop ::</span> <span class="dt">Ptr</span> () <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">C.CInt</span></a>
<a class="sourceLine" id="cb2-19" data-line-number="19">pop s <span class="fu">=</span> [C.exp<span class="fu">|</span> int { pop(<span class="fu">$</span>(void <span class="fu">*</span>s)) } <span class="fu">|</span>]</a></code></pre></div>
<p>In the code below I import this module qualified.</p>
<h2 id="representing-a-program">Representing a program</h2>
<p>To represent a sequence of calls I first used a custom type, but later realised that there really was no reason at all to not use a wrapped list:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">newtype</span> <span class="dt">Program</span> a <span class="fu">=</span> <span class="dt">P</span> [a]</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Foldable</span>, <span class="dt">Functor</span>, <span class="dt">Show</span>, <span class="dt">Traversable</span>)</a></code></pre></div>
<p>Then each of the C API functions can be represented with</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="kw">data</span> <span class="dt">Statement</span> <span class="fu">=</span> <span class="dt">Create</span> <span class="fu">|</span> <span class="dt">Push</span> <span class="dt">Int</span> <span class="fu">|</span> <span class="dt">Pop</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a></code></pre></div>
<h2 id="arbitrary-for-statement"><code>Arbitrary</code> for <code>Statement</code></h2>
<p>My implementation of <code>Arbitrary</code> for <code>Statement</code> is very simple:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">Arbitrary</span> <span class="dt">Statement</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2">    arbitrary <span class="fu">=</span> oneof [return <span class="dt">Create</span>, return <span class="dt">Pop</span>, liftM <span class="dt">Push</span> arbitrary]</a>
<a class="sourceLine" id="cb5-3" data-line-number="3">    shrink (<span class="dt">Push</span> i) <span class="fu">=</span> <span class="dt">Push</span> <span class="fu">&lt;$&gt;</span> shrink i</a>
<a class="sourceLine" id="cb5-4" data-line-number="4">    shrink _ <span class="fu">=</span> []</a></code></pre></div>
<p>That is, <code>arbitrary</code> just returns one of the constructors of <code>Statement</code>, and shrinking only returns anything for the one constructor that takes an argument, <code>Push</code>.</p>
<h2 id="prerequisites-of-arbitrary-for-program-statement">Prerequisites of <code>Arbitrary</code> for <code>Program Statement</code></h2>
<p>I want to ensure that all <code>Program Statement</code> are valid, which means I need to define the model for running the program and functions for checking the precondition of a statement as well as for updating the model (i.e. for running the <code>Statement</code>).</p>
<p>Based on the <a href="#the-c-api">C API</a> above it seems necessary to track creation, the contents of the stack, and even if it isn’t explicitly mentioned it’s probably a good idea to track the popped value. Using <a href="http://hackage.haskell.org/package/record"><code>record</code></a> (<code>Record</code> is imported as <code>R</code>, and <code>Record.Lens</code> as <code>RL</code>) I defined it like this:</p>
<div class="sourceCode" id="cb6"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb6-1" data-line-number="1"><span class="kw">type</span> <span class="dt">ModelContext</span> <span class="fu">=</span> [R.r<span class="fu">|</span> {<span class="ot"> created ::</span> <span class="dt">Bool</span>,<span class="ot"> pop ::</span> <span class="dt">Maybe</span> <span class="dt">Int</span>,<span class="ot"> stack ::</span> [<span class="dt">Int</span>] } <span class="fu">|</span>]</a></code></pre></div>
<p>Based on the rather informal specification I coded the pre-conditions for the three statements as</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="ot">preCond ::</span> <span class="dt">ModelContext</span> <span class="ot">-&gt;</span> <span class="dt">Statement</span> <span class="ot">-&gt;</span> <span class="dt">Bool</span></a>
<a class="sourceLine" id="cb7-2" data-line-number="2">preCond ctx <span class="dt">Create</span> <span class="fu">=</span> not <span class="fu">$</span> RL.view [R.l<span class="fu">|</span> created <span class="fu">|</span>] ctx</a>
<a class="sourceLine" id="cb7-3" data-line-number="3">preCond ctx (<span class="dt">Push</span> _) <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> created <span class="fu">|</span>] ctx</a>
<a class="sourceLine" id="cb7-4" data-line-number="4">preCond ctx <span class="dt">Pop</span> <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> created <span class="fu">|</span>] ctx</a></code></pre></div>
<p>That is</p>
<ul>
<li><code>Create</code> requires that the stack hasn’t been created already.</li>
<li><code>Push i</code> requires that the stack has been created.</li>
<li><code>Pop</code> also requires that the stack has been created.</li>
</ul>
<p>Furthermore the “specification” suggests the following definition of a function for running a statement:</p>
<div class="sourceCode" id="cb8"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb8-1" data-line-number="1"><span class="ot">modelRunStatement ::</span> <span class="dt">ModelContext</span> <span class="ot">-&gt;</span> <span class="dt">Statement</span> <span class="ot">-&gt;</span> <span class="dt">ModelContext</span></a>
<a class="sourceLine" id="cb8-2" data-line-number="2">modelRunStatement ctx <span class="dt">Create</span> <span class="fu">=</span> RL.set [R.l<span class="fu">|</span> created <span class="fu">|</span>] <span class="dt">True</span> ctx</a>
<a class="sourceLine" id="cb8-3" data-line-number="3">modelRunStatement ctx (<span class="dt">Push</span> i) <span class="fu">=</span> RL.over [R.l<span class="fu">|</span> stack <span class="fu">|</span>] (i <span class="fu">:</span>) ctx</a>
<a class="sourceLine" id="cb8-4" data-line-number="4">modelRunStatement ctx <span class="dt">Pop</span> <span class="fu">=</span> [R.r<span class="fu">|</span> { created <span class="fu">=</span> c, pop <span class="fu">=</span> headMay s, stack <span class="fu">=</span> tail s } <span class="fu">|</span>]</a>
<a class="sourceLine" id="cb8-5" data-line-number="5">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb8-6" data-line-number="6">        c <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> created <span class="fu">|</span>] ctx</a>
<a class="sourceLine" id="cb8-7" data-line-number="7">        s <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> stack <span class="fu">|</span>] ctx</a></code></pre></div>
<p>(This definition assumes that the model satisfies the pre-conditions, as can be seen in the use of <code>tail</code>.)</p>
<h2 id="arbitrary-for-program-statement"><code>Arbitrary</code> for <code>Program Statement</code></h2>
<p>With this in place I can define <code>Arbitrary</code> for <code>Program Statement</code> as follows.</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="kw">instance</span> <span class="dt">Arbitrary</span> (<span class="dt">Program</span> <span class="dt">Statement</span>) <span class="kw">where</span></a>
<a class="sourceLine" id="cb9-2" data-line-number="2">    arbitrary <span class="fu">=</span> liftM <span class="dt">P</span> <span class="fu">$</span> ar baseModelCtx</a>
<a class="sourceLine" id="cb9-3" data-line-number="3">        <span class="kw">where</span></a>
<a class="sourceLine" id="cb9-4" data-line-number="4">            ar m <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-5" data-line-number="5">                push <span class="ot">&lt;-</span> liftM <span class="dt">Push</span> arbitrary</a>
<a class="sourceLine" id="cb9-6" data-line-number="6">                <span class="kw">let</span> possible <span class="fu">=</span> filter (preCond m) [<span class="dt">Create</span>, <span class="dt">Pop</span>, push]</a>
<a class="sourceLine" id="cb9-7" data-line-number="7">                <span class="kw">if</span> null possible</a>
<a class="sourceLine" id="cb9-8" data-line-number="8">                    <span class="kw">then</span> return []</a>
<a class="sourceLine" id="cb9-9" data-line-number="9">                    <span class="kw">else</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb9-10" data-line-number="10">                        s <span class="ot">&lt;-</span> oneof (map return possible)</a>
<a class="sourceLine" id="cb9-11" data-line-number="11">                        <span class="kw">let</span> m&#39; <span class="fu">=</span> modelRunStatement m s</a>
<a class="sourceLine" id="cb9-12" data-line-number="12">                        frequency [(<span class="dv">499</span>, liftM2 (<span class="fu">:</span>) (return s) (ar m&#39;)), (<span class="dv">1</span>, return [])]</a></code></pre></div>
<p>The idea is to, in each step, choose a valid statement given the provided model and cons it with the result of a recursive call with an updated model. The constant 499 is just an arbitrary one I chose after running <code>arbitrary</code> a few times to see how long the generated programs were.</p>
<p>For shrinking I take advantage of the already existing implementation for lists:</p>
<div class="sourceCode" id="cb10"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb10-1" data-line-number="1">    shrink (<span class="dt">P</span> p) <span class="fu">=</span> filter allowed <span class="fu">$</span> map <span class="dt">P</span> (shrink p)</a>
<a class="sourceLine" id="cb10-2" data-line-number="2">        <span class="kw">where</span></a>
<a class="sourceLine" id="cb10-3" data-line-number="3">            allowed <span class="fu">=</span> and <span class="fu">.</span> snd <span class="fu">.</span> mapAccumL go baseModelCtx</a>
<a class="sourceLine" id="cb10-4" data-line-number="4">                <span class="kw">where</span></a>
<a class="sourceLine" id="cb10-5" data-line-number="5">                    go ctx s <span class="fu">=</span> (modelRunStatement ctx s, preCond ctx s)</a></code></pre></div>
<h2 id="some-thoughts-so-far">Some thoughts so far</h2>
<p>I would love making an implementation of <code>Arbitrary s</code>, where <code>s</code> is something that implements a type class that contains <code>preCond</code>, <code>modelRunStatement</code> and anything else needed. I made an attempt using something like</p>
<div class="sourceCode" id="cb11"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb11-1" data-line-number="1"><span class="kw">class</span> <span class="dt">S</span> a <span class="kw">where</span></a>
<a class="sourceLine" id="cb11-2" data-line-number="2">    <span class="kw">type</span> <span class="dt">Ctx</span><span class="ot"> a ::</span> <span class="fu">*</span></a>
<a class="sourceLine" id="cb11-3" data-line-number="3"></a>
<a class="sourceLine" id="cb11-4" data-line-number="4"><span class="ot">    baseCtx ::</span> <span class="dt">Ctx</span> a</a>
<a class="sourceLine" id="cb11-5" data-line-number="5"><span class="ot">    preCond ::</span> <span class="dt">Ctx</span> a <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> <span class="dt">Bool</span></a>
<a class="sourceLine" id="cb11-6" data-line-number="6">    <span class="fu">...</span></a></code></pre></div>
<p>However, when trying to use <code>baseCtx</code> in an implementation of <code>arbitrary</code> I ran into the issue of injectivity. I’m still not entirely sure what that means, or if there is something I can do to work around it. Hopefully someone reading this can offer a solution.</p>
<h2 id="running-the-c-code">Running the C code</h2>
<p>When running the sequence of <code>Statement</code> against the C code I catch the results in</p>
<div class="sourceCode" id="cb12"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb12-1" data-line-number="1"><span class="kw">type</span> <span class="dt">RealContext</span> <span class="fu">=</span> [r| { o :: Ptr (), pop :: Maybe Int } |]</a></code></pre></div>
<p>Actually running a statement and capturing the output in a <code>RealContext</code> is easily done using <code>inline-c</code> and <code>record</code>:</p>
<div class="sourceCode" id="cb13"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb13-1" data-line-number="1"><span class="ot">realRunStatement ::</span> <span class="dt">RealContext</span> <span class="ot">-&gt;</span> <span class="dt">Statement</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">RealContext</span></a>
<a class="sourceLine" id="cb13-2" data-line-number="2">realRunStatement ctx <span class="dt">Create</span> <span class="fu">=</span> CApi.create <span class="fu">&gt;&gt;=</span> \ ptr <span class="ot">-&gt;</span> return <span class="fu">$</span> RL.set [R.l<span class="fu">|</span> o <span class="fu">|</span>] ptr ctx</a>
<a class="sourceLine" id="cb13-3" data-line-number="3">realRunStatement ctx (<span class="dt">Push</span> i) <span class="fu">=</span> CApi.push o (toEnum i) <span class="fu">&gt;&gt;</span> return ctx</a>
<a class="sourceLine" id="cb13-4" data-line-number="4">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb13-5" data-line-number="5">        o <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> o <span class="fu">|</span>] ctx</a>
<a class="sourceLine" id="cb13-6" data-line-number="6">realRunStatement ctx <span class="dt">Pop</span> <span class="fu">=</span> CApi.pop o <span class="fu">&gt;&gt;=</span> \ v <span class="ot">-&gt;</span> return <span class="fu">$</span> RL.set [R.l<span class="fu">|</span> pop <span class="fu">|</span>] (<span class="dt">Just</span> (fromEnum v)) ctx</a>
<a class="sourceLine" id="cb13-7" data-line-number="7">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb13-8" data-line-number="8">        o <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> o <span class="fu">|</span>] ctx</a></code></pre></div>
<h2 id="comparing-states">Comparing states</h2>
<p>Comparing a <code>ModelContext</code> and a <code>RealContext</code> is easily done:</p>
<div class="sourceCode" id="cb14"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb14-1" data-line-number="1"><span class="ot">compCtx ::</span> <span class="dt">ModelContext</span> <span class="ot">-&gt;</span> <span class="dt">RealContext</span> <span class="ot">-&gt;</span> <span class="dt">Bool</span></a>
<a class="sourceLine" id="cb14-2" data-line-number="2">compCtx mc rc <span class="fu">=</span> mcC <span class="fu">==</span> rcC <span class="fu">&amp;&amp;</span> mcP <span class="fu">==</span> rcP</a>
<a class="sourceLine" id="cb14-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb14-4" data-line-number="4">        mcC <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> created <span class="fu">|</span>] mc</a>
<a class="sourceLine" id="cb14-5" data-line-number="5">        rcC <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> o <span class="fu">|</span>] rc <span class="fu">/=</span> nullPtr</a>
<a class="sourceLine" id="cb14-6" data-line-number="6">        mcP <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> pop<span class="fu">|</span>] mc</a>
<a class="sourceLine" id="cb14-7" data-line-number="7">        rcP <span class="fu">=</span> RL.view [R.l<span class="fu">|</span> pop<span class="fu">|</span>] rc</a></code></pre></div>
<h2 id="verifying-a-program-statement">Verifying a <code>Program Statement</code></h2>
<p>With all that in place I can finally write a function for checking the validity of a program:</p>
<div class="sourceCode" id="cb15"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb15-1" data-line-number="1"><span class="ot">validProgram ::</span> <span class="dt">Program</span> <span class="dt">Statement</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> <span class="dt">Bool</span></a>
<a class="sourceLine" id="cb15-2" data-line-number="2">validProgram p <span class="fu">=</span> and <span class="fu">&lt;$&gt;</span> snd <span class="fu">&lt;$&gt;</span> mapAccumM go (baseModelCtx, baseRealContext) p</a>
<a class="sourceLine" id="cb15-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb15-4" data-line-number="4">        runSingleStatement mc rc s <span class="fu">=</span> realRunStatement rc s <span class="fu">&gt;&gt;=</span> \ rc&#39; <span class="ot">-&gt;</span> return (modelRunStatement mc s, rc&#39;)</a>
<a class="sourceLine" id="cb15-5" data-line-number="5"></a>
<a class="sourceLine" id="cb15-6" data-line-number="6">        go (mc, rc) s <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb15-7" data-line-number="7">            ctxs<span class="fu">@</span>(mc&#39;, rc&#39;) <span class="ot">&lt;-</span> runSingleStatement mc rc s</a>
<a class="sourceLine" id="cb15-8" data-line-number="8">            return (ctxs, compCtx mc&#39; rc&#39;)</a></code></pre></div>
<p>(This uses <code>mapAccumM</code> from an <a href="/posts/2015-06-09-000-mapaccum-in-monad.html">earlier post of mine</a>.)</p>
<h2 id="the-property-finally">The property, finally!</h2>
<p>To wrap this all up I then define the property</p>
<div class="sourceCode" id="cb16"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb16-1" data-line-number="1"><span class="ot">prop_program ::</span> <span class="dt">Program</span> <span class="dt">Statement</span> <span class="ot">-&gt;</span> <span class="dt">Property</span></a>
<a class="sourceLine" id="cb16-2" data-line-number="2">prop_program p <span class="fu">=</span> monadicIO <span class="fu">$</span> run (validProgram p) <span class="fu">&gt;&gt;=</span> assert</a></code></pre></div>
<p>and a main function</p>
<div class="sourceCode" id="cb17"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb17-1" data-line-number="1"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb17-2" data-line-number="2">main <span class="fu">=</span> quickCheck prop_program</a></code></pre></div>
<hr />
<p><em>Edit 2015-07-17</em>: Adjusted the description of the pre-conditions to match the code.</p>]]></summary>
</entry>
<entry>
    <title>mapAccum in monad</title>
    <link href="http://magnus.therning.org/posts/2015-06-09-000-mapaccum-in-monad.html" />
    <id>http://magnus.therning.org/posts/2015-06-09-000-mapaccum-in-monad.html</id>
    <published>2015-06-09T00:00:00Z</published>
    <updated>2015-06-09T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I recently had two functions of very similar shape, only difference was that one was pure and the other need some I/O. The former was easily written using <code>mapAccumL</code>. I failed to find a function like <code>mapAccumL</code> that runs in a monad, so I wrote up the following:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">mapAccumM ::</span> (<span class="dt">Monad</span> m, <span class="dt">Traversable</span> t) <span class="ot">=&gt;</span> (a <span class="ot">-&gt;</span> b <span class="ot">-&gt;</span> m (a, c)) <span class="ot">-&gt;</span> a <span class="ot">-&gt;</span> t b <span class="ot">-&gt;</span> m (a, t c)</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">mapAccumM f a l <span class="fu">=</span> swap <span class="fu">&lt;$&gt;</span> runStateT (mapM go l) a</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">        go i <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">            s <span class="ot">&lt;-</span> get</a>
<a class="sourceLine" id="cb1-6" data-line-number="6">            (s&#39;, r) <span class="ot">&lt;-</span> lift <span class="fu">$</span> f s i</a>
<a class="sourceLine" id="cb1-7" data-line-number="7">            put s&#39;</a>
<a class="sourceLine" id="cb1-8" data-line-number="8">            return r</a></code></pre></div>
<p>Bring on the comments/suggestions/improvements/etc!</p>]]></summary>
</entry>
<entry>
    <title>Oh no!  Success</title>
    <link href="http://magnus.therning.org/posts/2015-06-04-000-oh-no---success.html" />
    <id>http://magnus.therning.org/posts/2015-06-04-000-oh-no---success.html</id>
    <published>2015-06-04T00:00:00Z</published>
    <updated>2015-06-04T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p><a href="http://www.automatedtrader.net/news/at/153887/otas-launches-otas-lingo">This</a> can’t possibly be good for Haskell…</p>
<blockquote>
<p>We chose Haskell and the FP Complete stack because we knew the complexity of the problem required a new approach. The result was that we built better software faster than ever, and delivered it defect-free into a production environment where it has proven robust and high-performance</p>
</blockquote>]]></summary>
</entry>
<entry>
    <title>Re. cryptonite</title>
    <link href="http://magnus.therning.org/posts/2015-06-03-000-re.-cryptonite.html" />
    <id>http://magnus.therning.org/posts/2015-06-03-000-re.-cryptonite.html</id>
    <published>2015-06-03T00:00:00Z</published>
    <updated>2015-06-03T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p><a href="http://tab.snarc.org/posts/haskell/2015-06-02-announcing_cryptonite.html">Excellent work</a> Vincent, but then you’ve never done anything but! I’m looking forward to purging all the old crypto packages from ArchHaskell in the near future.</p>
<p>Oh, BTW, why Disqus for comments?</p>]]></summary>
</entry>
<entry>
    <title>Facebook and PGP</title>
    <link href="http://magnus.therning.org/posts/2015-06-02-000-facebook-and-pgp.html" />
    <id>http://magnus.therning.org/posts/2015-06-02-000-facebook-and-pgp.html</id>
    <published>2015-06-02T00:00:00Z</published>
    <updated>2015-06-02T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I wonder if <a href="http://boingboing.net/2015/06/01/facebook-users-can-now-use-pgp.html">this</a> will have any impact at all on PGP usage… I doubt it, but it’s still nice to see it done. The Register also <a href="http://www.theregister.co.uk/2015/06/01/facebook_pgp_support/">writes about it</a>.</p>]]></summary>
</entry>
<entry>
    <title>Timing out in conduit</title>
    <link href="http://magnus.therning.org/posts/2015-03-01-000-timing-out-in-conduit.html" />
    <id>http://magnus.therning.org/posts/2015-03-01-000-timing-out-in-conduit.html</id>
    <published>2015-03-01T00:00:00Z</published>
    <updated>2015-03-01T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The previous post in my play-with-state-machines-and-conduit series was a bit of a detour into <a href="/posts/2015-01-22-000-combining-inputs-in-conduit.html">how to combine inputs in <code>conduit</code></a>. In this post I’ll try to wrap it up by making a <code>conduit</code> pipeline with a timeout.</p>
<h3 id="prelude">Prelude</h3>
<p>This post doesn’t reflect my first attempt at writing a state machine that times out. My first attempt was to actually write a state machine that timed out. I combined the input from the user with a tick generated once a second into a single event using <code>Either</code>. Each state of the machine then needed to keep track of the number of ticks it had seen in order to time out on seeing the fourth one. The counter was reset on each state transition. Obviously this led to a rather complicated state machine.</p>
<p>Then I decided to attempt to explore having more but simpler parts in the <code>conduit</code> pipeline. I think that strategy resulted in a much cleaner and simpler design.</p>
<h3 id="a-state-machine-for-timing-out">A state machine for timing out</h3>
<p>The state machine for the addition is unchanged from the <a href="/posts/2015-01-06-000-adder-as-a-state-machine.html">earlier post</a> and the timing out is added via a second state machine:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">TOState</span> <span class="fu">=</span> <span class="dt">TOGood</span> <span class="dt">Int</span> <span class="fu">|</span> <span class="dt">TOTimedOut</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb1-3" data-line-number="3"></a>
<a class="sourceLine" id="cb1-4" data-line-number="4"><span class="kw">data</span> <span class="dt">TOSignal</span> a <span class="fu">=</span> <span class="dt">TONoop</span> <span class="fu">|</span> <span class="dt">TOVal</span> a <span class="fu">|</span> <span class="dt">TOTime</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5"></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="ot">toMachine ::</span> <span class="dt">Machine</span> <span class="dt">TOState</span> (<span class="dt">Either</span> t a) (<span class="dt">TOSignal</span> a)</a>
<a class="sourceLine" id="cb1-7" data-line-number="7">toMachine <span class="fu">=</span> createMachine (<span class="dt">TOGood</span> <span class="dv">0</span>) go</a>
<a class="sourceLine" id="cb1-8" data-line-number="8">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-9" data-line-number="9">        go (<span class="dt">TOGood</span> _) (<span class="dt">Right</span> e) <span class="fu">=</span> (<span class="dt">TOGood</span> <span class="dv">0</span>, <span class="dt">TOVal</span> e)</a>
<a class="sourceLine" id="cb1-10" data-line-number="10"></a>
<a class="sourceLine" id="cb1-11" data-line-number="11">        go (<span class="dt">TOGood</span> n) (<span class="dt">Left</span> _)</a>
<a class="sourceLine" id="cb1-12" data-line-number="12">            <span class="fu">|</span> n <span class="fu">&gt;</span> <span class="dv">3</span> <span class="fu">=</span> (<span class="dt">TOTimedOut</span>, <span class="dt">TOTime</span>)</a>
<a class="sourceLine" id="cb1-13" data-line-number="13">            <span class="fu">|</span> otherwise <span class="fu">=</span> (<span class="dt">TOGood</span> (n <span class="fu">+</span> <span class="dv">1</span>), <span class="dt">TONoop</span>)</a>
<a class="sourceLine" id="cb1-14" data-line-number="14"></a>
<a class="sourceLine" id="cb1-15" data-line-number="15">        go <span class="dt">TOTimedOut</span> _ <span class="fu">=</span> (<span class="dt">TOTimedOut</span>, <span class="dt">TOTime</span>)</a></code></pre></div>
<p>It’s a tiny machine of only two states. The ticks come in via <code>Left _</code> values and cause the counter to be increased each time. The <code>Right e</code> values are simply re-wrapped and passed on together with a resetting of the counter.</p>
<h3 id="the-process">The process</h3>
<p>The process is built up by combing the users input with a ticker source, the combination is passed through the time-out machine. After that the signal is inspected to see if it’s time to terminate, if not the inner value is extracted so it can be shown to the user.</p>
<p>The ticker source is a source that repeatedly waits followed by yielding a <code>Tick</code>:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">sourceTick ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Int</span> <span class="ot">-&gt;</span> <span class="dt">Source</span> m <span class="dt">Tick</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2">sourceTick w <span class="fu">=</span> forever <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3">    liftIO <span class="fu">$</span> threadDelay w</a>
<a class="sourceLine" id="cb2-4" data-line-number="4">    C.yield (<span class="dt">Tick</span> w)</a></code></pre></div>
<p>The full process can then be written like this:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">process ::</span> <span class="dt">ResourceT</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">process <span class="fu">=</span> ticker source <span class="fu">$=</span> timerOuter <span class="fu">$=</span> calcRes <span class="fu">$$</span> output</a>
<a class="sourceLine" id="cb3-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-4" data-line-number="4">        source <span class="fu">=</span> stdinC <span class="fu">$=</span> concatMapC unpack <span class="fu">$=</span> calculator <span class="fu">$=</span> mapC <span class="dt">Right</span></a>
<a class="sourceLine" id="cb3-5" data-line-number="5"></a>
<a class="sourceLine" id="cb3-6" data-line-number="6">        calculator <span class="fu">=</span> wrapMachine calcMachine</a>
<a class="sourceLine" id="cb3-7" data-line-number="7"></a>
<a class="sourceLine" id="cb3-8" data-line-number="8">        ticker s <span class="fu">=</span> whyTMVar s tickSource <span class="fu">=$=</span> wrapMachine toMachine</a>
<a class="sourceLine" id="cb3-9" data-line-number="9">            <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-10" data-line-number="10">                tickSource <span class="fu">=</span> sourceTick <span class="dv">1000000</span> <span class="fu">$=</span> mapC <span class="dt">Left</span></a>
<a class="sourceLine" id="cb3-11" data-line-number="11"></a>
<a class="sourceLine" id="cb3-12" data-line-number="12">        timerOuter <span class="fu">=</span> takeWhileC p1 <span class="fu">=$=</span> filterC p2 <span class="fu">=$=</span> mapC u</a>
<a class="sourceLine" id="cb3-13" data-line-number="13">            <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-14" data-line-number="14">                p1 <span class="dt">TOTime</span> <span class="fu">=</span> <span class="dt">False</span></a>
<a class="sourceLine" id="cb3-15" data-line-number="15">                p1 _ <span class="fu">=</span> <span class="dt">True</span></a>
<a class="sourceLine" id="cb3-16" data-line-number="16"></a>
<a class="sourceLine" id="cb3-17" data-line-number="17">                p2 (<span class="dt">TOVal</span> _) <span class="fu">=</span> <span class="dt">True</span></a>
<a class="sourceLine" id="cb3-18" data-line-number="18">                p2 _ <span class="fu">=</span> <span class="dt">False</span></a>
<a class="sourceLine" id="cb3-19" data-line-number="19"></a>
<a class="sourceLine" id="cb3-20" data-line-number="20">                u (<span class="dt">TOVal</span> a) <span class="fu">=</span> a</a>
<a class="sourceLine" id="cb3-21" data-line-number="21">                u _ <span class="fu">=</span> undefined <span class="co">-- partial, but all right</span></a>
<a class="sourceLine" id="cb3-22" data-line-number="22"></a>
<a class="sourceLine" id="cb3-23" data-line-number="23">        calcRes <span class="fu">=</span> mapC f</a>
<a class="sourceLine" id="cb3-24" data-line-number="24">            <span class="kw">where</span></a>
<a class="sourceLine" id="cb3-25" data-line-number="25">                f <span class="dt">CalcNothing</span> <span class="fu">=</span> <span class="st">&quot;&quot;</span></a>
<a class="sourceLine" id="cb3-26" data-line-number="26">                f (<span class="dt">CalcResult</span> n) <span class="fu">=</span> <span class="st">&quot; =&quot;</span> <span class="fu">++</span> show n <span class="fu">++</span> <span class="st">&quot;\n&quot;</span></a>
<a class="sourceLine" id="cb3-27" data-line-number="27">                f e<span class="fu">@</span>(<span class="dt">CalcError</span> {}) <span class="fu">=</span> <span class="st">&quot;\n*** &quot;</span> <span class="fu">++</span> show e <span class="fu">++</span> <span class="st">&quot;\n&quot;</span></a>
<a class="sourceLine" id="cb3-28" data-line-number="28"></a>
<a class="sourceLine" id="cb3-29" data-line-number="29">        output <span class="fu">=</span> mapC pack <span class="fu">=$</span> stdoutC</a></code></pre></div>
<p>Using the same definition of <code>main</code> as from the earlier post results in the following behaviour:</p>
<pre><code>% runhaskell CalculatorTimeOut.hs
5 6 + =11
5 6 7
*** CalcError (ReadOperator 5 6) &quot;Bad operator&quot;
42 17 + =59</code></pre>
<p>The process can be terminated by either pressing Ctrl-C, just like before, or by waiting for a time-out, like I did above.</p>]]></summary>
</entry>
<entry>
    <title>Combining inputs in conduit</title>
    <link href="http://magnus.therning.org/posts/2015-01-22-000-combining-inputs-in-conduit.html" />
    <id>http://magnus.therning.org/posts/2015-01-22-000-combining-inputs-in-conduit.html</id>
    <published>2015-01-22T00:00:00Z</published>
    <updated>2015-01-22T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The code the post on using my <a href="/posts/2015-01-13-000-state-machine-with-conduit.html">simplistic state machine together with conduit</a> will happily wait forever for input, and the only way to terminate it is pressing Ctrl-C. In this series I’m writing a simple adder machine, but my actual use case for these ideas are protocols for communication (see the <a href="/posts/2015-01-03-000-simple-state-machines-in-haskell.html">first post</a>), and waiting forever isn’t such a good thing; I want my machine to time out if input doesn’t arrive in a timely fashion.</p>
<p>I can think of a few ways to achieve this:</p>
<ol type="1">
<li>Use a watchdog thread that is signalled from the main thread, e.g. by a <code>Conduit</code> that sends a signal as each <code>Char</code> passes through it.</li>
<li>As each <code>Char</code> is read kick off a “timeout thread” which throws an exception back to the main thread, unless the main thread kills it before the timeout expires.</li>
<li>Run a thread that creates ticks that then can be combined with the <code>Char</code>s read and fed into the state machine itself.</li>
</ol>
<p>Since this is all about state machines I’ve opted for option 3. Furthermore, since I’ve recently finished reading the excellent <em>Parallel and Concurrent Programming in Haskell</em> I decided to attempt writing the <em>conduit</em> code myself instead of using something like <a href="http://hackage.haskell.org/package/stm-conduit">stm-conduit</a>.</p>
<p>The idea is to write two functions:</p>
<ol type="1">
<li>one <code>Source</code> to combine two <code>Source</code>s, and</li>
<li>one <code>Sink</code> that writes its input into a <code>TMVar</code>.</li>
</ol>
<p>The latter of the two is the easiest one. Given a <code>TMVar</code> it just awaits input, stores it in the <code>TMVar</code> and then calls itself:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">sinkTMVar ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">TMVar</span> a <span class="ot">-&gt;</span> <span class="dt">Sink</span> a m ()</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">sinkTMVar tmv <span class="fu">=</span> forever <span class="fu">$</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    v <span class="ot">&lt;-</span> await</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    <span class="kw">case</span> v <span class="kw">of</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">        <span class="dt">Nothing</span> <span class="ot">-&gt;</span> return ()</a>
<a class="sourceLine" id="cb1-6" data-line-number="6">        <span class="dt">Just</span> v&#39; <span class="ot">-&gt;</span> liftIO (atomically <span class="fu">$</span> putTMVar tmv v&#39;)</a></code></pre></div>
<p>The other one is only slightly more involved:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">whyTMVar ::</span> <span class="dt">MonadIO</span> m <span class="ot">=&gt;</span> <span class="dt">Source</span> (<span class="dt">ResourceT</span> <span class="dt">IO</span>) a <span class="ot">-&gt;</span> <span class="dt">Source</span> (<span class="dt">ResourceT</span> <span class="dt">IO</span>) a <span class="ot">-&gt;</span> <span class="dt">Source</span> m a</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">whyTMVar src1 src2 <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3">    t1 <span class="ot">&lt;-</span> liftIO newEmptyTMVarIO </a>
<a class="sourceLine" id="cb2-4" data-line-number="4">    t2 <span class="ot">&lt;-</span> liftIO newEmptyTMVarIO</a>
<a class="sourceLine" id="cb2-5" data-line-number="5">    void <span class="fu">$</span> liftIO <span class="fu">$</span> async <span class="fu">$</span> fstProc t1</a>
<a class="sourceLine" id="cb2-6" data-line-number="6">    void <span class="fu">$</span> liftIO <span class="fu">$</span> async <span class="fu">$</span> sndProc t2</a>
<a class="sourceLine" id="cb2-7" data-line-number="7">    forever <span class="fu">$</span> liftIO (atomically <span class="fu">$</span> takeTMVar t1 <span class="ot">`orElse`</span> takeTMVar t2) <span class="fu">&gt;&gt;=</span> C.yield</a>
<a class="sourceLine" id="cb2-8" data-line-number="8">    </a>
<a class="sourceLine" id="cb2-9" data-line-number="9">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-10" data-line-number="10">        fstProc t <span class="fu">=</span> runResourceT <span class="fu">$</span> src1 <span class="fu">$$</span> sinkTMVar t</a>
<a class="sourceLine" id="cb2-11" data-line-number="11">        sndProc t <span class="fu">=</span> runResourceT <span class="fu">$</span> src2 <span class="fu">$$</span> sinkTMVar t</a></code></pre></div>
<p>Rather short and sweet I think. However, there are a few things that I’m not completely sure of yet.</p>
<dl>
<dt><code>forkIO</code> vs. <code>async</code> vs. <code>resourceForkIO</code></dt>
<dd>There is a choice between at least three functions when it comes to creating the threads and I haven’t looked into which one is better to use. AFAIU there may be issues around exception handling and with resources. For now I’ve gone with <code>async</code> for no particular reason at all.
</dd>
<dt>Using <code>TMVar</code></dt>
<dd>In this example the input arrives rather slowly, which means having room for a single piece at a time is enough. If the use case changes and input arrives quicker then this decision has to be revisited. I’d probably choose to use <a href="http://hackage.haskell.org/package/stm-conduit">stm-conduit</a> in that case since it uses <code>TMChan</code> internally.
</dd>
<dt>Combining only two <code>Source</code>s</dt>
<dd>Again, this use case doesn’t call for more than two <code>Source</code>s, at least at this point. If the need arises for using more <code>Source</code>s I’ll switch to <a href="http://hackage.haskell.org/package/stm-conduit">stm-conduit</a> since it already defines a function to combine a list of <code>Source</code>s.
</dd>
</dl>
<p>The next step will be to modify the <em>conduit</em> process and the state machine.</p>]]></summary>
</entry>
<entry>
    <title>Thought on JavaScript</title>
    <link href="http://magnus.therning.org/posts/2015-01-14-000-thought-on-javascript.html" />
    <id>http://magnus.therning.org/posts/2015-01-14-000-thought-on-javascript.html</id>
    <published>2015-01-14T00:00:00Z</published>
    <updated>2015-01-14T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Over the holidays I’ve been reading Douglas Crockford’s excellent book <a href="http://goo.gl/ed3nUz">JavaScript: The Good Parts</a>. About halfway through I came to the conclusion that JavaScript is an “anti-LISP”. There are <a href="http://www.paulgraham.com/avg.html">many</a> <a href="http://nickmudge.info/?post=27">reasons</a> <a href="https://groovyguts.wordpress.com/2010/01/08/10-reasons-to-learn-clojure-or-any-other-lisp/">to</a> <a href="https://acm.wustl.edu/functional/whyfp.php">learn</a> LISP, but none of them is “LISP is widely used in industry.” As Eric Raymond is famous words claim, knowing LISP will make you a better programmer. On the other hand there seems to be almost no reasons to learn JavaScript. It sure doesn’t seem to teach anything that’ll make you a better programmer. The <em>only</em> reason I can come up with is “JavaScript is widely used in industry.”</p>]]></summary>
</entry>
<entry>
    <title>State machine with conduit</title>
    <link href="http://magnus.therning.org/posts/2015-01-13-000-state-machine-with-conduit.html" />
    <id>http://magnus.therning.org/posts/2015-01-13-000-state-machine-with-conduit.html</id>
    <published>2015-01-13T00:00:00Z</published>
    <updated>2015-01-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Previously I wrote about a <a href="/posts/2015-01-03-000-simple-state-machines-in-haskell.html">simple state machine in Haskell</a> and then offered a simple example, an <a href="/posts/2015-01-06-000-adder-as-a-state-machine.html">adder</a>. As I wrote in the initial post I’ve been using this to implement communication protocols. That means IO, and for that I’ve been using <a href="http://hackage.haskell.org/package/conduit">conduit</a>. In this post I thought I’d show how easy it is to wrap up the adder in <em>conduit</em>, read input from <em>stdin</em> and produce the result on <em>stdout</em>.</p>
<p>At the top level is a function composing three parts:</p>
<ol type="1">
<li>input from <em>stdin</em></li>
<li>processing in the <em>adder</em></li>
<li>output on <em>stdout</em></li>
</ol>
<p>This can be written as this</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">process ::</span> <span class="dt">ResourceT</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-2" data-line-number="2">process <span class="fu">=</span> source <span class="fu">$=</span> calc <span class="fu">$$</span> output</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-4" data-line-number="4">        source <span class="fu">=</span> stdinC</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"></a>
<a class="sourceLine" id="cb1-6" data-line-number="6">        output <span class="fu">=</span> stdoutC</a>
<a class="sourceLine" id="cb1-7" data-line-number="7"></a>
<a class="sourceLine" id="cb1-8" data-line-number="8">        calc <span class="fu">=</span> concatMapC unpack <span class="fu">=$=</span> wrapMachine calcMachine <span class="fu">=$=</span> filterC f <span class="fu">=$=</span> mapC (pack <span class="fu">.</span> show)</a>
<a class="sourceLine" id="cb1-9" data-line-number="9">            <span class="kw">where</span></a>
<a class="sourceLine" id="cb1-10" data-line-number="10">                f (<span class="dt">CalcResult</span> _) <span class="fu">=</span> <span class="dt">True</span></a>
<a class="sourceLine" id="cb1-11" data-line-number="11">                f _ <span class="fu">=</span> <span class="dt">False</span></a></code></pre></div>
<p>The input to the <em>adder</em> is ordinary <code>Char</code>s so the <code>ByteString</code>s generated by <code>stdinC</code> need to be converted before being handed off to the state machine, which is wrapped to make it a <code>Conduit</code> (see below). The state machine is generating more signals that I want printed, so I filter out everything but the <code>CalcResult</code>s. Then it’s passed to <code>show</code> and turned into a <code>ByteString</code> before sent to <code>stdoutC</code>.</p>
<p>I think the implementation of <code>wrapMachine</code> shows off the simplicity of <em>conduit</em> rather well:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">wrapMachine ::</span> (<span class="dt">Monad</span> m) <span class="ot">=&gt;</span> <span class="dt">Machine</span> state event signal <span class="ot">-&gt;</span> <span class="dt">Conduit</span> event m signal</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">wrapMachine mach <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3">    maybeEvent <span class="ot">&lt;-</span> await</a>
<a class="sourceLine" id="cb2-4" data-line-number="4">    <span class="kw">case</span> maybeEvent <span class="kw">of</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">        <span class="dt">Nothing</span> <span class="ot">-&gt;</span> return ()</a>
<a class="sourceLine" id="cb2-6" data-line-number="6">        <span class="dt">Just</span> event <span class="ot">-&gt;</span> <span class="kw">let</span></a>
<a class="sourceLine" id="cb2-7" data-line-number="7">                (newMach, signals) <span class="fu">=</span> stepMachine mach event</a>
<a class="sourceLine" id="cb2-8" data-line-number="8">            <span class="kw">in</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-9" data-line-number="9">                yield signals</a>
<a class="sourceLine" id="cb2-10" data-line-number="10">                wrapMachine newMach</a></code></pre></div>
<p>The <code>main</code> function is as short as this:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">main <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb3-3" data-line-number="3">    hSetBuffering stdin <span class="dt">NoBuffering</span></a>
<a class="sourceLine" id="cb3-4" data-line-number="4">    hSetBuffering stdout <span class="dt">NoBuffering</span></a>
<a class="sourceLine" id="cb3-5" data-line-number="5">    runResourceT process</a></code></pre></div>
<p>The result is about as exciting as expected</p>
<pre><code>% runhaskell Calculator.hs
56 42 +CalcResult 98
17 56 +CalcResult 73</code></pre>
<p>It never exits on its own so one has to use Ctrl-C to terminate. I thought I’d try to address that in the next post by adding a time-out to the adder machine.</p>]]></summary>
</entry>
<entry>
    <title>Adder as a state machine</title>
    <link href="http://magnus.therning.org/posts/2015-01-06-000-adder-as-a-state-machine.html" />
    <id>http://magnus.therning.org/posts/2015-01-06-000-adder-as-a-state-machine.html</id>
    <published>2015-01-06T00:00:00Z</published>
    <updated>2015-01-06T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>In my <a href="/posts/2015-01-03-000-simple-state-machines-in-haskell.html">previous post</a> I wrote about a, probably rather naïve, approach to constructing state machines in Haskell. I ended it with a saying that the pattern matching of Haskell makes it rather simple to manually write the step function required to create a working state machines. Hopefully this post can convince you I’m right.</p>
<p>The vehicle I’ve chosen is a very simple machine capable of reading two integers and adding them. In slightly more detail it’s a machine that:</p>
<ol type="1">
<li>reads a total of three parts, two integers followed by a plus sign (<code>+</code>)</li>
<li>each part is separated by whitespace</li>
<li>on errors the machine resets and starts over</li>
</ol>
<p>The signals (a.k.a. the output alphabet) is the following type</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">CalcSignal</span> <span class="fu">=</span> <span class="dt">CalcNothing</span> <span class="fu">|</span> <span class="dt">CalcResult</span> <span class="dt">Int</span> <span class="fu">|</span> <span class="dt">CalcError</span> <span class="dt">CalcStates</span> <span class="dt">String</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a></code></pre></div>
<p>The events (a.k.a. the input alphabet) is simply <code>Char</code>. The states are</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="kw">data</span> <span class="dt">CalcStates</span> <span class="fu">=</span> <span class="dt">Start</span> <span class="fu">|</span> <span class="dt">ReadFirst</span> <span class="dt">Int</span> <span class="fu">|</span> <span class="dt">ReadSecond</span> <span class="dt">Int</span> <span class="dt">Int</span> <span class="fu">|</span> <span class="dt">ReadOperator</span> <span class="dt">Int</span> <span class="dt">Int</span></a>
<a class="sourceLine" id="cb2-2" data-line-number="2">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a></code></pre></div>
<p>where the names hopefully are self explanatory. The type of the machine is then</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="kw">type</span> <span class="dt">CalcMachine</span> <span class="fu">=</span> <span class="dt">Machine</span> <span class="dt">CalcStates</span> <span class="dt">Char</span> <span class="dt">CalcSignal</span></a></code></pre></div>
<p>The machine itself can then be written like this:</p>
<div class="sourceCode" id="cb4"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb4-1" data-line-number="1"><span class="ot">calcMachine ::</span> <span class="dt">CalcMachine</span></a>
<a class="sourceLine" id="cb4-2" data-line-number="2">calcMachine <span class="fu">=</span> createMachine <span class="dt">Start</span> go</a>
<a class="sourceLine" id="cb4-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb4-4" data-line-number="4">        go <span class="dt">Start</span> e</a>
<a class="sourceLine" id="cb4-5" data-line-number="5">            <span class="fu">|</span> isNumber e <span class="fu">=</span> (<span class="dt">ReadFirst</span> (read [e]), <span class="dt">CalcNothing</span>)</a>
<a class="sourceLine" id="cb4-6" data-line-number="6">            <span class="fu">|</span> otherwise <span class="fu">=</span> (<span class="dt">Start</span>, <span class="dt">CalcError</span> <span class="dt">Start</span> <span class="st">&quot;No number&quot;</span>)</a>
<a class="sourceLine" id="cb4-7" data-line-number="7"></a>
<a class="sourceLine" id="cb4-8" data-line-number="8">        go s<span class="fu">@</span>(<span class="dt">ReadFirst</span> i) e</a>
<a class="sourceLine" id="cb4-9" data-line-number="9">            <span class="fu">|</span> isNumber e <span class="fu">=</span> (<span class="dt">ReadFirst</span> (<span class="dv">10</span> <span class="fu">*</span> i <span class="fu">+</span> read [e]), <span class="dt">CalcNothing</span>)</a>
<a class="sourceLine" id="cb4-10" data-line-number="10">            <span class="fu">|</span> isSpace e <span class="fu">=</span> (<span class="dt">ReadSecond</span> i <span class="dv">0</span>, <span class="dt">CalcNothing</span>)</a>
<a class="sourceLine" id="cb4-11" data-line-number="11">            <span class="fu">|</span> otherwise <span class="fu">=</span> (<span class="dt">Start</span>, <span class="dt">CalcError</span> s <span class="st">&quot;Bad format&quot;</span>)</a>
<a class="sourceLine" id="cb4-12" data-line-number="12"></a>
<a class="sourceLine" id="cb4-13" data-line-number="13">        go s<span class="fu">@</span>(<span class="dt">ReadSecond</span> l i) e</a>
<a class="sourceLine" id="cb4-14" data-line-number="14">            <span class="fu">|</span> isNumber e <span class="fu">=</span> (<span class="dt">ReadSecond</span> l (<span class="dv">10</span> <span class="fu">*</span> i <span class="fu">+</span> read [e]), <span class="dt">CalcNothing</span>)</a>
<a class="sourceLine" id="cb4-15" data-line-number="15">            <span class="fu">|</span> isSpace e <span class="fu">=</span> (<span class="dt">ReadOperator</span> l i, <span class="dt">CalcNothing</span>)</a>
<a class="sourceLine" id="cb4-16" data-line-number="16">            <span class="fu">|</span> otherwise <span class="fu">=</span> (<span class="dt">Start</span>, <span class="dt">CalcError</span> s <span class="st">&quot;Bad format&quot;</span>)</a>
<a class="sourceLine" id="cb4-17" data-line-number="17"></a>
<a class="sourceLine" id="cb4-18" data-line-number="18">        go s<span class="fu">@</span>(<span class="dt">ReadOperator</span> i j) e</a>
<a class="sourceLine" id="cb4-19" data-line-number="19">            <span class="fu">|</span> e <span class="fu">==</span> <span class="ch">&#39;+&#39;</span> <span class="fu">=</span> (<span class="dt">Start</span>, <span class="dt">CalcResult</span> (i <span class="fu">+</span> j))</a>
<a class="sourceLine" id="cb4-20" data-line-number="20">            <span class="fu">|</span> isSpace e <span class="fu">=</span> (s, <span class="dt">CalcNothing</span>)</a>
<a class="sourceLine" id="cb4-21" data-line-number="21">            <span class="fu">|</span> otherwise <span class="fu">=</span> (<span class="dt">Start</span>, <span class="dt">CalcError</span> s <span class="st">&quot;Bad operator&quot;</span>)</a></code></pre></div>
<p>That’s rather simple and easy to read I find. Though I’m not sure it scales too well to much larger machines. I’ve not really used any DSLs to create large machines either, so I don’t know how well <em>any</em> method scales ;)</p>
<p>To do a bit of exploratory testing it’s handy to create the following function</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">calculate ::</span> <span class="dt">String</span> <span class="ot">-&gt;</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb5-2" data-line-number="2">calculate <span class="fu">=</span> foldM_ go calcMachine</a>
<a class="sourceLine" id="cb5-3" data-line-number="3">    <span class="kw">where</span> </a>
<a class="sourceLine" id="cb5-4" data-line-number="4">        go mach c <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb5-5" data-line-number="5">            <span class="kw">let</span> (m, s) <span class="fu">=</span> stepMachine mach c</a>
<a class="sourceLine" id="cb5-6" data-line-number="6">            print s </a>
<a class="sourceLine" id="cb5-7" data-line-number="7">            return m</a></code></pre></div>
<p>Using that function it’s easy to check if the machine works as intended.</p>
<pre><code>&gt; calculate &quot;56 67 +&quot;
CalcNothing
CalcNothing
CalcNothing
CalcNothing
CalcNothing
CalcNothing
CalcResult 123</code></pre>
<p>So far so good. What about the behaviour on errors?</p>
<pre><code>&gt; calculate &quot;5a6 67 +&quot;
CalcNothing
CalcError (ReadFirst 5) &quot;Bad format&quot;
CalcNothing
CalcNothing
CalcNothing
CalcNothing
CalcNothing
CalcResult 73</code></pre>
<p>That looks good enough to me. Though there is (at least) one detail of how the machine works that might be surprising and hence should be fixed, but I’ll leave that as an exercise for the reader ;)</p>
<p>As I mentioned in the previous post I’ve been using this method for writing state machines to implement two different protocols. For the IO I used <a href="http://hackage.haskell.org/package/conduit">conduit</a> which means I had to turn the state machine into a conduit. I’ll write about that in a later post though.</p>]]></summary>
</entry>
<entry>
    <title>Simple state machines in Haskell</title>
    <link href="http://magnus.therning.org/posts/2015-01-03-000-simple-state-machines-in-haskell.html" />
    <id>http://magnus.therning.org/posts/2015-01-03-000-simple-state-machines-in-haskell.html</id>
    <published>2015-01-03T00:00:00Z</published>
    <updated>2015-01-03T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>During this past autumn I’ve had to work with products implementing two small and simple protocols, one was USB based, the other completely proprietary. In both cases I found it very helpful to write clients where the central behaviour was modelled as a state machine. In the case of the proprietary protocol I’ve ended up writing a couple of servers implementing a subset of the behaviour of the real product, also here I found using a state machine as the central piece to be very useful.</p>
<p>I started out having a look at [Hackage][] to see if there was already some tool or DSL that would help me create machines. Surprisingly I found nothing, which probably just means I didn’t look carefully enough. Anyway, after reading the Wikipedia articles on <a href="https://en.wikipedia.org/wiki/Finite-state_machine">Finite-state machine</a>, <a href="https://en.wikipedia.org/wiki/Finite_state_transducer">finite state transducer</a>, <a href="https://en.wikipedia.org/wiki/Moore_machine">Moore machine</a> and <a href="https://en.wikipedia.org/wiki/Mealy_machine">Mealy machine</a> I decided to write my own naïve implementation of something resembling Mealy machines.</p>
<p>A Mealy machine is a 6-tuple comprising</p>
<ul>
<li>a finite set of states</li>
<li>a start state</li>
<li>a finite set called the input alphabet</li>
<li>a finite set called the output alphabet</li>
<li>a transition function mapping a state and an input symbol to the next state</li>
<li>an output function mapping a state and an input symbol to an output symbol</li>
</ul>
<p>If the transition and output functions are combined into a single function, and we don’t care about being able to reset a machine, it’s possible to use a representation as simple as this:</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">data</span> <span class="dt">Machine</span> state event signal <span class="fu">=</span> <span class="dt">Machine</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">    {<span class="ot"> mCurState ::</span> state</a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    ,<span class="ot"> mTransFunction ::</span> state <span class="ot">-&gt;</span> event <span class="ot">-&gt;</span> (state, signal)</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    }</a></code></pre></div>
<p>I’ve opted to call the input alphabet <em>event</em> and the output alphabet <em>signal</em>.</p>
<p>Stepping a machine is then performed by passing the current state and an event to the combined function and updating the machine with the result. Of course the generated signal has to be dealt with too. Or in other words:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">stepMachine ::</span> <span class="dt">Machine</span> state event signal <span class="ot">-&gt;</span> event <span class="ot">-&gt;</span> (<span class="dt">Machine</span> state event signal, signal)</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">stepMachine machine event <span class="fu">=</span> (machine {mCurState <span class="fu">=</span> newState}, output)</a>
<a class="sourceLine" id="cb2-3" data-line-number="3">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-4" data-line-number="4">        curState <span class="fu">=</span> mCurState machine</a>
<a class="sourceLine" id="cb2-5" data-line-number="5">        (newState, output) <span class="fu">=</span> mTransFunction machine curState event</a></code></pre></div>
<p>I also decided to add a function for creating a machine given a start state and a function. With the definition above it becomes trivial:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">createMachine ::</span> state <span class="ot">-&gt;</span> (state <span class="ot">-&gt;</span> event <span class="ot">-&gt;</span> (state, signal)) <span class="ot">-&gt;</span> <span class="dt">Machine</span> state event signal</a>
<a class="sourceLine" id="cb3-2" data-line-number="2">createMachine <span class="fu">=</span> <span class="dt">Machine</span></a></code></pre></div>
<p>That’s it, really. Except of course that the actual transition function still has to be written. However, with the pattern matching of Haskell I’ve found that to be rather simple, but I’ll keep that for the next post.</p>]]></summary>
</entry>
<entry>
    <title>Adding some strictness</title>
    <link href="http://magnus.therning.org/posts/2014-12-26-000-adding-some-strictness.html" />
    <id>http://magnus.therning.org/posts/2014-12-26-000-adding-some-strictness.html</id>
    <published>2014-12-26T00:00:00Z</published>
    <updated>2014-12-26T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I’ve finally found some time to solve a few more exercises on <a href="http://exercism.io/">exercim.io</a>. One exercise, called <em>Robot Simulation</em>, I solved in a straight forward way and promptly got the feedback that my choice of data type could lead to space leaks. Since it’s a dimension of Haskell programming that I rarely (read “never”) have had to think about I thought I’d take the chance to play a bit.</p>
<h3 id="a-simplified-solution">A simplified solution</h3>
<p>I don’t want to give away the solution to the exercise so I’m using some code of a similar shape.</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="kw">import</span> <span class="dt">Data.List</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2"></a>
<a class="sourceLine" id="cb1-3" data-line-number="3"><span class="kw">newtype</span> <span class="dt">P</span> <span class="fu">=</span> <span class="dt">P</span> (<span class="dt">Int</span>, <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb1-5" data-line-number="5"></a>
<a class="sourceLine" id="cb1-6" data-line-number="6"><span class="ot">step ::</span> <span class="dt">P</span> <span class="ot">-&gt;</span> <span class="dt">Char</span> <span class="ot">-&gt;</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb1-7" data-line-number="7">step (<span class="dt">P</span> c) <span class="ch">&#39;A&#39;</span> <span class="fu">=</span> <span class="dt">P</span> (c <span class="ot">`add`</span> (<span class="dv">0</span>, <span class="dv">1</span>))</a>
<a class="sourceLine" id="cb1-8" data-line-number="8">step _ _ <span class="fu">=</span> undefined</a>
<a class="sourceLine" id="cb1-9" data-line-number="9"></a>
<a class="sourceLine" id="cb1-10" data-line-number="10"><span class="ot">add ::</span> (<span class="dt">Num</span> a, <span class="dt">Num</span> b) <span class="ot">=&gt;</span> (a, b) <span class="ot">-&gt;</span> (a, b) <span class="ot">-&gt;</span> (a, b)</a>
<a class="sourceLine" id="cb1-11" data-line-number="11">add (x0, y0) (x1, y1) <span class="fu">=</span> (x0 <span class="fu">+</span> x1, y0 <span class="fu">+</span> y1)</a>
<a class="sourceLine" id="cb1-12" data-line-number="12"></a>
<a class="sourceLine" id="cb1-13" data-line-number="13"><span class="ot">p ::</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb1-14" data-line-number="14">p <span class="fu">=</span> foldl&#39; step (<span class="dt">P</span> (<span class="dv">0</span>, <span class="dv">0</span>)) <span class="fu">$</span> replicate <span class="dv">5000000</span> <span class="ch">&#39;A&#39;</span></a>
<a class="sourceLine" id="cb1-15" data-line-number="15"></a>
<a class="sourceLine" id="cb1-16" data-line-number="16"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb1-17" data-line-number="17">main <span class="fu">=</span> print p</a></code></pre></div>
<p>There’s nothing complicated about the code, but the memory usage is rather ridiculous:</p>
<pre><code>   1,132,746,080 bytes allocated in the heap
   1,149,823,448 bytes copied during GC
     346,747,064 bytes maximum residency (11 sample(s))
       3,817,608 bytes maximum slop
             600 MB total memory in use (0 MB lost due to fragmentation)

                                    Tot time (elapsed)  Avg pause  Max pause
  Gen  0      2148 colls,     0 par    0.58s    0.58s     0.0003s    0.0006s
  Gen  1        11 colls,     0 par    0.56s    0.56s     0.0510s    0.2278s

  INIT    time    0.00s  (  0.00s elapsed)
  MUT     time    0.25s  (  0.25s elapsed)
  GC      time    1.15s  (  1.14s elapsed)
  EXIT    time    0.02s  (  0.02s elapsed)
  Total   time    1.42s  (  1.41s elapsed)

  %GC     time      80.9%  (81.0% elapsed)

  Alloc rate    4,593,597,274 bytes per MUT second

  Productivity  19.0% of total user, 19.1% of total elapsed</code></pre>
<p>Surely it ought to be possible to add 5 million numbers in less than 600MB, right?</p>
<p>The reason, AFAIU is that tuples are lazy, which means a lot of thunk building when doing the additions.</p>
<h3 id="using-a-stricter-data-type">Using a stricter data type</h3>
<p>The maybe easiest way to add some strictness is to add a few exclamations marks in the data type. Modifying the data type brings a few other changes though, so here’s first a version without any strictness annotations so I have something to compare with later on<a href="#fn1" class="footnote-ref" id="fnref1"><sup>1</sup></a>:</p>
<div class="sourceCode" id="cb3"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb3-1" data-line-number="1"><span class="ot">{-# LANGUAGE RecordWildCards #-}</span></a>
<a class="sourceLine" id="cb3-2" data-line-number="2"><span class="kw">import</span> <span class="dt">Data.List</span></a>
<a class="sourceLine" id="cb3-3" data-line-number="3"></a>
<a class="sourceLine" id="cb3-4" data-line-number="4"><span class="kw">data</span> <span class="dt">P</span> <span class="fu">=</span> <span class="dt">P</span> {<span class="ot"> pX ::</span> <span class="dt">Int</span>,<span class="ot"> pY ::</span> <span class="dt">Int</span> }</a>
<a class="sourceLine" id="cb3-5" data-line-number="5">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb3-6" data-line-number="6"></a>
<a class="sourceLine" id="cb3-7" data-line-number="7"><span class="ot">p ::</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb3-8" data-line-number="8">p <span class="fu">=</span> foldl&#39; step (<span class="dt">P</span> <span class="dv">0</span> <span class="dv">0</span>) <span class="fu">$</span> replicate <span class="dv">5000000</span> <span class="ch">&#39;A&#39;</span></a>
<a class="sourceLine" id="cb3-9" data-line-number="9"></a>
<a class="sourceLine" id="cb3-10" data-line-number="10"><span class="ot">step ::</span> <span class="dt">P</span> <span class="ot">-&gt;</span> <span class="dt">Char</span> <span class="ot">-&gt;</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb3-11" data-line-number="11">step q<span class="fu">@</span>(<span class="dt">P</span> {<span class="fu">..</span>}) <span class="ch">&#39;A&#39;</span> <span class="fu">=</span> q { pX <span class="fu">=</span> pX <span class="fu">+</span><span class="dv">1</span> }</a>
<a class="sourceLine" id="cb3-12" data-line-number="12">step _ _ <span class="fu">=</span> undefined</a>
<a class="sourceLine" id="cb3-13" data-line-number="13"></a>
<a class="sourceLine" id="cb3-14" data-line-number="14"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb3-15" data-line-number="15">main <span class="fu">=</span> print p</a></code></pre></div>
<p>This solution uses about half as much memory:</p>
<pre><code>     766,417,952 bytes allocated in the heap
     639,064,600 bytes copied during GC
     155,866,064 bytes maximum residency (11 sample(s))
       1,914,016 bytes maximum slop
             345 MB total memory in use (0 MB lost due to fragmentation)

                                    Tot time (elapsed)  Avg pause  Max pause
  Gen  0      1460 colls,     0 par    0.32s    0.32s     0.0002s    0.0006s
  Gen  1        11 colls,     0 par    0.33s    0.33s     0.0303s    0.1071s

  INIT    time    0.00s  (  0.00s elapsed)
  MUT     time    0.15s  (  0.15s elapsed)
  GC      time    0.65s  (  0.65s elapsed)
  EXIT    time    0.01s  (  0.01s elapsed)
  Total   time    0.81s  (  0.81s elapsed)

  %GC     time      80.2%  (80.2% elapsed)

  Alloc rate    5,212,863,895 bytes per MUT second

  Productivity  19.8% of total user, 19.8% of total elapsed</code></pre>
<p>Halving the memory usage is rather nice, but adding some strictness ought to make it better still. The code with added exclamation marks:</p>
<div class="sourceCode" id="cb5"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb5-1" data-line-number="1"><span class="ot">{-# LANGUAGE RecordWildCards #-}</span></a>
<a class="sourceLine" id="cb5-2" data-line-number="2"><span class="kw">import</span> <span class="dt">Data.List</span></a>
<a class="sourceLine" id="cb5-3" data-line-number="3"></a>
<a class="sourceLine" id="cb5-4" data-line-number="4"><span class="kw">data</span> <span class="dt">P</span> <span class="fu">=</span> <span class="dt">P</span> {<span class="ot"> pX ::</span> <span class="fu">!</span><span class="dt">Int</span>,<span class="ot"> pY ::</span> <span class="fu">!</span><span class="dt">Int</span> }</a>
<a class="sourceLine" id="cb5-5" data-line-number="5">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb5-6" data-line-number="6"></a>
<a class="sourceLine" id="cb5-7" data-line-number="7"><span class="ot">p ::</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb5-8" data-line-number="8">p <span class="fu">=</span> foldl&#39; step (<span class="dt">P</span> <span class="dv">0</span> <span class="dv">0</span>) <span class="fu">$</span> replicate <span class="dv">5000000</span> <span class="ch">&#39;A&#39;</span></a>
<a class="sourceLine" id="cb5-9" data-line-number="9"></a>
<a class="sourceLine" id="cb5-10" data-line-number="10"><span class="ot">step ::</span> <span class="dt">P</span> <span class="ot">-&gt;</span> <span class="dt">Char</span> <span class="ot">-&gt;</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb5-11" data-line-number="11">step q<span class="fu">@</span>(<span class="dt">P</span> {<span class="fu">..</span>}) <span class="ch">&#39;A&#39;</span> <span class="fu">=</span> q { pX <span class="fu">=</span> pX <span class="fu">+</span><span class="dv">1</span> }</a>
<a class="sourceLine" id="cb5-12" data-line-number="12">step _ _ <span class="fu">=</span> undefined</a>
<a class="sourceLine" id="cb5-13" data-line-number="13"></a>
<a class="sourceLine" id="cb5-14" data-line-number="14"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb5-15" data-line-number="15">main <span class="fu">=</span> print p</a></code></pre></div>
<p>The runtime behaviour of this code is remarkably more frugal with memory:</p>
<pre><code>     480,054,816 bytes allocated in the heap
          57,968 bytes copied during GC
          44,312 bytes maximum residency (2 sample(s))
          21,224 bytes maximum slop
               1 MB total memory in use (0 MB lost due to fragmentation)

                                    Tot time (elapsed)  Avg pause  Max pause
  Gen  0       918 colls,     0 par    0.00s    0.00s     0.0000s    0.0000s
  Gen  1         2 colls,     0 par    0.00s    0.00s     0.0000s    0.0000s

  INIT    time    0.00s  (  0.00s elapsed)
  MUT     time    0.09s  (  0.09s elapsed)
  GC      time    0.00s  (  0.00s elapsed)
  EXIT    time    0.00s  (  0.00s elapsed)
  Total   time    0.09s  (  0.09s elapsed)

  %GC     time       2.4%  (2.4% elapsed)

  Alloc rate    5,630,441,644 bytes per MUT second

  Productivity  97.3% of total user, 98.2% of total elapsed</code></pre>
<p>Indeed, 1MB is a bit more like it!</p>
<h3 id="strictness-with-the-original-type">Strictness with the original type</h3>
<p>However impressive the results when using a stricter data type, it might still be preferable to keep the original, somewhat simpler, type. It is possible to do just that by placing some explicit strictness in well chosen places. Since my suspicion is that it’s the addition in <code>step</code> that causes the space leak we’ll rewrite it:</p>
<div class="sourceCode" id="cb7"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb7-1" data-line-number="1"><span class="kw">import</span> <span class="dt">Data.List</span></a>
<a class="sourceLine" id="cb7-2" data-line-number="2"></a>
<a class="sourceLine" id="cb7-3" data-line-number="3"><span class="kw">import</span> <span class="dt">Control.DeepSeq</span></a>
<a class="sourceLine" id="cb7-4" data-line-number="4"></a>
<a class="sourceLine" id="cb7-5" data-line-number="5"><span class="kw">newtype</span> <span class="dt">P</span> <span class="fu">=</span> <span class="dt">P</span> (<span class="dt">Int</span>, <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb7-6" data-line-number="6">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb7-7" data-line-number="7"></a>
<a class="sourceLine" id="cb7-8" data-line-number="8"><span class="ot">add ::</span> (<span class="dt">Num</span> a, <span class="dt">Num</span> b) <span class="ot">=&gt;</span> (a, b) <span class="ot">-&gt;</span> (a, b) <span class="ot">-&gt;</span> (a, b)</a>
<a class="sourceLine" id="cb7-9" data-line-number="9">add (x0, y0) (x1, y1) <span class="fu">=</span> (x0 <span class="fu">+</span> x1, y0 <span class="fu">+</span> y1)</a>
<a class="sourceLine" id="cb7-10" data-line-number="10"></a>
<a class="sourceLine" id="cb7-11" data-line-number="11"><span class="ot">p ::</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb7-12" data-line-number="12">p <span class="fu">=</span> foldl&#39; step (<span class="dt">P</span> (<span class="dv">0</span>, <span class="dv">0</span>)) <span class="fu">$</span> replicate <span class="dv">5000000</span> <span class="ch">&#39;A&#39;</span></a>
<a class="sourceLine" id="cb7-13" data-line-number="13"></a>
<a class="sourceLine" id="cb7-14" data-line-number="14"><span class="ot">step ::</span> <span class="dt">P</span> <span class="ot">-&gt;</span> <span class="dt">Char</span> <span class="ot">-&gt;</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb7-15" data-line-number="15">step (<span class="dt">P</span> c) <span class="ch">&#39;A&#39;</span> <span class="fu">=</span> <span class="kw">let</span> np <span class="fu">=</span> c <span class="ot">`add`</span> (<span class="dv">0</span>, <span class="dv">1</span>) <span class="kw">in</span> np <span class="ot">`deepseq`</span> <span class="dt">P</span> np</a>
<a class="sourceLine" id="cb7-16" data-line-number="16">step _ _ <span class="fu">=</span> undefined</a>
<a class="sourceLine" id="cb7-17" data-line-number="17"></a>
<a class="sourceLine" id="cb7-18" data-line-number="18"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb7-19" data-line-number="19">main <span class="fu">=</span> print p</a></code></pre></div>
<p>Note that it’s not enough to use <code>seq</code>; a bit more force is needed, which is what <code>deepseq</code> offers. It delivers sure enough:</p>
<pre><code>     920,052,632 bytes allocated in the heap
         198,736 bytes copied during GC
          44,312 bytes maximum residency (2 sample(s))
          21,224 bytes maximum slop
               1 MB total memory in use (0 MB lost due to fragmentation)

                                    Tot time (elapsed)  Avg pause  Max pause
  Gen  0      1774 colls,     0 par    0.00s    0.00s     0.0000s    0.0000s
  Gen  1         2 colls,     0 par    0.00s    0.00s     0.0001s    0.0001s

  INIT    time    0.00s  (  0.00s elapsed)
  MUT     time    0.29s  (  0.29s elapsed)
  GC      time    0.00s  (  0.00s elapsed)
  EXIT    time    0.00s  (  0.00s elapsed)
  Total   time    0.30s  (  0.30s elapsed)

  %GC     time       1.4%  (1.4% elapsed)

  Alloc rate    3,124,303,717 bytes per MUT second

  Productivity  98.5% of total user, 98.9% of total elapsed</code></pre>
<p>A further slight change is possible: implement <code>NFData</code> for <code>P</code> itself. That way it’s not necessary to pull out and force evaluation of the innards of <code>P</code>. Using the default implementation of <code>rnf</code> did not cut it though, since it uses <code>seq</code> it just isn’t forceful enough:</p>
<div class="sourceCode" id="cb9"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb9-1" data-line-number="1"><span class="kw">import</span> <span class="dt">Data.List</span></a>
<a class="sourceLine" id="cb9-2" data-line-number="2"></a>
<a class="sourceLine" id="cb9-3" data-line-number="3"><span class="kw">import</span> <span class="dt">Control.DeepSeq</span></a>
<a class="sourceLine" id="cb9-4" data-line-number="4"></a>
<a class="sourceLine" id="cb9-5" data-line-number="5"><span class="kw">newtype</span> <span class="dt">P</span> <span class="fu">=</span> <span class="dt">P</span> (<span class="dt">Int</span>, <span class="dt">Int</span>)</a>
<a class="sourceLine" id="cb9-6" data-line-number="6">    <span class="kw">deriving</span> (<span class="dt">Eq</span>, <span class="dt">Show</span>)</a>
<a class="sourceLine" id="cb9-7" data-line-number="7"></a>
<a class="sourceLine" id="cb9-8" data-line-number="8"><span class="kw">instance</span> <span class="dt">NFData</span> <span class="dt">P</span> <span class="kw">where</span></a>
<a class="sourceLine" id="cb9-9" data-line-number="9">    rnf (<span class="dt">P</span> q) <span class="fu">=</span> deepseq q ()</a>
<a class="sourceLine" id="cb9-10" data-line-number="10"></a>
<a class="sourceLine" id="cb9-11" data-line-number="11"><span class="ot">add ::</span> (<span class="dt">Num</span> a, <span class="dt">Num</span> b) <span class="ot">=&gt;</span> (a, b) <span class="ot">-&gt;</span> (a, b) <span class="ot">-&gt;</span> (a, b)</a>
<a class="sourceLine" id="cb9-12" data-line-number="12">add (x0, y0) (x1, y1) <span class="fu">=</span> (x0 <span class="fu">+</span> x1, y0 <span class="fu">+</span> y1)</a>
<a class="sourceLine" id="cb9-13" data-line-number="13"></a>
<a class="sourceLine" id="cb9-14" data-line-number="14"><span class="ot">p ::</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb9-15" data-line-number="15">p <span class="fu">=</span> foldl&#39; step (<span class="dt">P</span> (<span class="dv">0</span>, <span class="dv">0</span>)) <span class="fu">$</span> replicate <span class="dv">5000000</span> <span class="ch">&#39;A&#39;</span></a>
<a class="sourceLine" id="cb9-16" data-line-number="16"></a>
<a class="sourceLine" id="cb9-17" data-line-number="17"><span class="ot">step ::</span> <span class="dt">P</span> <span class="ot">-&gt;</span> <span class="dt">Char</span> <span class="ot">-&gt;</span> <span class="dt">P</span></a>
<a class="sourceLine" id="cb9-18" data-line-number="18">step (<span class="dt">P</span> c) <span class="ch">&#39;A&#39;</span> <span class="fu">=</span> <span class="kw">let</span> np <span class="fu">=</span> <span class="dt">P</span> (c <span class="ot">`add`</span> (<span class="dv">0</span>, <span class="dv">1</span>)) <span class="kw">in</span> force np</a>
<a class="sourceLine" id="cb9-19" data-line-number="19">step _ _ <span class="fu">=</span> undefined</a>
<a class="sourceLine" id="cb9-20" data-line-number="20"></a>
<a class="sourceLine" id="cb9-21" data-line-number="21"><span class="ot">main ::</span> <span class="dt">IO</span> ()</a>
<a class="sourceLine" id="cb9-22" data-line-number="22">main <span class="fu">=</span> print p</a></code></pre></div>
<p>The result is close to the previous:</p>
<pre><code>     920,052,632 bytes allocated in the heap
         198,736 bytes copied during GC
          44,312 bytes maximum residency (2 sample(s))
          21,224 bytes maximum slop
               1 MB total memory in use (0 MB lost due to fragmentation)

                                    Tot time (elapsed)  Avg pause  Max pause
  Gen  0      1774 colls,     0 par    0.00s    0.00s     0.0000s    0.0000s
  Gen  1         2 colls,     0 par    0.00s    0.00s     0.0001s    0.0001s

  INIT    time    0.00s  (  0.00s elapsed)
  MUT     time    0.33s  (  0.33s elapsed)
  GC      time    0.00s  (  0.00s elapsed)
  EXIT    time    0.00s  (  0.00s elapsed)
  Total   time    0.33s  (  0.33s elapsed)

  %GC     time       1.2%  (1.2% elapsed)

  Alloc rate    2,817,175,186 bytes per MUT second

  Productivity  98.7% of total user, 99.0% of total elapsed</code></pre>
<p>It looks like it’s slightly slower though, which might be caused by the double call to <code>deepseq</code> in this version.</p>
<h3 id="conclusion">Conclusion</h3>
<p>From this little example it’s clear that a little strictness in well-chosen places can go a long way in reducing memory footprint. The various solutions also show that the results can vary quite a bit depending on the strategy for introducing strictness. In this particular case I think the stricter data type (see <a href="#using-a-stricter-data-type">Using a stricter data type</a> above) is the best choice, it’s both faster and uses less memory than any of the others.</p>
<section class="footnotes">
<hr />
<ol>
<li id="fn1"><p>I’m using <code>RecordWildCards</code> to simplify the code a bit, which possibly might influence evaluation as well. I’m simply not sure.<a href="#fnref1" class="footnote-back">↩</a></p></li>
</ol>
</section>]]></summary>
</entry>
<entry>
    <title>FP101x completed</title>
    <link href="http://magnus.therning.org/posts/2014-12-07-000-fp101-completed.html" />
    <id>http://magnus.therning.org/posts/2014-12-07-000-fp101-completed.html</id>
    <published>2014-12-07T00:00:00Z</published>
    <updated>2014-12-07T00:00:00Z</updated>
    <summary type="html"><![CDATA[<h3 id="tldr">TL;DR</h3>
<p>As of last evening I finished the last exercise/homework of the course <a href="https://www.edx.org/node/2126">FP101x Introduction to Functional Programming</a>. I mostly signed up for fun, but did intend all along to go through with it, watch the lectures, do the homework… in short, take the course ;)</p>
<p>Now that it’s done I’m happy I did. It gave about as much as I expected on the programming side, and it gave me some experience with using the <a href="https://www.edx.org/">edX</a> <a href="https://en.wikipedia.org/wiki/Massive_open_online_course">MOOC</a>. I will suggest the course to colleagues and I’m already looking around for the next course to take myself.</p>
<h3 id="thoughts-about-the-course">Thoughts about the course</h3>
<p>The course was, on the whole, very good. It was well planned; I felt it started from a level suitable for people unfamiliar with FP and then progressed at a reasonable pace. It should be noted though that the lectures probably isn’t enough for someone who’s new to FP; reading the book or researching the subjects on one’s own is required to follow along.</p>
<p>There were a few things in the course that I don’t really see the point of though. The thing that stands out in particular is the 12th lecture that covers proofs by induction of functions that work on lists. I think it would have been worthwhile spending some time on making more “realistic programs.” Furthermore, picking the “Countdown Problem” as the topic of an entire lecture feels very “academic”. Personally I would have liked an example that comes a bit closer to a problem that better reflects that FP can be used to solve problems that occur in a professional developer’s work.</p>
<h3 id="thoughts-about-the-edx-mooc">Thoughts about the edX MOOC</h3>
<p>The edX site is fairly nice to work with. There are several things that I liked from the beginning:</p>
<ol type="1">
<li>Several methods of logging in are offered, I opted to use my Google-plus account since I like to avoid creating yet another account if I can.</li>
<li>Interacting with the site is natural and finding my way around was easy from the start.</li>
<li>The page I land on after logging in shows my current courses and it’s easy to from there.</li>
<li>There’s a page showing my progress in the course; that turned out to be the page I go to all the time since I can find the next lecture from there.</li>
</ol>
<p>There are also a few things I find really irritating:</p>
<ol type="1">
<li>In many of the exercises the code isn’t plain text but rather images; this makes it impossible to copy the code and paste it into a REPL.</li>
<li>In some cases the layout of the code in the exercises, especially in the answer options, were very badly laid out. For instance in FP101x there are several question where the answer options are code and rather wordy, due to the layout the code becomes unnecessarily difficult to read. (And thanks to the previous issue with the site, it was not an option to copy the code and reformat it to make it easier to read.)</li>
</ol>]]></summary>
</entry>
<entry>
    <title>Regular Haskelling.  How?</title>
    <link href="http://magnus.therning.org/posts/2014-11-16-000-regular-haskelling.html" />
    <id>http://magnus.therning.org/posts/2014-11-16-000-regular-haskelling.html</id>
    <published>2014-11-16T00:00:00Z</published>
    <updated>2014-11-16T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>Ever since <a href="http://icfpconference.org/icfp2014/">ICFP 2014</a> I’ve had as a goal to get into the habit of coding in Haskell. It’s been the language I enjoy most for a few years now, but being surrounded by and talking to so many brilliant developers as I did during that week really drove home that I will only have more fun the more code I write. My goal was not very ambitious; just write something in Haskell most days every week. So far I’ve managed to keep it up.</p>
<p>These are a few tricks I’ve used and they’ve worked well for me so far.</p>
<h3 id="just-write-no-matter-what-just-write">Just write, no matter what, just write</h3>
<p>In ninth grade a rather successful Swedish author visited my school and what I remember most from that is one thing he said:</p>
<blockquote>
<p>Just read! It doesn’t matter what. It doesn’t matter if what you read isn’t considered good literature; read Harlequin books if that’s what you like, read magazines, read comics. Just read!</p>
</blockquote>
<p>I think the same holds for writing code; it’s only with practice that one gets comfortable expressing oneself in a particular language.</p>
<h3 id="fix-warts">Fix warts</h3>
<p>I can’t actually think of any piece of code I’ve written that doesn’t have some warts. It may be in the form of missing features, or quirks (bugs) in the implementation that forces the user to regularly work in a less-than-optimal way. I’ve found fixing warts in tools and libraries I use myself to be one of the most rewarding tasks to take on; the feedback is so immediate that every fix cause a boost in motivation to fix the next one.</p>
<h3 id="exercise-sites">Exercise sites</h3>
<p>Sometimes it’s simply difficult to find the motivation to tackle working on an existing project, and inspiration for starting something new might be lacking too. This happens to me regularly, and I used to simply close the lid on the computer earlier, but now I try to find some exercises to do instead.</p>
<p>There are several sources of exercises. I know <a href="https://projecteuler.net/problems">Project Euler</a> is rather popular among new Haskellers, but there are others.</p>
<ul>
<li><a href="https://www.codeeval.com/">CodeEval</a> is a site with problems in three different levels. It may be extra interesting for people in the US since some of the problems are sponsored by companies which seem to use the site as a place for recruiting. So far I’ve only seen American companies do that, but I suppose it might catch on in other parts of the world too. Haskell is one of several languages supported.</li>
<li><a href="http://exercism.io/">Exercism</a> is both a site and a tool. The goal is to facilitate learning of languages. On first use the tool will download the first exercise, and after completion one uses it to upload the solution to the site. Once uploaded the solution is visible to other users, and they are allowed to “nitpick” (comment). After uploading a solution to one exercise the next exercise in the series becomes available. It supports a rather long list programming languages.</li>
</ul>
<p>I like both of these, but I’ve spent more time on the latter one. Personally I find the idea behind Exercism very appealing and I’ve been recommending it to a couple of co-workers already.</p>
<p>Feel free to put links to other sources of exercises in the comments.</p>
<h3 id="simplify-old-code">Simplify old code</h3>
<p>With more practice comes more and more insights into what functions are available and how to string them together. When I don’t even feel like doing a full exercise on Exercism I just dig out something that smells a little and clean it up. Anything is fair game, no matter how tiny. Just take a look at my implementation of <a href="https://github.com/magthe/cblrepo/commit/eefe3b9015c96d8f69392e4043c6e31c7dc36219"><code>reThrowError</code></a>.</p>
<h3 id="what-else">What else?</h3>
<p>I’d love to hear tips and tricks from other people who aren’t lucky enough to have a day job where they get to write Haskell. How do you keep up the learning and practice?</p>]]></summary>
</entry>
<entry>
    <title>Dijkstra quotes from EWD1284</title>
    <link href="http://magnus.therning.org/posts/2014-10-20-000-dijkstra-ewd1284.html" />
    <id>http://magnus.therning.org/posts/2014-10-20-000-dijkstra-ewd1284.html</id>
    <published>2014-10-20T00:00:00Z</published>
    <updated>2014-10-20T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>I recently read through this <em>long</em> post entitles <a href="http://www.smashcompany.com/technology/object-oriented-programming-is-an-expensive-disaster-which-must-end">Object Oriented Programming is an expensive disaster which must end</a>. I have to agree I largely agree with what he writes, but I don’t think I ever could have written such a well-researched article, and absolutely never one of equal length ;)</p>
<p>It does include some nice quotes and references and so far I’ve only read one of the many that I bookmarked, <a href="https://www.cs.utexas.edu/users/EWD/transcriptions/EWD12xx/EWD1284.html">Computing Science: Achievements and Challenges (EWD1284)</a>. It does include a few quips that, based on other articles I’ve read, seem fairly typical to Dijkstra. I simply love the way he expressed his opinions at times.</p>
<p>This one really ought to have been in the lengthy post on the OOP disaster:</p>
<blockquote>
<p>After more than 45 years in the field, I am still convinced that in computing, elegance is not a dispensable luxury but a quality that decides between success and failure; in this connection I gratefully quote from The Concise Oxford Dictionary a definition of “elegant”, viz. “ingeniously simple and effective”. Amen. (For those who have wondered: I don’t think object-oriented programming is a structuring paradigm that meets my standards of elegance.)</p>
</blockquote>
<p>And his thoughts on formal methods are well-known of course, as are his thoughts on iterative design. However, I rather like how he expresses a certain level of disgust of the Anglo-Saxon world when writing about those topics:</p>
<blockquote>
<p>The idea of a formal design discipline is often rejected on account of vague cultural/philosophical condemnations such as “stifling creativity”; this is more pronounced in the Anglo-Saxon world where a romantic vision of “the humanities” in fact idealizes technical incompetence. Another aspect of that same trait is the cult of iterative design.</p>
</blockquote>
<p>It amazes me every time I read something by someone like Dijkstra, just how much things stay the same, even in a field like computing science, which is said to be moving so fast.</p>]]></summary>
</entry>
<entry>
    <title>Optparse-applicative and custom argument parsers</title>
    <link href="http://magnus.therning.org/posts/2014-10-13-000-optparse-applicative.html" />
    <id>http://magnus.therning.org/posts/2014-10-13-000-optparse-applicative.html</id>
    <published>2014-10-13T00:00:00Z</published>
    <updated>2014-10-13T00:00:00Z</updated>
    <summary type="html"><![CDATA[<p>The latest update of <a href="http://hackage.haskell.org/package/optparse-applicative">optparse-applicative</a> triggered me to look over the functions in <a href="http://hackage.haskell.org/package/cblrepo">cblrepo</a> for parsing a few custom command line options. I used to do the parsing in a rather ad-hoc way with lots of use of list functions to split on specific characters. For instance, some option values are pairs of package name and version separated by a comma: <code>PKG-NAME,VERSION</code>. This worked fine and was easy to plug into version 0.10 of <em>optparse-applicative</em>. It was also easily extended to triples, <code>PKG-NAME,VERSION,RELEASE</code>, but it started feeling a bit brittle when some tuples got extended with an optional list of flag assignments, <code>PKG-NAME,VERSION[:FLAG,FLAG,FLAG,...]</code>. The recent release of version 0.11 of <em>optparse-applicative</em> changed the API for custom option value parsers radically; instead of passing a string to the parser, the parser has to use <code>readerAsk</code> to get the string. In short, <code>ReaderM</code> turned into a state monad.</p>
<p>In adjusting to the new API I noticed that the code was organised in such a way that some low-level parsing functions were used directly from command line option definitions, while also being used as building blocks for the more complex parsers. This of course meant that the structuring of the functions needed to be changed completely to deal with the API change.</p>
<p>It turns out there already was a parser that was written in a different style (here already adjusted to the 0.11 API):</p>
<div class="sourceCode" id="cb1"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb1-1" data-line-number="1"><span class="ot">readerGhcVersion ::</span> <span class="dt">ReadM</span> <span class="dt">Version</span></a>
<a class="sourceLine" id="cb1-2" data-line-number="2">readerGhcVersion <span class="fu">=</span></a>
<a class="sourceLine" id="cb1-3" data-line-number="3">    arg <span class="ot">&lt;-</span> readerAsk</a>
<a class="sourceLine" id="cb1-4" data-line-number="4">    <span class="kw">case</span> lastMay <span class="fu">$</span> readP_to_S parseVersion arg <span class="kw">of</span></a>
<a class="sourceLine" id="cb1-5" data-line-number="5">        <span class="dt">Just</span> (v, <span class="st">&quot;&quot;</span>) <span class="ot">-&gt;</span> return v</a>
<a class="sourceLine" id="cb1-6" data-line-number="6">        _ <span class="ot">-&gt;</span> fail <span class="fu">$</span> <span class="st">&quot;cannot parse value `&quot;</span> <span class="fu">++</span> arg <span class="fu">++</span> <span class="st">&quot;`&quot;</span></a></code></pre></div>
<p>So I rewrote the rest of the parsers in a similar style. The arguably most complicated is this one:</p>
<div class="sourceCode" id="cb2"><pre class="sourceCode haskell"><code class="sourceCode haskell"><a class="sourceLine" id="cb2-1" data-line-number="1"><span class="ot">readPkgNVersion ::</span> <span class="dt">ReadP</span> (<span class="dt">String</span>, <span class="dt">Version</span>)</a>
<a class="sourceLine" id="cb2-2" data-line-number="2">readPkgNVersion <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-3" data-line-number="3">    n <span class="ot">&lt;-</span> many (satisfy (<span class="fu">/=</span> <span class="ch">&#39;,&#39;</span>))</a>
<a class="sourceLine" id="cb2-4" data-line-number="4">    char <span class="ch">&#39;,&#39;</span></a>
<a class="sourceLine" id="cb2-5" data-line-number="5">    v <span class="ot">&lt;-</span> parseVersion</a>
<a class="sourceLine" id="cb2-6" data-line-number="6">    return (n, v)</a>
<a class="sourceLine" id="cb2-7" data-line-number="7"></a>
<a class="sourceLine" id="cb2-8" data-line-number="8"><span class="ot">readFlag ::</span> <span class="dt">ReadP</span> (<span class="dt">FlagName</span>, <span class="dt">Bool</span>)</a>
<a class="sourceLine" id="cb2-9" data-line-number="9">readFlag <span class="fu">=</span> readNegFlag <span class="fu">&lt;++</span> readPosFlag</a>
<a class="sourceLine" id="cb2-10" data-line-number="10">    <span class="kw">where</span></a>
<a class="sourceLine" id="cb2-11" data-line-number="11">        readNegFlag <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-12" data-line-number="12">            char <span class="ch">&#39;-&#39;</span></a>
<a class="sourceLine" id="cb2-13" data-line-number="13">            n <span class="ot">&lt;-</span> many (satisfy (<span class="fu">/=</span> <span class="ch">&#39;,&#39;</span>))</a>
<a class="sourceLine" id="cb2-14" data-line-number="14">            return (<span class="dt">FlagName</span> n, <span class="dt">False</span>)</a>
<a class="sourceLine" id="cb2-15" data-line-number="15"></a>
<a class="sourceLine" id="cb2-16" data-line-number="16">        readPosFlag <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-17" data-line-number="17">            n0 <span class="ot">&lt;-</span> get</a>
<a class="sourceLine" id="cb2-18" data-line-number="18">            n <span class="ot">&lt;-</span> many (satisfy (<span class="fu">/=</span> <span class="ch">&#39;,&#39;</span>))</a>
<a class="sourceLine" id="cb2-19" data-line-number="19">            return (<span class="dt">FlagName</span> (n0 <span class="fu">:</span> n), <span class="dt">True</span>)</a>
<a class="sourceLine" id="cb2-20" data-line-number="20"></a>
<a class="sourceLine" id="cb2-21" data-line-number="21"><span class="ot">strCblPkgArgReader ::</span> <span class="dt">ReadM</span> (<span class="dt">String</span>, <span class="dt">Version</span>, <span class="dt">FlagAssignment</span>)</a>
<a class="sourceLine" id="cb2-22" data-line-number="22">strCblPkgArgReader <span class="fu">=</span> <span class="kw">let</span></a>
<a class="sourceLine" id="cb2-23" data-line-number="23">        readWithFlags <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-24" data-line-number="24">            (n, v) <span class="ot">&lt;-</span> readPkgNVersion</a>
<a class="sourceLine" id="cb2-25" data-line-number="25">            char <span class="ch">&#39;:&#39;</span></a>
<a class="sourceLine" id="cb2-26" data-line-number="26">            fas <span class="ot">&lt;-</span> sepBy readFlag (char <span class="ch">&#39;,&#39;</span>)</a>
<a class="sourceLine" id="cb2-27" data-line-number="27">            return (n, v, fas)</a>
<a class="sourceLine" id="cb2-28" data-line-number="28"></a>
<a class="sourceLine" id="cb2-29" data-line-number="29">        readWithoutFlags <span class="fu">=</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-30" data-line-number="30">            (n, v) <span class="ot">&lt;-</span> readPkgNVersion</a>
<a class="sourceLine" id="cb2-31" data-line-number="31">            return (n, v, [])</a>
<a class="sourceLine" id="cb2-32" data-line-number="32"></a>
<a class="sourceLine" id="cb2-33" data-line-number="33">    <span class="kw">in</span> <span class="kw">do</span></a>
<a class="sourceLine" id="cb2-34" data-line-number="34">        s <span class="ot">&lt;-</span> readerAsk</a>
<a class="sourceLine" id="cb2-35" data-line-number="35">        <span class="kw">case</span> lastMay (readP_to_S (readWithFlags <span class="fu">&lt;++</span> readWithoutFlags) s) <span class="kw">of</span></a>
<a class="sourceLine" id="cb2-36" data-line-number="36">            <span class="dt">Just</span> (r, <span class="st">&quot;&quot;</span>) <span class="ot">-&gt;</span> return r</a>
<a class="sourceLine" id="cb2-37" data-line-number="37">            _ <span class="ot">-&gt;</span> fail <span class="fu">$</span> <span class="st">&quot;Cannot parse: &quot;</span> <span class="fu">++</span> s</a></code></pre></div>
<p>It is slightly longer, but it’s rather a lot easier to read what’s happening after this rewrite. <code>ReadP</code> feels like a lighter option than pulling in <a href="http://hackage.haskell.org/package/parsec">parsec</a> as a dependency, but I’d love to hear any comments or suggestions, as well as pointers to how other people deal with parsing of non-trivial types of arguments in combination with <em>optparse-applicative</em>.</p>]]></summary>
</entry>

</feed>
